# HANDOFF — สถานะโปรเจกต์ RefX ณ 12 ส.ค. 2026

> เอกสารนี้มีไว้ให้ **session ใหม่ของ coder agent** อ่านเพื่อรับช่วงต่อ
> อ่านไฟล์นี้ให้จบก่อน แล้วค่อยอ่าน `CLAUDE.md` → `ARCHITECTURE.md §1` → `docs/09` → `ROADMAP.md`
>
> ⚠️ **วันที่ในเอกสารทุกฉบับเชื่อไม่ได้ 100%** — หลายจุดถูกประทับจากความจำ ไม่ได้ตรวจกับหลักฐาน
> (พบแล้วหนึ่งครั้ง: หัวข้อ CI เขียน 28 ก.ค. ทั้งที่ `git log` ยืนยันว่า 1 ส.ค.)
> **`git log` คือแหล่งความจริงของลำดับเหตุการณ์** ถ้าเจอวันที่ขัดกับ git ให้เชื่อ git แล้วแก้เอกสาร

---

## 1. สรุปสถานะ

| Phase | สถานะ |
|---|---|
| **P0-1 ถึง P0-9** | ✅ เสร็จครบ ผ่านรีวิวแล้ว |
| **P1-1 ถึง P1-6** | ✅ เสร็จ (P1-6 ครบแล้วหลังทำ `TextureAllocator`) |
| **P1-7** working texture | ✅ เสร็จ (`9e1033e`) — mip บน CPU · LRU · 17 draw call ที่ 100 ภาพ |
| **P1-8** drag & drop + clipboard paste | ✅ เสร็จ — **P1 ปิดครบแล้ว** |
| **แยกชั้น `refx-asset`** (§2.0 เดิม) | ✅ เสร็จ (`41c2693`) — ดู §2.0 ด้านล่าง |
| **P2-1** `Arena`/`Board`/`Item` | ✅ เสร็จ (`43b76d5`) |
| **P2-2** `Command` + `History` + merge/seal | ✅ เสร็จ (`43b76d5`) — **ผ่านรีวิวแล้ว** |
| **P2-3** `SpatialIndex` + hit-test | ✅ เสร็จ (`7a81b4e`) |
| **ย้าย `refx-ui` ไปใช้ `Board`** | ✅ เสร็จ (`0cc1e76`) — เทียบเส้นฐานครบ ไม่มีตัวไหนแย่ลงเกิน 5% |
| **P2-4** select / rubber-band / multi-select | ✅ **เสร็จสมบูรณ์** (`dd51d60` · `439f380` · `a6ac127` · `0f6071c`) — รวม Ctrl+Z/Ctrl+Y ยืนยันด้วยภาพหน้าจอ |
| **`selection` ย้ายออกจาก `Board`** (docs/02 §2.9) | ✅ เสร็จ (`439f380`) — คลิกดูภาพไม่ทำให้เอกสาร dirty อีกต่อไป |
| **P2-5** move | ✅ เสร็จ (`d020219`) — ลากค้าง = 1 undo · ปล่อยแล้ว seal · ยืนยันด้วยภาพหน้าจอ |
| **P2-5** scale handle / rotate + Shift 15° | ✅ **เสร็จ — P2-5 ปิดครบ** ดู §2.4 · ยืนยันด้วยภาพหน้าจอ |
| **audit ฟิลด์ → shader** | ✅ เสร็จ (`9eea230`) — ดู §2.2d |
| **P2-6** z-order + ลบภาพ + อายุของ thumbnail | ✅ **เสร็จ** ดู §2.5 · ยืนยันด้วยภาพหน้าจอ |
| **P2-7** crop แบบ non-destructive | ✅ **เสร็จ** ดู §2.6 · ยืนยันด้วยภาพหน้าจอ |
| **P2-8** grayscale / flip / opacity / filter | ✅ **เสร็จ** ดู §2.7 · **สลับ grayscale 1000 ภาพ = 0 texture upload** |
| **P2-9** align / distribute + ไกด์ | ✅ **เสร็จ — ยืนยันบนของจริงครบสามข้อแล้ว** ดู §2.8 |
| **P2-10** color picker + measure | ✅ **เสร็จ** ดู §2.9 · **สีที่คืนคือสีของไฟล์ ไม่ใช่สีบนจอ** พิสูจน์ด้วยตัวเลข |
| **P2-11** text note | ✅ **เสร็จ — P2 ปิดครบทั้ง 11 task** ดู §2.10 |
| **P3-1** `ItemMeta` + tag/rating/color label + inspector | ✅ **เสร็จ** ดู §2.11 · **`ColorLabel` round-trip ได้แล้วตั้งแต่ต้น** |
| **P3-2** layout engine 5 ตัว | ✅ **เสร็จ** ดู §2.13 · **`fuzz_layout` ถูกต่อแล้ว** — ยิงจริงบน CI **3,650,952 รอบ** |
| **P3-6** Sort by canvas order | ✅ **เสร็จ** ดู §2.19 · อ่านแบบหนังสือ + tolerance 50% ของความสูงเฉลี่ย |
| **P3-5** `ApplyLayoutCommand` (Arrange → Canvas) | ✅ **เสร็จ** ดู §2.18 · **undo ครั้งเดียว = พิกเซลต่างจากเดิม 0 ไบต์** · `respect_pinned` ตัดสินใหม่แล้ว |
| **P3-4** sort + filter + cache ตาม `revision` | ✅ **เสร็จ** ดู §2.17 · **เลื่อน 200 เฟรม = กรอง+เรียง 1 ครั้ง** · เจอช่องข้อความที่เผาซีพียู 13 fps มาตั้งแต่ P2-11 |
| **P3-3** virtual scrolling | ✅ **เสร็จ** (`dc42eca` + `8e4cf04`) ดู §2.14 / §2.15 · **วาดจริง 28 จาก 3,072 · culling ประหยัด 43%** · เพดาน 3,072 เป็นค่าที่ ROADMAP ยอมรับแล้ว และ**ลากเกินเพดานไม่เงียบอีกต่อไป** |
| **P3-7** group / ungroup | ✅ **เสร็จ** (`cd47544`) ดู §2.20 · ปลดล็อกสามฟิลด์พร้อมกัน · ยืนยันด้วยภาพหน้าจอ · I-1 ครบสามสถานะใหม่ |
| **P3-8** สลับ mode 100 ครั้ง | ✅ **เสร็จ — P3 ปิดครบทั้ง 8 task** (`cbf0889`) ดู §2.21 · เทียบ `Board` ทั้งก้อน + `revision` ไม่ใช่แค่ `dirty` |
| **P4-1** `.refx` format v1 (linked) + DTO | ✅ **เสร็จ** (`2d99d86`) ดู §2.22 · round-trip property test · `fuzz_document` ยิงจริง 51,434 รอบ · `Board::view` ถูกซิงค์แล้ว |
| **P4-2** atomic save + `.refx.bak` | ✅ **เสร็จ** (`c7fdb36` · `9abaeac`) ดู §2.23 · **ฆ่าโปรเซส 100 ครั้ง เสียหาย 0** · `Ctrl+S`/`Ctrl+Shift+S` + ถามก่อนปิด · `MOVEFILE_WRITE_THROUGH` |
| **P4-3** autosave snapshot | ✅ **เสร็จ** (`4272f5c` + `f53dfa7` + `e0f7222`) ดู §2.24 · **วัดแล้วเสียมากสุด = ระยะเว้นพอดี** · 3,072 ใบ = 29 KB / 2.9 KB/s · ไม่แก้อะไร = ไม่เขียนดิสก์เลย · ⚠️ **ตอน `4272f5c` ยังไม่ครบจริงสองข้อ** — นาฬิกาปลุกไม่มีผู้รับ (`on_wake`) และเทสต์วัดด้วยการหาร ทั้งคู่ปิดแล้วใน `f53dfa7`/`e0f7222` ดูกล่อง ⚠️ ใน §2.24 |
| **P4-4** crash recovery + dialog 3 ตัวเลือก + เปิดไฟล์ | ✅ **เสร็จ** (`c0a87b9` · `f53dfa7` · `6f79c76`) ดู §2.26 · `<data_dir>/recovery/<session>.refx` · ฆ่าโปรเซสระหว่างแก้งานที่ **ไม่เคยมี path เลย** 8 รอบ กู้ได้ทุกรอบ เสียมากสุด 913ms · **บนแอปจริง: ลาก 6 ใบ ไม่กด Ctrl+S ฆ่าโปรเซส → เปิดใหม่กู้ได้ครบ 6/6 = 100%** · `Ctrl+O` มีแล้ว · I-1 ในสถานะใหม่: Frames 7 → 7 ใน 20 วินาที |
| **`AssetRef::hash` เป็นคีย์ของเนื้อ** (ก่อน P4-6) | ✅ **เสร็จ** ดู §2.29 · `hash_file` ถูกร้อยกลับมาถึง `Board` แล้ว (มันถูกเรียกอยู่แล้วแต่ผลไปจบที่ cache) · **ยืนยันบนแอปจริง**: สำเนาเดียวกันสองชื่อ = working texture **ใบเดียว** (682 KB → 341 KB ที่ซูมเท่ากัน) · เอกสารของ build เก่าเปิดได้ปกติ 4/4 ไม่ crash · **cache เดิมไม่ miss แม้แถวเดียว** (10,855 นิ่ง) · ★ ปลดด่านของ P4-6 ขั้น 3/5 |
| **P4-6** relink flow 5 ขั้น | ✅ **เสร็จ** ดู §2.30 · ตรรกะเป็น pure function ใน `refx-core::relink` (IO เป็น closure) · **ยืนยันบนแอปจริงครบทั้งห้าขั้น**: ย้ายทั้งโฟลเดอร์ → เจอ 4/4 · ย้ายไปที่ที่ไม่มีใครรู้ → `Missing` พร้อมชื่อไฟล์บน placeholder · ชี้ไฟล์เดียว → **กลับมาครบ 4 ใบ** · undo/redo คืนสภาพได้ทั้งสองทาง · เจอสองบั๊กจากการกด `Ctrl+Z` บนของจริง (จอไม่ตรงกับ `Board` · undo ข้ามงวด) |
| **P4-5** packed mode + asset table | ⏳ **ชิ้นที่ 1 เสร็จ** ดู §2.28 · ชั้น io ครบตั้งแต่ §2.27 · **ต่อสาย paste → spool → ภาพคม แล้ว**: `DecodePool` รับ `PastedImageStore` · `Done { spooled }` + `Spooled` ที่ส่ง **หลัง** ภาพขึ้นจอ · `AssetRef.hash/path` ผูกกับ spool · เพดานเป็นไบต์ (512 MB) + ข้อความเมื่อลบไม่ได้ · **ยืนยันบนแอปจริง**: contrast 0.01 → **5.09** · วางซ้ำ 3 ครั้ง = **1 ไฟล์** · ฆ่าโปรเซสแล้วกู้ได้ 3/3 · latency **+6 ms** · I-1 ครบ 3 สถานะใหม่ · ★ **เหลือชิ้นที่ 2** (Save As packed · extract ตอนเปิด · status bar บอกโหมด) |

**เกณฑ์คุณภาพล่าสุดที่ผ่าน:** `fmt` / `clippy --all-targets --all-features -D warnings` (workspace + `fuzz/`) /
`nextest 815/815` / `deny check` **เขียวครบสี่หมวด** (advisories/bans/licenses/sources — RUSTSEC-2026-0257 ปิดแล้ว ดู §1) /
**binary 17.06 MB จากเพดาน 25 MB (บังคับใน CI · negative control ยังล้มเป็น)** /
GUI dep ใน `fuzz/Cargo.lock` = **0** (dev-dep ของ `refx-io` ไม่ตามไป — §2.23) /
`cargo tree -d | grep '^wgpu'` ว่าง / ไม่มี network dep / `w/crlf` = 0

crate ที่มี: `refx-app` `refx-asset` `refx-core` `refx-io` `refx-platform` `refx-render` `refx-ui`

### ✅ `RUSTSEC-2026-0257` ปิดแล้ว (13 ส.ค. 2026) — **ทำครบทั้งสองทาง แยกเป็นสอง commit**

`webbrowser 1.2.1` — *Unix `BROWSER` handling allows browser argument injection*
· เข้ามาทาง **`egui-winit` feature `links` ที่เปิดอยู่โดยปริยาย** · RefX
ไม่เคยเรียกเลย (ไม่มี hyperlink สักที่ · I-8 ห้าม network)

| ขั้น | commit | ทำอะไร |
|---|---|---|
| 1 | `b774173` | `cargo update -p webbrowser` → 1.2.4 (อยู่ในช่วง `^1.0.5` ที่ egui-winit ประกาศไว้อยู่แล้ว) — **หยุดเลือดก่อน ความเสี่ยงเป็นศูนย์** |
| 2 | `40a1134` | **ถอด feature `links` ทิ้ง** — เอาโค้ดที่ไม่เคยเรียกออกจาก binary ไปเลย ตรงกับ I-8 มากกว่า |

**ผลของขั้น 2 (ตัวเลขจริง ไม่ใช่ "คอมไพล์ผ่าน"):**

```
egui-winit ก่อน: arboard,bytemuck,clipboard,default,links,smithay-clipboard,wayland,webbrowser,x11
egui-winit หลัง: arboard,bytemuck,clipboard,smithay-clipboard,wayland,x11
```

หายสามตัว (`default` เป็นแค่ตัวรวม ไม่มี `cfg` ไหนอ่าน · `links` · `webbrowser`)
· **ไม่มี feature ไหนถูกเพิ่ม** · `cargo tree -i webbrowser` ตอบ *"did not match
any packages"* ทั้ง Windows และ Linux และหายจาก `Cargo.lock` แล้ว — ไม่ใช่แค่ถูกปิด `cfg`
· binary **17,006,080 → 16,716,800 ไบต์ (−282.5 KB, −1.70%)**

ของที่ load-bearing **เหมือนเดิมเป๊ะทุกตัวทั้งสองเป้าหมาย** (diff กราฟ feature
ทั้งก้อน ~1,000–1,400 บรรทัด): `winit` · `arboard` · `smithay-clipboard` ·
`wayland-client` · `x11-dl` · `sctk-adwaita`

#### ★★ สูตรใน HANDOFF เดิมทำตามตัวอักษรไม่ได้ — และเหตุผลที่ไม่เป็นไร

เขียนไว้ว่า `default-features = false` แล้วเปิด `clipboard`/`wayland`/`x11`/**`winit/default`**
กลับ · ตัวสุดท้าย cargo ปฏิเสธตรง ๆ (*"not allowed to contain slashes"*) —
slash ใช้ได้เฉพาะใน `[features]` ของ crate ตัวเอง ไม่ใช่ในรายการ feature ของ
dependency · ไม่ต้องหาทางอ้อมเพราะ **เราเปิด default ของ `winit` เองอยู่แล้ว**
ผ่าน `winit = "0.30"` ที่ `refx-platform`/`refx-ui`/`refx-app` พึ่งอยู่ทั้งสามตัว
แล้ว cargo รวม feature ทั้งกราฟให้ — **ยืนยันด้วยการวัด ไม่ใช่การให้เหตุผล**

#### ★★★ ที่ยืนยันไม่ได้ และต้องไม่อ้างว่าผ่าน

**ไม่มีเทสต์ไหนพิสูจน์ว่า clipboard ทำงานจริงบน Linux**
· `reading_the_real_clipboard_never_panics` / `writing_to_the_real_clipboard_never_panics`
รับ `Err` ว่าผ่าน (เจตนาเดิมคือ "ต้องไม่ panic บนเครื่องที่ไม่มี display")
บน runner headless มันจึงเขียวได้โดยไม่ได้แตะ clipboard จริงสักครั้ง
**CI เขียวไม่ใช่หลักฐานของข้อนี้** — หลักฐานที่มีคือ feature ที่ resolve ได้เหมือนเดิมเป๊ะ

> เส้นทาง `Ctrl+V` ของ P1-8 ไม่ได้เดินผ่าน `egui-winit` เลย — มันไป
> `refx-platform::SystemClipboard` → `arboard` ซึ่งเป็น dep **ตรง** ของ
> `refx-platform` อยู่แล้ว · feature `clipboard` ที่ยังเปิดไว้เป็นของ copy/paste
> ในช่องข้อความของ egui เอง

★ **ห้ามใส่ข้อยกเว้นใน `deny.toml`** — ทำตามแล้ว ไม่มีข้อยกเว้นถูกเพิ่ม

### ✅ CI กลับมาแล้ว (11 ส.ค. 2026) — และ **ช่องที่ไม่มีใครตรวจถูกปิดแล้ว**

เจ้าของโปรเจกต์อัปเป็น **GitHub Pro (3,000 นาที/เดือน)** + แก้ spending limit
· สาเหตุที่ CI ตายตั้งแต่ 9 ส.ค. คือ **โควตาหมด** ตามที่ตั้งสมมติฐานไว้จริง
(อาการ: ทุก job ล้มใน 2–13 วิ · `steps: []` · `runner_name` ว่าง · ขอ log ได้
`BlobNotFound` = ไม่เคยมี runner รับงาน)

**run `31511817912`** (`workflow_dispatch`, matrix เต็มรวม Windows) — **เขียวทั้ง 6 job**
ครอบ commit ตั้งแต่ `192f876` ถึง `5cf8ca5` ที่เคยลงไปโดยไม่มี CI ตรวจ
→ **ช่วงนั้นปิดแล้ว ไม่ต้องสงสัยอีก** (รวม `refx-asset`/`refx-ui` ที่ cross-check
ในเครื่องครอบไม่ถึง)

#### ★★★ ประตูสองตัวที่เขียนตอน CI ตาย — ได้เห็นมันเดินบน runner จริงแล้ว

`docs/08 §3.9` ถือว่าเครื่องมือตรวจที่ไม่เคยเดินจริง = ไม่มีอยู่ · ทั้งคู่ผ่านแล้ว:

| ประตู | หลักฐานจาก runner จริง |
|---|---|
| **Linux บังคับ GPU** (`REFX_REQUIRE_GPU=1` ย้ายมาจาก Windows) | log พิมพ์ `GPU tests: ทำงานจริงบน llvmpipe (LLVM 20.1.2, 256 bits) (Vulkan / Cpu)` แล้วชุดเต็มได้ **588 tests run: 588 passed, 0 skipped** — รันจริงทุกตัว ไม่มีการข้าม |
| **`changes` (ข้าม push ที่แตะแต่ `.md`)** | ดู run ของ commit `6bc3ae4` ในตารางด้านล่าง |

> ★ "70 skipped" ที่โผล่ในขั้น **"job นี้ตรวจ GPU ด้วย adapter ตัวไหน"** ไม่ใช่การ
> ข้ามเทสต์ GPU — ขั้นนั้นใช้ `-E 'test(gpu_tests_run_where_they_are_required)'`
> เลือกมาตัวเดียวจาก 71 ตัวโดยตั้งใจ ที่เหลือถูกกรองออกด้วย *ตัวกรองชื่อ*
> ไม่ใช่เพราะไม่มี adapter · ตัวเลขที่ตอบคำถามคือ **0 skipped** ของชุดเต็ม

ตารางโควตาก่อน/หลัง และเหตุผลของแต่ละข้ออยู่ในหัวข้อถัดไป — เพดานใหม่คือ
**3,000 นาที/เดือน** ส่วนที่ประมาณไว้คือ ~2,000

### ⚠️ `0dd5a6c` ไม่ได้แตะแต่ `.md` ตามที่ข้อความ commit เขียนไว้

`git add -A` กวาดโค้ด P3-4 ที่กำลังทำค้างอยู่ (`query.rs` ทั้งไฟล์ · `revision`
ใน `board.rs` · cache ใน `arrange.rs` · UI ใน `shell.rs`/`text.rs`) กับ `docs/05`
ที่เจ้าของโปรเจกต์แก้ค้างไว้ เข้าไปด้วยทั้งหมด

- **ไม่มีของเสียหลุดไป** — โค้ดชุดนั้นเขียวทั้งในเครื่องและบน CI (run ของ `0dd5a6c` ผ่านทุก job)
- **แต่ประวัติผิด**: commit ที่ชื่อเรื่อง CI มีเนื้อ P3-4 อยู่ข้างใน · ไม่ rewrite
  เพราะ push ไปแล้ว (§1 ห้ามหลัง push แรก) — `a430ca0` เป็นส่วนที่เหลือและอธิบายไว้
- **การทดสอบประตู `changes` ในรอบนั้นใช้ไม่ได้** เพราะ push นั้นมีโค้ดจริง
  → commit นี้คือตัวทดสอบจริง: **แตะ `HANDOFF.md` ไฟล์เดียว**

> ★ บทเรียน: `git add -A` ตอนที่มีงานค้างในต้นไม้ = commit ที่ข้อความไม่ตรงกับเนื้อ
> · เจตนาว่า "จะ commit อะไร" ต้องบังคับด้วย **การเลือกไฟล์** ไม่ใช่ด้วยความจำ

### ★ วิธีรอผล CI (ตัดสิน 10 ส.ค. 2026 — เจ้าของโปรเจกต์)

**push แล้วรายงานจบรอบไปเลย ห้ามตั้งลูปรอผล** · ไปดูผลของรอบก่อน
ด้วย `gh run list --limit 5` **ครั้งเดียวตอนเริ่มรอบถัดไป**

เหตุผล: เครื่องถูกปิดระหว่าง CI รันเป็นเรื่องปกติ (ไม่กระทบ CI ซึ่งรันบน
เซิร์ฟเวอร์ GitHub แต่ทำให้ลูปเฝ้าดูค้างข้ามคืน — เคยค้างจริง 6 ชั่วโมง) ·
และการรอ 15 นาทีต่อรอบไม่ได้ให้ข้อมูลที่รอไม่ได้: เกณฑ์ในเครื่องผ่านครบ
ก่อน push อยู่แล้ว ส่วนสิ่งที่ CI จับเพิ่ม (Linux, GPU คนละตัว) ยังจับได้
เท่าเดิม แค่รู้ช้าลงหนึ่งรอบ

### ★★ ลดการใช้โควตา Actions ลง ~72% (10 ส.ค. 2026 · `192f876`)

ทำโดยไม่ลดสิ่งที่ถูกตรวจต่อ push แม้แต่อย่างเดียว — คิดจากตัวเลขจริงของ run
ที่เขียวล่าสุด (`31270309368` และอีกสาม run ข้างเคียง)

| | ก่อน | หลัง |
|---|---|---|
| push ที่แตะแต่ `.md` | matrix เต็ม ~41 นาทีของโควตา | **~1 นาที** (job `changes` อย่างเดียว ที่เหลือ skipped พร้อมเหตุผลใน log) |
| push ที่แตะโค้ด | windows 15 นาที **×2** + ubuntu 8 + เล็ก ๆ 3 = ~41 | **~12 นาที** (ubuntu + เล็ก ๆ + `changes`) |
| Windows | ทุก push | **schedule รายสัปดาห์ + กด Run workflow เอง** |
| Fuzz | ทุกคืน ~46 นาที | **จ/พ/ศ** (15 นาที/target เท่าเดิม) |
| รวมต่อเดือนที่อัตราเดิม (46 run / 10 วัน) | **~7,100 นาที** | **~2,000 นาที** |

★ **Windows ไม่ได้ถูกตัดทิ้ง** — เป็นแพลตฟอร์มหลักของผู้ใช้ · แตะโค้ดที่มี
`cfg(windows)`, เรื่อง GPU/driver, หรือ path handling **ให้กด Run workflow
เองก่อน merge** · เหตุผลที่ย้ายได้: GitHub คิดนาที Windows **×2** และผลของมัน
ตรงกับ Linux ทุก run ที่ผ่านมา

#### ★★★ ของที่ต้องแก้พร้อมกัน ไม่งั้นจะได้เขียวปลอม

Windows เคยเป็น job เดียวที่ตั้ง `REFX_REQUIRE_GPU=1` (บังคับว่าต้องมี adapter จริง)
ส่วน Linux "ได้ lavapipe ก็รัน ไม่ได้ก็ข้ามพร้อมพิมพ์เหตุผล" — พอ Windows
ย้ายไปรอบสัปดาห์ **ทุก push จะไม่มีใครบังคับ GPU เลย** ซึ่งคือสภาพที่
`docs/08 §3.9` ข้อ 11 ห้ามไว้ตรง ๆ → เลื่อน Linux ขึ้นมาเป็นตัวบังคับแล้ว
(คอมเมนต์เดิมใน `ci.yml` เปิดทางไว้ว่า "ถ้ามันนิ่งพอค่อยเลื่อนขึ้นมาบังคับ")

#### ทำไมไม่ใช้ `paths-ignore` ระดับ workflow

มันทำให้ commit นั้น **ไม่มี status เลย** ซึ่งอ่านไม่ออกว่า "ไม่ต้องตรวจ"
หรือ "ยังไม่ได้ตรวจ" · ใช้ job `changes` + `if:` แทน → job โผล่เป็น
**skipped พร้อมเหตุผลใน log** (`docs/08 §3.9` ข้อ 2: ข้ามได้ แต่ต้องข้ามอย่างชัดเจน)
· ค่าปริยายของประตูคือ **"ตรวจ"** ทุกกรณีที่เดาไม่ออก (push แรกของ branch,
`before` หาย, push ว่าง) — ประตูที่เดาผิดแล้ว*ข้าม*คือประตูที่ปล่อยของเสียผ่าน
ส่วนเดาผิดแล้ว*ตรวจเกิน*แค่เปลืองนาที

ตรรกะถูกยิงกับประวัติจริงในเครื่องแล้วหกเคสก่อน push: md ล้วน → ข้าม ·
code → ตรวจ · **md+code ปนในพุชเดียว → ตรวจ** · branch ใหม่ → ตรวจ ·
schedule/dispatch → ตรวจ + Windows

> ⚠️ ยังพิสูจน์ไม่ได้ว่าประตูนี้ทำงานถูกบน runner จริง เพราะ CI ยังไม่รัน
> **รอบหน้าที่ CI กลับมา ต้องดูว่า push ที่แตะแต่ `.md` ขึ้น skipped จริง**

### สถานะ CI (มีชีวิตแล้วตั้งแต่ 1 ส.ค. 2026)

repo: **`github.com/tanks2547-crypto/RefX` (private)** · branch `main`

| | windows-latest | ubuntu-latest |
|---|---|---|
| GPU ที่ใช้ตรวจจริง | **WARP** (Dx12/Cpu) | **lavapipe** (Vulkan/Cpu) |
| เทสต์ | 562/562 | 562/562 |
| binary | 16.00 MB (เครื่องพัฒนา) | — |

**`Fuzz (nightly)` มีสี่ job แล้วตั้งแต่ P3-2** — `fuzz_decode` · **`fuzz_layout`** · `unwired-targets` · `fuzz-lint`
ทั้งหมดเขียวบน nightly · ยิงเต็ม 15 นาทีต่อ target
ดูเหตุที่เคยเขียวปลอมใน §5 ข้อ false green

**เทสต์ GPU ถูกรันจริงทั้งสองแพลตฟอร์ม** — `REFX_REQUIRE_GPU=1` ทำให้ "ไม่มี adapter" = แดง
ไม่ใช่ข้ามเงียบ ๆ · `.config/nextest.toml` ฆ่าเทสต์ที่เกิน 4 นาที เพื่อให้เทสต์ค้างเป็น
"แดงพร้อมชื่อ" ไม่ใช่ซอมบี้กิน runner หลายชั่วโมง

> ★ **ก่อน 1 ส.ค. 2026 CI ไม่เคยรันเลยสักครั้ง** (ไม่มี remote) — ทุกอย่างที่สร้างมา
> (เพดาน binary, fuzz cron, `deny` รายสัปดาห์, เทสต์ GPU) เป็นทฤษฎีล้วน
> พอรันจริงครั้งแรกก็เจอบั๊ก 2 ตัวทันที รวมถึงโค้ด `#[cfg(target_os = "linux")]`
> ที่**ไม่เคยถูกคอมไพล์เลยตลอดโปรเจกต์**
>
> **บทเรียน:** เครื่องมือตรวจที่ไม่เคยเดินจริง = ไม่มีเครื่องมือ (ดู `docs/08 §3.9`)
> รอบแรก cold cache ~40 นาที · รอบถัดมา 7–14 นาที (`rust-cache` อุ่นแล้ว)

### สถานะ git

repo อยู่ใต้ git แล้ว (branch **`main`**) และ **push ขึ้น GitHub แบบ private แล้ว**
(`origin` = `tanks2547-crypto/RefX`) — `git fsck` สะอาด working tree สะอาด

- **identity ตั้งไว้แบบ repo-local** คือ `RefX <297583263+tanks2547-crypto@users.noreply.github.com>`
  (global ยังว่าง จงใจ — repo อื่นในเครื่องไม่ถูกกระทบ)
  ถ้า `git commit` ฟ้อง `unable to auto-detect email address` แปลว่าอยู่คนละ repo หรือ config หาย
  > ★ ใช้ **noreply ของ GitHub** ไม่ใช่อีเมลจริง — อีเมลใน commit เป็นสาธารณะเสมอ
  > ถ้า repo ถูกเปิดเป็น public วันหนึ่ง อีเมลทั้งประวัติจะโผล่ตามไปด้วย
  > GitHub ผูก commit เข้ากับโปรไฟล์ด้วย**อีเมล**ไม่ใช่ชื่อ ชื่อจึงยังเป็น `RefX` ได้ตามเดิม
  > ประวัติทั้งก้อนถูกเขียนใหม่ครั้งเดียว **ก่อน push ครั้งแรก** — หลังจากนี้ห้ามทำอีก
- **`.gitattributes` ตรึง line ending เป็น LF ทั้งโปรเจกต์** และ renormalize ไปแล้ว
  ตรวจได้ด้วย `git ls-files --eol | grep -c 'w/crlf'` → ต้องเป็น **0** เสมอ
  > ★ ระวัง: `core.autocrlf = true` ถูกตั้งไว้ที่ **system level** ของ Git for Windows
  > `eol=lf` ใน `.gitattributes` ชนะอยู่ จึงไม่ต้องแก้ system config
  > แต่ถ้าแก้ไฟล์ด้วยเครื่องมือที่แปลง `\n` → `\r\n` เอง (เช่น Python `open(f,'w')` บน Windows)
  > ไฟล์บนดิสก์จะกลายเป็น CRLF อีก — index ยังเป็น LF เพราะ git normalize ให้ตอน add
  > แต่ควรเช็คตัวเลขข้างบนก่อน commit
- **`.claude/settings.local.json` ไม่ถูก track แล้ว** (อยู่ใน `.gitignore`)
  เป็นตั้งค่าต่อเครื่อง ไม่ใช่ของโปรเจกต์ — ไฟล์ยังอยู่บนดิสก์ตามปกติ

---

## 2. ★ งานถัดไป (เรียงตามลำดับ ห้ามสลับ)

> ### ★★ **P4-6 ปิดแล้ว (21 ส.ค. 2026)** — งานถัดไปคือ **P4-5 ชิ้นที่ 2**
>
> ★★★ relink ครบห้าขั้นและยืนยันบนแอปจริงแล้ว (§2.30) · ที่ค้างอยู่คือ
> **P4-5 ชิ้นที่ 2**: Save As แบบ packed · เปิดไฟล์ packed → `extract` กลับลง
> spool · แถบสถานะบอกโหมด (**สภาวะ ไม่ใช่เหตุการณ์** ตาม `docs/03 §1`)
> — ดู §2.28 / §2.27 · แล้วต่อด้วย P4-7 (multi-board tabs) และ P4-8/P4-9
>
> <details><summary>P4-4 ปิดครบแล้ว (18 ส.ค. 2026)</summary>
>
> ทั้งสองช่องที่ P4-4 ต้องปิดถูกปิดแล้ว:
>
> 1. ✅ **งานที่ยังไม่เคยบันทึกมี autosave แล้ว** (`c0a87b9` · `f53dfa7`)
>    `<data_dir>/recovery/<session-id>.refx` · ★ **ห้ามวางใน `cache_dir`**
>    มีเทสต์คุมที่ `AppPaths` (`recovery_never_lives_under_the_cache_dir`)
>    และ negative control แดงจริง
> 2. ✅ **dialog 3 ตัวเลือก + `Ctrl+O`** (`6f79c76`) ดู §2.26
>
> ★★ **เกณฑ์ของ docs/07 §4 ผ่านครบทั้งสองระดับแล้ว** — ระดับไฟล์ (ฆ่าจริง 8 รอบ
> กู้ได้ทุกรอบ เสียมากสุด 913ms) และ **ระดับแอป** (ลาก 6 ใบ ไม่กด Ctrl+S
> ฆ่าโปรเซส → เปิดใหม่เห็นแถบ กดแล้วภาพครบ 6 ใบกลับขึ้น canvas · ภาพหน้าจอ)
>
> ★ P3-5 ฝากไว้: ตัวเลือก "ขอบเขต/จุดวาง" ของ docs/03 §4.1 ยังทำแค่ทางเดียว
>
> </details>
>
> ★★ **ยังไม่มี "เปิดไฟล์" (`Ctrl+O`)** — ตอนนี้ยืนยัน round-trip ในแอปจริงไม่ได้เลย
> มีแต่ที่ระดับ container (magic/version/CRC) กับ property test ของ P4-1
>
> ★ P3-5 ฝากไว้: ตัวเลือก "ขอบเขต/จุดวาง" ของ docs/03 §4.1 ยังทำแค่ทางเดียว
> (ทุกใบที่ผ่านตัวกรอง · วางที่เดิมของกลุ่ม) — ดู §2.18

> **P1 ปิดครบแล้ว** — งานที่เสร็จแล้วถูกย้ายออกจากหัวข้อนี้ ดูประวัติใน git log
> ต้นเหตุและทางแก้ของบั๊กสำคัญบันทึกไว้ใน `docs/` แล้ว ไม่ใช่ในไฟล์นี้

> **`clipboard-win` — ตัดสินแล้วว่าไม่เพิ่ม** ดูเหตุผลใน `docs/06 §2.6`
> (แก้ได้เฉพาะ Windows · เกราะที่ทำงานแพลตฟอร์มเดียวให้ความมั่นใจผิด ๆ)
>
> **ภาพจาก clipboard คมได้แค่ระดับ thumbnail** — ปล่อยไว้ รอทำพร้อม **P4-5 packed mode**
> ห้ามแก้ด้วยไฟล์ชั่วคราวใน cache (ภาพที่ board อ้างถึงจะหายไปกับ LRU = ผิด I-3)

### 2.0 ✅ แยกชั้น `refx-asset` ออกจาก `refx-platform` (เสร็จ `41c2693`)

เก็บไว้เพราะ **ผลลัพธ์เป็นข้อผูกมัดต่อไป** — ห้ามใส่ `refx-platform` กลับเข้า `refx-asset`

เดิม `fuzz/` → `refx-asset` → `refx-platform` → `rfd` → `ashpd` → `zbus` ทำให้ fuzz
ต้องคอมไพล์ทั้งกอง GUI ด้วย nightly แล้วแตกที่ `zbus` (ไม่ใช่โค้ดเรา)

**ข้อเสนอเดิมในไฟล์นี้ผิดบางส่วน:** เขียนว่าย้าย `panic_guard` อย่างเดียวแล้ว
"dependency หายทั้งเส้น" — ของจริง `refx-asset` ใช้จาก `refx-platform` **สามอย่าง**

| | เดิม | ตอนนี้ |
|---|---|---|
| `panic_guard` | `refx-platform` | `refx-core::panic_guard` (std ล้วน) |
| `total_ram()` | เรียกเองใน `with_defaults` | ผู้เรียกส่งเข้ามา (`refx-ui`) |
| clipboard | เรียก `arboard` ผ่าน `refx-platform` | DTO + trait `ClipboardReader` อยู่ `refx-core` · ตัวที่คุย `arboard` อยู่ `refx-platform` · `refx-ui` เสียบให้ |

หลักการเดียวกับ `WakeHandle` ที่ทำให้ `refx-asset` ไม่รู้จัก `winit` อยู่ก่อนแล้ว
ต่างกันแค่ใช้ trait แทน closure เพราะมี error type ที่สองฝั่งต้องแชร์

**ผลข้างเคียงที่ ARCHITECTURE §2 บันทึกไว้แล้ว:** `refx-platform` ไม่ใช่ leaf อีกต่อไป
(พึ่ง `refx-core`) กราฟยังเป็น DAG ทิศทางเดียว

ยืนยันด้วย `cargo tree`: `refx-asset` และ `fuzz/` ไม่เหลือ `rfd`/`winit`/`arboard`/`refx-platform`

### 2.1 ✅ P2-1 + P2-2 (เสร็จ `43b76d5` ผ่านรีวิว 2 ส.ค. 2026)

`Arena` + `ItemId`/`GroupId`/`BoardId` · `Board`/`Item`/`ItemCanvas`/`ItemMeta`/`AssetRef` ·
`Selection` · `ViewState` · `Command` + `History` + merge/seal + `AddItems`/`RemoveItems`/
`TransformItems`/`ReorderZ`/`EditMeta`

**สิ่งที่ต้องรู้ก่อนเขียน `Command` ตัวใหม่** (รายละเอียดอยู่ใน doc comment ของ `command.rs`):

1. `apply` ที่คืน `Err` **ต้องไม่แตะ board เลย** — คำสั่งที่แตะหลาย item ย้อนสิ่งที่ทำไปแล้วคืนก่อน
2. `undo` คืนสภาพ **เป๊ะ**: `ItemId` เดิม · ชั้น z เดิม · ธง `dirty` เดิม (`selection` ไม่อยู่ใน `Board` แล้ว — docs/02 §2.9)
3. **redo ต้องได้ `ItemId` ชุดเดิม** ไม่งั้นคำสั่งถัดไปในสาย redo ชี้ไปที่ว่าง
   (นี่คือเหตุผลที่ `Arena::insert_at` มีอยู่ และเหตุผลที่ไม่ใช้ `slotmap`)
4. `heap_size()` **ไม่มีค่าเริ่มต้น** — ต้องเขียนเองทุกตัว ไม่งั้นเพดาน 64 MB ไม่นับคำสั่งใหม่
5. `affected()` **ไม่มีค่าเริ่มต้น** เช่นกัน — บอกว่าคำสั่งแตะ item ไหน เพื่อให้ชั้น editor
   ตั้ง selection ตามหลัง undo/redo (การเลือกไม่ได้ถูก undo มันตามผลลัพธ์)

### 2.2 ✅ ย้าย `refx-ui` มาใช้ `refx_core::Board` (ปิดครบ 3 ส.ค. 2026)

**เคยเป็นสิ่งที่ขวาง P2-4 ครึ่งหลังอยู่ และไม่มีใครเคยลงมันไว้ใน ROADMAP**

`refx-ui` ยังเก็บสถานะเองเป็น `Vec<QuadInstance>` คู่ขนานกับ `Vec<BoardItem>`
(`app.rs` — มี TODO เขียนไว้ตั้งแต่ P1 ว่า "P2 จะแทนที่ด้วย `Board`/`Item` ตัวจริง")
ตราบใดที่ยังเป็นแบบนี้ ของที่ P2-1…P2-4 สร้างไว้ **ไม่มีทางไปถึงผู้ใช้เลย**

สิ่งที่พร้อมใช้แล้วและรอ caller อยู่:

| ของ | อยู่ที่ | ใครควรเรียก |
|---|---|---|
| `Board` / `Item` / `Command` / `History` | `refx-core` | `RefxApp` แทน `board_items` + `quads` |
| `SpatialIndex` | `refx_core::spatial` | culling ต่อเฟรม + hit-test |
| `SelectTool` (+ `Selection` ที่ editor ถือเอง) | `refx_core::interact` | ตัวแปลง pointer ของ egui |

> ⚠️ **ของที่เทสต์ครบแต่ไม่มีใครเรียก คือสภาพเดียวกับที่ทำให้ `fuzz_decode`
> เป็น stub อยู่ 5 session** ต่างกันแค่ตรงนี้ไม่ได้ *โกหก* ว่าทำงานอยู่
> ถ้ารอบหน้าไม่ได้ต่อให้ครบ ต้องเขียนไว้ตรงนี้ว่าทำไม อย่าปล่อยเงียบ

**ลำดับที่แนะนำ** — ★ **แก้ 2 ส.ค. 2026 หลังไปดูโค้ดจริง:**

> ข้อ 1–3 ที่เคยเขียนไว้ว่าแยกกันได้ **แยกไม่ได้จริง** ต้องลงพร้อมกันเป็นก้อนเดียว
>
> เหตุผล: ตอนนี้ `quads` **เป็นแหล่งความจริงของเรขาคณิตอยู่คนเดียว** — ตำแหน่ง/ขนาด
> ถูกคำนวณสด ๆ ตอน ingest (`app.rs` ราวบรรทัด 767: ตาราง 16 คอลัมน์ + สเกลจาก
> `thumb.source_width`) ไม่ได้เก็บไว้ที่ไหนอีก การให้ `RefxApp` ถือ `Board` ไว้เฉย ๆ
> โดยที่ `quads` ยังคำนวณเองอยู่ = **แหล่งความจริงสองที่** ซึ่งคือปัญหาที่กำลังจะแก้พอดี

1. ✅ **เสร็จแล้ว** (`0cc1e76`) — `Gfx` ถือ `Board`+`History`+`SpatialIndex` ·
   ingest ผ่าน `AddItems` · `quads` สร้างจาก `items_in_z_order()` ที่ `rebuild_quads`
   จุดเดียว · `ItemRender` เป็น side map คีย์ด้วย `ItemId`
   > `Board`/`History` อยู่ใน `Gfx` ซึ่งเป็นบ้านที่ไม่ตรงความหมาย (`Gfx` คือของที่ผูก
   > กับ device) — ไม่ใช่บั๊กเพราะ `recover_device()` แก้ฟิลด์ทีละตัว ไม่ได้สร้าง `Gfx`
   > ใหม่ทั้งก้อน แต่ควรย้ายออกตอน P4-7 multi-board
2. ✅ **เสร็จแล้ว** (`a6ac127`) — `allocate_response` + `SelectTool` + วาดกรอบเลือก/rubber-band
   > `egui_owns_pointer` ที่เดาเอาถูกลบทิ้งแล้ว · pan ย้ายไป **ปุ่มกลาง**
   > (ปุ่มซ้ายเป็นของการเลือก · space จะชนกับ text note ของ P2-11)
3. ✅ **เสร็จแล้ว** (`0f6071c`) — Ctrl+Z / Ctrl+Y / Ctrl+Shift+Z เข้า `History` ·
   id จาก `affected()` ไปตั้ง selection · **กล้องเลื่อนไปหาสิ่งที่ถูกย้อนถ้ามันนอกจอ**
   > ★ §2.2 ปิดครบแล้ว — ของที่ P2-1…P2-4 สร้างไว้มีคนเรียกครบทุกตัว
   > **งานถัดไปคือ P2-5** move / scale (handle) / rotate + snap

### 2.2b ★ audit: enum ที่จะถูก serialize (ทำครั้งเดียว 3 ส.ค. 2026)

docs/02 §2.2.5 บังคับว่า **ทุก enum ที่ลง `.refx` หรือ `cache.sqlite` ต้องมี variant สำรอง**
เจอมาแล้วสองตัวแบบไล่ทีละตัวตอนกำลังเขียนโค้ดทับ — นี่คือการกวาดทั้งหมดพร้อมกัน

**ยังไม่มีตัวไหนถูก serialize จริง** เพราะ `refx-io::dto` ยังเป็นไฟล์ TODO บรรทัดเดียว
ตัวที่ยังไม่มีคนใช้จึง **ไม่ต้องเขียนล่วงหน้า** (โครงเปล่าที่ docs/08 §3.9 ข้อ 2 ห้าม)
— บันทึกไว้ว่ารู้แล้วและจะจัดการยังไงตอนถึงคิว (P4-1 file format)

| enum | ลงไฟล์ผ่าน | มีทางออกแล้ว | แผน |
|---|---|---|---|
| `ImageFormat` | `AssetRef.format` | ✅ `Unknown` | ทำแล้ว (`0cc1e76`) |
| `MissingReason` | `ItemKind::Missing.reason` | ✅ **ถือค่าดิบแล้ว P4-1** | ทำแล้ว (`43b76d5`) |
| `Flip` | `ItemCanvas.flip` | ✅ **ทำแล้ว P4-1** | `Unknown(NonZeroU8)` ถือค่าดิบแล้วเขียนกลับ — §2.9 ยกระดับจาก "ตกเป็น None" เพราะการพลิกคือสิ่งที่ผู้ใช้สั่งเอง |
| `ColorLabel` | `ItemMeta.color_label` | ✅ **ทำแล้ว** | `Unknown(NonZeroU8)` ตั้งแต่ P3-1 · เทสต์เดินเคสผู้ใช้ทั้งเส้นใน P4-1 (เปิด → บันทึกทับ → เปิดใหม่ ค่าอยู่ครบ) |
| `ItemKind` | `Item.kind` | ❌ | ★ **ไม่ต้องมี `Unknown` ของตัวเอง** — kind ที่ไม่รู้จักให้ตกเป็น `Missing { reason: Unknown }` ซึ่งมีอยู่แล้วและถูกต้องตามความหมาย (item ยังอยู่บน board ผู้ใช้เห็นว่ามีของ) |
| `SortKey` | `ArrangeState.sort` | ❌ | ค่าที่ไม่รู้จัก → `AddedAt` (ค่าเริ่มต้น) ไม่มีข้อมูลผู้ใช้หาย |
| `Mode` | `ViewState.mode` | ❌ | ค่าที่ไม่รู้จัก → `Canvas` ไม่มีข้อมูลผู้ใช้หาย |
| `ThumbFormat` | `cache.sqlite` | ✅ (คนละแบบ) | `from_i64` คืน `None` → **นับเป็น cache miss ไม่ใช่ error** ซึ่งถูกต้องกว่ามี variant สำรอง: cache หายไม่ใช่งานหาย |

**ไม่เข้าข่าย** (เป็น error type / runtime เท่านั้น ไม่ลงไฟล์): `ArenaError` `BoardError`
`CmdError` `ClipboardError` `ClipboardContent` `CanvasButton` `CanvasEvent` `MetaField`
`Slot<T>` · `ItemFilter` เป็น struct ของ bool/f32 ไม่ใช่ enum จึงไม่มีปัญหานี้

### 2.2c ✅ กวาดเทสต์ที่ assert เวลานาฬิกา (ทำครั้งเดียว 3 ส.ค. 2026)

`docs/08 §3.9 ข้อ 5b` ห้าม assert เวลานาฬิกาแล้ว — ไล่ทั้ง workspace พร้อมกัน
แทนที่จะรอให้แดงทีละตัว (เกิดมาแล้วสองครั้ง: `lower_priority_value_runs_first`
แล้ว `hashes_100mb_file_quickly`)

**ผล: ไม่เหลือ assert เวลาแล้วสักตัว**

| ที่ | สภาพ | ตัดสิน |
|---|---|---|
| `hash::…::hashes_100mb_file_quickly` | `assert!(elapsed < 200ms)` | ✅ แก้แล้ว (`bddf863`) → พลิกไบต์กลางไฟล์แล้ว hash ต้องไม่เปลี่ยน |
| `spatial::…::hit_testing_a_thousand_items…` | นับ `items_examined`/`cells_visited` | ✅ ถูกอยู่แล้ว — พิมพ์เวลาแต่ไม่ assert |
| `window::…::idle_produces_no_redraw` | นอน 5 วิแล้ว assert **ตัวนับ = 0** | ✅ ถูกอยู่แล้ว — เครื่องช้าแค่ไหนตัวนับก็ไม่ขยับ |
| `device::…::time_trigger_*` | `Instant::now()` เทียบ deadline 60 วิ / instant ที่ประกอบเอง | ✅ ทดสอบ**ตรรกะ deadline** ไม่ใช่ความเร็ว |
| `cache::…` `recv_timeout(5s)` ×3 | รอ IO thread ตอบ | ⚠️ → ขยายเป็น 30 วิ พร้อมคอมเมนต์: มันคือตาข่ายจับ**ค้างจริง** ไม่ใช่การวัดความเร็ว ตั้งพอดีเมื่อไหร่กลายเป็น assert เวลาทันที |
| `pool.rs` `DECODE_TIMEOUT`, `app.rs` bench/wake | โค้ดจริง ไม่ใช่เทสต์ | — |

> เวลารอที่เป็น "ตาข่ายจับค้าง" ให้ตั้ง **หลวม ๆ** เสมอ แล้วปล่อยให้
> `.config/nextest.toml` (ฆ่าที่ 4 นาที) เป็นเพดานจริง — ตัวเลขที่ตั้งพอดีกับ
> เครื่องนักพัฒนาคือ flake ที่รอวันเกิดบน runner ที่แชร์ CPU

### 2.2d ★ audit: ฟิลด์ของ `ItemCanvas` ตัวไหนถึง shader แล้ว (ทำครั้งเดียว 4 ส.ค. 2026)

`rotation` เงียบอยู่ **ตั้งแต่ P0 ถึง P2-5** แล้วเจอเพราะบังเอิญมีคนไปทำฟีเจอร์
ที่ต้องใช้มัน — รูปแบบเดิมครั้งที่หก (`fuzz_decode` stub · `zune-jpeg` · `memmap2` ·
`slotmap` · คอลัมน์ `format` ที่เขียน `0` ตายตัว · `rotation`)
ทุกครั้งคือ **"มีที่ว่างรอไว้ ไม่มีใครเติม ไม่มีอะไรส่งเสียง"** → กวาดทั้ง struct พร้อมกัน

| ฟิลด์ | ถึง GPU แล้ว | ที่ไหน / คิวไหน |
|---|---|---|
| `pos` | ✅ | `quad_for` → `transform` (translation) |
| `size` | ✅ | `quad_for` → `transform` (scale) |
| `rotation` | ✅ | `quad_for` → `Obb::axes()` — **เพิ่งต่อตอน P2-5** (`80b12d9`) |
| `visible` | ✅ | `quad_for` คืน `None` — **เพิ่งต่อตอน audit นี้** ดูด้านล่าง |
| `locked` | — | **ไม่ใช่เรื่องของการวาดโดยตั้งใจ** คุมแค่การแก้ไข (`SelectTool`) |
| `crop` | ✅ | หด `uv_rect` ลงในช่องของ atlas — **ต่อแล้วตอน P2-7** (`80b12d9` ตามด้วย P2-7) |
| `flip` | ✅ | สลับปลายช่วง `uv` (`crop_uv`) — **ต่อแล้วตอน P2-8** · ไม่แตะเรขาคณิต hit-test จึงไม่เพี้ยน |
| `opacity` | ✅ | คูณลง `tint[3]` · pipeline alpha-blend อยู่แล้ว — **ต่อแล้วตอน P2-8** |
| `filter.grayscale` / `.invert` | ✅ | ธงใน `flags` ที่ shader อ่านอยู่แล้ว — **ต่อแล้วตอน P2-8** |
| `filter.brightness` / `.contrast` | ✅ | **เขียนโค้ด shader ใหม่ทั้งฝั่ง** · เป็น **f32 เต็ม** ในฟิลด์ `adjust` ตั้งแต่ `cadf3c4` (docs/04 §3.5) |

**ตัวที่ ❌ ไม่ใช่บั๊ก** มีคิวของมันอยู่แล้วและ**ไม่ต้องรีบเขียนล่วงหน้า**
(โครงเปล่าที่ `docs/08 §3.9` ข้อ 2 ห้าม) — audit นี้แค่ทำให้ "ยังไม่ถึง"
เป็นสิ่งที่มีคนเซ็นรับรองไว้ แทนที่จะเป็นสิ่งที่ไม่มีใครรู้

#### ★ ตัวเดียวที่แก้ทันที: `visible`

เป็น **ความไม่สมมาตร** ไม่ใช่ฟีเจอร์ที่ยังไม่ถึงคิว: hit-test (`spatial.rs`) กับ
กรอบเลือกเคารพ `visible` อยู่แล้ว แต่ `rebuild_quads` ไม่เคารพ → ภาพที่ซ่อนไว้
จะ**ยังขึ้นจอแต่กดไม่โดน** · วันนี้ยังไม่มีใครตั้ง `visible = false` ได้ **แต่ `.refx`
จะพามันมาตอน P4-1** จึงไม่ใช่โครงเปล่า แก้ที่ `quad_for` → `Option<QuadInstance>`

#### ★ กลไกที่ทำให้ฟิลด์ถัดไปส่งเสียงเอง

เทสต์ `no_item_canvas_field_reaches_the_gpu_without_us_knowing` (`refx-ui`) ปิดสองชั้น:

1. **destructure `ItemCanvas` ครบทุกฟิลด์ ไม่มี `..`** → เพิ่มฟิลด์ใหม่เมื่อไหร่
   **คอมไพล์ไม่ผ่าน** จนกว่าจะมีคนตัดสินว่ามันถึง GPU ไหม (หลักการเดียวกับ §4 ข้อ 16)
2. **เทียบพฤติกรรมจริง ไม่ใช่เจตนา** — ตารางข้างบนอยู่ในเทสต์เป็นข้อมูล
   ถ้าวันหนึ่ง P2-7/P2-8 ต่อ `crop`/`flip`/`opacity`/`filter` ให้ถึง GPU
   **เทสต์แดงทันที** บังคับให้มาแก้ตาราง ไม่ใช่ปล่อยให้โค้ดกับความเข้าใจแยกทางกัน

ยืนยันว่าล้มเป็นแล้ว: แก้ตารางให้บอกว่า `opacity` ถึง GPU → แดงพร้อมชี้ชื่อฟิลด์

### 2.4 ✅ P2-5 ปิดครบแล้ว: scale handle · rotate (4 ส.ค. 2026)

ส่วนย้าย `d020219` · **ส่วน handle จบแล้ว** — `docs/03 §2` ทำครบทุกช่อง:

| | ทำอะไร | modifier | สถานะ |
|---|---|---|---|
| Scale | ลาก handle **มุม** | `Shift` = คงสัดส่วน · `Alt` = ยืดจากจุดกึ่งกลาง | ✅ |
| Rotate | ลาก **นอก** handle มุม | `Shift` = สแนป 15° (สแนป**มุมสุดท้าย** ไม่ใช่ระยะที่หมุน) | ✅ |

ของที่เตือนไว้ทำครบแล้วทั้งสี่ข้อ: ขนาดคงที่บนจอ (`พิกเซล × ppp ÷ zoom` เหมือน
`drag_threshold`) · handle มาก่อน hit-test ของภาพ · `obb().corners()` ทำให้ handle
หมุนตามภาพ · index อัปเดตหลัง transform ทุกครั้ง

**ที่อยู่ของโค้ด:** `refx_core::interact::selection_frame()` (กรอบที่ handle เกาะ)
+ `Grab` ใน `SelectTool` · ชั้น UI แค่ส่งระยะเป็น world เข้ามาแล้ววาดสี่เหลี่ยม 4 มุม

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง (ห้ามย้อนโดยไม่อ่านเหตุผล)

1. **เลือกหลายใบ → สเกลกับกรอบรวม และ*บังคับคงสัดส่วน*เสมอ** (ไม่ว่ากด `Shift` หรือไม่)
   กรอบรวมเป็นแนวแกน แต่ภาพข้างในหมุนได้ การยืดแกนเดียวของกรอบแนวแกนต้องการ
   **การเฉือน (shear)** ซึ่ง `ItemCanvas` ไม่มีที่เก็บ (มีแค่ pos/size/rotation)
   ฝืนทำ = ภาพผิดรูปจากที่ผู้ใช้เห็นตอนลาก
2. **ลากผ่านจุดยึดไม่กลับด้าน** — ตัวคูณถูก clamp ที่ `MIN_SCALE`
   `sanitized()` บังคับ `size` เป็นบวกอยู่แล้ว ค่าติดลบจึงกลายเป็นภาพที่ยุบหายเงียบ ๆ
   ไม่ใช่ภาพกลับด้าน · การกลับด้านเป็นหน้าที่ของ `Flip` (ปุ่ม `H`) ที่ผู้ใช้สั่งเอง
3. **ภาพที่ล็อกไว้ไม่มี handle เลย** (`selection_frame` คืน `None`) กรอบเลือกยังวาดปกติ
   handle ที่ลากแล้วไม่มีอะไรเกิดขึ้น ผู้ใช้อ่านว่าโปรแกรมค้าง ไม่ใช่ว่าภาพถูกล็อก
4. **`Shift`/`Alt` อ่านค่า*ทุกเฟรม* ไม่ใช่ตอนกด** — `CanvasEvent::Move` มี `modifiers`
   แล้ว คนกด Shift หลังเริ่มลากเป็นเรื่องปกติ · การเลือก/rubber-band ยังใช้ค่าตอนกด
   เหมือนเดิม (เปลี่ยนกลางคันจะทำให้ชุดที่เลือกไว้กระโดด)
5. **วงหมุนอยู่นอกกรอบเท่านั้น** — ในกรอบใกล้มุมยังเป็นการย้าย ไม่ใช่หมุน
6. **พื้นที่กด handle (7 px) กว้างกว่ารูปที่วาด (9 px ด้าน)** — พลาดแล้วกลายเป็นย้ายภาพ
   เจ็บกว่ากดโดนตอนไม่ได้ตั้งใจ

#### ★ บั๊กที่เจอระหว่างทาง: การกดหนึ่งครั้งกลายเป็น `Press` **สองครั้ง**

`canvas_widget` นับ `response.drag_started_by(Primary)` เป็น "ปุ่มลง" ด้วย
แต่ egui รายงาน `drag_started` ใน**เฟรมหลัง**เคอร์เซอร์ขยับพ้นระยะของ egui เอง
ผลคือการกดจริงหนึ่งครั้งได้ `Press` สองครั้ง ครั้งที่สองอยู่ห่างออกไปหลายพิกเซล
แล้วมันไป**ทับสถานะการกดเดิม**

> **การย้าย (P2-5 รอบก่อน) บังคับอาการนี้ไม่ออก** เพราะจุดที่สองยังอยู่บนภาพเดิม
> จึงได้ `Grab::Move` เหมือนกัน ต่างแค่ origin เลื่อนไปนิดเดียว — มองไม่เห็นด้วยตา
> พอมี handle อาการโผล่ทันที: จับ handle แล้วลาก **กลายเป็นลากกรอบเลือก**
> และของที่เลือกไว้หายไปด้วย

แก้ที่ต้นเหตุ (ตัด `drag_started_by` ออกจาก `primary_pressed`) + เทสต์
`one_physical_press_delivers_exactly_one_press_event` ที่ยิง event จริงเข้า egui
แล้วนับ — ยืนยันแล้วว่าใส่โค้ดเดิมกลับไปมันแดง

#### ★ `quad_for` เพิ่งเริ่มส่งการหมุนให้ GPU จริง ๆ

`transform` เดิมเป็น `[sx, 0, 0, sy, tx, ty]` ตายตัว — **`rotation` ไม่เคยถึง shader เลย**
(ไม่มีใครสังเกตเพราะยังไม่มีทางหมุนภาพ) ตอนนี้ประกอบจาก `Obb::axes()` ซึ่งเป็น
นิยามเดียวกับที่ hit-test ใช้ · เทสต์ `a_rotated_quad_is_drawn_exactly_where_its_obb_says`
บังคับว่าสองที่นี้ต้องตรงกัน ถ้าหลุด ผู้ใช้จะกดที่ที่เห็นภาพแล้วไม่โดน

`flip` ยังไม่ถึง shader เหมือนเดิม — นั่นคือ **P2-8** ไม่ใช่ของที่ลืม

### ★ snap — ตัดสินไปแล้วว่าการ**ย้าย**ไม่มี snap (3 ส.ค. 2026)

`docs/03 §2` ระบุ snap ไว้สองที่เท่านั้น และทั้งคู่เป็น modifier ของ handle
(`Shift` คงสัดส่วน · `Shift` 15°) ส่วน "Snap/align" ในรายการเครื่องมือหมายถึง
**align/distribute ซึ่งเป็น P2-9**

การลากย้ายจึงไม่มี snap โดยตั้งใจ: mood board คือการวางอิสระ การ snap เข้ากริด
ที่ผู้ใช้ไม่ได้ขอ = ภาพเด้งไปที่ที่เขาไม่ได้ตั้งใจ **แย่กว่าไม่มี snap**
สิ่งที่นักวาดอยากให้ตรงกันจริงคือ **ขอบและกึ่งกลางของภาพอื่น** (alignment guide)
ซึ่งเป็นเรื่องเดียวกับ P2-9 → ทำพร้อมกันที่นั่น

`BoardSettings::snap` ยังเป็น `0.0` และยังไม่มีใครเซ็ต — **จงใจ ไม่ใช่ลืม**

### 2.5 ✅ P2-6: z-order + ลบภาพ + **ใครเป็นเจ้าของอายุของ thumbnail** (4 ส.ค. 2026)

| ปุ่ม | ผล |
|---|---|
| `]` / `[` | ขึ้น/ลงหนึ่งชั้น |
| `Shift+]` / `Shift+[` | ขึ้นบนสุด / ลงล่างสุด |
| `Delete` / `Backspace` | ลบสิ่งที่เลือก (ภาพที่ล็อกไว้ไม่ถูกลบ) |

ตรรกะย้ายชั้นอยู่ที่ `refx_core::zorder::reordered()` — **pure function** คืน `Option`
โดย `None` = ไม่มีอะไรเปลี่ยน (อยู่สุดขอบแล้ว) ผู้เรียกจึงไม่สร้างคำสั่งและไม่ขอเฟรม

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **`Shift+[` / `Shift+]` = สุดหัว/สุดท้าย** — `docs/03 §5` ระบุแค่ `[` `]`
   ไม่ใช้ `Ctrl+[` เพราะ Ctrl ถูกจองให้คำสั่งระดับเอกสารหมดแล้ว
   ★ ต้องรับอักขระ **`{` `}`** ด้วย: คีย์บอร์ดส่วนใหญ่ส่ง `{` มาเลยตอนกด Shift+`[`
   ไม่ได้ส่ง `[` พร้อมธง shift — ดูแต่ธงแล้วปุ่มนี้จะตายเงียบบนเครื่องส่วนใหญ่
2. **เลือกหลายใบขยับเป็นก้อน** ลำดับภายในก้อนไม่สลับ และก้อนหยุดเมื่อชนขอบ
3. **`Backspace` = `Delete`** แล็ปท็อปหลายรุ่นไม่มีปุ่ม Delete แยก
4. **ลบไม่ซ้ำตอนกดค้าง** (ต่างจาก `[` `]` ที่ซ้ำได้) — กดค้างหนึ่งวินาทีแล้วลบทั้ง board
   คือหายนะที่ต้องกด Ctrl+Z หลายสิบครั้ง ทั้งที่ผู้ใช้ตั้งใจกดครั้งเดียว
5. **ภาพที่ล็อกไว้ลบไม่ได้** และ**ตัวที่รอดยังถูกเลือกอยู่** หลังลบชุดที่เหลือ

#### ★★ คำตอบของ "ใครเป็นเจ้าของอายุของ thumbnail" (หนี้จาก §6 ตั้งแต่ P2-4)

**`History` เป็นคนถือ** — `ItemRender` ตายเมื่อไม่มีคำสั่งไหนพา item กลับมาได้อีก
API คือ `History::take_forgotten()` ส่วนชั้น UI มี `collect_forgotten()` มารับ

แยกทรัพยากรสองก้อนออกจากกัน **เพราะมันมีอายุคนละแบบ**:

| ทรัพยากร | คืนเมื่อไหร่ | ทำไม |
|---|---|---|
| **ช่องใน atlas (VRAM)** | **ทันทีที่ลบ** | เป็นของหายาก (งบ 384 MB) และเติมกลับได้ฟรีจาก RAM |
| **pixel ของ thumbnail (RAM)** | เมื่อ `History` ลืมถาวร | ถ้าทิ้งตอนลบ undo จะต้อง **decode ใหม่** — ช้า และ**ล้มถาวรถ้าผู้ใช้ลบไฟล์ต้นทางไปแล้ว** (ผิด I-3) |

`ItemRender` เปลี่ยนจาก `uv_rect`+`layer`+`flags` เป็น **`slot: Option<AtlasSlot>`**
ตัวเดียว เพราะ `Atlas::free()` ต้องการช่องเดิมคืน — เก็บแต่ค่าที่คำนวณมาจากมันแล้ว
จะ**คืนช่องไม่ได้เลย** ส่วน `uv_rect`/`layer`/ธง `PLACEHOLDER` คำนวณสดใน `quad_for`

#### ★★★ บั๊กที่เจอตอนรันจริง 400 ภาพ — undo แล้ว "ภาพกลับมาแต่ไม่ขึ้นจอ"

สัญญาแรกของ `take_forgotten()` คือ "รายงานทุก id แล้วให้ผู้เรียกเช็ค `board` เอง"
**ถูกเฉพาะถ้าผู้เรียกเช็คทันที** ซึ่งของจริงไม่ได้ทำ (ingest 400 ภาพไม่ได้ drain เลย
รายการค้างไว้ แล้วไป drain ตอนลบ) พอถึงตอนนั้นภาพชุดนั้นถูกลบไปพอดี
จึงถูกตัดสินว่าตายแล้ว → thumbnail ถูกทิ้ง → **กด Ctrl+Z แล้วภาพกลับเข้า `board`
ครบ 400 แต่ครึ่งหนึ่งไม่ขึ้นจอเลย** ซึ่งผู้ใช้แยกไม่ออกจาก "งานหาย"

แก้โดยย้ายการตัดสินเข้าไปใน `History` ซึ่งมี `board` อยู่ในมือตอนนั้นพอดี
และเงื่อนไขที่**ถูกจริง** มีสองข้อ ไม่ใช่ข้อเดียว:

1. ไม่อยู่บน `board` แล้ว
2. **ไม่มีคำสั่งไหนที่เหลืออยู่พามันกลับมาได้** (`can_still_restore`)

ข้อ 2 คือข้อที่พลาดง่ายที่สุด: `AddItems(X)` ถูกตัดตามเพดาน**หลัง** `X` ถูกลบด้วย
`RemoveItems(X)` ที่ยังอยู่ในสแตก — ข้อ 1 จริงแต่ผู้ใช้ยัง Ctrl+Z ได้อยู่

> ★ **บทเรียน: สัญญาที่ถูกต้องเฉพาะเมื่อ "เรียกทันที" คือกับดัก**
> เทสต์ชุดแรกผ่านหมดเพราะมันเรียก `take_forgotten` ทันทีทุกตัว
> ทำให้สัญญาถูกโดยไม่ขึ้นกับเวลาที่เรียกดีกว่าเขียนคอมเมนต์เตือน
> regression test: `an_item_a_pending_undo_can_restore_is_never_forgotten`

#### ตัวเลขที่วัดได้จริง (400 ภาพ)

| | ก่อนลบ | ลบ 288 ใบ | undo |
|---|---|---|---|
| items | 400 | 112 | 400 |
| VRAM | 32.0 MB (2 layer) | 32.0 MB | **32.0 MB** |
| RAM (working set) | 176 MB | 175 MB | 176 MB |

★ **VRAM ที่ไม่ลดคือผลที่ถูกต้อง** — atlas เป็น pool: `free()` คืน*ช่อง*เข้า free list
ไม่ได้คืน layer (การหด layer = สร้าง texture ใหม่แล้วอัปโหลดใหม่ทั้งหมด)
**สิ่งที่พิสูจน์ว่าช่องถูกคืนจริงคือ undo แล้วยังเป็น 2 layer** — ถ้าช่องรั่ว
การเติม 288 ใบกลับต้องใช้ช่องใหม่ทั้งหมด → 400+288 = 688 ช่อง → **3 layer = 48 MB**

★ **RAM ที่ไม่ลดตอนลบก็คือผลที่ถูกต้อง** — thumbnail ยังต้องอยู่จนกว่าจะ undo ไม่ได้
(undo 288 ใบไม่มี decode job สักงานเดียวใน log) · มันลดตอน `History` ลืม

### 2.6 ✅ P2-7: crop แบบไม่ทำลายต้นฉบับ (4 ส.ค. 2026)

`C` เข้าเครื่องมือครอป · `V` กลับไปเลือก · ลาก handle ครอป · **ดับเบิลคลิกรีเซ็ต**
ปุ่มบน toolbar เป็นตัวเดียวกัน (`Select`/`Crop` เป็นปุ่มสลับจริงแล้ว)

| | เครื่องมือเลือก | เครื่องมือครอป |
|---|---|---|
| handle | 4 มุม (สเกล) | **8 ตัว** — มุม + กลางด้าน |
| สี handle | ฟ้า | **ส้ม** (ต้องต่างด้วยสี ไม่ใช่แค่จำนวน) |
| ลากบนตัวภาพ | ย้าย | **ไม่ทำอะไร** (`Grab::Inert`) |
| ลากนอกมุม | หมุน | ไม่มี |

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **`crop` กับ `pos`/`size` เปลี่ยนพร้อมกันเสมอ** — ถ้าแก้แต่ `crop` ส่วนที่เหลือ
   จะถูกยืดเต็มขนาดเดิม ผู้ใช้เห็นเป็น *ซูมเข้า* ไม่ใช่ *ตัดขอบ*
2. **กลางด้านมีเฉพาะตอนครอป** — "ตัดขอบบนออกนิดเดียว" ไม่ควรต้องไปลากมุม
   แล้วอีกแกนเปลี่ยนตามโดยไม่ได้ตั้งใจ
3. **ลากกลับออกได้จนสุดขอบภาพต้นฉบับ** ไม่ใช่ตัดเข้าอย่างเดียว
4. **ครอปทีละใบเท่านั้น** — กรอบรวมของหลายใบไม่ได้ผูกกับ pixel ของภาพไหนเลย
5. **เครื่องมือครอปย้ายภาพไม่ได้** — พลาดไปโดนกลางภาพแล้วภาพเลื่อนคือสิ่งที่แย่กว่า
   การที่ลากแล้วไม่มีอะไรเกิดขึ้น (แต่ยังคลิกเลือกภาพอื่นได้)
6. **`SetCrop` เป็นคำสั่งของตัวเอง** ทั้งที่เขียน `ItemCanvas` เหมือน `TransformItems`
   — เพราะ `docs/02 §3` กำหนดไว้ · ชื่อในเมนู undo ต้องเป็น "Crop" · และ
   **merge ต้องแยกตามชนิด** ไม่งั้นครอปแล้วย้ายต่อจะยุบเป็น undo เดียว
   (ตัวเนื้อในยืม `TransformItems` ทั้งดุ้น ไม่ก๊อปตรรกะมาไว้สองที่)

#### ★★ สองบั๊กที่**ภาพหน้าจอ**จับได้ ไม่ใช่เทสต์

**1. ดับเบิลคลิกรีเซ็ตแล้วภาพกระโดด** — ตอนแรกคืนขนาดโดยยึดจุดกึ่งกลาง
ภาพทั้งใบจึงเลื่อนไปครึ่งหนึ่งของส่วนที่งอกกลับมา · ที่ถูกคือ**ส่วนที่ยังเห็นอยู่
ต้องไม่ขยับเลย** ขอบที่ถูกตัดงอกกลับ**เฉพาะด้านที่มันถูกตัด**
เทสต์เดิมผ่านเพราะมัน assert แค่ `size` กับ `crop` ไม่ได้ assert ว่าขอบอยู่ที่เดิม

**2. กด `C` แล้วไม่มีอะไรเกิดขึ้น แต่กดปุ่มบน toolbar ได้ปกติ**
`ShellState` ถือ `tool` เป็นสถานะของตัวเอง แล้วชั้นแอปเขียนกลับด้วย
`if shell.tool != gfx.tool { gfx.tool = shell.tool }` ทุกเฟรม → คีย์ลัดที่เขียน
`gfx.tool` โดน**ค่าเก่าของ `shell` เขียนทับกลับทันที** · เป็น `docs/08 §3.9 ข้อ 8.1`
ตรง ๆ: แก้โดยแยก "สิ่งที่แสดง" (`tool`) ออกจาก "สิ่งที่ผู้ใช้ขอ" (`tool_request`)
เจ้าของค่าจริงจึงมีคนเดียวและไม่ต้องพึ่งลำดับการเขียน

> ทั้งสองข้อ**เทสต์ชุดแรกผ่านหมด** — ข้อแรกเพราะ assert ไม่ครบ ข้อสองเพราะ
> ตรรกะแต่ละชิ้นถูกหมด ผิดแค่ตอนประกอบกัน · นี่คือเหตุผลที่ `docs/08 §3.9` ข้อ 5
> บังคับภาพหน้าจอจริง

### 2.7 ✅ P2-8: grayscale / flip / opacity / filter ผ่าน shader (4 ส.ค. 2026)

**★ เกณฑ์หลักผ่านแล้ว: สลับ grayscale ทั้ง board ที่ 1000 ภาพ = 0 texture upload**
(ตัวนับบน status bar: **2536 → 2536** ก่อน/หลังกด `G`)

| ปุ่ม / ที่อยู่ | ผล |
|---|---|
| `G` | grayscale **ทั้ง board** — uniform ตัวเดียว (`view_b.z`) |
| `H` | พลิกแนวนอนของสิ่งที่เลือก (ผ่าน `SetFilter` → undo ได้) |
| Inspector | opacity · grayscale · invert · brightness · contrast · flip 4 แบบ |

#### ★ สองชั้นที่ต้องแยกกันให้ขาด

| | `G` ทั้ง board | ช่องใน Inspector |
|---|---|---|
| อยู่ที่ไหน | `ShellState` (สถานะการมองเห็น) | `ItemCanvas.filter` (เอกสาร) |
| ผ่าน `Command` | ❌ | ✅ `SetFilter` |
| กิน undo / ทำให้ dirty | ❌ | ✅ |
| ราคาตอนสลับ | **เขียน uniform 32 ไบต์ครั้งเดียว** | เขียน instance ของ item ที่เปลี่ยน |

`G` ไม่เข้า undo stack โดยตั้งใจ — ผู้ใช้ที่กด `G` ดูค่าน้ำหนักแล้วกด Ctrl+Z
ต้องได้ **งาน** คืน ไม่ใช่ได้สีคืน (เหตุผลเดียวกับที่ `selection` ออกจาก `Board`)

#### ★ ที่เก็บ brightness/contrast — ทวงที่คืนจาก `tint` (ตัดสิน `docs/04 §3.5`)

รอบแรกยัดลง 16 บิตบนของ `flags` (ละเอียด 1/127) เพราะ `QuadInstance` ตรึงที่ 64 ไบต์
**เจ้าของโปรเจกต์ตัดสินให้ดีกว่านั้น:** `tint: [f32; 4]` ใช้ 16 ไบต์เก็บตัวคูณสีช่วง 0..1
ทั้งที่ปลายทางเป็น framebuffer 8 บิตต่อช่อง → เปลี่ยนเป็น `[u8; 4]` (`Unorm8x4`)
**คืนที่มา 12 ไบต์** พอให้ `brightness`/`contrast` เป็น **f32 เต็ม** และยังเป็น 64 ไบต์เป๊ะ

| ฟิลด์ | ไบต์ | offset |
|---|---|---|
| `transform` | 24 | 0 |
| `uv_rect` | 16 | 24 |
| `tint` (`[u8; 4]`) | 4 | 40 |
| `layer` · `flags` | 8 | 44 |
| `adjust` = `[brightness, contrast]` | 8 | 52 |
| `reserved` | 4 | 60 |

- **shader ไม่ต้องแก้ชนิดเลย** — `Unorm8x4` มาถึงเป็น `vec4<f32>` เหมือนเดิม
  GPU ขยาย 0..255 → 0.0..1.0 ให้ฟรี
- `adjust` ไปที่ **location 7** เพราะ location 6 เป็นของ vertex buffer มุม quad อยู่แล้ว
- กับดักเดิมหายไปด้วย: ค่ากลางของ `adjust` คือ **ศูนย์ตรง ๆ** ไม่ใช่ 128
  (ตอนแพ็กบิต การลืมใส่ค่ากลางแปลว่า brightness = −1.0 → ภาพดำทั้งจอ)

> **ข้อจำกัดที่ยอมรับ:** `tint` เกิน 1.0 ไม่ได้อีก (ไม่มี HDR multiply)
> ไม่กระทบเพราะการทำให้สว่างกว่าต้นฉบับเป็นหน้าที่ของ `brightness`

**เทสต์ที่พิสูจน์ว่าได้ความละเอียดกลับมาจริง** — `the_reclaimed_bytes_actually_buy_finer_brightness_steps`
ยิงสองค่าที่ **เคยตกถังเดียวกันตอนแพ็ก 8 บิต** (`25/127 ± 0.0035`) แล้ว pixel ต้องต่างกัน

> ★ **เทสต์รุ่นแรกของข้อนี้ผ่านทั้งที่ยังแพ็ก 8 บิตอยู่** เพราะเลือกช่วงกว้างเกินหนึ่งถัง
> — negative control จับได้ ถ้าไม่ลองทำให้มันพัง เราจะเชื่อผิดว่าการเปลี่ยน layout
> ได้ผล ทั้งที่เทสต์ไม่ได้วัดสิ่งนั้นเลย

**และมีเทสต์คุม layout เอง** — `every_attribute_points_at_the_field_it_claims_to`
เทียบ offset ที่ `vertex_attr_array!` คำนวณ กับ offset จริงของฟิลด์ทุกตัว
มาโครไล่บวกจากขนาด format ที่ประกาศมาก่อนหน้า **ไม่ได้อ่านจาก struct** สลับฟิลด์
เมื่อไหร่สองฝั่งจะเงียบ ๆ ไม่ตรงกัน แล้ว GPU จะอ่าน `tint` เป็น `layer` โดยไม่มี error

#### ★★ สองบั๊กชนิด "ทุกชิ้นถูก ประกอบผิด" ที่เจอตอนรันจริง

**1. inspector ทับสิ่งที่คีย์ลัดเพิ่งเปลี่ยน** — เดิมชั้น `app` อ่าน `shell.appearance`
(ค่าที่เติมไว้ *เฟรมก่อน* เพื่อ **แสดง**) กลับไปเขียนลง board ทุกเฟรม
กด `H` → คีย์ลัดตั้ง `flip = Horizontal` → เฟรมเดียวกัน inspector เขียนค่าเก่าทับ
→ **ภาพพลิกแล้วเด้งกลับทันที** ไม่มี error ไม่มี log และ unit test ทุกตัวผ่าน

แก้ตาม `docs/08 §3.9` ข้อ 8.1: แยก **"ค่าที่แสดง"** (`appearance`) ออกจาก
**"สิ่งที่ผู้ใช้ขอ"** (`appearance_edit`) — ตัวหลังถูกเขียนเฉพาะตอน widget รายงาน
`.changed()` และถูก `take()` ทันที **เจ้าของมีคนเดียว ไม่ต้องพึ่งลำดับ**
เทสต์: `the_inspector_asks_for_nothing_when_nobody_touches_it`

**2. `flip` ทำงานตอนซูมออก แต่ไม่ทำงานตอนซูมเข้า** — เส้นทางวาดมีสองทาง
(atlas กับ working texture) และ uv ของทาง working ถูกคำนวณด้วย**สูตรที่เขียนแยก**
ซึ่งลืมใส่ `flip` · แก้โดยให้ทั้งสองทางเรียก `crop_uv()` ตัวเดียวกัน
(working texture = "ช่อง" ที่กินทั้ง texture คือ `[0,0,1,1]`)
เทสต์: `flipping_reaches_both_draw_paths_not_just_one`

> ★ ทั้งสองข้อจับได้เพราะ**รันของจริงแล้ววัด pixel** ไม่ใช่เพราะเทสต์ —
> และข้อ 2 ยังหลอกตาได้อีกชั้นเพราะลายของภาพทดสอบเกือบสมมาตร
> **ต้อง diff pixel เป็นตัวเลข** ถึงจะรู้ว่ามันไม่ได้พลิกจริง (ตาอ่านว่า "ก็เหมือนจะพลิกนะ")

#### เทสต์ที่พิสูจน์ว่า pixel เปลี่ยนจริง ไม่ใช่แค่ธงถูกตั้ง

`refx-render` มีเทสต์ที่ **เรนเดอร์ออก texture แล้วอ่านสีกลับมา** 5 ตัว
(`brightness_and_contrast_actually_change_the_rendered_pixel` ฯลฯ) รันบน GPU จริง
ทั้งสอง runner ของ CI · ข้อนี้สำคัญเพราะ `brightness`/`contrast` เขียน shader ใหม่ทั้งฝั่ง
ถ้าทำแต่ฝั่ง Rust **ประตู audit จะยังเขียว** (ธงเปลี่ยนจริง) แต่ภาพไม่ขยับเลย

### 2.8 ✅ P2-9: align / distribute + ไกด์ (ยืนยันบนของจริง 5 ส.ค. 2026)

> เทสต์ 15 ตัวใน `refx_core::align` ผ่านมาตั้งแต่ `7265ebc` แต่นั่นคือหลักฐานว่า
> **แต่ละชิ้นถูก ไม่ใช่ว่าประกอบถูก** — สามข้อด้านล่างคือการรันของจริง

#### ★★★ "ลากแล้วภาพไม่ขยับ" **ไม่ใช่บั๊กของโค้ด — สคริปต์ไม่เคยส่งคลิกถึงแอปเลย**

session ก่อนสงสัยว่ากลับไปเป็นบั๊ก "กดหนึ่งครั้ง = `Press` สองครั้ง" ของ P2-5 อีก
**ไม่ใช่** ต้นเหตุอยู่ที่ `scratchpad/drive.ps1` ล้วน ๆ:

`SetForegroundWindow` **ถูก Windows ปฏิเสธ** เมื่อโปรเซสที่เรียกไม่ได้อยู่ foreground เอง
สคริปต์เดิมไม่ได้ดูค่าที่คืนมา → หลังจากนั้น `SetCursorPos`/`mouse_event` ทุกครั้ง
ตกไปที่**หน้าต่างที่บังอยู่ข้างบน** และ `CopyFromScreen` ก็ถ่ายพิกเซลของหน้าต่างนั้น
ออกมาเป็นไฟล์ที่ดูเหมือนหลักฐาน · RefX **ไม่เคยเห็นการกดพวกนั้นเลยสักครั้ง**

ยืนยันซ้ำได้: รอบนี้เจออาการเดิมทันทีในภาพแรก (ภาพที่ถ่ายได้เป็นหน้าต่างของ
Claude Code ไม่ใช่ RefX) พอบังคับ foreground ด้วย `AttachThreadInput` แล้ว
**ทุกอย่างทำงานถูกตั้งแต่ครั้งแรก** ไม่ต้องแก้โค้ดแอปแม้แต่บรรทัดเดียว

| | ผล |
|---|---|
| คลิกบนภาพหนึ่งครั้ง | กรอบเลือก **หนึ่งกรอบ** ที่ภาพนั้น (`SELECT` 212 px ที่ x 385..520) |
| ลาก (452,386) → (512,436) | ภาพขยับ **+60,+50 เป๊ะ** ไม่มี rubber-band ไม่มีของที่เลือกไว้หาย |

→ บั๊ก `drag_started_by` ของ P2-5 **ยังถูกปิดอยู่** เทสต์
`one_physical_press_delivers_exactly_one_press_event` ยังคุมอยู่ตามเดิม

> ★★ **บทเรียน: harness ที่ล้มเงียบแย่กว่าไม่มี harness** (`docs/08 §3.9` ข้อ 2)
> — และครั้งนี้มันไม่ได้แค่เงียบ มันผลิต**หลักฐานปลอม** ออกมาเป็นไฟล์ PNG
> ทางแก้อยู่ใน `scripts/ui-drive.ps1` แล้ว: ตรวจ `GetForegroundWindow()`
> ก่อนทุกขั้นที่คลิก/พิมพ์/ถ่ายภาพ และ **exit 1 แทนที่จะถ่ายภาพที่โกหก**

#### 1. ไกด์โผล่ตอนลาก และภาพ snap เข้าไปจริง

เล็งเคอร์เซอร์ให้ **เยื้องจากเส้น 4 px** แล้วปล่อย · ภาพกว้าง 125 px ขอบซ้ายของ
ภาพเป้าหมายอยู่ที่ x=548

| เล็งเยื้องไป | ขอบซ้ายที่ได้ | เส้นไกด์ |
|---|---|---|
| **+4 px** | **548 เป๊ะ** (ถูกดึงกลับ 4 px) | เส้นตั้งที่ x=547 ยาว y 344..602 — คลุมทั้งภาพที่ลากและภาพที่มันตรงด้วย |
| **+20 px** (นอกระยะ `GUIDE_SNAP_PX` = 6) | **568** = ตรงที่เคอร์เซอร์อยู่ ไม่ถูกดึง | **ไม่มีเลย** |

แถวที่สองคือ **negative control** (`docs/08 §3.9` ข้อ 1): ถ้าไม่ลองให้มันไม่ snap
เราจะแยกไม่ออกว่า −4 px มาจากไกด์หรือมาจากการปัดเศษ ·
ปล่อยเมาส์แล้ว **เส้นหายทันที** (`GUIDE stroke: none`) ตรงกับเทสต์
`guides_appear_while_dragging_and_vanish_on_release`

#### 2. ปุ่มทั้งแปดกดจริงในแอปแล้ว — และให้ผล**ต่างกันจริงทั้งหก**ทิศ

ฉากทดสอบ: สามใบขนาดไม่เท่ากัน (สองใบ 125×85 · หนึ่งใบ 83×56 ที่ย่อด้วย handle จริง)
กระจายกันอยู่ · กรอบรวม L=268 R=902 T=258 B=662 → กึ่งกลาง (585, 460)

| ปุ่ม | ผลที่วัดได้ | ถูก |
|---|---|---|
| `L` | ขอบซ้ายทั้งสาม = **268** · y ไม่ขยับ | ✅ |
| `C` | `cx` ทั้งสาม = **585** = กึ่งกลางกรอบรวม | ✅ |
| `R` | ขอบขวาทั้งสาม = **902** | ✅ |
| `T` | ขอบบนทั้งสาม = **258** · x ไม่ขยับ | ✅ |
| `M` | `cy` ทั้งสาม = **460** | ✅ |
| `B` | ขอบล่างทั้งสาม = **662** | ✅ |
| `↔` | หัวท้ายไม่ขยับ · ช่องว่าง **150 / 151 px** | ✅ |
| `↕` | หัวท้ายไม่ขยับ · ช่องว่าง **91 / 91 px** | ✅ |

★ **ขนาดต้องไม่เท่ากันถึงจะเป็นหลักฐาน** — ถ้าทั้งสามใบกว้างเท่ากัน `L`/`C`/`R`
จะให้ผลเหมือนกันเป๊ะทั้งสามปุ่ม แล้วภาพหน้าจอจะพิสูจน์ไม่ได้ว่าปุ่มไม่ได้ต่อสายมั่ว

#### 3. 1000 ภาพแล้วลากหนึ่งใบ — frame time ไม่ตก

วัดสองรุ่นบนเครื่องเดียวกันในเซสชันเดียว (`docs/08 §3.9` ข้อ 7) ผ่าน worktree
ของ `cadf3c4` (ก่อน P2-9) · `--bench-seconds` · `PresentMode::Immediate` · p50 (ms)
· **กดค้างไว้ทั้งช่วงวัด** เพราะ `app.rs` ส่ง `CanvasEvent::Move` **ทุกเฟรม**
ที่ปุ่มยังลงอยู่ ไม่ใช่เฉพาะตอนเคอร์เซอร์ขยับ — นั่นคือกรณีที่แพงที่สุดของการหาไกด์

| | ก่อน (`cadf3c4`) | หลัง (P2-9) | ต่าง |
|---|---|---|---|
| ไม่ลาก | 0.354 · 0.358 · 0.364 · 0.368 · 0.379 → **0.364** | 0.380 · 0.385 · 0.388 · 0.395 · 0.413 → **0.388** | +6.6% |
| **ลากค้าง** | 0.461 · 0.469 → **0.465** | 0.494 · 0.495 → **0.4945** | +6.3% |
| **ส่วนต่างที่การลากเพิ่ม** | **0.101 ms** | **0.107 ms** | **+0.006 ms** |

★ **ตัวเลขที่ตอบคำถามคือแถวล่างสุด** — ต้นทุนของการลากเพิ่มขึ้น **6 µs/เฟรม**
ที่ 1000 ภาพ ซึ่งอยู่ใต้ noise (รุ่นเดียวกันแกว่างกันเอง 7–9% อยู่แล้ว)
p99 ตอนลาก 1.08 ms จากเพดาน 16 ms · **การหาไกด์ไม่ได้ทำให้เฟรมตก**

#### ★★ แล้ว +6.6% ตอน**ไม่ลาก**มาจากไหน — พิสูจน์แล้วว่าคือ **ปุ่มแปดตัวบน toolbar**

เส้นทาง idle ไม่มีทางเรียก `snap_to_neighbours` เลย (ไม่มีปุ่มไหนลงอยู่) ตัวเลขจึง
ต้องมาจากที่อื่น · ทดสอบด้วยการ **ถอดเฉพาะลูป `ARRANGE_BUTTONS` ออกจาก HEAD**
แล้ววัดใหม่ (build ชั่วคราว ไม่ commit):

| build | idle p50 |
|---|---|
| `cadf3c4` (ก่อน P2-9) | 0.364 |
| HEAD | 0.369 · 0.368 · 0.375 → **0.369** |
| **HEAD ที่ถอดปุ่มแปดตัวออก** | 0.353 · 0.355 · 0.359 → **0.355** |

ถอดสาเหตุที่สงสัยออกแล้วอาการหายหมดและ**ต่ำกว่าเส้นฐานเดิมด้วยซ้ำ** →
ต้นทุนคือ **~0.014 ms/เฟรม = ~1.8 µs ต่อปุ่ม** ของ egui widget ที่ถูกวาดทุกเฟรม
คิดเป็น **0.18% ของงบ 8 ms** และอยู่ในเกณฑ์ `docs/08 §4` ข้อ 6 (ไม่เกิน 10%)

> ★ ตรงกับสิ่งที่ §3 บันทึกไว้อยู่แล้วว่า ที่ 1000 ภาพ **98% ของต้นทุนคือ egui shell
> + present** ไม่ใช่ quad pipeline — ปุ่มที่เพิ่มบน toolbar จึงมีราคาที่วัดได้
> ทุกครั้ง ไม่ว่าผู้ใช้จะกดมันหรือไม่ · **ไม่ใช่เหตุให้ถอด** แต่เป็นตัวเลขที่ควรรู้
> ก่อนจะเติมปุ่มชุดถัดไปเข้าไปอีก

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **เพิ่ม `scripts/ui-drive.ps1` + `scripts/ui-measure.py` เข้า repo** — เดิมมันอยู่ใน
   scratchpad ซึ่งตายไปพร้อม session · `docs/08 §3.9` ข้อ 5 บังคับภาพหน้าจอจริง
   ทุก task ที่ผู้ใช้มองเห็น เครื่องมือที่ผลิตหลักฐานนั้นจึงควรมีอายุเท่าโปรเจกต์
   ไม่ใช่เท่า session · **ไม่ได้ต่อเข้า CI** (ต้องมีหน้าจอจริง) และไม่ได้เพิ่ม dependency
   ของ Rust เลย — `ui-measure.py` อ่าน PNG ด้วย stdlib ล้วน
2. **dataset ของข้อ 3 ใช้ 800×600 ไม่ใช่ 4000×3000** เหมือนใน §3 — สิ่งที่วัดคือ
   ต้นทุนต่อเฟรมเมื่อ board มี 1000 item ซึ่งไม่ขึ้นกับขนาดไฟล์ต้นทาง (thumbnail
   ในatlas ขนาดเท่ากันหมด) · ทำให้ decode เสร็จใน ~300 ms จึงกินช่วงวัดน้อย
3. **วัดแบบ "กดค้าง" ไม่ใช่ "ลากไปมา"** — ให้ครอบคลุมทุกเฟรมของช่วงวัด
   ถ้าขยับเมาส์เป็นช่วง ๆ เฟรมส่วนใหญ่จะไม่ได้เดินเส้นทางที่กำลังจะวัด

#### สิ่งที่ทำเสร็จแล้วและมีเทสต์คุม

`refx_core::align` เป็น pure function ทั้งไฟล์เหมือน `zorder`:

- `aligned()` ใช้ **กรอบรวมของสิ่งที่เลือก** เป็นเป้า ไม่ใช่ขอบ board
- `distributed()` ทำให้ **ช่องว่างระหว่างขอบ** เท่ากัน (ไม่ใช่ระยะระหว่างกึ่งกลาง)
  เพราะภาพขนาดต่างกันมากเป็นเรื่องปกติบน mood board · หัวท้ายเป็นสมอไม่ขยับ
- ทั้งคู่คืน `None` เมื่อไม่มีอะไรเปลี่ยน → ไม่กิน undo เปล่า (I-1)
- `snap_to_neighbours()` ค้นด้วย **แถบบางสองแถบตัดด้วย viewport**

> ★ ดีไซน์แรกค้นแค่ "กรอบที่ลากขยายออก" ซึ่ง **ใช้ไม่ได้** — เทสต์จับได้:
> ภาพที่ควรตรงกันมัก **ไม่ทับกันเลย** (ลากภาพลงมาให้ขอบซ้ายตรงกับภาพข้างบน)
> และไกด์ที่มองไม่เห็นก็ไม่มีประโยชน์ การตัดด้วย viewport จึงถูกทั้งความหมายและต้นทุน

#### ★ `BoardSettings::snap` — ลบทิ้งแล้ว (ตัดสิน 4 ส.ค. 2026)

มันถูกนิยามเป็น "ระยะ snap เข้ากริด หน่วย world" และเป็น `0.0` มาตลอด
P2-5 ตัดสินไปแล้วว่า **การลากย้ายไม่มี snap เข้ากริด** ส่วนระยะของไกด์เป็น
**พิกเซลบนจอ** และเป็นค่าความรู้สึกของเครื่องมือ ไม่ใช่ข้อมูลของเอกสาร
→ อยู่กับค่าคงที่ของ interaction (`GUIDE_SNAP_PX`) ไม่ใช่ใน `BoardSettings`

เก็บฟิลด์ที่ชื่อและหน่วยไม่ตรงกับสิ่งที่ระบบทำจริงไว้ = กับดักของคนอ่านรอบหน้า

#### ★★ ฟอนต์ที่ฝังไม่มี glyph ที่คิดว่ามี — ตรวจก่อนใช้เสมอ

เลือกสัญลักษณ์เส้นตาราง (`┣ ┫ ┳ ┻ ┃ ━`) ให้ปุ่ม align เพราะดูเหมาะที่สุด
ผลคือปุ่มขึ้นเป็น **กล่องสี่เหลี่ยมว่าง** บนจอจริง — ไม่มี error ไม่มี log
เห็นได้ทางเดียวคือเปิดโปรแกรมแล้วดู (docs/03 §0: ไม่ฝัง CJK เพราะเพดาน binary)

**ผลการตรวจจริงด้วย `has_glyph`:**

| มี | ไม่มี |
|---|---|
| `↔` U+2194 · `↕` U+2195 · ASCII ทั้งหมด | `←` `→` `↑` `↓` (U+2190–2193) · box drawing ทั้งชุด · `⇤` `⇥` · `⊢` `⊣` `⊤` `⊥` |

> ★ **เดาไม่ได้เลย** — ลูกศรสองหัวมี แต่ลูกศรหัวเดียวไม่มี
> (Noto Sans Thai มี U+2194/2195 ให้ ส่วน egui default font ไม่มีลูกศรเลย)

→ ป้ายปุ่มตอนนี้เป็น `L C R T M B ↔ ↕` และย้ายไปอยู่ใน `shell::ARRANGE_BUTTONS`
เพื่อให้เทสต์ `arrange_button_labels_all_have_glyphs` ไล่ตรวจได้ทุกครั้งที่ build
**เพิ่ม/เปลี่ยนป้ายเมื่อไหร่ เทสต์จะแดงทันทีถ้าฟอนต์ไม่มี glyph นั้น**

### 2.9 ✅ P2-10: color picker + ไม้บรรทัด (5 ส.ค. 2026)

| ปุ่ม | เครื่องมือ | ผล |
|---|---|---|
| `I` | Picker | จิ้มหนึ่งครั้ง = อ่านสีของ **pixel ต้นฉบับ** หนึ่งจุด · สี + hex ขึ้นบน status bar |
| `M` | Measure | ลากวัด → ระยะ · กว้าง×สูง · มุม เป็น **world unit** |

#### ★★ ทำไม picker ต้อง decode ไฟล์ใหม่ ทั้งที่ thumbnail อยู่ใน RAM แล้ว

ROADMAP บังคับว่า **"อ่านจาก pixel ต้นฉบับ ไม่ใช่จากจอ"** และมีสองทางที่ผิดได้:

1. **อ่านจากจอ** → ได้สีที่ผ่าน grayscale/brightness/contrast/invert/opacity
   และการผสมพื้นหลังมาแล้ว · นักวาดเปิด `G` ดู value แล้วจิ้มดูสีจริงเป็นเรื่องปกติ
   ถ้าอ่านจากจอเขาจะได้ **สีเทาทุกครั้ง**
2. **อ่านจาก `ItemRender::thumb`** (ล่อใจที่สุด เพราะอยู่ใน RAM แล้ว) → thumbnail
   คือ 128×128 ที่ถูก **บีบเป็นจัตุรัส** และเฉลี่ยมาแล้ว · ภาพ 4000×3000 หนึ่ง pixel
   ของ thumb = **31×23 pixel ของจริง** สีที่ได้จึงเป็นค่าเฉลี่ยของบริเวณ
   ซึ่ง **ดูสมเหตุสมผลจนกว่าจะเอาไปเทียบกับต้นฉบับ**

→ `JobTarget::Sample { u, v }` decode ไฟล์จริงบน worker แล้วอ่าน pixel เดียว
ใช้เกราะ · เพดาน RAM · timeout · cancellation ชุดเดียวกับเส้นทางปกติทั้งหมด (I-2, I-4)

★ ราคาคือ **decode หนึ่งครั้งต่อการจิ้มหนึ่งครั้ง** จึงบังคับให้ picker ทำงาน
**ตอนกด ไม่ใช่ตอนลาก** — ถ้าทำงานทุกเฟรมที่ปุ่มยังลงอยู่ จะสั่ง decode ภาพ 4000²
รัว ๆ ทั้งที่ผู้ใช้ขอสีจุดเดียว · เทสต์ `holding_the_picker_down_asks_only_once` คุมไว้

#### ตัวเลขที่ยืนยันบนของจริง (diff เป็นตัวเลข ตาม `docs/08 §3.9` ข้อ 9)

| สภาพ | สีบนจอ | สีที่ picker คืน | สีในไฟล์ |
|---|---|---|---|
| ปกติ | `#E23A2F` | `#E23A2F` | `#E23A2F` |
| **grayscale ON** | **`#7A7A7A`** | **`#E23A2F`** ✅ | `#E23A2F` |
| **opacity 50%** | **`#AB4042`** | **`#E23A2F`** ✅ | `#E23A2F` |

สองแถวล่างคือข้อพิสูจน์ทั้งหมด: สีบนจอต่างไปคนละเรื่อง แต่สีที่คืนยังตรงกับไฟล์เป๊ะ
· และจิ้มสี่ช่องของภาพ quadrant ได้สีตรงช่องทุกครั้ง (TL `#FF0000` · BR `#FFFF00`)
ซึ่งพิสูจน์ว่าการแปลงพิกัดลงจุดถูกที่ ไม่ใช่แค่ได้สีอะไรสักสีจากภาพนั้น

#### ★★ สูตร crop+flip ถูกยุบเหลือที่เดียว

`pick::source_span()` เป็นเจ้าของสูตร ส่วน `refx-ui::crop_uv` **เรียกมัน**
เพราะ picker ต้องเดินย้อนทางเดียวกับที่ shader วาด · ถ้าเขียนแยกกันสองที่
วันที่มีคนแก้ข้างเดียว **ผู้ใช้จะจิ้มตรงที่เห็นสีหนึ่งแล้วได้อีกสีหนึ่ง**
โดยไม่มี error ที่ไหน (รูปแบบเดียวกับบั๊ก `flip` ที่ไม่ถึงทาง working texture ตอน P2-8)

> ★ เทสต์ `the_picker_reads_the_same_pixel_the_shader_draws` เฝ้า **การย้ายกลับ**
> (หมุนกลับ · สเกลกลับ · ทิศของ lerp) ซึ่งยังเขียนสองที่ · ส่วนตัวสูตรเองเพี้ยน
> ไม่ได้ **เชิงโครงสร้าง** แล้ว — ลองถอด `flip` ออกจาก `source_span` แล้วเทสต์ยังเขียว
> เพราะทั้งสองฝั่งพังพร้อมกัน **ซึ่งถูกต้อง** (แข็งแรงกว่าการมีเทสต์คอยจับ)
> จดไว้ในเทสต์แล้วว่าอันไหนจับได้อันไหนจับไม่ได้ — อย่าเชื่อว่ามันคุมทุกอย่าง

#### ★ ไม้บรรทัด: เก็บ world ไม่ใช่พิกเซลบนจอ

วัดครั้งเดียวแล้วซูม **46% → 62% → 85%** ตัวเลขเป็น **128.2 u ทั้งสามครั้ง**
ขณะที่สิ่งที่วัดกว้าง **58 → 79 → 108 px** บนจอ · ถ้าเก็บพิกเซล ตัวเลขจะเปลี่ยน
ทุกครั้งที่ซูมแล้วมันไม่มีความหมายเลย (วัดตอนซูม 50% แล้วซูมเข้าเทียบต่อไม่ได้)

ไม่ผ่าน `Command` และไม่กิน undo — เป็นสถานะของ *เครื่องมือ* ไม่ใช่ของ *เอกสาร*
เหมือนกล้องและการเลือก (docs/02 §2.9)

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **`hex()` เป็น 6 หลัก ไม่ใส่ alpha** — ช่อง hex ของ Photoshop / Clip Studio /
   Krita รับ 6 หลัก การใส่ 8 หลักทำให้วางแล้วขึ้นว่าค่าผิด
2. **ภาพจาก clipboard จิ้มสีไม่ได้** และบอกตรง ๆ ว่าอ่านไม่ได้ — มันไม่มีไฟล์ให้
   กลับไปอ่าน (หนี้เดิมใน §6) · ยอมบอกว่าทำไม่ได้ ดีกว่าแอบตอบด้วยสีที่เฉลี่ยจาก thumbnail
3. **ไม้บรรทัดค้างอยู่หลังปล่อยเมาส์** และหายเมื่อสลับเครื่องมือ — ถ้าหายตอนปล่อย
   ผู้ใช้ต้องลากค้างไว้แล้วเหลือบไปอ่าน status bar พร้อมกัน

#### ★★ ที่ spec เขียนไว้แล้วยัง **ทำไม่ครบ**: "คัดลอก hex"

`docs/03 §2` เขียนว่า picker ต้อง "อ่านสีจากภาพต้นฉบับ **+ คัดลอก hex**"
— ส่วนคัดลอกยังไม่ได้ทำ **โดยตั้งใจ ไม่ใช่ลืม**

การเขียน clipboard บล็อกได้เป็นวินาทีถ้าโปรแกรมอื่นถือมันค้างอยู่ (I-2 ห้ามบน UI thread)
· ทางที่มีอยู่ทางเดียวคือ `ClipboardReader` ซึ่งเป็น **ตัวอ่าน** บน decode worker
การขยายมันเป็นตัวเขียนแตะทั้ง `refx-core` / `refx-platform` / `refx-ui` และเป็น
การตัดสินใจเรื่อง threading ซึ่ง `CLAUDE.md` บอกให้ **ถามก่อน** → ยังไม่ทำ
ตอนนี้ hex อยู่บน status bar ให้อ่านและพิมพ์ตามได้

### 2.10 ✅ P2-11: โน้ตข้อความ — **P2 ปิดครบ** (5 ส.ค. 2026)

`T` เข้าเครื่องมือโน้ต · คลิก **ที่ว่าง** เพื่อวาง · แก้ข้อความในช่อง Inspector

โน้ตเป็น **item เต็มตัว** เหมือนภาพ: อยู่บน board มี z-order ย้าย/ย่อ/ขยาย ลบ
undo ได้ตามปกติ · ต่างกันแค่ **ไม่มี quad และไม่มีช่องใน atlas** เพราะมันเป็นข้อความ
ไม่ใช่ pixel — `rebuild_quads` ข้ามมันเองเพราะไม่มี `render_state`
ชั้น canvas จึงเป็นที่เดียวที่วาดมัน ด้วย egui ที่มี font atlas อยู่แล้ว

#### ★★★ ประตูที่สำคัญกว่าตัวฟีเจอร์: คีย์ลัดต้องเงียบตอนผู้ใช้พิมพ์

ตั้งแต่มีช่องข้อความ ตัวอักษรที่ผู้ใช้พิมพ์คือ **ข้อมูล** ไม่ใช่ **คำสั่ง**
แต่คีย์ลัดทุกตัวถูกจัดการที่ชั้น **winit** ซึ่งไม่เคยรู้เลยว่า egui กำลังรับ keyboard
อยู่หรือไม่ · **ก่อนหน้านี้ไม่มีอาการเพราะยังไม่มีช่องข้อความสักช่องในโปรแกรม**

ถ้าไม่กั้น การพิมพ์คำว่า `vict` จะสลับไปเครื่องมือเลือก เปิดครอป จิ้มสี แล้วเปิด
โน้ตอีกใบ · `Delete` จะลบภาพที่เลือกอยู่ · `Ctrl+Z` จะย้อน **เอกสาร** แทนตัวอักษร

→ กั้นทั้งก้อนด้วย match arm เดียวที่ครอบ `KeyboardInput` **ทุก** event เมื่อ
`egui_wants_keyboard_input()` และวางไว้ **ก่อน** arm อื่น
**คีย์ลัดที่จะเพิ่มทีหลังจึงถูกคุ้มครองอัตโนมัติ ไม่ต้องมีใครจำไปเติมเงื่อนไข**

> ★ เป็นเหตุผลเดียวกับที่ P2-4 เลือก **ปุ่มกลาง** ให้ pan แทน space (§2.2 ข้อ 2) —
> space เป็นอักขระจริงในโน้ต · ตอนนั้นเป็นการคาดการณ์ ตอนนี้เป็นของจริงแล้ว

ยืนยันบนของจริง — พิมพ์ `vict mgh]` แล้วกด `Delete` ในช่องโน้ต:

| | ผล |
|---|---|
| ตัวอักษร | ลงครบทุกตัว **รวมทั้ง space** |
| เครื่องมือ | ยังเป็น Note (`v`/`i`/`c`/`t`/`m` ไม่สลับ) |
| จำนวน item | ยัง **4 items** (`Delete` ไม่ลบโน้ตที่เลือกอยู่) |
| `]` · `g` · `h` | ลงเป็นอักขระ · ไม่ย้ายชั้น · ไม่ toggle grayscale · ไม่พลิกภาพ |

#### ★ undo ของการพิมพ์

`EditText` **merge ได้เฉพาะ item เดียวกัน** — พิมพ์รัว ๆ ในใบเดียวยุบเป็น undo
ขั้นเดียว (ไม่งั้นประโยคเดียวกิน undo stack ทั้งสแตกจนคำสั่งอื่นถูกตัดตามเพดาน 200 ขั้น)
แต่ **ห้ามข้ามใบ** ไม่งั้นแก้โน้ต A แล้วแก้ B จะย้อนทีเดียวเสียทั้งสองใบ

ยืนยันจริง: `Ctrl+Z` ครั้งแรก → 4 items + ข้อความว่าง (**ทั้งประโยคเป็นขั้นเดียว**)
· ครั้งที่สอง → 3 items (โน้ตหาย) · `Ctrl+Y` สองครั้ง → กลับมาครบพร้อมข้อความเดิม

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **คลิกโดนภาพขณะอยู่ในเครื่องมือโน้ต = เลือกภาพนั้น ไม่ใช่วางโน้ตทับ** —
   โน้ตที่ซ้อนใต้ภาพโดยไม่ได้ตั้งใจจะไม่มีใครเห็นจนกว่าจะย้ายภาพ
2. **จุดที่คลิกคือมุมซ้ายบนของโน้ต** ไม่ใช่กึ่งกลาง — โน้ตงอกลงขวาจากที่จิ้ม
   เหมือนเครื่องมือข้อความของโปรแกรมอื่นทุกตัว
3. **โน้ตใหม่ถูกเลือกทันที** ไม่งั้นผู้ใช้ต้องคลิกซ้ำก่อนถึงจะพิมพ์ได้
4. **`board.set_text` ปฏิเสธ item ที่ไม่ใช่โน้ต** ไม่ใช่แค่ id ที่ตายแล้ว —
   การเขียนข้อความทับภาพทำให้ `AssetRef` หายทั้งก้อน ซึ่ง I-3 ห้ามตรง ๆ
5. **แก้ข้อความใน Inspector ไม่ใช่บน canvas** — การพิมพ์ในที่ที่ egui ไม่ได้เป็น
   เจ้าของ focus แปลว่าต้องเขียน text input เองทั้งชุด (คีย์ลูกศร เลือกข้อความ IME)
   ซึ่งไม่มีอยู่ใน spec และเสี่ยงกว่าประโยชน์ที่ได้ · แก้ในช่องที่ egui ดูแลให้แล้ว
   ปลอดภัยกว่า และเป็นที่ที่ผู้ใช้หาเจอเพราะช่องอื่นก็อยู่ตรงนั้น
6. **ตัดบรรทัดในโน้ตประมาณจากขนาดฟอนต์ (0.55×)** ไม่ได้วัดจริงกับ `Fonts`
   เพราะการวัดต้องยืม lock เดียวกับที่ painter ถืออยู่ · ไม่เป๊ะแต่ **ข้อความไม่ล้น
   ออกนอกกรอบ** ซึ่งเป็นสิ่งที่ผู้ใช้สังเกต

### 2.11 ✅ P3-1: `ItemMeta` + tag / rating / color label (6 ส.ค. 2026)

`I` picker · `M` measure · `T` note มาก่อนหน้านี้แล้ว — รอบนี้คือแผง Arrange:
ดาว 0..5 · ป้ายสี 6 สี · ปักหมุด · แท็ก · โน้ต **ทุกช่องผ่าน `Command`**

#### ★★★ `ColorLabel` — ออกแบบให้ round-trip ได้ตั้งแต่ต้น ไม่ใช่รอ P4-1

`docs/02 §2.9` ยกเคสนี้เป็นตัวอย่างหลักของกฎ *"ทนได้ยังไม่พอ ต้องส่งคืนค่าเดิมได้"*

เพิ่ม variant **`Unknown(NonZeroU8)`** ที่ถือ **ค่าดิบ** ไว้ · `NonZeroU8` ไม่ใช่ `u8`
เพราะ **0 คือ "ไม่มีป้าย"** ซึ่งแทนด้วย `None` อยู่แล้ว — `Unknown(0)` จะเขียนออกไป
เป็น "ไม่มีป้าย" แล้วป้ายก็หายอยู่ดี **ชนิดข้อมูลปิดรูนั้นแทนที่จะต้องมีคนจำ**

★ เก็บใน **ชนิดของโดเมนเอง ไม่ใช่ใน DTO ตอน P4-1** — ถ้าอยู่ใน DTO การ
"ลืมเขียนกลับ" เป็นไปได้เสมอ · อยู่ตรงนี้แล้วมันเดินทางไปพร้อม `ItemMeta` ทุกที่

| | ค่าบนสาย |
|---|---|
| ไม่มีป้าย | `0` (= `None` — **คนละอย่างกับ "ไม่รู้จัก"**) |
| Red…Purple | `1`…`6` — **สัญญาถาวร ห้ามสลับ** |
| ป้ายจากรุ่นใหม่กว่า | `7..=255` → `Unknown(raw)` เขียนกลับเท่าเดิม |

เทสต์ไล่ **ครบทั้ง 256 ค่า** · negative control: เอา `Unknown` ออกให้ตกเป็น `None`
แบบดีไซน์เดิม → **แดงสามตัว** พร้อมข้อความ `ค่า 7 หายไปเป็น None`

★ UI **ห้ามวาดป้ายที่ไม่รู้จักเป็นสีใดสีหนึ่ง** (`rgb()` คืน `None`) — ถ้าเดาสีให้
ผู้ใช้จะคิดว่าเป็นสีจริงแล้วเผลอกดทับ ซึ่งคือการทำลายค่าที่อุตส่าห์ถือไว้

#### ★ ตารางชื่อแท็กอยู่ที่ `Board` ไม่ใช่ `Workspace` (สิ่งที่ spec ไม่ระบุ)

คอมเมนต์เดิมของ `TagId` เขียนว่า "ตารางชื่อแท็กจริงอยู่ระดับ workspace" แต่
`Workspace` ใน `docs/02 §2` มีแค่ `boards`/`active`/`order` — **ไม่มีตารางแท็ก**
และ `Workspace` เองก็ยังไม่ถูกสร้าง (P4-7) · ที่อยู่ของตารางนี้จึงไม่เคยถูกระบุจริง

เลือก `Board` เพราะ **`Board` คือสิ่งที่ลงไฟล์ `.refx`** — ส่งไฟล์ให้เพื่อนแล้ว
ชื่อแท็กไปด้วย · ถ้าอยู่ระดับแอป ไฟล์ที่ส่งไปจะมีแต่ `TagId` เปล่า ๆ ที่ไม่มี
ความหมาย = **ข้อมูลผู้ใช้หายตอนส่งต่อ** · ทิศ board → workspace เป็นการ*เพิ่ม*
ส่วน workspace → board ต้อง migrate ไฟล์

`TagItems` เป็นคำสั่งของตัวเองเพราะการติดแท็กแตะ **สองที่**: `ItemMeta.tags`
ของแต่ละใบ **และ** `TagTable` ของ board ทั้งคู่อยู่ใน `Board` จึงต้องย้อนพร้อมกัน
ไม่งั้นกด Ctrl+Z ครั้งเดียวจะได้ item ที่ถือ `TagId` ซึ่งไม่มีชื่อในตารางแล้ว ·
ลบชื่อตอน undo **เฉพาะตัวที่คำสั่งนี้สร้างเอง** (ตัวที่ใบอื่นใช้อยู่ต้องไม่หาย)

#### ยืนยันบนของจริง

ดาว 4 + ป้ายน้ำเงิน → `Ctrl+Z` ครั้งแรก **ล้างป้าย ดาวยังเป็น 4** → ครั้งที่สอง
**ดาวเป็น 0** · พิสูจน์ว่า `EditMeta` merge **ต่อ field** จริง ย้อนป้ายไม่พาดาวไปด้วย
· ติดแท็กแล้วกดที่แท็กเพื่อถอดออกได้ (ทดสอบด้วยชื่อ**ภาษาไทย** ซึ่งผ่าน
`normalize` แบบตัดตาม**อักขระ** ไม่ใช่ไบต์ — ตัดกลาง UTF-8 จะ panic)

#### ★ สัญลักษณ์บน UI ตรวจ glyph แล้วทุกตัว (บทเรียน P2-9)

| มี | ไม่มี |
|---|---|
| `★` U+2605 · `☆` U+2606 · `×` U+00D7 · `○` U+25CB · `■` U+25A0 | `●` U+25CF · `□` U+25A1 · `✓` U+2713 · `✗` U+2717 · `▲` `▼` |

ปักหมุดใช้ `checkbox` ของ egui (วาดเครื่องหมายเอง ไม่พึ่งฟอนต์) เพราะ `✓` ไม่มี
· เทสต์ `arrange_inspector_glyphs_all_exist` คุมไว้ **แต่กันได้แค่การเปลี่ยนค่า
ของสัญลักษณ์ที่มีอยู่ ไม่ได้กันการเพิ่มตัวใหม่แล้วลืมมาต่อท้าย** (จดไว้ในโค้ดแล้ว)

### 2.12 ★★★ บั๊กที่การยืนยัน P3-1 เปิดโปง: คีย์ลัดตายหมดบน layout ที่ไม่ใช่ละติน

ยืนยัน P3-1 บนเครื่องที่ **layout เป็นไทย** (HKL `0x41E`) แล้วกด `Ctrl+Z` ไม่ย้อน
ไล่ต่อพบว่ากด `C` ก็ไม่สลับเครื่องมือ กด `G` ก็ไม่ toggle — **ไม่มีคีย์ลัดไหน
ทำงานเลยสักปุ่ม และไม่มี error ที่ไหน**

ตัวจับคู่ทุกตัว (`tool_shortcut` · `history_shortcut` · `is_paste` ·
`zorder_shortcut` · `appearance_shortcut`) ดูแต่ **logical key** ซึ่งเป็น
*ตัวอักษรที่ layout ผลิตออกมา* — คีย์บอร์ดไทยกด `C` ได้ `แ` รัสเซียได้ `с`
กรีกได้ `ψ` **ไม่มีตัวไหนตรงกับ `"c"` ทั้งสิ้น**

> ★ `docs/03 §0` ระบุว่า **ไทยคือภาษาที่สองของโปรแกรมนี้** — กลุ่มผู้ใช้ที่เรา
> ตั้งใจรองรับจึงใช้คีย์ลัดไม่ได้เลย มาตั้งแต่ P2-4 โดยไม่มีใครรู้

แก้ที่ `shortcut_char()`: **logical ก่อน · physical เป็นตาข่ายรอง**
★ ลำดับสำคัญ — ถาม physical ก่อน **Dvorak พัง** · ถาม logical อย่างเดียว
**ไทย/รัสเซีย/กรีกพัง** ต้องสองชั้นตามลำดับนี้เท่านั้น (เทสต์คุมทั้งสองทิศ)

ยืนยันบนของจริง layout ไทยเครื่องเดียวกัน: **ก่อนแก้กด `C` ยังเป็น Select ·
หลังแก้กด `C` เป็น Crop**

#### ★★ ปิดคำถามแล้ว: **การสลับ layout ไม่ทำให้แอปค้าง**

รอบก่อนจดไว้ว่า "น่าจะเป็นความผิดของสคริปต์เอง" ซึ่ง **ยังไม่ใช่หลักฐาน** —
ถ้าแอปค้างจริงตอนสลับ layout นั่นคือบั๊กร้ายแรงสำหรับกลุ่มผู้ใช้ที่เพิ่งแก้ให้
(คนไทย/ญี่ปุ่นสลับ layout วันละหลายสิบครั้งระหว่างพิมพ์โน้ตกับใช้คีย์ลัด) จึงตามให้จบ

| ทาง | รอบ | ผล |
|---|---|---|
| **Win+Space** (ทางที่ผู้ใช้ทำจริง) | 6 | **ไม่ค้าง** — pump ตอบทุกครั้ง |
| `PostMessage(WM_INPUTLANGCHANGEREQUEST)` | 4 | ไม่ค้าง |
| สลับสลับกันสองทาง **พร้อมพิมพ์ในช่องแท็กของแผง Arrange** | 10 | ไม่ค้าง |

→ **`scripts/ui-layout-switch.ps1`** เก็บการตรวจนี้ไว้ให้รันซ้ำได้
(`Get-WinUserLanguageList` ต้องมีอย่างน้อยสอง layout — เครื่องนี้มี 0409 + 041E)

> ★ ทำไม Alt+Shift ไม่สลับให้ตอนลองรอบก่อน: `HKCU:\Keyboard Layout\Toggle`
> ตั้ง `Hotkey = 4` ซึ่งแปลว่า **Not Assigned** · **Win+Space ใช้ได้เสมอ** ไม่ขึ้นกับค่านี้

#### ★★★ แล้วอะไรทำให้ค้าง — **`AttachThreadInput` ในสคริปต์ของเราเอง**

ยิงติด ๆ กัน 30 รอบแล้ว **จับได้ครั้งหนึ่ง**: `Responding=False` · pump เงียบ ·
**ไม่ฟื้นเลยแม้รอ 20 วินาที** · ส่วนรอบเดียวบนแอปที่เพิ่งเปิดใหม่ไม่เป็นอะไร
→ เป็นอาการที่เกิดยากและเกิดจาก **การผูก input queue สองเธรดเข้าด้วยกัน**
ซึ่งเป็นสิ่งเดียวที่ยกล็อก foreground ของ Windows ได้ · ทำให้เลี่ยงไม่ได้ทั้งหมด
แต่ `Force-Foreground` เรียกมัน **เฉพาะตอน focus หลุดจริง** อยู่แล้ว

#### ★★ สิ่งที่ทำให้ผมอ่านผลผิดหลายรอบ — และประตูที่ปิดมันแล้ว

**หน้าต่างที่ค้าง กับหน้าต่างที่เพิกเฉยต่อ input แยกไม่ออกจากภาพหน้าจอ** —
Windows โชว์เฟรมสุดท้ายค้างไว้ให้ ภาพที่ถ่ายได้จึงดูปกติทุกอย่าง
ผมจึงสรุปผิดว่า "กดปุ่มแล้วไม่มีอะไรเกิดขึ้น" ทั้งที่โปรเซสตายไปแล้ว

→ `scripts/ui-drive.ps1` มี `Assert-Alive` แล้ว: ถาม `WM_NULL` แบบมี timeout
(`SMTO_ABORTIFHUNG`) + `Process.Responding` **ก่อนทุกภาพหน้าจอและก่อน attach**
แล้ว **exit 3 แทนที่จะถ่ายภาพที่โกหก** — ชนิดเดียวกับที่ `docs/08 §3.9` ข้อ 9 ห้าม

★ ตำแหน่งของด่านสำคัญ: ต้องอยู่ **ก่อน** `MoveWindow`/`ShowWindow`/
`SetForegroundWindow` เพราะทั้งสามตัวส่งข้อความรอคำตอบข้างใน ถ้าเป้าหมายค้าง
มันจะบล็อกตลอดกาลแล้วสคริปต์ไม่มีวันเดินไปถึงด่านของตัวเอง (เจอจริงตอนทดสอบ)

**negative control ผ่านแล้ว**: `SuspendThread` เธรด UI ของ RefX → สั่งถ่ายภาพ →
ได้ `TARGET HUNG before 'attach'` · exit **3** · **ไม่มีไฟล์ภาพถูกสร้าง** ·
`ResumeThread` แล้วกลับมาปกติ

### 2.13 ✅ P3-2: layout engine ทั้งห้าตัว + `fuzz_layout` (9 ส.ค. 2026)

`crates/refx-core/src/layout.rs` **ไม่ใช่ไฟล์ TODO บรรทัดเดียวอีกต่อไป**
Grid · Masonry · JustifiedRows · ShelfPack · Radial — ฟังก์ชันบริสุทธิ์ทั้งไฟล์

#### ★ จุดที่ deterministic พังได้ง่ายที่สุด — **การตัดสินเสมอ**

`Masonry` เลือกคอลัมน์เตี้ยสุด: ตอนเริ่มทุกคอลัมน์สูง 0 เท่ากันหมด →
บังคับให้ **ซ้ายสุดชนะ** เสมอ · `ShelfPack` เรียงจากสูงไปเตี้ยแล้วมี
**ดัชนีเดิมเป็นตัวตัดสินท้าย** · ถ้าปล่อยให้ค่าเท่ากันตัดสินกันเอง ผลจะขึ้นกับ
รายละเอียดของตัวเปรียบเทียบ ซึ่งคือ "ภาพเรียงไม่เหมือนเดิมทุกครั้งที่กด"

#### ★★ `Placed::top_left` **ไม่ใช่** `ItemCanvas::pos`

`ItemCanvas::pos` คือ **จุดกึ่งกลาง** (docs/02 §2.1) · ตั้งชื่อให้ต่างกัน
โดยตั้งใจเพราะเคยพลาดมาแล้วตอน P2-6 — ถ้าตั้งชื่อว่า `pos` เฉย ๆ การเผลอ
เอาไปใส่ตรง ๆ จะทำให้ **ทุกภาพเลื่อนไปครึ่งตัว** ซึ่งดูเหมือน "layout เพี้ยน
นิดหน่อย" มากกว่าดูเหมือนบั๊ก · ใช้ `Placed::centre()` แทน

#### ★★★ negative control ที่ **ไม่แดงรอบแรก** — และสิ่งที่มันบอก

ถอดด่านสุดท้าย (`sane_pos`/`sane_size`) ออกแล้วให้ fuzzer หา → **157,000 รอบไม่แดง**

สาเหตุ: assertion ในตัว target เขียนว่า `size > 0.0` ซึ่ง **อ่อนกว่าสัญญาจริง**
ที่ประกาศไว้ว่า `size >= MIN_SIDE` — ขนาด `0.525` จึงผ่านไปได้สบาย

> ★ **assertion ที่อ่อนกว่าสัญญา = target ที่เขียวโดยไม่ได้ตรวจอะไร**
> ชนิดเดียวกับที่ `fuzz_decode` เป็นมา 5 session ต่างกันแค่รอบนี้จับได้เพราะ
> **ลองทำให้พังก่อน** ตาม `docs/08 §3.9` ข้อ 1

แก้โดยเปิด `MIN_SIDE`/`MAX_SIDE` เป็น `pub` (มันคือ**สัญญาที่ผู้เรียกเชื่อได้**
ไม่ใช่รายละเอียดภายใน) แล้ว assert สัญญาจริง → ถอดด่านออกอีกครั้ง **fuzzer แดง
ภายในไม่กี่วินาที** (`Vec2(1.0, 0.001)` จาก Masonry) → ใส่ด่านกลับ เขียวอีกครั้ง

#### `fuzz_layout` ต่อครบสามข้อแล้ว

(1) `fuzz_target!` ยิงจริง (2) `[[bin]]` ใน `fuzz/Cargo.toml` (3) อยู่ใน matrix
ของ `fuzz.yml` · และเอา `check fuzz_layout` ออกจาก job `unwired-targets`

**รันจริงสองที่:**

| ที่ | รอบ | ผล |
|---|---|---|
| เครื่องพัฒนา (Windows) | 480,938 / 61 วิ | ไม่เจอ crash |
| **CI (ubuntu, nightly)** | **3,650,952 / 901 วิ** | ไม่เจอ crash |

★ ตัวที่นับคือ **ของ CI** — มันยิงเต็ม 15 นาทีทุกคืน ส่วนบนเครื่องพัฒนา
เป็นแค่การยืนยันว่าต่อสายถูกจริงก่อน push

> ⚠️ บน Windows `cargo fuzz run` ล้มด้วย `STATUS_DLL_NOT_FOUND` จนกว่าจะเอา
> `clang_rt.asan_dynamic-x86_64.dll` เข้า PATH — อยู่ที่
> `…\VC\Tools\MSVC\<ver>\bin\Hostx64\x64\` · CI รันบน ubuntu จึงไม่เจอปัญหานี้

#### ★ `respect_pinned` ที่ spec ระบุแต่ **ไม่ได้ทำ** (ตั้งใจ)

`docs/03 §3` ใส่ `respect_pinned: bool` ไว้ใน `LayoutParams` แต่ **input ที่ spec
กำหนดเองคือ `(ItemId, aspect)` ซึ่งไม่มีธง pin และไม่มีกรอบจริงของภาพที่ pin ไว้**
engine จึงมองไม่เห็นสิ่งที่ต้องจัดรอบ — ธงตัวนั้นไม่มีทางมีผล

เก็บฟิลด์ที่ไม่มีผลไว้ = กับดักของคนอ่านรอบหน้า (เหตุผลเดียวกับที่ลบ
`BoardSettings::snap` ตอน P2-9) จึงไม่ใส่ แล้วแยกงานตามความจริง:

| | ทำได้เมื่อไหร่ |
|---|---|
| "ไม่ขยับภาพที่ pin" | ผู้เรียก **กรองออกก่อนส่งมา** — ทำได้เลยตั้งแต่ P3-5 |
| "จัดรอบภาพที่ pin" | ปัญหาคนละข้อ ต้องรู้กรอบจริงของภาพที่ pin แล้วหลบมัน → **ตัดสินตอน P3-5** ที่นั่นมี `Board` อยู่ในมือ |

★ และ **เพิ่ม `width`** ซึ่ง spec ไม่มี: `JustifiedRows` ต้องรู้ว่าแถวกว้างเท่าไหร่
ถึงจะขึ้นแถวใหม่ และ `Grid` ต้องรู้ว่าช่องกว้างเท่าไหร่ — เอกสารระบุแต่
`target_row_height` ซึ่งคุมแค่แกนเดียว

### 2.14 ✅ P3-3: virtual scrolling ของ Arrange mode (10 ส.ค. 2026 · `dc42eca`)

`crates/refx-ui/src/arrange.rs` ไม่ใช่ไฟล์ TODO บรรทัดเดียวอีกต่อไป และ
**Arrange ไม่ได้ใช้ viewport เดียวกับ Canvas แล้ว** — มันมีแผ่น กล้อง และชุด
instance ของตัวเอง

| | Canvas | Arrange |
|---|---|---|
| instance ที่ส่ง GPU | ทั้ง board ตามลำดับ z | **เฉพาะแถบที่อยู่ในจอ + กันชน 1 หน้าจอบนล่าง** |
| ล้อ | ซูมเข้าหาเคอร์เซอร์ | **เลื่อนแผ่น** (ปุ่มกลางลากก็เลื่อน) |
| working texture ชั้น B | ใช้ | **ไม่ใช้เลย** (ตรึงที่ขนาด thumbnail จึงไม่มีใบไหนต้องการภาพคมกว่า) |
| ของประดับชั้น egui | กรอบเลือก · handle · ไกด์ · ไม้บรรทัด | ยังไม่มี (ดูข้อจำกัดท้ายหัวข้อ) |

#### ตัวเลขจริงบนจอ (3,072 ภาพ · หน้าต่าง 1280×800 · RTX 4060 / Vulkan)

| | ค่า |
|---|---|
| status bar | `drawn 28 (in view 16) of 3072 · checked 28` |
| เลื่อนไปกลางแผ่น | `drawn 44 (in view 16) · checked 44` |
| **หยุดเลื่อนแล้ว** | ตัวนับเฟรม **2380 → 2380** หลังรอ 6 วินาที (I-1) |
| สลับ Arrange → Canvas → Arrange | พิกเซลของช่องกลาง **ต่างกัน 0 ไบต์** จากก่อนสลับ |

★ ตัวเลข `drawn/checked` อยู่บน status bar ถาวร ด้วยเหตุผลเดียวกับ `atlas uploads`
ของ P2-8: เกณฑ์ของ ROADMAP เป็น **ตัวเลข** จึงต้องมีตัวเลขให้ดู ไม่ใช่คำว่า "มี culling"

#### วัดสองฝั่งบนเครื่องเดียวกันในเซสชันเดียว (`docs/08 §3.9` ข้อ 7)

3,072 ภาพ · `--bench-seconds` · `PresentMode::Immediate` · p50 (ms) · รุ่นละ 2 รอบสลับกัน

| build | โหมด | วาดจริง | p50 | p99 |
|---|---|---|---|---|
| ก่อน (`c1881d7`) | Canvas | 3072 | 0.574 · 0.578 → **0.576** | 1.28 |
| **HEAD** | Canvas | 3072 | 0.582 · 0.556 → **0.569** | 1.35 |
| **HEAD** | **Arrange** | **28** | 0.353 · 0.335 → **0.344** | 1.02 |
| HEAD ที่ **ถอด culling ออก** (build ชั่วคราว) | Arrange | 3072 | 0.601 · 0.614 → **0.608** | 1.41 |
| **HEAD** | Arrange **ระหว่างหมุนล้อค้าง 30 วิ** | 40 | **0.339** | 0.913 |

- **Canvas ไม่ถดถอย** (−1.2% ซึ่งอยู่ใต้ความแกว่างของรุ่นเดียวกันที่ ~4%)
- **culling ประหยัด 0.264 ms/เฟรม (−43%)** ที่ 3,072 ใบ — และภาพบนจอ *เหมือนกันเป๊ะ*
  ทั้งสอง build (`in view 16` เท่ากัน) ต่างกันแค่จำนวน instance ที่ส่งให้ GPU
- **การเลื่อนไม่มีราคา**: 0.339 ตอนหมุนล้อค้าง เทียบกับ 0.344 ตอนอยู่นิ่ง

> ★ build ที่ถอด culling ออกคือ **negative control ระดับทั้งแอป** — ถ้าไม่ทำ
> เราจะมีแต่ตัวเลขที่ดูดี โดยไม่รู้ว่ามันดีเพราะ culling หรือเพราะ 3,072 quad
> มันถูกอยู่แล้ว (ที่จำนวนนี้ quad pipeline คิดเป็น ~40% ของเฟรม)

#### ★★★ ของจริงเปิดโปงบั๊กของ P3-2: `sane_pos` ใช้เพดานผิดตัว

`layout::sane_pos` clamp **ตำแหน่ง** ด้วย `MAX_SIDE` (100,000) ซึ่งเป็นเพดานของ
**ด้านหนึ่งด้าน** ไม่ใช่ของแผ่น · แผ่น 10,000 ใบ 4 คอลัมน์ช่องละ ~200 หน่วยสูง
**~532,000 หน่วย** → ทุกแถวตั้งแต่แถวที่ ~490 ลงไป **ถูกกดมาทับกันที่ y = 100,000
เท่ากันหมด** ภาพหลายพันใบซ้อนเป็นกองเดียว **โดยไม่มี error ที่ไหนเลย**

- แยกเป็น `layout::MAX_COORD = 1_000_000.0` (f32 ยังละเอียด ~0.06 หน่วยที่ระยะนั้น)
- เทสต์ `ten_thousand_items_do_not_pile_up_on_one_row` — นับ **จำนวน y ที่ต่างกัน**
  ต้องเท่ากับจำนวนแถวเป๊ะ และยืนยันว่าแผ่นสูงเกิน `MAX_SIDE` จริง ไม่งั้นเทสต์
  ไม่ได้ตรวจอะไร · negative control: เอา `MAX_COORD` ออก → แดงทันที
- `fuzz_layout` assert เพดานตำแหน่งด้วย **ค่าคงของสัญญา** ตาม §4 ข้อ 21
  (ยิงจริงบนเครื่องพัฒนา 167,877 รอบ · negative control: บีบเพดานลง 1e9 เท่า → แดงใน 2 วินาที)

> ★ นี่คือรูปแบบเดิมอีกครั้ง: **ของที่เทสต์ครบแต่ยังไม่มีใครเอาไปใช้จริง**
> P3-2 มี property test + fuzz หลายล้านรอบ แต่ไม่มีใครเคยขอให้มันวาง 10,000 ใบ
> จริง ๆ เพราะ fuzz จำกัดที่ 1,024 ใบ (กัน OOM) และ unit test ใช้หลักสิบ

#### ★★★ ทำไมยืนยันที่ 3,072 ไม่ใช่ 10,000 — **board ตันที่ขนาดของ atlas**

ลาก 10,000 ไฟล์เข้าไปแล้วได้ **3,072 items พอดี** พร้อมข้อความบน status bar ว่า
*"The thumbnail storage is full (all 12 layers are in use)"* · 3,072 = 12 layer × 256 ช่อง
= เพดาน VRAM 384 MB ÷ 2 (อีกครึ่งเป็นของ working texture) ÷ 16 MB ต่อ layer

**ต้นเหตุอยู่ที่นโยบายของ ingest ตั้งแต่ P1:** `drain_decode_results` เรียก
`upload_thumb` ก่อน แล้ว **ถ้าไม่มีช่องใน atlas มันไม่สร้าง item เลย** (`continue`)
— ไม่ใช่บั๊กของ P3-3 และ virtual scrolling ก็ไม่เกี่ยวข้องกับมัน

★ **ไม่แก้ในงานนี้โดยตั้งใจ** เพราะการยกเพดานนี้คือการตัดสินใจเรื่องหน่วยความจำ
ซึ่ง `CLAUDE.md` สั่งให้ถามก่อน — และมันมีสองชั้นที่ต้องแก้พร้อมกัน:

| ชั้น | สภาพตอนนี้ | ถ้าจะรับ 10,000 ใบ |
|---|---|---|
| **ช่องใน atlas (VRAM)** | มาก่อนได้ก่อน · เต็มแล้วปฏิเสธภาพใหม่ | ต้องเป็น **LRU ตามสิ่งที่อยู่ในจอ** (I-6 บอกว่าทุก cache ต้องมี LRU — atlas ยังไม่มี) · virtual scrolling ทำให้ทำได้ง่ายเพราะชุดที่ต้องอยู่มีแค่ ~30 ใบ |
| **pixel ของ thumbnail (RAM)** | ทุกใบอยู่ใน RAM ตลอดชีวิต item (64 KB/ใบ) | 10,000 ใบ = **640 MB** ซึ่งเกินงบทันที · ต้องอ่านกลับจาก `cache.sqlite` แบบ async ได้ (แตะ threading → ต้องถามก่อนเช่นกัน) |

**ถ้ายกเพดานแต่ไม่ทำสองข้อนี้** ผลคือ item ที่เกิน 3,072 จะเป็นสี่เหลี่ยมสีเทา
ตลอดกาล และ RAM จะไต่ขึ้นไม่มีเพดาน — แย่กว่าสภาพปัจจุบันที่บอกผู้ใช้ตรง ๆ ว่าเต็ม

**ที่ยืนยันได้แล้วว่าเกณฑ์ 10,000 ทำได้จริงเชิงกลไก:** เทสต์
`ten_thousand_items_draw_fewer_than_sixty` วาง 10,000 ใบจริงในหน่วยความจำแล้ววัด
→ **ในจอ 12 · วาด 24 · ตรวจ 24 · แผ่นสูง 532,488 หน่วย** (พิมพ์ออกมาด้วย `--nocapture`)

**RAM/VRAM ที่วัดได้จริงตอน 3,072 ใบ:** VRAM **192 MB จากเพดาน 384 MB** (atlas 12 layer
ซึ่งเป็นเพดานของมันพอดี) · working set ของโปรเซส **410 MB** (thumbnail 3,072 × 64 KB
= 192 MB อยู่ในนั้น) · RAM ของ decode pool **0 จาก 256 MB** ตอนโหลดเสร็จแล้ว

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **จำนวนคอลัมน์มาจากความกว้างของจอ ไม่ใช่จากจำนวนภาพ** — `LayoutParams::columns`
   เป็น `None` แปลว่า engine เดาเป็น √n ซึ่งที่ 10,000 ใบ = **100 คอลัมน์**
   (ภาพเล็กกว่าไอคอน แผ่นกว้างกว่าจอ 20 เท่า) · contact sheet ต้องคิดจากช่อง
   เป้าหมาย 200 pt แล้วหารความกว้างจอ
2. **ล้อ = เลื่อน ไม่ใช่ซูม** ใน Arrange · ขนาดภาพมาจากจำนวนคอลัมน์ ไม่ใช่จากกล้อง
   (`ArrangeView::camera()` ตรึง zoom = 1.0) — ตารางที่ซูมด้วยล้อไม่มีในโปรแกรมไหน
3. **`rotation` ถูกตัดออกตอนวาดในโหมดนี้** — Arrange เป็นตาราง ไม่ใช่ระนาบอิสระ
   แต่ crop/flip/filter/opacity ยังติดมา เพราะนั่นคือ "ภาพที่ผู้ใช้แต่งไว้"
   · **ไม่มีอะไรถูกเขียนกลับลง board** (docs/03 §4.3 — พิสูจน์ด้วยการสลับโหมดไปกลับ
   แล้ว diff พิกเซลได้ 0)
4. **แผ่นถูก cache ไว้ ไม่ได้จัดใหม่ทุกเฟรม** — docs/03 §3 บังคับ cache ไว้กับ
   *filter* ด้วยเหตุผลเดียวกัน · ตอนนี้ใช้ (จำนวน item · engine · params) เป็นคีย์
   เพราะ `Board` ยังไม่มี `revision` (นั่นคือ **P3-4** ตามที่ docs/03 §3 เขียนไว้)
   > ★ คีย์มีจำนวน item อยู่ด้วยโดยตั้งใจ ไม่ได้พึ่ง `invalidate()` อย่างเดียว —
   > สัญญาที่ถูกเฉพาะเมื่อผู้เรียก "เรียกถูกจังหวะ" คือกับดัก (`docs/08 §3.9` ข้อ 8)
5. **เพิ่ม `--mode=canvas|arrange`** — `--bench-seconds` เริ่มจับเวลาตั้งแต่เฟรมแรก
   ถ้าต้องกดปุ่มสลับโหมดก่อน ตัวเลขจะเป็นของสองโหมดปนกันโดยแยกไม่ออก
6. **รายงานของ benchmark พิมพ์จำนวน instance ที่วาดจริงในโหมดนั้น** ไม่ใช่ `quads`
   เสมอ — ไม่งั้นโหมด Arrange จะพิมพ์ "3072" ทั้งที่วาด 28 ใบ ซึ่งทำให้คนอ่าน
   เทียบสองโหมดผิดทั้งหมด (`docs/08 §3.9` ข้อ 9: เครื่องมือที่ผลิตหลักฐานต้องไม่โกหก)

#### เทสต์ที่คุมอยู่ + negative control ที่ยืนยันว่าล้มเป็น

| เทสต์ | ถอดอะไรออกแล้วมันแดง |
|---|---|
| `ten_thousand_items_draw_fewer_than_sixty` | (เกณฑ์ ROADMAP ตรง ๆ) |
| `finding_the_visible_band_does_not_scan_the_whole_board` | เปลี่ยน binary search เป็นไล่ทั้งรายการ → `ตรวจ 10000 ใบ` |
| `a_very_tall_item_is_still_found_when_its_top_is_far_above` | ถอดการถอยหลังเท่า `tallest` |
| `the_band_reaches_one_screen_above_and_below_the_view` | ตั้ง `BUFFER_SCREENS = 0` |
| `scrolling_never_recomputes_the_layout` | ถอด cache ของแผ่น |
| `ten_thousand_items_do_not_pile_up_on_one_row` (core) | `sane_pos` กลับไปใช้ `MAX_SIDE` |

> ★★ **`finding_the_visible_band…` รอบแรกไม่แดง** ทั้งที่ถอด binary search ออกแล้ว
> เพราะมันวัดที่ `scroll = 0` ซึ่ง "ใบที่อยู่เหนือแถบ" มีศูนย์ใบพอดี การไล่ทั้ง
> รายการจึงให้ตัวเลขเท่ากันเป๊ะ · แก้โดยเลื่อนไปสุดแผ่นก่อนวัด — **negative control
> ที่ไม่แดงคือคำตอบว่าเทสต์อ่อน ไม่ใช่ว่าโค้ดแข็ง** (`docs/08 §3.9` ข้อ 1)

#### ข้อจำกัดที่รู้ตัว (ไม่ใช่ของที่ลืม)

1. **คลิกเลือกภาพในโหมด Arrange ยังไม่ได้** — การเลือกทั้งหมดยังทำใน Canvas
   แผง Inspector ของ Arrange (P3-1) จึงอ่านจากสิ่งที่เลือกไว้จากอีกโหมด
   · hit-test ในแผ่นทำได้ถูกมาก (ชุดที่อยู่ในจอมีแค่ ~30 ใบ) แต่มันเป็นเรื่องของ
   **การเลือก** ไม่ใช่ของ virtual scrolling → รอทำพร้อม P3-4/P3-5
2. **โน้ตข้อความไม่ปรากฏบนแผ่น** (ไม่มี pixel ให้วาด) — เห็นเป็นช่องว่างในตาราง
   · รูปแบบของโน้ตใน contact sheet เป็นเรื่องของ P3-7
3. **ยังไม่มีการเรียง/กรอง** — แผ่นเรียงตามลำดับ z ของ board ซึ่งคือลำดับที่เพิ่มเข้ามา
   (`SortKey::AddedAt` ที่เป็นค่าปริยายพอดี) · การเรียงจริงคือ **P3-4**
4. **ไฟล์ที่เกินความจุ atlas ทำให้ log บวม** — ทุกใบที่ถูกปฏิเสธเขียน `WARN`
   หนึ่งบรรทัด · ลอง 10,000 ไฟล์แล้วได้ **37,606 บรรทัด** ในไฟล์เดียว
   ซึ่งเป็นอาการเดียวกับที่ §4 ข้อ 9 ห้ามไว้ตอน decode (log หมุนทับ crash log จริง)
   → ควรยุบเป็นบรรทัดเดียวต่อชุดตอนที่แก้เรื่องเพดาน

### 2.15 ✅ board เต็มแล้วต้องไม่เงียบ (10 ส.ค. 2026 · `8e4cf04`)

เจ้าของโปรเจกต์แก้ ROADMAP P3-3 แล้ว: **เพดาน 3,072 คือค่าที่ยอมรับ**
(10,000 เป็นตัวเลขที่เดาไว้ตอนเขียน ชุดเดียวกับ "1000 ภาพ cache เย็น 4 วินาที")
สิ่งที่ต้องแก้คือ **อาการ ไม่ใช่เพดาน**

| | ก่อน | หลัง |
|---|---|---|
| ลาก 10,000 ไฟล์ | ได้ 3,072 ใบ **เงียบ ๆ** | ขึ้นข้อความสีเตือน บอกครบสามตัวเลข + ทางออก |
| ข้อความที่เห็นจริง | "atlas เต็ม 12 layer · ลองเพิ่มเพดานใน Settings" | "board นี้เต็มที่ 3072 ภาพ — เพิ่มอีก 6928 ใบจาก 10000 ใบที่เปิดเข้ามาไม่ได้ ลองแยกเป็นหลาย board" |
| log | WARN **บรรทัดละใบ** (วัดจริง 37,606 บรรทัด) | WARN ใบแรกใบเดียว + สรุปหนึ่งบรรทัดตอนจบงวด |
| รายงานสรุปตอนจบงวด | **ไม่เคยทำงานเลย** เมื่อมีใบที่ถูกปฏิเสธ | ทำงานเสมอ |

#### ★★★ ต้นเหตุที่ทำให้เงียบคือ **การนับไม่ครบ** ไม่ใช่แค่ "ไม่มีข้อความ"

เงื่อนไขจบงวดคือ `drop_shown >= drop_expected` ซึ่ง **ไม่มีวันเป็นจริง**
เมื่อมีใบที่ไม่ถูกสร้างเป็น item — ทั้งบล็อกรายงาน (เวลาที่ใช้ · จำนวนไฟล์ ·
`undo_depth`) จึงตายไปด้วยกันทั้งหมดโดยไม่มีใครสังเกต เพราะมันเป็น
"สิ่งที่ไม่เกิดขึ้น" ไม่ใช่ "สิ่งที่ผิดพลาด"

→ ยุบตัวนับสามตัวเป็น `DropBatch` ที่บังคับว่า **ทุกใบที่ส่งเข้าไปต้องลงเอยที่
ช่องใดช่องหนึ่งเสมอ**: `added` · `rejected` (board เต็ม) · `failed` (ไฟล์เปิดไม่ได้)
`settled()` จึงเป็นจริงได้แม้ทุกใบจะถูกปฏิเสธ

★ `failed` ต้องนับด้วย ไม่งั้น **ไฟล์เสียใบเดียวก็ทำให้งวดค้างตลอดกาล**
(หนี้เดิมที่ไม่มีใครเคยเจอเพราะ dataset ทดสอบไม่มีไฟล์เสียปนมา)

#### ★ negative control ที่ชนิดข้อมูลบังคับไว้เอง

`board_full_message()` คืน `Option<String>` — ไม่มีใบตกหล่น = **ไม่มีข้อความ
ให้แสดง** ผู้เรียกจึงเขียนโค้ดที่แสดงข้อความผิดจังหวะไม่ได้เลย
(หลักการเดียวกับ `zorder::reordered()` ที่คืน `None` เมื่อไม่มีอะไรเปลี่ยน)

ยืนยันบนจอจริงครบสามด้าน:

| | ผล |
|---|---|
| ไทย (`--lang=th`) | `board นี้เต็มที่ 3072 ภาพ — เพิ่มอีก 6928 ใบจาก 10000 ใบที่เปิดเข้ามาไม่ได้ ลองแยกเป็นหลาย board` |
| อังกฤษ (`--lang=en`) | `This board is full at 3072 images — 6928 of the 10000 you opened could not be added. Try splitting them across several boards.` |
| **negative control** ลาก 200 ไฟล์ | ไม่มีข้อความนี้ · ขึ้น `เปิด 200 ไฟล์ใน 108 ms` ตามปกติ · สีปกติไม่ใช่สีเตือน |
| I-1 | ตัวนับเฟรมนิ่งที่ **20 → 20** หลังรอ 8 วินาที |

★ 3,072 + 6,928 = 10,000 พอดี — ตัวเลขบนจอบวกกันได้ครบ ไม่ใช่ค่าประมาณ

#### ★★ เทสต์ตัวใหม่จับบั๊กเก่าได้ทันทีสามจุด

`no_string_is_secretly_padded_with_whitespace` (`refx-ui::text`) — เจอว่า
`ErrBadHeader` (อังกฤษ + ไทย) และ `ErrDecode` (ไทย) ขึ้นบรรทัดสองด้วย
**newline ดิบในสตริง** แล้วพา *ช่องว่างย่อหน้า 13 ตัว* ติดไปโผล่บนจอผู้ใช้ด้วย

> ★★★ ผมทำพลาดแบบเดียวกันเป๊ะในข้อความ `BoardFull` ที่เพิ่งเขียน:
> ใช้ `\` ต่อบรรทัด แล้ว `cargo fmt` ยุบให้เหลือบรรทัดเดียว **โดยเก็บช่องว่างไว้**
> โค้ดอ่านแล้วปกติทุกอย่าง เทสต์ทุกตัวเขียว — **ภาพหน้าจอเป็นตัวจับ**
> (ช่องโหว่กว้าง 14 ตัวอักษรกลางประโยคบน status bar)
> เป็นบั๊กชนิดที่สามในตารางของ `docs/08 §3.9` ข้อ 5 อีกครั้ง
> · เทสต์เก็บทุกกรณีก่อนแล้วค่อยล้ม ไม่ใช่ล้มที่ตัวแรก — ไม่งั้นต้องรันซ้ำ
> ทีละรอบกว่าจะเห็นครบทั้งสาม

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **ข้อความขึ้น status bar ไม่ใช่ dialog** — ตามหลักเดียวกับ "ก๊อปไม่ติด"
   (HANDOFF §2.9): ภาพที่เพิ่มไม่ได้ยังไม่ใช่ *งานที่ทำไปแล้วหาย* ผู้ใช้ยัง
   แยก board ได้ทันที · แต่เพิ่ม `ShellState::status_warn` ให้ขึ้นเป็น**สีเตือน**
   เพราะ status bar เป็นที่รวมของทุกข้อความตั้งแต่ "พร้อม" ไปจนถึงเรื่องแบบนี้
   ถ้าสีเดียวกันหมด ข้อความสำคัญจะกลืนหายไปกับข้อความประจำวัน
2. **ข้อความ board เต็มมาก่อนข้อความสรุปเวลา** — "เปิด 3,072 ไฟล์ใน 9 วินาที"
   ที่ขึ้นตอนผู้ใช้ลากมา 10,000 ไฟล์ **เป็นความจริงที่หลอก** เขาจะอ่านว่าสำเร็จครบ
3. **`OpenedFiles` รายงาน `added` ไม่ใช่ `requested`** — เดิมพิมพ์จำนวนที่ *ขอ*
   ซึ่งแปลว่าถ้ามีใบตกหล่น ตัวเลขบนจอจะไม่ตรงกับจำนวน item จริงบน board
4. **VRAM ไม่พอ (`OutOfVram`) ยังใช้ข้อความเดิม** — คนละสาเหตุ คนละทางแก้
   กับ board เต็ม และมันบอกตัวเลขที่ใช้อยู่/เพดานให้แล้ว

#### ยังเหลืออยู่ (ไม่ใช่ของที่ลืม)

- **`Cancelled` ยังไม่ถูกนับเข้า `DropBatch`** — job ที่ถูกยกเลิกกลางทางจะทำให้
  งวดค้างเหมือนเดิม · ไม่นับเพราะ `JobResult::Cancelled` ใช้ร่วมกับงาน
  working texture ด้วย การนับมั่วจะทำให้งวดจบเร็วเกินจริง → ต้องแยกชนิดงาน
  ในผลลัพธ์ก่อน ซึ่งแตะ `refx-asset::pool` (ยังไม่จำเป็นตอนนี้: การยกเลิก
  ไม่เกิดกับเส้นทาง ingest ในการใช้งานปกติ)

### 2.16 ✅ ปิดสองรูที่ทำให้โค้ดตายเงียบ (10 ส.ค. 2026)

สองบั๊กที่เจอตอนทำ §2.15 มีค่ามากกว่าฟีเจอร์ที่ทำอยู่ เพราะมันเป็น**ชนิดที่
เครื่องมือทุกตัวของเรามองไม่เห็น** — เลยทำประตูถาวรให้ทั้งคู่

#### 1. ★★★ ประตูช่องว่างในข้อความ (`refx-ui::text`)

`ErrBadHeader` (อังกฤษ + ไทย) และ `ErrDecode` (ไทย) พา **ช่องว่างย่อหน้าของโค้ด
13 ตัว** ไปโผล่บนจอผู้ใช้ตั้งแต่ตอนทำ i18n · เทสต์เขียวหมด · review ไม่เห็น
เพราะในโค้ดมันคือการจัดย่อหน้าที่ดูปกติที่สุด

`padding_problem()` ตรวจสี่อาการ แล้ว `no_string_is_secretly_padded_with_whitespace`
ไล่ทุก `Key` และทุก `Template` **ทั้งสองภาษา** (รูปแบบเดียวกับประตู glyph ของ P2-9
และประตูตัวยึดตำแหน่งของ `thai_templates_use_the_same_placeholders_as_english`):

| อาการ | ที่มา |
|---|---|
| บรรทัดที่ 2+ ขึ้นต้นด้วยช่องว่าง | **newline ดิบในสตริง** → ย่อหน้าของโค้ดหลุดเข้ามา (ของจริงสามจุด) |
| ช่องว่างติดกันกลางบรรทัด | `cargo fmt` ยุบ `\` ต่อบรรทัดให้เหลือบรรทัดเดียว **โดยเก็บช่องว่างไว้** |
| ขึ้นต้น/ลงท้ายทั้งก้อนด้วยช่องว่าง | คัดลอกวางมาไม่ครบ |
| บรรทัดลงท้ายด้วยช่องว่าง | มองไม่เห็นในทุก editor |

★ negative control อยู่ในเทสต์ของตัวเอง (`the_padding_gate_catches_every_shape_of_the_bug`)
ยิงทั้งหกรูปแบบว่าต้องจับได้ **และ**สามแบบที่ถูกต้องว่าต้องไม่จับ — ประตูที่จับ
ทุกอย่างมีค่าเท่ากับประตูที่ไม่จับอะไรเลย · ยืนยันซ้ำด้วยการเอาช่องว่างของจริง
ใส่กลับเข้า `ErrBadHeader` → แดงพร้อมชี้ชื่อและเหตุผล

> ★★ ผมทำพลาดแบบเดียวกันเป๊ะในข้อความ `BoardFull` ที่เพิ่งเขียนรอบนี้เอง
> และ **ภาพหน้าจอเป็นตัวจับ ไม่ใช่เทสต์** — ตอนนี้เครื่องจับได้แล้ว
> ไม่ต้องรอให้ใครเปิดโปรแกรมดู

#### 2. ★★★ `JobResult::Cancelled` ไม่บอกว่ามันเป็นงานชนิดไหน

งวดที่ผู้ใช้ลากเข้ามาจะจบก็ต่อเมื่อทุกใบมีคำตอบ · ใบที่ถูกยกเลิกไม่มีวันกลับมา
เป็น item แต่ชั้น UI **แยกไม่ออกว่าใบไหนเป็นงานของงวดนี้**:

| ทางเลือกตอนที่ยังไม่มี `target` | ผล |
|---|---|
| นับทุกใบที่ถูกยกเลิก | งาน working texture ปนเข้ามา → งวดจบเร็วเกินจริง ตัวเลขบนจอผิด |
| ไม่นับเลย (ของเดิม) | **pan ระหว่างลากไฟล์ → งวดค้างถาวร** แถบ "กำลังโหลด N/M" ไม่หาย และข้อความสรุป (รวม "board เต็ม") ไม่มีวันขึ้น |

→ `Cancelled` และ `Failed` พก **`target: JobTarget`** ติดมาด้วย (ชนิดที่มีอยู่แล้ว
ไม่ได้เพิ่มของใหม่) · ชั้น UI นับเฉพาะ `Thumbnail` เข้างวด ส่วน `Working`/`Sample`
ไปเข้าเส้นทางของตัวเอง — **แยกแคบ ไม่ได้รื้อ pool**

★★ **ของแถมที่การมี `target` เพิ่งทำให้เขียนได้:** `working_pending` ถูกปลด
เฉพาะตอน **สำเร็จ** เท่านั้นมาตลอด งานที่ถูกยกเลิก/ล้มจึงทิ้งคีย์ค้างไว้ตลอดอายุ
โปรแกรม แล้ว `plan_working_textures` เห็นว่า "ขอไปแล้ว" ตลอดกาล →
**ภาพใบนั้นเบลอถาวรทุกครั้งที่ซูมเข้า โดยไม่มี error ที่ไหน** · ก่อนหน้านี้
เขียนโค้ดแก้ไม่ได้เลยเพราะ UI ไม่รู้ว่าใบที่หายไปเป็นงานชนิดไหน

#### ★★★ สิ่งที่ยืนยันไม่ได้ และเหตุผลที่มันสำคัญ

**ไม่มีใครในแอปตั้ง `Job::cancel = true` เลยสักที่** — ค้นทั้ง workspace แล้ว
ตัวที่ตั้งมีแต่เทสต์ของ `pool` เอง (`cancelled_before_start_is_skipped`,
`bulk_cancellation_actually_skips_work`)

แปลว่า:

* สถานการณ์ "pan แล้วงวดค้าง" **เกิดไม่ได้วันนี้** เพราะไม่มีงาน ingest ใบไหน
  ถูกยกเลิกจริง · การลาก 10,000 ไฟล์แล้ว pan 24 ครั้งระหว่างโหลด (ทำจริงแล้ว)
  จึงผ่าน **ด้วยเหตุผลที่ผิด** — ไม่ใช่เพราะการนับถูก แต่เพราะไม่มีอะไรถูกยกเลิก
* ตัวที่พิสูจน์ว่าการนับถูกคือ unit test (`panning_while_files_load_does_not_stall_the_batch_forever`,
  `a_cancelled_job_does_not_hide_the_board_full_message`) และเทสต์ระดับ pool
  (`a_cancelled_job_says_what_kind_of_job_it_was`) เท่านั้น

> ★★ **`Job::cancel` คือกลไกที่เทสต์ครบแต่ยังไม่มีใครเรียก** — รูปแบบเดียวกับที่
> HANDOFF §2.2 เตือนไว้ และเดียวกับ `fuzz_decode` ที่เป็น stub อยู่ 5 session
> ต่างกันแค่รอบนี้เรารู้ตัวและจดไว้ · P1-4 ออกแบบให้ "pan ผ่านไปแล้วต้องยกเลิกงาน"
> แต่ชั้น UI ไม่เคยถือ `Arc<AtomicBool>` ไว้เพื่อสั่งยกเลิกเลย
>
> **งานที่ต้องทำ (ยังไม่ได้ทำ):** ให้ชั้น UI เก็บธงยกเลิกของงานที่ส่งไป แล้วสั่ง
> ยกเลิกงานที่ผู้ใช้เลื่อนผ่านไปแล้ว — เป็นการตัดสินใจเรื่อง *นโยบาย* (ยกเลิกใบไหน
> ตอนไหน) ไม่ใช่แค่ต่อสาย จึงไม่เดาเอง · ตอนนี้ตัวนับ `decode cancelled` บน
> status bar จึงเป็น **0 เสมอ** ซึ่งเป็นความจริงที่อ่านผิดได้ง่ายว่า "cancellation ทำงานดี"

#### ยืนยันบนจอจริง (ลาก 10,000 ไฟล์ + pan 24 ครั้งระหว่างโหลด)

| | ผล |
|---|---|
| งวดจบไหม | ✅ แถบ "กำลังโหลด" หายไป |
| ข้อความ board เต็ม | ✅ `board นี้เต็มที่ 3072 ภาพ — เพิ่มอีก 6928 ใบจาก 10000 ใบที่เปิดเข้ามาไม่ได้` |
| I-1 | ✅ ตัวนับเฟรม **1081 → 1081** หลังรอ 8 วินาที |

### 2.17 ✅ P3-4: sort + filter + cache ตาม `board.revision` (11 ส.ค. 2026)

`Board::revision()` มีแล้ว · `refx_core::query` เป็นโมดูลใหม่ (pure ทั้งไฟล์
เหมือน `layout`/`align`/`zorder`) · แถบ Arrange มีตัวเรียงและตัวกรองจริง

#### ★★★ เกณฑ์ ROADMAP: ไม่คำนวณซ้ำเมื่อไม่มีอะไรเปลี่ยน — **วัดด้วย counter**

`ArrangeView::queries()` นับจำนวนครั้งที่ **กรอง+เรียงจริง**

| สถานการณ์ | counter |
|---|---|
| เปิดมาแล้ววาดครั้งแรก | 1 |
| **เลื่อน 200 เฟรมติดกัน** | **ยังเป็น 1** |
| เปลี่ยนวิธีเรียง · สลับทิศ · เปลี่ยนตัวกรอง · ติดดาวหนึ่งใบ · undo | +1 ต่อครั้ง |
| ตั้งค่าเดิมซ้ำ (`set_sort`/`set_filter` ด้วยค่าเดิม) | ไม่ขยับ |

★★ **counter ที่คืนเลขน้อย ๆ เฉย ๆ ก็ผ่านข้อแรกได้สบาย** — เทสต์
`the_counter_moves_for_every_way_the_result_can_change` จึงไล่ทุกทางที่ทำให้ผล
เปลี่ยนได้ทีละทาง แล้วบังคับว่าตัวเลขต้องขยับทุกครั้ง (negative control ของตัวเอง)

#### สองชั้นของ cache ที่แยกกันโดยตั้งใจ

เปลี่ยนคนละจังหวะกัน จึงมีคีย์คนละชุด — รวมเป็นคีย์เดียวจะทำให้ย่อหน้าต่าง
(ซึ่งไม่ได้เปลี่ยนลำดับเลย) ไปสั่งกรองใหม่ทั้งกระดานฟรี ๆ

| ชั้น | คีย์ | เปลี่ยนเมื่อ |
|---|---|---|
| กรอง+เรียง (`order`) | `revision` · sort · descending · filter | เนื้อหา board หรือเงื่อนไขเปลี่ยน |
| จัดแผ่น (`sheet`) | engine · params · จำนวน · **`order_generation`** | ข้างบนเปลี่ยน หรือขนาดจอ/engine เปลี่ยน |

★ `order_generation` ขาดไม่ได้: "ลำดับเปลี่ยนแต่จำนวนเท่าเดิม" คือสิ่งที่เกิดทุกครั้ง
ที่กดสลับทิศ — คีย์ที่ดูแค่จำนวนจะไม่จัดแผ่นใหม่ แล้ว **ภาพบนจอไม่ขยับเลย**
ทั้งที่ `order` ถูกแล้ว (เทสต์ `changing_the_sort_moves_the_images_not_just_the_list`)

#### ★★ `Board::revision` — เลขรุ่นที่ตัวแก้ทุกตัวต้องบวก

- **ไม่อยู่ใน `PartialEq`** (เขียน impl เองพร้อม destructure ครบทุกฟิลด์) — เลขรุ่น
  เดินหน้าอย่างเดียว ถ้านับด้วย เทสต์ทุกตัวที่ยืนยันว่า undo คืนสภาพได้จะแดงถาวร
  (§4 ข้อ 20 · เทสต์ `the_revision_is_not_part_of_being_equal`)
- `mark_dirty` **ไม่บวก** — "บันทึกแล้ว" ไม่ใช่การเปลี่ยนเนื้อหา ถ้าบวกด้วย
  การกด Save จะทิ้ง cache ทุกตัวฟรี ๆ
- ★★ `every_mutation_bumps_the_revision` เรียกตัวแก้ **ทุกตัว** ทีละตัวแล้วเทียบ
  เลขก่อน/หลัง — ตัวแก้ที่ลืมบวกคือ cache ที่ค้างรุ่นเก่า **เงียบ ๆ** (ผู้ใช้ติดดาว
  แล้วรายการไม่ขยับ → สรุปว่าโปรแกรมไม่รับคำสั่ง) · negative control: ถอด `touch()`
  ออกจาก `set_meta` → แดงพร้อมชี้ชื่อ

#### ★★★ เจอของจริง: ช่องข้อความที่มี focus เผาซีพียู **13 เฟรม/วินาที ตลอดเวลา**

วัดตอนตรวจ I-1 ของ P3-4: พิมพ์คำค้นแล้วปล่อยไว้ → ตัวนับเฟรม **924 → 1042
ใน 8 วินาที** · คลิกออกจากช่องแล้วหยุดสนิททันที (189 → 189)

ต้นเหตุคือ **เคอร์เซอร์ข้อความกะพริบ** ซึ่ง egui ขอวาดใหม่ทุกครั้งที่มันสลับ
· ไม่ใช่ของใหม่ของ P3-4: **โน้ตของ P2-11 และช่องแท็กของ P3-1 เป็นแบบนี้มาตลอด**
โดยไม่มีใครวัด — P3-4 แค่ทำให้มันเจอง่ายขึ้นเพราะช่องค้นหาอยู่บนแถบเครื่องมือ

→ ปิดการกะพริบ (`visuals.text_cursor.blink = false`) ตอนสร้าง `Context`
· **หลังแก้: 46 → 46 เฟรม ใน 10 วินาที** ทั้งที่ช่องยังมี focus และมีข้อความอยู่

> ★ นี่คือสิ่งที่ I-1 มีไว้กันตรง ๆ: โปรแกรมนี้ถูกเปิดค้างทั้งวันข้าง Photoshop
> ผู้ใช้ที่พิมพ์คำค้นแล้วเดินไปชงกาแฟ ทิ้งมันไว้กินซีพียูทั้งบ่ายโดยที่จอนิ่งสนิท
> · เคอร์เซอร์ที่วาดทึบตลอดยังเห็นชัดเท่าเดิม แลกกันคุ้ม

#### ยืนยันบนจอจริง (24 ภาพ คละสัดส่วน · ชื่อไม่เรียงตามสัดส่วน)

| | ผล |
|---|---|
| เรียงตาม **สัดส่วนภาพ** | แนวตั้ง 8 ใบขึ้นก่อน แล้วจัตุรัส แล้วแนวนอน — เห็นด้วยตาทันที |
| เรียงตาม **ชื่อไฟล์** + สลับทิศ | ลำดับกลับด้านจริง |
| **กรองด้วยคำค้น `tall`** | เหลือ 8 ใบ · status bar ขึ้นสีเตือน **"กรองอยู่ 8 จาก 24"** · ปุ่ม "ล้าง" โผล่ |
| กด "ล้าง" | กลับมา 24 ใบ — **ของไม่ได้หายไปไหน แค่ถูกซ่อน** |
| I-1 | ไม่แตะอะไรเลย 10 วินาที: เฟรม **4 → 4** · มี focus ในช่องค้นหา: **46 → 46** |

#### frame time — วัดสองฝั่งเครื่องเดียวกันเซสชันเดียว (`docs/08 §3.9` ข้อ 7)

3,072 ภาพ · Arrange · `--bench-seconds=20` · `PresentMode::Immediate` · p50 (ms)

| build | รอบ 1 | รอบ 2 | สรุป |
|---|---|---|---|
| ก่อน (`0dd5a6c`, P3-3) | 0.408 | 0.378 | **0.393** |
| **หลัง (P3-4)** | 0.385 | 0.355 | **0.370** |

**ไม่ถดถอย** (−5.9% ซึ่งอยู่ใต้ความแกว่างของรุ่นเดียวกัน: ก่อน 7.9% · หลัง 8.5%)
· วิดเจ็ตที่เพิ่มบนแถบ (combo + ปุ่มทิศ + ดาว 5 + ป้ายสี 7 + ช่องค้นหา) ไม่โผล่
เหนือ noise — ต่างจากตอน P2-9 ที่ปุ่มแปดตัวโผล่มาเป็น +0.014 ms เพราะตอนนั้น
วัดที่ **1,000 quad ที่วาดจริงทั้งหมด** ส่วน Arrange วาดแค่ ~28

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **สถานะของการเรียง/กรองอยู่ที่ชั้น UI ไม่ใช่ใน `Board`** — `Board::arrange`
   มี `sort`/`descending` อยู่จริงและ persist ลง `.refx` แต่ทุกการแก้ `Board`
   ต้องผ่าน `Command` ซึ่งแปลว่า **กดเปลี่ยนวิธีเรียงแล้วกิน undo หนึ่งขั้น**
   · ผู้ใช้ที่สลับไปดูแบบเรียงตามดาวแล้วกด Ctrl+Z ต้องได้ *งาน* คืน ไม่ใช่ได้
   วิธีเรียงคืน (เหตุผลเดียวกับที่ `G` ทั้ง board ไม่เข้า undo — §2.7)
   · และ **ตัวกรองไม่มีที่เก็บใน `Board` เลย** ซึ่งเป็นหลักฐานว่ามันเป็นสถานะ
   ของ *เครื่องมือ* → ให้ทั้งคู่อยู่ด้วยกัน · ถ้าอยากให้ persist ทำตอน P4-1
   พร้อมตัดสินเรื่อง undo ไปด้วยกัน
2. **มี `SortKey` แค่ 5 ตัวจาก 9 ที่ docs/03 §3 ระบุ** — ใส่เฉพาะตัวที่มีข้อมูล
   รองรับจริง (ดูตารางข้างล่าง) · ไม่ใส่ variant ที่ไม่มีทางทำงานตามหลักเดียวกับ
   ที่ P3-2 ไม่ใส่ `respect_pinned` และ P2-9 ลบ `BoardSettings::snap`
3. **การกรองซ่อนเฉพาะใน Arrange ไม่แตะ Canvas** — ตัวกรองเป็นมุมมอง ไม่ใช่การลบ
   · สลับกลับไป Canvas เห็นครบทุกใบเหมือนเดิม
4. **เรียง/กรองใหม่แล้วเด้งกลับบนสุด** — ไม่งั้นผู้ใช้ที่เลื่อนอยู่กลางแผ่นกดเรียงใหม่
   แล้วยังเห็นกลางแผ่นเหมือนเดิม ซึ่งอ่านว่า "กดแล้วไม่มีอะไรเกิดขึ้น"
5. **ตัวกรองป้ายสีเป็นช่องสี ไม่ใช่ ComboBox** — ป้ายสีไม่มี "ชื่อ" ในโปรแกรมนี้
   (มีแต่สี) การตั้งชื่อให้มันที่เดียวจะเป็นคำที่ผู้ใช้ไม่เคยเห็นที่อื่น
   · กดสีเดิมซ้ำ = เลิกกรอง (ไม่ต้องหาปุ่มล้าง)
6. **ทิศทางการเรียงเป็นคำ ไม่ใช่ลูกศร** — ฟอนต์ที่ฝังไม่มี U+2191/2193 (ตรวจไว้
   ตั้งแต่ P2-9 ว่ามีแต่ลูกศรสองหัว) และคำอ่านออกทันทีว่าทางไหน
7. **`LabelFilter` เป็น enum ของตัวเอง ไม่ใช่ `Option<ColorLabel>`** — ตัวหลัง
   พูดว่า "เอาเฉพาะใบที่ไม่มีป้าย" ไม่ได้ เพราะ `None` ในนั้นแปลว่า "ไม่กรอง" ไปแล้ว

#### ★ ที่ spec ระบุไว้แล้วยัง **ทำไม่ได้** — และต้องมีอะไรถึงจะทำได้

| `docs/03 §3` ระบุ | ทำไมยังไม่ได้ | ต้องมีอะไร |
|---|---|---|
| sort `date_modified` | ไม่มี mtime เก็บไว้ที่ไหนใน `Board` (cache key ใช้แต่ไม่ได้เก็บต่อ) | เพิ่มฟิลด์ใน `AssetRef` แล้วร้อยจาก ingest |
| sort `file_size` | เหมือนกัน | เหมือนกัน |
| sort `dominant_hue` | สีเด่นอยู่ใน `ItemRender` ของชั้น UI ไม่ใช่ใน `Board` | ส่งเข้ามาแบบเดียวกับ aspect หรือย้ายขึ้น `Board` |
| sort `canvas_order` | เป็น **P3-6** ซึ่งมีอัลกอริทึมของตัวเอง (docs/03 §4.2 อ่านแบบหนังสือ + tolerance 50%) | ทำที่ P3-6 |
| filter `format` · ช่วงขนาด | `AssetRef.format` เป็น `Unknown` เสมอ (หนี้เดิมใน §6) · ขนาดไฟล์ไม่มีเก็บ | แก้หนี้คอลัมน์ `format` + เพิ่มขนาดไฟล์ |

★ ตัวกรองที่ทำแล้ว: **ดาว ≥ N · ป้ายสี (รวม "ไม่มีป้าย") · แท็ก (any/all/none)
· คำค้นในชื่อไฟล์และโน้ต · เฉพาะที่ปักหมุด** — ครอบสิ่งที่ผู้ใช้ใช้บ่อยที่สุด

> ★★ **แท็กกับปักหมุดกรองได้แล้วแต่ยังไม่มีปุ่มบนแถบ** — `Filter` รองรับครบและ
> มีเทสต์คุม แต่ UI ยังมีแค่ดาว/ป้ายสี/คำค้น · การเลือกแท็กต้องมีรายการแท็กของ
> board ให้เลือก ซึ่งเป็น widget ที่ใหญ่กว่าที่แถบเครื่องมือรับไหว → รอ P3-7
> (group/ungroup) ที่จะมีแผงของตัวเอง · **จดไว้ตรงนี้ว่าเป็นของที่มีแล้วแต่ยังกดไม่ได้**

### 2.18 ✅ I-1 ทั้งหกสถานะ · sort ที่ปลดล็อกได้ · P3-5 (12 ส.ค. 2026)

#### ★★★ ไล่วัด I-1 ย้อนหลังทุกสถานะตาม `docs/08 §3.9` ข้อ 11

วัดด้วยตัวนับเฟรมบน status bar สองครั้งห่างกัน 10–12 วินาที · **เท่ากัน = หลับจริง**

| สถานะ | เฟรม | |
|---|---|---|
| ว่างเปล่า ไม่มีภาพ | 160 → 160 | ✅ |
| มีภาพ 24 ใบ (Canvas) | 17 → 17 | ✅ |
| **เต็ม board 3,072 ใบ (Arrange)** | 838 → 838 | ✅ |
| **focus ในช่องค้นหาของ Arrange** | 46 → 46 | ✅ *(ก่อนแก้: 924 → 1042 ใน 8 วิ)* |
| **focus ในช่องโน้ตของ Inspector (P2-11)** | 43 → 43 | ✅ *(อาการเดียวกัน แก้ด้วยกัน)* |
| เครื่องมือ Crop เปิดอยู่ | 32 → 32 | ✅ |
| มีของถูกเลือกอยู่ | 192 → 192 | ✅ |
| หลังไฟล์เปิดไม่ได้ (ลากไฟล์เสียเข้ามา) | 7 → 7 → 7 → 7 (60 วิ) | ✅ *(ดูหมายเหตุ)* |
| **เพิ่มเอง: เคอร์เซอร์ค้างเหนือ inspector / canvas / ปุ่ม** | 10→10 · 13→13 · 38→38 | ✅ |

★ เพิ่มสถานะที่เจ็ดเองเพราะสงสัยว่า hover ของ egui อาจ animate ค้าง — **ไม่ค้าง**
ตัดออกจากรายการผู้ต้องสงสัยได้ถาวรแล้ว

> ⚠️ **หนึ่งครั้งที่วัดได้ 24 → 62 ใน 10 วินาที (สถานะไฟล์เปิดไม่ได้) แล้วทำซ้ำไม่ได้**
> รันซ้ำสภาพเดียวกันได้ 7 → 7 → 7 → 7 ตลอด 60 วินาที · หาสาเหตุไม่เจอและ
> ไม่ได้แก้อะไร — **จดไว้ตามกฎ flake ของ §6**: เจออีกเมื่อไหร่ให้จดสภาพให้ครบทันที
> (สิ่งที่ทำอยู่ก่อนหน้า · เคอร์เซอร์อยู่ไหน · มีอะไรค้างในคิว)

#### ★★ ประตูของภาพหน้าจอมีรูที่ปิดแล้ว — ตรวจ focus **หลัง** copy ด้วย

ระหว่างไล่วัด มีภาพหนึ่งที่ถ่ายออกมาเป็น **หน้าต่างของโปรแกรมอื่นทั้งใบ**
ทั้งที่ `Assert-Focus` ผ่าน · เพราะด่านเดิมตรวจ **ก่อน** `CopyFromScreen`
เหลือช่องว่างไม่กี่ร้อยมิลลิวินาทีให้โปรแกรมอื่นแย่ง foreground ไปได้

→ `scripts/ui-drive.ps1` ตรวจ foreground **อีกครั้งหลังก๊อปพิกเซลเสร็จ** แล้ว
**ทิ้ง bitmap ไปเลยแทนที่จะบันทึก** (exit 1) · เป็นรูเดียวกับที่ P2-9 เสียไปทั้ง
session ต่างกันแค่หน้าต่างเวลาแคบกว่า (`docs/08 §3.9` ข้อ 9)

#### ★ sort สองตัวที่ปลดล็อกได้ — ข้อมูลอยู่ในมืออยู่แล้ว

`AssetRef` เก็บ `mtime` (unix millis) + `file_size` ตาม `docs/02 §2.3` แล้ว
· **ไม่ได้อ่านดิสก์เพิ่มบน UI thread** — `SourceMeta::read` ทำงานบน decode worker
ตอน ingest (`stat` หนึ่งครั้งต่อไฟล์ ครั้งเดียวต่อภาพ ไม่ใช่ต่อเฟรม)

- `SortKey::ModifiedAt` · `SortKey::FileSize` → **เรียงได้ 7 จาก 9 ตัวใน docs/03 §3**
- ★ `added_at` **ไม่เคยมีใครเซ็ตมาก่อน** (เป็น 0 ทุกใบ) — ตอนนี้เซ็ตตอนสร้าง item
  จริง ๆ แล้ว การเรียง "เวลาที่เพิ่ม" จึงเพิ่งมีความหมายจริงในรอบนี้
- ★★ `SortKey::ALL` + `every_sort_key_can_be_picked_from_the_toolbar` — วิธีเรียงที่มี
  ใน enum แต่ไม่มีปุ่มให้กด = ฟีเจอร์ที่ไม่มีอยู่จริงสำหรับผู้ใช้ · และ
  `all_lists_every_sort_key` ใช้ `match` ที่ไม่มี `_` บังคับให้คนที่เพิ่ม variant
  ต้องมาเห็น `ALL` (ถ้าใช้แค่ `assert_eq!(len, 7)` การแก้เลขให้ผ่านเป็นเรื่องง่ายเกินไป)

ที่ยังทำไม่ได้เหมือนเดิม: `dominant_hue` (อยู่ `ItemRender` ของชั้น UI) ·
`canvas_order` (**P3-6** มีอัลกอริทึมของตัวเอง) · filter `format` (ติดหนี้
`ImageFormat::Unknown` เสมอ — §6)

#### ✅ P3-5: `ApplyLayout` (Arrange → Canvas)

**นี่คือจุดแรกที่ layout engine ของ P3-2 ไปแตะข้อมูลจริง** — P3-3/P3-4 อ่านอย่างเดียว

| เกณฑ์ | ผลบนจอจริง (24 ภาพ) |
|---|---|
| กด "ส่งเข้า Canvas" | ภาพเรียงเป็นตาราง 4 คอลัมน์ · สลับไปโหมด Canvas ให้เอง · status bar: "จัด 24 ภาพลง canvas แล้ว" |
| **undo ครั้งเดียว** | **พิกเซลของช่องกลางต่างจากก่อนจัด 0 ไบต์** (เทียบกับตอนจัดแล้วซึ่งต่าง 179,790 ไบต์) |

★ ต้องล้างการเลือกก่อนเทียบพิกเซล — undo ตั้ง selection จาก `affected()` (P2-4)
ทำให้มีกรอบเลือก 24 กรอบวาดทับอยู่ ต่างกัน 15,720 ไบต์ทั้งที่ **ข้อมูลเหมือนกันเป๊ะ**
· วัดผิดครั้งแรกเพราะข้อนี้ — พิกเซลรวมกรอบชั่วคราวไม่ใช่ตัววัดของข้อมูล

#### ★★★ `respect_pinned` — ตัดสินใหม่ตอนที่มี `Board` อยู่ในมือแล้ว

P3-2 ลบ `respect_pinned` ทิ้งเพราะ input ของ engine ไม่มีทั้งธง pin และกรอบจริง
· ตอนนี้ `ApplyLayout::from_placed` มี `&Board` จึงตัดสินได้จริง:

| `docs/03 §4.1` เขียนว่า | ทำแล้ว? |
|---|---|
| "ไม่ขยับภาพที่ `pinned = true`" | ✅ ข้ามทั้งใบที่ปักหมุด **และใบที่ล็อกไว้** |
| "(layout จะจัดรอบมันแทน)" | ❌ **ยังไม่ได้ทำ** — ต้องมีการตรวจการทับกันในตัว engine ซึ่งไม่มีตัวไหนรองรับ |

★ ผลตอนนี้: ของที่จัดใหม่ **อาจไปวางทับใบที่ปักหมุดได้** ซึ่งผู้ใช้ลากแก้เองได้
— ต่างจากการ *ย้ายใบที่เขาสั่งไม่ให้ย้าย* ซึ่งแก้กลับเองไม่ได้เลย จึงเลือกทางนี้
· ★ ใบที่ `locked` ก็ไม่ถูกแตะด้วยเหตุผลเดียวกัน (ล็อก = ห้ามแก้ — การจัดทั้ง
กระดานต้องไม่เป็นทางลัดที่ข้ามมันไปได้)

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง (P3-5)

1. **กลุ่มอยู่ที่เดิม** — จุดกึ่งกลางกรอบรวมหลังจัดถูกเลื่อนให้ตรงกับก่อนจัด ·
   `Placed` เริ่มที่ `(0,0)` เสมอ ถ้าใส่ตรง ๆ ภาพทั้งกระดานจะกระโดดไปมุมซ้ายบน
   ของ world แล้วผู้ใช้อ่านว่า "กดแล้วภาพหายหมด"
   · docs/03 §4.1 มีอีกสองตัวเลือก (กลางจอ canvas · ต่อท้ายด้านล่าง) **ยังไม่ได้ทำ**
2. **`merge` คืน `false` เสมอ** — จัดสองครั้งติดกันคือสองการตัดสินใจ (ลอง Grid
   แล้วลอง Masonry) ถ้ายุบเป็นขั้นเดียว Ctrl+Z จะข้ามกลับไปก่อนจัดครั้งแรก
3. **จัดแล้วไม่มีอะไรขยับ = ไม่มีคำสั่ง** (`from_placed` คืน `Option`) และบอกผู้ใช้ว่า
   "ไม่มีอะไรขยับ" — ไม่ใช่เงียบ และไม่กิน undo เปล่า
4. **สลับไป Canvas ให้เองหลังกด** — ปุ่มชื่อ "ส่งเข้า canvas" แล้วอยู่ที่เดิมคือ
   การกดที่ไม่มีอะไรเกิดขึ้นในสายตาผู้ใช้ (ผลอยู่อีกโหมดหนึ่ง)
5. **การจัดแตะแค่ `pos`/`size`** — crop/flip/filter/opacity/rotation ที่ผู้ใช้แต่งไว้
   ไม่ถูกล้าง (มีเทสต์คุม)

### 2.19 ★ audit: ฟิลด์ที่ **ไม่มีใครเขียนค่าจริงลงไป** + P3-6 (12 ส.ค. 2026)

#### ★★★ รูปแบบที่สาม: ฟิลด์ที่มีอยู่ อ่านได้ แต่ค่าไม่เคยมาจากผู้ใช้

ต่อจาก audit สองรอบก่อน (§2.2b enum ที่ขาดทางออก · §2.2d ฟิลด์ที่ไม่ถึง shader)
· รอบนี้ต่างจากสองรอบนั้นตรงที่ **คอมไพเลอร์ช่วยไม่ได้เลย** — ฟิลด์มีอยู่จริง
อ่านได้จริง ใช้งานได้จริง แค่ค่าข้างในเป็นค่าปริยายตลอดอายุโปรแกรม

| ฟิลด์ | มีคนเขียนไหม | อยู่คิวไหน / ทำอะไรกับมัน |
|---|---|---|
| `ItemCanvas` pos · size · rotation · flip · opacity · crop · filter | ✅ | P2-5 · P2-7 · P2-8 |
| `ItemCanvas::locked` | ❌ | **ไม่มีปุ่มล็อก** — โค้ดที่ *อ่าน* มีครบแล้ว (`SelectTool`, `ApplyLayout`) รอ UI |
| `ItemCanvas::visible` | ❌ | ไม่มีปุ่มซ่อน · `.refx` จะพาค่ามาตอน P4-1 (§2.2d) |
| `ItemMeta` tags · rating · color_label · note · pinned | ✅ | แผง Arrange (P3-1) |
| `ItemMeta::added_at` | ✅ **เพิ่งแก้** | เป็น `0` ทุกใบมาตลอด → เรียง "เวลาที่เพิ่ม" ไม่มีความหมาย · ตั้งตอนสร้าง item แล้ว (§2.18) |
| `ItemMeta::group` | ✅ **เพิ่งแก้** | `Ctrl+G`/`Ctrl+Shift+G` → `GroupItems`/`Ungroup` (P3-7 · §2.20) |
| `AssetRef` hash · path · px_size | ✅ | ingest |
| `AssetRef` mtime · file_size | ✅ **เพิ่งเพิ่ม** | §2.18 |
| `AssetRef::format` | ❌ | เป็น `Unknown` เสมอ — ต้องร้อยจาก `image::guess_format` พร้อมตัดสินคอลัมน์ `format` ใน `cache.sqlite` (§6) |
| `AssetRef::embedded` | ❌ | packed mode — **P4-5** |
| `BoardSettings::background` | ❌ (เขียน) / ✅ (อ่าน **เพิ่งต่อ**) | **ไม่มีใครอ่านมาก่อนเลย** — render pass ใช้ค่าคงที่ `CLEAR_COLOR` แทน · ตอนนี้ `clear_colour(board)` อ่านจากฟิลด์จริงแล้ว เหลือแค่ UI ให้เปลี่ยนสี |
| `ArrangeState` sort · descending | ❌ | P3-4 ตัดสินให้อยู่ชั้น UI (§2.17) — จะมีคนเขียนก็ต่อเมื่อ P4-1 ตัดสินว่าต้อง persist |
| `Group` name · collapsed | ✅ **เพิ่งแก้** | ชื่อตั้งอัตโนมัติตอนสร้าง + ช่องเปลี่ยนชื่อในแผง Arrange · `collapsed` **ยุบแผ่นจริง** ใน `query::select` (P3-7 · §2.20) |
| `Board` id · name | ⚠️ | ตั้งตอน `Board::new` ครั้งเดียว **ไม่มีทางเปลี่ยนชื่อ** — P4-7 หลาย board |
| `Board::view` (`ViewState`) | ❌ | **กล้องที่ persist ลงไฟล์ไม่เคยถูกซิงค์กับกล้องจริง** (`Gfx::camera` แยกกันอยู่) → P4-1 จะบันทึกกล้องค่าปริยายถ้าไม่แก้ · **จังหวะที่ควรซิงค์เป็นการตัดสินใจของ P4-1** จึงยังไม่ทำ |
| `Board` items · z_order · tags · dirty · revision | ✅ | |
| `cache.sqlite` คอลัมน์ `format` | ❌ | เขียน `0` ตายตัว — หนี้เดียวกับ `AssetRef::format` (§6) |
| `cache.sqlite` คอลัมน์ `thumb_fmt` | ⚠️ | เขียน `Rgba8` เสมอ · **ตั้งใจ** ตาม spec (เก็บคอลัมน์ไว้เผื่ออนาคต ไม่มีโค้ดสาขา) |

#### ประตูที่ทำให้ฟิลด์ตัวถัดไปส่งเสียงเอง

`no_field_stays_unwritten_without_us_knowing` (`refx-core::board`) — destructure
`ItemCanvas`/`ItemMeta`/`AssetRef`/`BoardSettings`/`ArrangeState`/`Group`
**ครบทุกฟิลด์ไม่มี `..`** แล้วจับคู่กับสถานะ `Real` / `NoneYet(เหตุผล)`
→ เพิ่มฟิลด์ใหม่เมื่อไหร่ **คอมไพล์ไม่ผ่าน** จนกว่าจะมีคนตัดสินว่าใครจะเขียนมัน
· พิมพ์รายการออกมาทุกครั้งที่รัน (`--nocapture`) และล้มถ้าจำนวนเกินที่บันทึกไว้

> ★ **ที่มันทำไม่ได้และต้องรู้ไว้:** ตรวจไม่ได้ว่าโค้ดที่เคยเขียนยัง*เขียนอยู่จริง*ไหม
> — ถ้าวันหนึ่งมีคนลบเส้นทางที่เขียน `rating` ทิ้ง ตารางจะยังบอกว่า `Real`
> · กันได้แค่ฟิลด์ **ใหม่** ที่โผล่มาโดยไม่มีใครถามว่าใครจะเขียนมัน
> (`Board`/`Item` ไม่ต้องมีในตารางนี้เพราะ `impl PartialEq for Board` destructure
> ครบอยู่แล้ว — เพิ่มฟิลด์ที่นั่นก็คอมไพล์ไม่ผ่านเหมือนกัน)

#### ✅ P3-6: เรียงตามตำแหน่งบน canvas (Canvas → Arrange)

`SortKey::CanvasOrder` — อ่านแบบหนังสือ บน→ล่าง ซ้าย→ขวา · ระยะที่ยังนับว่า
แถวเดียวกัน = **50% ของความสูงเฉลี่ย** ตาม docs/03 §4.2

★★ **ตัวเดียวในกลุ่มที่ไม่ใช่การเปรียบเทียบรายคู่** — การตัดสินว่าสองใบอยู่แถว
เดียวกันไหมต้องรู้ความสูงเฉลี่ยของ *ทั้งชุด* ก่อน · ฟังก์ชันเปรียบเทียบที่เห็นทีละ
สองใบเขียนกฎนี้ไม่ได้ และถ้าฝืนเขียน ผลจะไม่ transitive ทำให้ `sort_by`
ให้ผลที่ไม่มีความหมาย → มีเส้นทางของตัวเองใน `select`

★ **หมุดของแถวคือใบแรกในแถว ไม่ใช่ค่าเฉลี่ยที่เลื่อนตาม** — ภาพที่วางไล่ระดับ
ลงมาทีละ 30 หน่วยจะกลายเป็นแถวเดียวยาวเหยียดทั้งที่ใบแรกกับใบสุดท้ายห่างกัน
270 หน่วย (negative control: เปลี่ยนเป็นหมุดที่เลื่อนตาม → เทสต์แดง)

★ ปุ่มของ docs/03 §4.2 คือ **ตัวเลือกในรายการเรียงอันเดิม** ไม่ใช่ปุ่มแยก —
P3-4 ย้ายการเรียงมาอยู่ชั้น UI แล้ว การมีปุ่มที่ทำสิ่งเดียวกับตัวเลือกในรายการ
คือสองทางที่ต้องซิงค์กันโดยไม่ได้อะไรเพิ่ม

negative control: ตั้งระยะเป็น 0 (เทียบ `y` ตรง ๆ) → เทสต์ "แถวไม่ตรงกันเป๊ะ" แดงทันที

### 2.20 ✅ P3-7: group / ungroup (13 ส.ค. 2026 · `cd47544`)

ปลดล็อกสามฟิลด์ที่รอมาตั้งแต่ P2-1 พร้อมกัน: `ItemMeta::group` · `Group::name`
· `Group::collapsed` แล้วแก้ตารางในประตู `no_field_stays_unwritten_without_us_knowing`
ให้ตรง (10 → 7) แทนที่จะปิดเทสต์

คำสั่งสามตัว undo/redo ได้ทุกตัว: `GroupItems` (`Ctrl+G`) · `Ungroup`
(`Ctrl+Shift+G`) · `SetGroup` (เปลี่ยนชื่อ/ยุบ · merge ต่อช่องเหมือน `EditMeta`)

#### ★★ กลุ่มไม่มีรายชื่อสมาชิกของตัวเอง — และผลที่ตามมา

ความเป็นสมาชิกอยู่ที่ `ItemMeta::group` **ฝั่งเดียว** ถ้าเก็บสองที่ (กลุ่มถือ
รายชื่อ *และ* item ถือ id กลุ่ม) ทั้งคู่จะ drift แล้ว undo ต้องคืนให้ตรงกัน
ทั้งคู่พอดี — แหล่งความจริงที่สองแบบเดียวกับที่ §2.2 เสียเวลาทั้ง session ไปแก้

ผลที่ตามมาคือ **กลุ่มที่สมาชิกย้ายออกหมดลบเองไม่ได้เลย** (ไม่มีสมาชิกให้เลือก
ไปสั่ง ungroup) จึงต้องมีการ **เก็บกวาดอัตโนมัติ** ไม่งั้นเป็นขยะถาวรที่ถูก
persist ลง `.refx` ไปด้วย · undo ใส่มันกลับที่ **คีย์เดิม** (`restore_group`)
ด้วยเหตุผลเดียวกับ `Arena::insert_at` — §4 ข้อ 19

#### ★ `collapsed` ต้อง *ทำงาน* ไม่ใช่แค่ถูกเขียน

`query::select` ยุบกลุ่มที่ `collapsed` ให้เหลือสมาชิกใบเดียวในแผ่น Arrange
· ตัวแทนคือใบแรก **ในลำดับที่เรียงเสร็จแล้ว** ไม่ใช่ใบแรกใน z-order (เรียงตาม
ดาวแล้วยุบ ควรเห็นใบดาวสูงสุดเป็นหน้ากลุ่ม) · checkbox ที่กดแล้วไม่มีอะไรเกิดขึ้น
โกหกผู้ใช้มากกว่าไม่มี checkbox — และเป็นโครงเปล่าที่ `docs/08 §3.9` ข้อ 2 ห้าม

★ id ที่ห้อยอยู่ (กลุ่มถูกลบแล้ว) = **แสดงตามปกติ ห้ามซ่อน** — ข้อมูลไม่ครบ
ต้องไม่แปลว่าภาพหาย (I-3)

#### ★★★ บั๊กที่ P3-7 เปิดโปง: undo ล้างการเลือกทั้งที่คำสั่งบอกว่าอย่าล้าง

`ReorderZ::affected()` คืนรายการว่าง **พร้อมคอมเมนต์ในตัวมันเองว่า "ปล่อยให้
selection เดิมอยู่ต่อ"** มาตั้งแต่ P2-6 แต่ `apply_history_request` เรียก
`restore(vec![])` ซึ่งคือการ **ล้าง** — สัญญาที่ `refx-core` ประกาศไว้ไม่เคยถูก
ทำตามเลยบนเส้นทาง undo

มองไม่เห็นมาตลอดเพราะการย้ายชั้นไม่มีแผงที่ผูกกับการเลือก · P3-7 มี:
กดยุบกลุ่ม → `Ctrl+Z` → **แผงกลุ่มหายไปทั้งแผง** กางกลับทันทีไม่ได้

แก้ที่ `selection_after_history()` (แยกเป็นฟังก์ชันบริสุทธิ์เพื่อให้เทสต์ได้
โดยไม่ต้องมีหน้าต่าง) · ★ ต้องดู `affected` **ก่อนกรอง** ไม่ใช่หลัง: undo ของ
การเพิ่มภาพรายงาน id มาจริงแต่ตายหมด — นั่นต้องล้างจริง ๆ ส่วน "ไม่ได้แตะ
ใครเลย" ต้องไม่แตะการเลือก **สองอย่างนี้แยกไม่ออกถ้าเช็ครายการหลังกรอง**

#### ★ สิ่งที่ spec ไม่ได้ระบุแล้วตัดสินเอง

1. **การยุบผ่าน `Command` และทำให้ `dirty`** — `Group::collapsed` อยู่ใน `Board`
   จึงเป็นเอกสารตาม `docs/08 §4` ข้อ 10 · ข้อยกเว้นมีแค่ `view` กับ `dirty`
   และ HANDOFF สั่งว่าเจอข้อที่สามให้ **หยุดถาม** ไม่ใช่เจาะรูเพิ่มเอง
2. **ชื่ออัตโนมัติมาจากชั้น UI** (`Key::GroupDefaultName`) ไม่ใช่ค่าคงที่ใน core
   — ชื่อถูก persist ลงไฟล์จึงเป็น *ข้อมูลของผู้ใช้* ต้องเป็นภาษาที่เขาใช้อยู่
   · `unused_group_name` ไล่หาเลขที่ **ว่างจริง** ไม่ใช่ `len() + 1`
   (สร้างสาม ลบตัวกลาง แล้วสร้างใหม่ = ชื่อซ้ำ)
3. **item อยู่ได้ทีละกลุ่ม ไม่ซ้อนชั้น** — `ItemMeta::group` เป็น `Option` ตัวเดียว
   ตาม `docs/02 §2.2` ไม่ใช่ต้นไม้ · `Ctrl+G` บนของที่มีกลุ่มแล้ว = **ย้าย**
4. **กด `Ctrl+G` ซ้ำบนกลุ่มเดิมทั้งกลุ่ม → `CmdError::Empty`** ไม่สร้างขั้นเปล่า
   ใน undo stack และไม่ขอเฟรม (I-1)
5. **ช่องเปลี่ยนชื่อโผล่เฉพาะตอนเลือกอยู่กลุ่มเดียวกันหมด** (`Mixed` แสดงข้อความแทน)
   ไม่งั้นพิมพ์ครั้งเดียวเขียนทับกลุ่มที่บังเอิญเลือกติดมา
6. **การจัดกลุ่มใช้ selection ของ Canvas** เพราะ Arrange ยังไม่มีการเลือกเป็นของ
   ตัวเอง (มีแค่เลื่อน) — ทางเดียวกับที่ P3-1 แก้ rating/tag อยู่แล้ว

#### ยืนยันบนของจริง + I-1

ภาพหน้าจอ 1280×800: เลือกสามใบใน Canvas → `Ctrl+G` → Arrange โชว์ "Group 1"
+ "3 in this group" · กด Collapsed → status bar **drawn 8 of 8 → drawn 6 of 6**
ขณะที่ board ยังเป็น "8 items" · `Ctrl+Z` → กลับเป็น 8 **และแผงกลุ่มยังอยู่**
· `Ctrl+Shift+G` → "Not in a group" · `Ctrl+Z` → กลับมาครบ

I-1 สามสถานะใหม่ (`docs/08 §3.9` ข้อ 11) วัดด้วยตัวนับเฟรมบน status bar:

| สถานะ | t0 | t+6s |
|---|---|---|
| **focus อยู่ในช่องชื่อกลุ่ม** (ช่องข้อความตัวใหม่) | 222 | **222** |
| Arrange + กลุ่มยุบอยู่ + มีของถูกเลือก | 332 | **332** |
| Canvas + มีของถูกเลือก + มีกลุ่มอยู่ | 341 | **341** |

★ ยืนยันว่า **ช่องมี focus จริง** ด้วยการครอปภาพมาดูขอบไฮไลต์กับเคอร์เซอร์
ไม่ใช่เชื่อว่าคลิกโดน — ตัวเลขที่ไม่ขยับเพราะคลิกพลาดคือเขียวปลอม
(การกะพริบถูกปิดทั้งโปรแกรมตั้งแต่ §2.17 ช่องใหม่จึงได้ประโยชน์ไปด้วย
แต่ **วัดแล้ว** ไม่ได้เดาจากข้อนั้น)

#### ★★ negative control ตัวที่ไม่แดงรอบแรก — และสิ่งที่มันบอก

ห้าตัว แดงครบหลังแก้ · ตัวที่สี่ (นับ id ที่ห้อยว่ายุบ) **ไม่แดงรอบแรก**
และมันบอกว่า *เทสต์อ่อน* ไม่ใช่ *ดีไซน์กันไว้*: `fold_collapsed_groups`
มีทางลัดที่ออกทันทีเมื่อไม่มีกลุ่มไหนยุบอยู่เลย เทสต์เดิมลบกลุ่มเดียวที่มีทิ้ง
ทางลัดจึงทำงานแล้วผลถูก **โดยบังเอิญ** → แก้เทสต์ให้มีกลุ่มที่ยุบอยู่จริง
ค้างไว้อีกกลุ่ม แล้วมันแดงทันที

### 2.21 ✅ P3-8: สลับ mode 100 ครั้ง — **P3 ปิดครบ** (13 ส.ค. 2026 · `cbf0889`)

เทสต์สองตัวพิสูจน์คนละครึ่งของข้อกำหนดหลักของดีไซน์สองโหมด

#### ★★★ ทำไมเช็คแค่ `dirty` ไม่พอ — และทำไม `revision` ถึงแรงกว่า `==`

`dirty` เป็นแค่ธงที่ `History` คำนวณจากความลึกของ stack — ยัง `false` อยู่ดี
ถ้ามีใครแก้ board **นอกเส้นทาง `Command`** ซึ่งคือสภาพที่แย่ที่สุดที่เป็นไปได้
(ข้อมูลเปลี่ยนโดยไม่มีทางย้อน และไม่มีอะไรบอกว่าต้องบันทึก)

`a_hundred_mode_switches_leave_the_board_exactly_as_it_was` จึงเทียบ **`Board`
ทั้งก้อน** บวก **`revision` ที่ `PartialEq` ไม่นับ** (§4 ข้อ 20) และ assert
`revision` **ก่อน** เพราะมันจับได้กว้างที่สุด

negative control (ฉีดคู่ `apply`+`undo` เข้าไปในลูป — คืน `dirty` เป็น false
และคืนทุกค่าจน `==` ผ่าน แต่ `revision` ขยับสองครั้ง):

| assert | ผล |
|---|---|
| `revision` | **แดง** (152 vs 52) |
| `dirty` | เขียว — มองไม่เห็น |
| `==` ทั้ง `Board` | เขียว — มองไม่เห็น |
| `redo_depth` | แดง — แต่เพราะการฉีดนั้นใช้ `undo` เท่านั้น |

★ **ที่ยังพิสูจน์ไม่ได้จากในเครทนั้น** (เขียนไว้ในเทสต์แล้ว): การเขียนที่ไม่ผ่าน
`History` เลย จะรอดทั้ง `dirty` และ `redo_depth` เหลือ `revision` เป็นตาข่ายเดียว
— เขียนเทสต์ให้ทำแบบนั้นไม่ได้เพราะตัวแก้ของ `Board` เป็น `pub(crate)` ของ
`refx-core` ซึ่งก็คือกำแพงที่ทำให้สภาพนั้นเกิดยากอยู่แล้ว **บันทึกไว้ว่ารู้
ไม่ใช่อ้างว่าตรวจแล้ว**

#### ★★★ ครึ่งที่สอง: ประตูที่บังคับตอนคอมไพล์

`ArrangeView::plan` กับ `query::select` รับ `&Board` คอมไพเลอร์กันการเขียนให้แล้ว
· **ทางเดียวที่เหลือ**คือ shell เขียน "คำขอ" ค้างไว้แล้ว `app` ห่อเป็น `Command`
ในเฟรมถัดไป (รูปแบบ `docs/08 §3.9` ข้อ 8.1 — ค่าที่ค้างแล้วถูกเขียนทุกเฟรม
จะทับสิ่งที่ undo เพิ่งคืนมา)

`switching_mode_a_hundred_times_never_asks_to_change_the_document` วาด shell จริง
ด้วย `ctx.run_ui` 100 รอบสลับโหมด **ด้วยสถานะที่มีของให้แตะครบ** (เลือกอยู่ ·
มีกลุ่ม · มีโน้ต) ไม่ใช่ `default()` เปล่าที่พิสูจน์อะไรไม่ได้

`asks_to_touch_the_document()` **destructure `ShellState` ครบทั้ง 44 ฟิลด์
ไม่มี `..`** แล้วจับคู่ว่าแต่ละตัวเป็น "คำขอแก้เอกสาร" หรือ "สถานะของมุมมอง"
→ เพิ่มช่องทางใหม่เมื่อไหร่ **คอมไพล์ไม่ผ่าน** จนกว่าจะมีคนตัดสิน
(หลักการเดียวกับ §4 ข้อ 16) · negative control สองตัว แดงทั้งคู่:
ถอด `.changed()` ของ checkbox ยุบกลุ่ม · ถอด `.changed()` ของช่องโน้ตในแผง Arrange

### 2.22 ✅ P4-1: `.refx` format v1 (linked) + DTO (13 ส.ค. 2026 · `2d99d86`)

DTO อยู่ `refx-io::dto` **แยกจากชนิดใน `refx-core` เสมอ** (docs/07 §3) —
refactor ภายในจึงไม่ทำให้ไฟล์เก่าเปิดไม่ได้ · `Board::load(BoardParts)` /
`to_parts()` เป็นการ **สร้างใหม่ ไม่ใช่แก้ board ที่มีอยู่** ตัวแก้ทุกตัวยังเป็น
`pub(crate)` ตามเดิม (docs/08 §4 ข้อ 10 ยังอยู่ครบ)

#### ★★★ สามข้อที่บังคับให้จัดการในรอบนั้น

1. **`Board::view` ไม่เคยถูกซิงค์เลย** → `set_view()` เรียก**ทุกเฟรม** จากชั้น UI
   · ไม่ผ่าน `Command` · ไม่ `dirty` · **ไม่บวก `revision`** (ตัวหลังสำคัญ:
   `revision` คือคีย์ cache ของ P3-4 ถ้ากล้องขยับแล้วบวก จะกรอง+เรียงใหม่ทุกเฟรม)
   ★ เลือกทุกเฟรมแทนตอน save เพราะ "ถูกเฉพาะถ้าเรียกถูกจังหวะ" คือกับดัก §3.9 ข้อ 8
2. **round-trip ของค่าที่ไม่รู้จัก** → `Flip` และ `MissingReason` ถือค่าดิบแบบ
   `ColorLabel` · เทสต์เดินเคสผู้ใช้ทั้งเส้น: เปิด → บันทึกทับโดยไม่แก้ → เปิดใหม่
   → ค่าอยู่ครบ · `Mode` **จงใจไม่ถือค่าดิบ** (เป็นมุมมองไม่ใช่เนื้อหา)
3. **`ImageFormat` เป็น `Unknown` เสมอ → ไม่เขียนลงไฟล์** ("อย่าบันทึกฟิลด์ที่
   ไม่มีความหมาย") · ตัดออกด้วยเหตุผลเดียวกันอีกสาม: `embedded` · `ArrangeState`
   (**ตอบคำถามที่ค้างว่าไม่ persist ใน v1**) · `id`/`dirty`/`revision`

#### ★ ไฟล์จากรุ่นใหม่กว่า — spec เคยขัดกันเอง

`docs/07 §3` เขียน "ปฏิเสธ" ส่วน `docs/02 §2.9` เขียน "อ่านอย่างเดียว" ·
**ตัดสินแล้ว (`770c2d1`) ให้ยอมตาม docs/07**: postcard ไม่ self-describing
อ่าน v2 ด้วยโครง v1 ได้ค่าเพี้ยนทั้งก้อนโดยไม่มีอะไรฟ้อง ซึ่งแย่กว่าเปิดไม่ได้
· ครึ่งที่ปกป้องผู้ใช้จริงคือ `may_overwrite()` ซึ่งทำครบ

#### fuzz_document ต่อครบสามจุด

**ยิงสามทางโดยตั้งใจ** — ยิงแต่ไบต์ดิบจะติดด่าน magic แล้วเกือบทุก input จบ
ตั้งแต่ 4 ไบต์แรก จึงประกอบหัวไฟล์ที่ถูกต้องให้ในทางที่ 2 (ยิง zstd) และ 3
(ยิง postcard ตรง ๆ) · **รันจริง 51,434 รอบ ไม่มี crash** · libFuzzer เก็บ
`"REFX"` เข้า dictionary เอง = หลักฐานว่าไปถึง parser จริง

> ★ **path ที่ไม่ใช่ UTF-8 เป็น lossy** — ยอมได้เพราะ `docs/02 §2.3` กำหนดว่า
> `hash` เป็นคีย์หลัก `path` เป็นแค่ hint · relink ยังหาไฟล์เจอด้วย hash
> **อย่าเปลี่ยนเป็นไบต์โดยไม่มีเหตุผลใหม่** — จะทำให้ไฟล์ย้ายข้าม OS ไม่ได้

### 2.23 ✅ P4-2: atomic save + `Ctrl+S` (13–14 ส.ค. 2026 · `c7fdb36` · `9abaeac`)

tmp → fsync → สำเนาเป็น `.bak` (atomic) → rename ทับตัวจริง

**เกณฑ์ ROADMAP วัดจริง: ฆ่าโปรเซส 100 ครั้ง — เก่า 43 / ใหม่ 57 / เสียหาย 0**
★ 43/57 คือหลักฐานว่าการฆ่าตกทั้งก่อนและหลัง rename · มี
`assert!(survived_as_new > 0)` คุมไว้ **และด่านนั้นจับบั๊กของเทสต์เองได้สองตัว**
(เหยื่อถูกฆ่าก่อนเริ่มเขียน · `--exact` ไม่จับชื่อสั้นจนลูกได้ "running 0 tests")

#### ★★★ negative control ที่ไม่แดง — และมันถูกต้อง

| ถอดอะไร | ผล |
|---|---|
| tmp + rename | ✅ แดง |
| **`sync_all()`** | ★ **ยังเขียว** |

"ดีไซน์ทำให้พังแบบนั้นไม่ได้" ไม่ใช่ "เทสต์อ่อน" — การฆ่าโปรเซสไม่ทำให้ข้อมูล
ที่ OS รับไปแล้วหาย `fsync` ปกป้อง**ไฟดับ** ซึ่ง `TerminateProcess` จำลองไม่ได้
→ **ห้ามอ้างว่าเทสต์นี้พิสูจน์ fsync**

#### ★★ I-5 เคยเข้าใจผิด — fsync การ rename บน Windows ทำได้

เคยเขียนว่า "ทำไม่ได้เพราะต้องใช้ `unsafe` ที่ I-5 ห้าม" — **I-5 ไม่ได้ห้ามทำ
มันบอกว่าต้องทำที่ไหน** และที่นั่นคือ `refx-platform` ·
`fsops::rename_durable` = `MOVEFILE_WRITE_THROUGH` บน Windows (**572 µs/ครั้ง**)
· fsync โฟลเดอร์บน Unix

★ `refx-io` พึ่ง `refx-platform` (normal dep) ไม่ได้ — ลาก `rfd`/`arboard` เข้า
`fuzz/` = ซ้ำรอย §4 ข้อ 18 → `save_atomic` รับ `RenameFn` เข้ามา
**ไม่มีค่าปริยาย** (การลืมส่ง = ลดความทนทานเงียบ ๆ) · ส่วนเทสต์ใช้
`refx-platform` เป็น **dev-dependency** ซึ่ง**ไม่ตามไป `fuzz/`**
(ยืนยัน: GUI dep ใน `fuzz/Cargo.lock` = 0 ทั้งก่อนและหลัง)

#### ★★★ บั๊กที่มีแต่การรันจริงเท่านั้นที่เห็น

กด "บันทึกแล้วปิด" → **บันทึกสำเร็จแต่โปรแกรมไม่ปิด** · การบันทึกอยู่เธรดอื่น
(I-2) ผลกลับมาหลัง `on_close_requested` ตอบไปแล้ว ธง `closing` เลยไม่มีใครอ่าน
→ เพิ่ม `AppDelegate::wants_exit()` ถูกถามใน `about_to_wait` (ก่อนหลับทุกครั้ง
**ไม่ใช่การวนเช็ค** จึงไม่แตะ I-1) · unit test ผ่านหมดทั้งก่อนและหลัง

### 2.24 ✅ P4-3: autosave snapshot (17 ส.ค. 2026 · `4272f5c`)

`<doc>.refx.autosave` — snapshot ทั้ง board ด้วย **DTO เดียวกับ `.refx` เป๊ะ**
เขียนผ่าน `save_atomic` ตัวเดิม → **ผิวรูปแบบไฟล์ใหม่เป็นศูนย์**
(`fuzz_document` ยิงมันอยู่แล้วโดยปริยาย)

★ เปลี่ยนดีไซน์จาก command journal เพราะ `Command` serialize ไม่ได้ ต้องสร้าง
`CommandDto` 13 variant = ผิวไฟล์ใหม่ทั้งชุด + ภาระถาวรต่อ command ใหม่
และถ้าพลาดหนึ่งจุดผู้ใช้ได้ **board ที่ผิดแบบเงียบ ๆ** — เหตุผลเต็มใน `docs/07 §4`

#### เกณฑ์ที่วัดได้ ไม่ได้อนุมาน

เหยื่อเพิ่ม item ทุก 20 ms (autosave ทุก 1 วิ) ถูกฆ่า 12 รอบ —
**จำนวน item ใน snapshot คือนาฬิกาที่อ่านย้อนหลังได้**

```
รอบ 3: มีชีวิต 1.91s · ทำไป 93 ใบ · snapshot มี 50 ใบ · เสีย 891ms
★ วัด 12 รอบ — เสียมากสุด 998ms · ระยะเว้นที่ตั้งไว้ 1.0s
```

> ### ⚠️ ตัวเลข "ทำไป ~N ใบ" ชุดแรกได้มาจากการ **หาร** — แก้แล้ว 18 ส.ค. 2026 (`e0f7222`)
>
> รุ่นแรกคำนวณ "ผู้ใช้ทำไปกี่ใบ" จาก `เวลาที่มีชีวิต ÷ 20ms` ซึ่งจริงบนเครื่อง
> พัฒนา (6 core ว่าง) และ **ไม่จริงบน runner 2 core ที่แชร์กับงานอื่น**
> → CI แดงจริงที่ run `31999371093`: *"รอบ 7: ทำไป ~163 ใบ · snapshot มี 17 ใบ ·
> เสีย ~2.92s"* ทั้งที่ autosave ทำงานถูกต้องทุกประการ
>
> เป็น `docs/08 §3.9` ข้อ 5b เป๊ะ ๆ — **assert นาฬิกาโดยอ้อมผ่านตัวเลขที่ดูเหมือน
> นับของ** · ตอนนี้เหยื่อ **จดความคืบหน้าของตัวเองลงไฟล์** (`refx_io::killclock`)
> พ่อจึงอ่านย้อนหลังได้ว่าใบที่ N เกิดตอนวินาทีที่เท่าไร แทนที่จะเดา
>
> ยืนยันสองทางบนเครื่องเดียวกันโดยรัน 12 ตัวกินซีพียูคู่ไปเพื่อจำลอง runner ที่แน่น:
> **วิธีใหม่ (วัด) 984ms เขียว · วิธีเก่า (หาร) 1.7s แดงด้วยข้อความเดียวกับ CI**

**ราคาบน board เต็ม:** 3,072 ใบ = **29 KB · เขียน 6.5 ms · ~2.9 KB/s**
ตอนแก้งานต่อเนื่อง · ★ ตัวเลขนี้เคยอ่านได้ **3.3 KB** ตอนที่เทสต์ใช้โน้ตข้อความ
ซึ่งบีบได้เกือบหมด — ของจริงมี `AssetRef` ที่มี hash 32 ไบต์บีบไม่ลง
**ตัวเลขที่ต่ำกว่าความจริงสิบเท่าคือตัวเลขที่พาไปตัดสินใจผิด**

#### ★★★ ข้อที่เกือบพลาด: dirty + idle = ไม่มี snapshot เลย

ผู้ใช้ลากภาพเสร็จแล้วลุกจากโต๊ะ → `ControlFlow::Wait` หลับสนิท (I-1 ทำงาน
**ถูกต้อง**) → ไม่มีเฟรม → ไม่มีใครถาม `should_write` → **snapshot ไม่ถูกเขียน
จนกว่าเขาจะกลับมาขยับเมาส์** ซึ่งคือช่วงที่ crash แล้วเสียงานพอดี
→ `Autosaver::next_deadline()` ไปรวมกับนาฬิกาตัวอื่นใน `wake_deadline`
→ `WaitUntil` · เธรดยังหลับสนิท **ไม่ใช่ `Poll`**

> ### ⚠️ ข้อความข้างบนเคยเขียนว่า "ปิดช่องแล้ว" — **ไม่จริง แก้แล้ว 18 ส.ค. 2026 (`f53dfa7`)**
>
> ต่อไว้**ครึ่งเดียว**: `next_deadline()` เข้า `wake_deadline()` แล้วจริง เธรดจึง
> **ตื่นตรงเวลา** — แต่ `on_wake()` **ไม่มีกิ่งไหนรับมัน** พอคืน `None`
> `about_to_wait` ก็ตั้ง `ControlFlow::Wait` แล้วหลับยาวต่อ
> → **กลไกทั้งอันไม่เคยทำงานเลยสักครั้งตั้งแต่วันที่เขียน** ฝั่งที่หายไปคือ *ผู้รับ*
> ไม่ใช่ฝั่งตั้งนาฬิกา ซึ่งเป็นเหตุผลที่อ่าน diff แล้วดูครบ
>
> วัดบนของจริง (60 ภาพ · ลากเข้ามาแล้วไม่แตะอะไรอีกเลย 45 วินาที):
>
> | | ผล |
> |---|---|
> | ก่อนแก้ | เขียน **ครั้งเดียว** (ครั้งที่มาจากเฟรมสุดท้ายที่ผู้ใช้ขยับเมาส์) |
> | หลังแก้ | `+1.1s = 220 ไบต์` · **`+11.1s = 3,098 ไบต์`** แล้วเงียบตลอด 34 วิที่เหลือ |
>
> log ยืนยันว่าโหลดครบ 60 ใบตั้งแต่ `+1.5s` — การเขียนครั้งที่สองจึงเกิดตอน
> **ไม่มี input ใด ๆ เข้ามา 9.6 วินาที** = นาฬิกาทำงานจริงแล้ว
>
> ★★★ **นี่คือที่มาของ `docs/08 §3.9` ข้อ 5c** — คำว่า "ปิดช่องแล้ว" ในเอกสารนี้
> ชี้ไปที่เทสต์ไม่ได้แม้แต่ตัวเดียว มันจึงเป็น *ความหวัง* ที่ถูกเขียนด้วยน้ำเสียง
> ของ *ข้อเท็จจริง* และอยู่แบบนั้นข้ามรอบโดยมีคนอ่านแล้วเชื่อ
> · ตอนนี้ตัวที่รักษาไว้คือ `a_board_already_captured_stops_asking_to_be_woken`
> (ฝั่ง I-1) + การวัดบนของจริงข้างบน (ฝั่งที่เทสต์เข้าไม่ถึงเพราะต้องมีหน้าต่างจริง)

#### สองด่านที่ต้องผ่านทั้งคู่

`dirty` เท่านั้น (ปล่อยไว้ 30 นาที = เขียน **0 ครั้ง** ไม่ใช่ "น้อยลง") +
เว้นระยะ ≥ N วินาที (ลาก 200 เฟรม = เขียน **ครั้งเดียว**)
★ ตรวจ `dirty` ก่อนเวลาเสมอ ไม่งั้นแยกไม่ออกว่า "ไม่มีอะไรแก้" กับ "เพิ่งเขียนไป"

★ `doc_hash` ของดีไซน์เดิมถูกแทนด้วย **การอนุมานชื่อจาก path** — ให้ผลเดียวกัน
(จับคู่ผิดไฟล์ไม่ได้ มีเทสต์ยืนยัน) โดยไม่ต้องเพิ่มฟิลด์ ซึ่งจะสร้างผิวไฟล์ใหม่
อันเป็นสิ่งที่ดีไซน์นี้มีไว้เลี่ยงพอดี

### 2.27 ⏳ P4-5 ครึ่งแรก: packed + spool (19 ส.ค. 2026)

**ชั้น io ครบและมีเทสต์คุมทั้งหมด · ชั้น UI ยังไม่ต่อเลยสักจุด**

#### ★★★ `version = 2` แปลว่า "ไฟล์นี้มี asset ฝังอยู่" ไม่ใช่ "ผู้ใช้เลือก packed"

รุ่นเก่าเปิดไฟล์ที่มี blob ได้ "สำเร็จ" โดยบังเอิญ (`decode` ตัดที่ `doc_len`)
แล้วแสดงภาพเป็น `Missing` · **ถ้าผู้ใช้ save ทับ รุ่นเก่าจะเขียน v1 ทับ →
ภาพที่ฝังไว้หายถาวร** ซึ่ง `docs/07 §2` ห้ามไว้ตรง ๆ (I-3)

→ ไฟล์ที่มี blob จริง = v2 (รุ่นเก่าปฏิเสธทั้งเปิดและเขียนทับ) · ไม่มี blob = v1
**เสียความเข้ากันได้เฉพาะไฟล์ที่มีของให้เสีย** · `FORMAT_VERSION` เปลี่ยนความหมาย
เป็น *เพดานที่รุ่นนี้อ่านได้* ส่วน `LINKED_VERSION` คือเลขที่ linked เขียน

#### ★★★ "Linked ก็ฝัง" — `plan_embeds` คือกฎที่อยู่ที่เดียว

ภาพจาก clipboard ไม่มีไฟล์ต้นทาง · บันทึก linked ตรง ๆ = `AssetRef` ชี้ไปที่ว่าง
= **ภาพหายถาวร** · `packed::plan_embeds(board, mode, locate)` ตัดสินให้:
`AssetBytes::Ours` ฝังเสมอทั้งสองโหมด · `UserFile` ฝังเฉพาะ packed

★ วางกฎไว้ชั้น io ไม่ใช่ชั้น UI โดยตั้งใจ — กฎที่ปกป้อง I-3 ถ้าอยู่ในตัวเรียกใช้
ทุกตัวเรียกใหม่ต้องจำเอง แล้ววันหนึ่งจะมีตัวที่ลืม

#### ★★ `embedded` ไม่เก็บลงไฟล์ — อนุมานจาก asset table

ค่าที่ถูกคือ "hash นี้อยู่ในตารางไหม" ซึ่งอ่านจากไฟล์ได้อยู่แล้ว · เก็บซ้ำ =
ขัดกันเองได้แล้วต้องตัดสินว่าเชื่ออันไหน · และไม่ต้องแตะ `DocumentDto` ซึ่งจะลาก
linked ขึ้น v2 ไปด้วยทั้งที่ไม่เกี่ยว

#### ★★★ กติกาเก็บกวาด spool — ข้อ 2 คือข้อที่ทั้งหมดนี้มีไว้เพื่อมัน

1. ห้ามลบไฟล์ที่ board ที่เปิดอยู่อ้างถึง
2. ★ ห้ามลบไฟล์ที่ **recovery snapshot** อ้างถึง (`referenced_by_recovery`)
3. ที่เหลือใช้เพดาน

**spool ถูกกวาดแต่ snapshot ยังอยู่ → กู้คืนสำเร็จแต่ได้ board เต็มไปด้วย
`Missing`** ซึ่งแย่กว่าไม่มี snapshot เลย เพราะผู้ใช้เชื่อไปแล้วว่าได้งานคืน

★ ข้อ 2 ทำ **กว้างกว่าตัวหนังสือเดิมใน spec** (คุ้มครองทุก snapshot ที่ยังอยู่
ไม่ใช่แค่ "ที่ยังไม่ถูกตอบ") เพราะตัวที่ตอบ "เก็บไว้ก่อน" ยังกู้ได้ —
การถือว่ามันจบแล้วจะทำให้ตัวเลือกที่สามของ P4-4 กลายเป็นคำโกหก
· **เจ้าของโปรเจกต์เห็นด้วยและแก้ `docs/07` ให้ตรงแล้ว**
· มีเทสต์คู่: ตัวที่พิสูจน์ข้อ 2 + ตัวที่จงใจ**ลืม**ข้อ 2 แล้ว assert ว่าภาพหายจริง

#### ★★★ ราคาของ PNG ที่วัดได้ (noise เต็มภาพ = worst case)

```
1920x1080: RGBA  7 MB → PNG  6 MB (86%) ใน  94 ms
4000x3000: RGBA 45 MB → PNG 39 MB (86%) ใน 501 ms
6000x4000: RGBA 91 MB → PNG 78 MB (86%) ใน 1.07 s
```

★ เดิมเทสต์ใช้ gradient แล้วได้ **1%** — ต่ำกว่าจริงหลายสิบเท่า กับดักเดียวกับ
ที่ §2.24 เจอตอนวัด snapshot ด้วยโน้ตข้อความล้วน → เปลี่ยนเป็น noise แล้ววัดใหม่

**1.07 วินาทียืนยันว่า `docs/07 §2` ถูกที่บังคับให้ encode ไม่ขวางภาพขึ้นจอ**
— thumbnail สร้างจาก RGBA ที่อยู่ใน RAM แล้ว การ encode ต้องเกิดหลังจากนั้น

#### ★★★ ต้องตัดสินก่อนต่อ: เพดาน spool เป็น **ไบต์** ไม่ใช่จำนวนไฟล์

**ตัดสินแล้ว (19 ส.ค. 2026): `512 MB` · และ ห้ามใช้ JPEG**

`spool::MAX_KEPT = 64` ที่เขียนไว้ตอนแรกตั้งก่อนรู้ตัวเลข 78 MB/ไฟล์ —
เพดานจริงจึงกลายเป็น **~5 GB** โดยไม่มีใครตั้งใจ

- เพดานต้องนับ **ไบต์รวม** ไม่ใช่จำนวนไฟล์ (ไฟล์หนึ่งใหญ่ได้ 78 MB)
- **ห้าม JPEG** แม้จะเล็กกว่ามาก: spool คือต้นฉบับเดียวที่เหลือของภาพที่วาง
  การบีบแบบสูญเสียแปลว่า**ต้นฉบับของผู้ใช้ถูกทำให้แย่ลงถาวร** โดยที่เขาไม่ได้สั่ง
  (และมันจะถูกฝังลง `.refx` ต่อไปในสภาพนั้น)

✅ **แก้แล้ว 20 ส.ค. 2026** — `MAX_KEPT` (ไฟล์) → **`MAX_BYTES = 512 << 20`**
· `sweep` รับไบต์ · `Swept` มี `protected_bytes`/`kept_bytes`/`removed_bytes`
+ `protected_over_cap()` · เทสต์ `the_cap_counts_bytes_not_files` ล้มเป็นกับ
เพดานแบบนับไฟล์ (สองไฟล์ 128 B ผ่านเพดาน "64 ไฟล์" ได้ทั้งคู่)

#### ★ ของที่รอรับอยู่แล้ว — ครึ่งหลังเป็นการต่อสายล้วน

| มีแล้ว | ทำอะไร |
|---|---|
| `refx_asset::encode::to_png` | RGBA → PNG (Fast) พร้อมตัวเลขราคา |
| `refx_core::spool::PastedImageStore` | trait ที่กลับทิศไว้ให้ worker เรียก |
| `refx_io::spool::store` / `sweep` / `referenced_by_recovery` | ที่พัก + กติกาเก็บกวาด |
| `refx_platform::paths::spool_dir()` | `<data_local_dir>/pasted` + เทสต์กัน cache |
| `refx_io::packed::plan_embeds` | ตัดสินว่าอะไรต้องฝัง |
| `refx_io::save::save_document` | เลือก v1/v2 เองตามของที่อยู่ข้างใน |
| `refx_io::packed::{read_index, extract}` | อ่าน table + แกะ blob แบบสตรีม |

✅ **ชิ้นที่ 1 ปิดแล้ว 20 ส.ค. 2026** — ดู §2.28

**เหลือทำ (ชิ้นที่ 2):** Save As แบบ packed · เปิดไฟล์ packed → `extract` กลับ
ลง spool · แถบสถานะบอกโหมด (**สภาวะ ไม่ใช่เหตุการณ์** ตาม `docs/03 §1`)

#### ★ เกณฑ์ผ่านที่ยังเหลือของชิ้นที่ 2

- วาง clipboard → บันทึก **linked** → ปิด → เปิดใหม่ → ภาพยังอยู่ **และซูมแล้วคม**
  (ครึ่งหลังของประโยคนี้ผ่านแล้วผ่านเส้นทางกู้คืน — ดู §2.28 · ที่เหลือคือ
  วงจร `Ctrl+S` → `Ctrl+O` ซึ่งต้องรอ Save As แบบ packed)
- packed → ลบโฟลเดอร์ต้นฉบับ → เปิด → ภาพครบ (ชั้น io ผ่านแล้ว ยังไม่ทำบนแอป)

### 2.30 ✅ P4-6: relink 5 ขั้น (21 ส.ค. 2026)

ตรรกะทั้งห้าขั้นอยู่ที่ `refx_core::relink` เป็น **pure function** ส่วน IO
(ไฟล์นี้มีอยู่ไหม · cache รู้จัก hash นี้ที่ไหน · hash ของไฟล์นี้คืออะไร)
ถูกส่งเข้าไปเป็น closure — ผู้เรียกจึงรันมันบน worker ได้ตาม I-2 และเทสต์
รันได้โดยไม่ต้องมีดิสก์จริง

| ขั้น | ทำอะไร | ยืนยันบนแอปจริง |
|---|---|---|
| 1 | ที่ path เดิม | เปิดเอกสารปกติ — **เงียบสนิท** ไม่มีข้อความอะไรเลย |
| 2 | โฟลเดอร์เดียวกับ `.refx` | ย้ายภาพทั้งชุดไปข้างเอกสาร → *"Found 4 of 4 images that had moved"* |
| 3 | ค้น `paths` ใน cache ด้วย hash | ย้ายไปโฟลเดอร์ที่ไม่มีใครรู้ แล้วเปิดโฟลเดอร์นั้นครั้งหนึ่ง → เจอครบ 4 · log: `step=KnownByHash` |
| 4 | ยังไม่เจอ → `Missing` | placeholder 4 กล่อง **พร้อมชื่อไฟล์** + *"4 images could not be found"* |
| 5 | ผู้ใช้ชี้ 1 ไฟล์ → ที่เหลือตามมา | ชี้ `img0.png` ใบเดียว → **กลับมาครบ 4 ใบ** · log: `scanned a folder … files=4` + `step=PickedByUser` ×4 |

#### ★★★ ลำดับของขั้น 1 ห้ามสลับ — และเหตุผลไม่ใช่เรื่องความเร็ว

ไฟล์ที่ยังอยู่ที่เดิม **ต้องไม่ถูกลากไปผูกกับสำเนาอื่นบนเครื่อง** แม้สำเนานั้น
จะมีเนื้อเหมือนกันเป๊ะ · ผู้ใช้จัดโฟลเดอร์ไว้แบบนั้นด้วยเหตุผลของเขา และ path
ที่เปลี่ยนเองคือสิ่งที่เขาไม่ได้สั่ง (เทสต์
`a_file_that_never_moved_is_never_relinked_to_a_copy` ล้มเป็นถ้าสลับ)

#### ★★ ขั้นที่ 5 จับคู่ด้วย hash ก่อน แล้วจึง **ชื่อไฟล์**

`docs/07 §2` เขียนว่าจับคู่ด้วย hash · แต่เอกสารที่บันทึกก่อน 19 ส.ค. มีคีย์
เป็น hash ของ *path* — hash จึงไม่มีวันตรง และผู้ใช้ที่มี board 200 ใบจะต้อง
กด "หาไฟล์เอง" สองร้อยครั้ง

→ รอบที่สองจับด้วย **ชื่อไฟล์ตรงเป๊ะ** ในโฟลเดอร์ที่ **ผู้ใช้ชี้มาเอง**
★ นี่ไม่ใช่การขยายกฎ: ขั้นที่ 2 ของ spec จับคู่ด้วยชื่อไฟล์อยู่แล้ว ที่นี่คือ
หลักการเดียวกันกับโฟลเดอร์ที่เจตนาชัดกว่าอีก · ความเสี่ยงที่ยอมรับคือโฟลเดอร์
นั้นอาจมีไฟล์ชื่อเดียวกันแต่คนละภาพ — **เห็นได้ทันทีบนจอและ undo ได้**

#### ★★★ สองบั๊กที่เจอเพราะกด `Ctrl+Z` บนแอปจริง (ไม่มีเทสต์ไหนจับได้ก่อน)

| อาการ | ต้นเหตุ | ตอนนี้ |
|---|---|---|
| undo แล้ว board บอก `Missing` แต่ **จอยังโชว์ภาพเดิม** | `rebuild_quads` ถามว่า *"มี `render_state` ไหม"* ไม่ได้ถามว่า *"`Board` บอกว่าใบนี้เป็นภาพไหม"* · `render_state` เป็น cache ที่อยู่ยาวกว่าสถานะของ item **โดยตั้งใจ** (§2.5) | ถามชนิดจาก `Board` ทั้งใน `rebuild_quads` และ `plan_working_textures` + เทสต์ `an_item_that_became_missing_stops_being_drawn` |
| `Ctrl+Z` ครั้งเดียว **ย้อนข้ามไปสองงวด** | `RelinkAssets` merge ตัวเอง (ต้อง merge — ผลทยอยกลับมาข้ามหลายเฟรม) แต่ไม่มีใคร `seal()` ระหว่างงวด การผูกไฟล์ตอนเปิดเอกสารกับตอนผู้ใช้กดปุ่มอีกสิบนาทีต่อมาจึงรวมเป็นขั้นเดียว | `apply_located` เรียก `history.seal()` ก่อนเริ่มงวดใหม่ + เทสต์ `a_sealed_run_is_never_merged_into_the_one_before_it` |

★ ทั้งคู่เป็นบั๊กชนิด **"ทุกชิ้นถูก แต่ประกอบผิด"** ที่ `docs/08 §3.9` ข้อ 5
บอกว่าจับได้ด้วยการรันของจริงอย่างเดียว — และมันจับได้จริงอีกครั้ง

#### ★★ ข้อความที่ถูกเขียนทับใน 3 มิลลิวินาที = ข้อความที่ไม่มีอยู่

รอบแรกเขียน *"Found N of M"* ทันทีที่ค้นเสร็จ แล้วรายงาน *"เปิด N ไฟล์ใน M ms"*
ของงวด decode ทับมันทันที · ผู้ใช้ที่เพิ่งย้ายโฟลเดอร์ภาพจึงไม่มีวันรู้ว่า
เกิดอะไรขึ้น (กับดักเดียวกับ §2.24) → เก็บไว้รายงาน **ตอนจบงวด** และจัดลำดับ
ความสำคัญ: board เต็ม > ผลการ relink > เวลาที่ใช้เปิด

#### ★ `--open=FILE.refx` — ตัดสินใจเพิ่มเพราะไม่มีทางยืนยันได้เลยถ้าไม่มี

native dialog ขับด้วยสคริปต์ไม่ได้ (บันทึกไว้ตั้งแต่ §2.26) แต่ P4-6 บังคับให้
**ยืนยันบนของจริง** ว่าเปิดเอกสารที่ภาพย้ายที่แล้วมันหาเจอ → เพิ่ม `--open=`
ซึ่งเดินเส้นทางเดียวกับ `Ctrl+O` ทุกประการ ต่างแค่ไม่ต้องผ่าน dialog
· ★ มันไม่ใช่ของสำหรับเทสต์อย่างเดียว — **นี่คือสิ่งที่ Explorer ทำตอนผู้ใช้
ดับเบิลคลิกไฟล์ `.refx`** ซึ่งเป็นพฤติกรรมที่โปรแกรมเดสก์ท็อปทุกตัวมี

#### ★ ของที่ไม่ได้ทำ / ข้อจำกัดที่ยังอยู่

- **ไม่มี index บนคอลัมน์ `hash`** ใน `paths` — การเพิ่ม index คือการแก้ schema
  ซึ่ง `CLAUDE.md` บังคับให้ถามก่อน · ตอนนี้เป็น full scan ของตารางที่มี
  ~10,000 แถว และเกิดเฉพาะตอนเปิดเอกสารที่มีภาพหาย ไม่ใช่ต่อเฟรม
- **ใบที่บันทึกไว้ว่า `Missing` ไม่มีคีย์ของเนื้อให้ใช้** ขั้นที่ 3 จึงช่วยมันไม่ได้
  (ใช้ `[0; 32]` เป็นคีย์ว่าง) — ขั้น 1/2/5 ยังทำงานให้ได้ตามปกติ
- **การพิมพ์ลง native dialog ยังขับไม่ได้เสถียร** · `dlgtype` ใหม่ทำงานได้จริง
  กับ **dialog เปิดไฟล์** (ยืนยันแล้วสามครั้ง) แต่กับ **Save As ยังไม่ได้**
  — เอกสารที่ใช้ทดสอบจึงสร้างจาก snapshot ของ autosave แทน
- ★ `Assert-Dialog` ใน `ui-drive.ps1` ปิดช่องที่ §2.26 เปิดค้างไว้: เดิม
  `Assert-Focus` **บังคับหน้าต่างหลักขึ้นหน้า** ซึ่งแย่ง focus จาก dialog พอดี
  — ตัวใหม่ตรวจว่า *dialog ของเรา* อยู่ foreground แล้วล้มถ้าไม่ใช่

### 2.29 ✅ `AssetRef::hash` = คีย์ของ **เนื้อ** ไม่ใช่ของ path (20 ส.ค. 2026)

`docs/02 §2.3` เขียนไว้ตั้งแต่ต้นว่า *"`hash` เป็นคีย์หลัก ไม่ใช่ `path`"* พร้อม
สัญญาสามข้อที่ผูกอยู่กับมัน · ของจริงตั้งแต่ P1-8 คือ **`hash_bytes(path)`**
ซึ่งแปลว่าไม่จริงสักข้อ:

| สัญญา | ของจริงก่อนแก้ | ใครพึ่งมัน |
|---|---|---|
| ย้ายไฟล์แล้ว thumbnail ไม่หาย | ย้ายแล้วได้คีย์ใหม่ | ผู้ใช้ที่จัดโฟลเดอร์ |
| relink หาไฟล์เจอด้วย hash | ไม่มีวันตรง | **P4-6 ขั้น 3 และ 5** |
| ไฟล์ซ้ำถูกยุบอัตโนมัติ | สำเนาเดียวกันสองที่ = สอง asset | VRAM · packed mode |

★ `hash_file()` มีมาตั้งแต่ P1-2 และ **ถูกเรียกอยู่แล้ว** ในเส้นทาง ingest —
แค่ผลของมันไปจบที่ `cache.sqlite` แล้วถูกทิ้ง ไม่เคยเดินทางกลับมาถึง `Board`

#### ★★ คีย์ของงาน ≠ คีย์ของ asset — และทั้งคู่ต้องมีอยู่

คีย์ของงานถูกตั้งบน UI thread **ก่อน**ใครจะได้แตะไฟล์ (จับคู่ผลลัพธ์กลับหา
`job_sources`/`relink_targets`) จะเป็นคีย์ของเนื้อไม่ได้โดยธรรมชาติ · ทางแก้คือ
ให้ worker **ส่งคีย์จริงกลับมา** ด้วย [`ContentOrigin`] แบบเดียวกับที่ §2.28
ทำกับภาพที่วาง — ตอนนี้ทั้งสองแหล่งใช้ช่องเดียวกัน:

| worker ตอบ | คีย์มาจาก | path |
|---|---|---|
| `File(hash)` | blake3 ของไบต์ในไฟล์ (`hash_file`) | ของผู้ใช้ |
| `Spooled(hash)` | blake3 ของพิกเซล (`hash_pasted`) | `<spool_dir>/<hash>.png` |
| `None` | ถอยไปใช้คีย์ของงาน — ภาพยังขึ้นจอ (I-3) | ของผู้ใช้ |

#### ★★★ กิ่งที่ลืมง่ายที่สุดคือ **cache hit** — และมันคือกิ่งที่ผู้ใช้เดินทุกวัน

`CacheLookup::Hit` เดิมคืนแต่ thumbnail · ถ้าปล่อยไว้ สัญญาทั้งสามข้อจะเป็นจริง
**เฉพาะตอน cache เย็น** แล้วอาการที่ออกมาคือ *"เครื่องนี้ยุบไฟล์ซ้ำได้ อีกเครื่อง
ไม่ได้"* ซึ่งไล่หาสาเหตุแทบไม่ได้เลย → `Hit { thumb, hash }`

★ negative control ยืนยันแล้วทั้งสองกิ่ง (20 ส.ค. 2026): ให้เส้นทางไฟล์คืน
`job.hash` → แดง 3 ตัว (`two_copies_of_one_file_get_one_key` ·
`moving_a_file_never_changes_its_key` ·
`the_key_does_not_depend_on_whether_the_cache_is_working`) ส่วนกิ่ง cache hit
มี `a_cache_hit_still_carries_the_key_of_the_bytes` จับได้ **ตัวเดียว**
(สามตัวบนไม่เคยเดินผ่านกิ่งนั้น)

#### ★★ item ที่มาจากไฟล์ **ไม่ถูกตั้งชื่อใหม่** — เอกสารเป็นเจ้าของคีย์ของตัวเอง

เส้นทาง relink เอาแต่ *พิกเซล* มาเติมให้ item ที่มีอยู่แล้ว `render_state` จึงใช้
**คีย์ที่เก็บอยู่ใน `Board`** ไม่ใช่คีย์ที่เพิ่ง hash ได้ · ถ้าเขียนทับจะเพี้ยน
ทันทีกับภาพที่วาง: ชื่อไฟล์ใน spool คือ hash ของ**พิกเซล** ส่วนการ hash ไฟล์
PNG นั้นให้คนละค่า — `render_state` กับ `Board` จะชี้คนละ asset ทั้งที่เป็น
ภาพใบเดียวกัน

→ ผลพลอยได้: **ไฟล์เก่ายังเปิดได้ปกติและไม่ถูกแก้เงียบ ๆ** (ดูตารางล่าง)

#### ตัวเลขจริง — A/B ในเซสชันเดียวกัน (binary ก่อนแก้ vs หลังแก้)

โฟลเดอร์ที่มีภาพเดียวกันสองชื่อ (`twin-a.png` / `twin-b-copy.png` · md5 ตรงกัน)
· เปิดด้วย `--open-dir` · **ซูมเท่ากันเป๊ะที่ 156% · 2 item เท่ากัน**

| | ก่อน | หลัง |
|---|---|---|
| working texture ที่ค้างใน VRAM | **Sharp 682 KB** (2 ใบ) | **Sharp 341 KB** (1 ใบ) |
| VRAM รวม | 16.7 MB | 16.3 MB |
| เปิด 2 ไฟล์ (cache อุ่น) | 16 ms | 6 ms |

★★ **cache เดิมไม่ถูกทำให้ miss แม้แต่แถวเดียว** — ตรงข้ามกับที่คาดไว้ตอนวางแผน:
`CacheKey` ประกอบจาก `hash_file` + `(mtime, size)` มาตั้งแต่ P1-3 **ไม่เคยเห็น
คีย์ของงานเลย** · หลักฐาน: จำนวนแถวใน `cache.sqlite` นิ่งที่ **10,855** ตลอด
การรัน build ใหม่ (ไม่มีแถวใหม่ถูกเขียน = hit ล้วน) และเวลาเปิดคือ 2–6 ms

#### ยืนยันบนของจริง

| ทำอะไร | เห็นอะไร |
|---|---|
| ★ เปิดโฟลเดอร์ที่มีสำเนาเดียวกันสองชื่อ | 2 items · **working texture ใบเดียว** (682 KB → 341 KB) |
| ★★ เอกสารที่ **build เก่าเขียนไว้** (คีย์เป็นของ path) เปิดด้วย build ใหม่ | *"Opened 4 files in 2 ms"* · 4 items · 4 uploads · **ไม่ crash ไม่มี Missing** |
| ซูมบนเอกสารเก่านั้น | `Sharp 1.3 MB · 4 draws` — ทำงานปกติ · **ฝาแฝดยังเป็นสอง asset ตามที่ไฟล์บอก** เพราะเราไม่แก้คีย์ที่บันทึกไว้แล้ว |

#### ★ ที่ยังไม่ได้ทำ / ตัดสินใจเอง

- **ไม่ซ่อมคีย์ในไฟล์เก่า** — เอกสารเป็นแหล่งความจริงของตัวเอง การเขียนทับคีย์
  ตอนเปิดคือการแก้งานของผู้ใช้โดยเขาไม่ได้สั่ง และต้องผ่าน `Command` ด้วย
  · ผลคือไฟล์ที่บันทึกก่อนวันนี้จะยังยุบไฟล์ซ้ำไม่ได้ และ relink ด้วย hash
  ไม่ได้ · **P4-6 ต้องตัดสินว่าจะซ่อมตอนไหน** (ตอน relink สำเร็จน่าจะเป็นจังหวะที่ถูก)
- **atlas ยังจอง 1 ช่องต่อ 1 item** (64 KB/ช่อง) — `docs/02 §2.3` เขียนว่า
  "1 texture เดียว" ซึ่งตอนนี้จริงกับ **working texture** แล้ว (คีย์เดียวกัน =
  ใบเดียว) แต่ยังไม่จริงกับ thumbnail atlas · การยุบต้องแก้เจ้าของอายุของช่อง
  ซึ่ง `History` ถืออยู่ (§2.5) → แตะ I-6 จึงแยกเป็นงานของตัวเอง
- **`hash_file` เพิ่มงานเฉพาะตอน cache เปิดไม่ได้** — เส้นทางปกติใช้ค่าที่
  `cache_lookup` คำนวณไว้อยู่แล้ว (`CacheKey::hash`) จึง **ไม่มีการ hash เพิ่ม
  แม้แต่ครั้งเดียว** · งาน `Working`/`Sample` คืนค่าก่อนถึงจุดนี้ ตอนซูมจึงไม่ hash

### 2.28 ✅ P4-5 ชิ้นที่ 1: วาง → spool → **ภาพคม** (20 ส.ค. 2026)

ชั้น UI ถูกต่อครบแล้ว · เกณฑ์ทั้งเจ็ดข้อยืนยันบนแอปจริง (ตัวเลข + ภาพหน้าจอ)

#### ★★★ `Done` ต้องออกไป **ก่อน** ไฟล์ลงดิสก์ — บังคับด้วยชนิดข้อมูล

`docs/07 §2` ห้ามให้การ encode มาขวางภาพขึ้นจอ · HANDOFF เดิมร่างไว้ว่า
`JobResult::Done { spooled: Option<PathBuf> }` ซึ่ง **ทำตามตัวอักษรไม่ได้**:
path จะมีค่าก็ต่อเมื่อเขียนไฟล์เสร็จแล้ว = ผู้ใช้รอ encode ทุกครั้งที่ `Ctrl+V`

ทางที่ใช้จริง — เปลี่ยนชนิด ไม่ใช่เปลี่ยนกฎ:

| | |
|---|---|
| `Done { spooled: Option<ContentHash> }` | **hash ไม่ใช่ path** — path หาได้จาก hash ล้วน ๆ (`spool_path`) จึงเขียนลง `AssetRef` ได้ทันทีทั้งที่ไฟล์ยังไม่เสร็จ |
| `run_job` คืน `(JobResult, Option<SpoolTask>)` | งานที่เลื่อนออกไปเป็น **ค่าคืน** — เขียนให้ encode ก่อนส่งผลไม่ได้เลยโดยโครงสร้าง |
| `JobResult::Spooled { hash, path }` | ยืนยันหลังไฟล์ลงดิสก์จริง → `refx-ui` สลับ `ItemRender::source` เป็น `File(path)` **นี่คือประตูที่ปลดล็อก working texture** |

★ เทสต์ `the_image_reaches_the_screen_before_the_png_reaches_the_disk` ใช้
`FakeSpool` ที่ **บล็อกจริง** — ย้าย encode ไปก่อน `tx.send` แล้วมันแดงพร้อม
`Done ต้องมาก่อน โดยไม่ต้องรอ store: Timeout` (ยืนยัน negative control แล้ว)

#### ★★★ `AssetRef::hash` ของภาพที่วาง = hash ของ **เนื้อภาพ** ไม่ใช่คีย์ของงาน

คีย์ของงานคือ `clipboard:N` ที่ต่างกันทุกครั้ง · ถ้าเอาไปใช้จะพังสองทางพร้อมกัน:

1. `spool::sweep` แปลงชื่อไฟล์กลับเป็น hash แล้วถาม board ว่าใครอ้างถึง —
   คีย์ไม่ตรง = **ลบภาพของผู้ใช้ทิ้ง** (I-3 เงียบที่สุด)
2. วางภาพเดิมซ้ำได้ไฟล์ละใบ (78 MB ต่อครั้ง)

→ `refx_asset::hash::hash_pasted(w, h, rgba)` · ★ **hash ทั้งก้อน ไม่ใช้ fast
path** ต่างจาก `hash_file`: fast path ปลอดภัยได้เพราะ cache key มี `mtime`
คร่อมจุดบอดไว้ (§4 ข้อ 4) ภาพที่วาง**ไม่มี mtime** คีย์ที่ชนกันจึงแปลว่าภาพ
ใบที่สองหายไปเงียบ ๆ · ราคาที่วัดได้: **+6 ms ที่ 6000×4000** (ดูตารางล่าง)

★ เทสต์คู่ `a_pasted_image_the_board_points_at_is_never_swept` +
`using_the_job_key_instead_is_what_loses_the_image` (ตัวหลังพิสูจน์ว่าตัวแรก
ไม่ได้เขียวเปล่า ๆ) และทั้งคู่เรียก `pasted_asset_location` **ตัวที่โค้ดจริงใช้**

#### ตัวเลขจริง — A/B ในเซสชันเดียวกัน (worktree ที่ `3fa6b4e` vs ของใหม่)

**ภาพคมขึ้นจริงไหม** — ภาพทดสอบคือ checker 1 px 2400×1600 (thumbnail จะเฉลี่ย
มันเป็นเทาเรียบ ส่วน working texture จะเก็บลายไว้) · วัดด้วย
`scripts/ui-sharpness.py` (ค่าต่างของพิกเซลข้างเคียง ในกรอบเดียวกัน)

| | zoom | neighbour-contrast | status bar |
|---|---|---|---|
| ก่อน (`3fa6b4e`) | 6056% | **0.01** | VRAM 16.0 MB · ไม่มีช่อง Sharp |
| หลัง | 6400% | **5.09** | VRAM 44.3 MB · **Sharp 28.3 MB** |
| หลัง (หลังกู้คืน) | 3832% | **8.57** | **Sharp 28.3 MB** |

> ★ zoom ของสองฝั่งไม่เท่ากันเป๊ะเพราะ egui รวบ event ล้อต่อเฟรม (build ที่วาด
> เฟรมมากกว่าจึงซูมได้ลึกกว่าด้วย input ชุดเดียวกัน) — **ไม่กระทบข้อสรุป**:
> thumbnail ที่ถูกยืดจะ *แบนลง* เมื่อซูมเข้า ตัวเลข 0.01 ที่ 6056% จึงเป็น
> เพดานบนของสิ่งที่รุ่นเก่าจะทำได้ที่ 6400% อยู่แล้ว

**การเขียน PNG หน่วงภาพขึ้นจอไหม** — ภาพ noise 6000×4000 (worst case) ·
เวลาจาก log `clipboard paste → image on screen` · รุ่นละ 3 รอบ สลับกัน

| | รอบ 1 | รอบ 2 | รอบ 3 | median |
|---|---|---|---|---|
| ก่อน (`3fa6b4e`) | 369.6 | 371.8 | 374.3 | **371.8 ms** |
| หลัง | 378.0 | 374.7 | 447.1 | **378.0 ms** |

**+6.2 ms (+1.7%)** ซึ่งคือค่า hash ของ RGBA 91 MB · ส่วนการ encode PNG จริง
วัดได้ **317–335 ms** และมันเกิด **หลัง** เส้น 378 ms นั้น (log
`spooled a pasted image ... elapsed=317ms`) — ถ้าอยู่ก่อนหน้า ผู้ใช้จะรอ ~700 ms
· ★ ตัวเลข 1.07 s ใน §2.27 มาจาก profile ของเทสต์กับภาพคนละใบ ของ release คือ ~320 ms

#### ยืนยันบนของจริง (ภาพหน้าจอ + ตัวเลข)

| ทำอะไร | เห็นอะไร |
|---|---|
| `Ctrl+V` ภาพจาก clipboard | *"Pasted an image from the clipboard in 72 ms"* · 1 items · 1 uploads |
| ★ ซูมเข้า | **Sharp 28.3 MB / 192.0 MB** · ลาย checker ขึ้นจริง (contrast 0.01 → 5.09) |
| ไฟล์ที่โผล่จริง | `%LOCALAPPDATA%\RefX\data\pasted\624c751f…adb0.png` (5.28 MB) |
| วางภาพเดิม **สามครั้ง** | board ได้ **3 items / 3 uploads** · spool มี **1 ไฟล์** |
| ★★ วาง → ไม่ save → ฆ่าโปรเซส → เปิดใหม่ | แถบ *"Unsaved work from last time · 3 items"* → กด **Bring it back** → *"Opened 3 files in 62 ms"* → ซูมแล้ว **Sharp** (contrast 8.57) |
| ★ spool 551 MB ที่ถูกคุ้มครองทั้งหมด | *"Pasted images are using 550 MB of space (the limit is 512 MB), and none of them can be cleared yet…"* · **ไม่ลบสักไฟล์** (7 → 7) |
| I-1 สถานะใหม่ 3 แบบ | หลังวาง: **Frames 15 → 15** · working texture ค้างอยู่: **493 → 493** · แถบกู้คืน + คำเตือนเกินเพดาน: **78 → 78** (ทั้งหมดใน 20 วินาที) |

★ เครื่องมือใหม่ที่อยู่ใน repo แล้ว (ไม่ใช่ใน session): `ui-drive.ps1` step
**`copyimg|<png>`** (วาง **ภาพ** จริง — step `paste` เดิมวางแต่ข้อความซึ่งไม่เคย
เดินผ่านเส้นทางนี้เลย) และ `scripts/ui-sharpness.py`

#### ★ ที่ยังยืนยันไม่ได้ / ตัดสินใจเองเพราะ spec ไม่ระบุ

- **เขียน `AssetRef::path` ตั้งแต่ตอน `Done` ทั้งที่ไฟล์ยังไม่เสร็จ** —
  จำเป็นเพราะ snapshot ที่ถูกเขียนในช่วง ~300 ms นั้นต้องกู้คืนได้เหมือนกัน
  (`request_thumbnails_for_board` ข้าม item ที่ `path` ว่าง) · ผลข้างเคียงที่
  ยอมรับ: ถ้าการเขียนไฟล์ล้ม `path` จะชี้ไปที่ว่าง → ผู้ใช้เห็น error ที่บอก
  ชื่อไฟล์ ซึ่ง**ดีกว่า item ที่หายไปเงียบ ๆ** · การขอ working texture ไม่ได้
  พึ่ง `path` แต่พึ่งการยืนยัน `Spooled` จึงไม่มี error ปลอมระหว่างนั้น
- **สลับ `ItemRender::source` แทนการแก้ `Board`** — `source` เป็นสถานะของชั้น UI
  การปลดล็อกภาพคมจึงไม่ต้องผ่าน `Command` และไม่ทำให้เอกสาร dirty
- **`sweep` ถูกเรียกสองจังหวะ**: หลังการสแกน recovery ตอนเปิดโปรแกรมจบ (ไม่ว่า
  จะมีงานค้างหรือไม่ — เครื่องที่ไม่เคย crash เลยก็ต้องถูกกวาด) และหลังผู้ใช้
  ตอบแถบกู้คืน (snapshot ที่ถูกสั่ง "ทิ้ง" หายไปแล้ว ของที่มันคุ้มครองจึงกวาดได้)
- **spec ไม่ได้บอกว่าใครเรียก `sweep`** — เลือกเธรดแยกแบบเดียวกับ
  `sweep_recovery_folder` แล้วปลุก UI ครั้งเดียวด้วย `WakeHandle` (ไม่ poll)

### 2.26 ✅ P4-4: กู้คืนงานที่ไม่เคยบันทึก + dialog 3 ตัวเลือก + เปิดไฟล์ (18 ส.ค. 2026)

`<data_dir>/recovery/<session-id>.refx` — snapshot ใช้ `dto`/`save_atomic` ตัวเดิม
ทั้งชุด (**ผิวรูปแบบไฟล์ใหม่เป็นศูนย์** เหมือน P4-3) · นโยบายว่าเมื่อไหร่ควรเขียน
ใช้ `Autosaver` **ตัวเดียวกัน** คนที่ยังไม่เคยบันทึกจึงได้การคุ้มครองเท่ากันเป๊ะ

#### ★★★ `data_dir()` ของ crate `directories` ชี้ **Roaming** บน Windows

`docs/07 §4` ระบุ `%LOCALAPPDATA%` · ตัวที่ถูกคือ **`data_local_dir()`**
· Roaming ทำให้งานที่ผู้ใช้กำลังแก้อยู่ถูกซิงค์ขึ้นเซิร์ฟเวอร์ทุกครั้งที่เขา
ล็อกอินบนเครื่องในโดเมน (และช้าด้วย เพราะ snapshot เขียนบ่อย)

★★ **เจอเพราะรันของจริงแล้วไปเปิดโฟลเดอร์ดู เทสต์ทุกตัวเขียวอยู่ก่อนหน้านั้น**
— และไม่มีเทสต์ไหนถามได้ว่า "โฟลเดอร์นี้ถูกซิงค์ขึ้นเน็ตไหม" · ตอนนี้กันด้วย
`on_windows_data_lives_under_local_appdata` + ชื่อ API ที่ถูกอยู่ใน `docs/07 §4` แล้ว

#### ★★ เพดานที่มีข้อยกเว้นคร่อมอยู่

10 ไฟล์ / 30 วัน **แต่ลบได้เฉพาะไฟล์ที่มีไฟล์ประทับ `.asked`** (`docs/07 §4`:
"ห้ามลบไฟล์ที่ยังไม่เคยถูกถาม") · เพดานที่ไม่มีข้อยกเว้นนี้จะไล่ snapshot ของ
งานจริงออกก่อนที่ผู้ใช้จะได้เห็นคำถามด้วยซ้ำ = กลายเป็นตัวทำงานหายเสียเอง

★ ประทับ **ตอนแถบโผล่ขึ้นจอ ไม่ใช่ตอนผู้ใช้ตอบ** — คนที่ปิดโปรแกรมทิ้งโดยไม่
แตะแถบจะทำให้ไฟล์นั้นไม่มีวันเข้าเกณฑ์เก็บกวาด แล้วโฟลเดอร์โตไม่รู้จบ

#### ★★★ ตัวเลือกที่สาม ("เก็บไว้ก่อน") = **ไม่แตะไฟล์แม้แต่ไบต์เดียว**

ถ้ามันลบหรือย้าย มันก็เป็นแค่ "ทิ้ง" ที่ใส่เสื้อคลุมสุภาพ ซึ่ง **แย่กว่าไม่มี
ตัวเลือกนี้เลย** เพราะคนที่ไม่แน่ใจจะเลือกมันด้วยความเข้าใจว่างานยังอยู่
· มีเทสต์คู่เป็น negative control ของกัน (`Later` ไม่แตะ / `Discard` ลบจริง)

#### ★★ `relink_targets` — สองเส้นทางที่ห้ามปนกัน

"ลากไฟล์เข้ามา" **สร้าง item ใหม่** จากผลลัพธ์ decode · "เปิดไฟล์ `.refx`" มี item
ครบแล้วพร้อมตำแหน่ง/หมุน/ครอป/แท็ก สิ่งที่ขาดคือ *พิกเซล* เท่านั้น
ถ้าไม่แยก ผู้ใช้จะเห็น **ภาพซ้ำสองชุด** ชุดหนึ่งอยู่ผิดที่ทั้งหมด

#### ★ `adopted_recovery` — จังหวะที่ลบตัวเก่าได้

กู้คืนแล้วห้ามลบทันที (ช่วงนั้นงานอยู่ใน RAM ที่เดียว) แต่ก็ห้ามค้างตลอดกาล
(จะถูกเสนอให้กู้ซ้ำทุกครั้ง) → **ลบหลัง snapshot ของ session นี้ลงดิสก์สำเร็จ**
หลักการเดียวกับ tmp → rename ของ `save_atomic`

#### ยืนยันบนของจริง (ภาพหน้าจอ)

| ทำอะไร | เห็นอะไร |
|---|---|
| ลาก 6 ใบ · **ไม่กด `Ctrl+S` เลย** · ฆ่าโปรเซส · เปิดใหม่ | แถบ *"Unsaved work from last time · 6 items from just now"* ครบสามปุ่ม |
| กด **Bring it back** | ภาพครบ **6 ใบ** กลับขึ้น canvas · `6 uploads` · **กู้ได้ 100%** |
| กด **Keep it, decide later** | ไฟล์ในโฟลเดอร์ **7 → 7** (ไม่ถูกแตะ) |
| `Ctrl+S` / `Ctrl+O` | *"Choose where to save"* / *"Choose a board to open"* |
| ★ I-1 สถานะใหม่: แถบกู้คืนโผล่ค้างไว้ 20 วินาที | **Frames 7 → 7** |

★ **ที่ยืนยันอัตโนมัติไม่ได้:** วงจร `Ctrl+S` → ปิด → `Ctrl+O` ผ่าน native dialog
— `ui-drive.ps1` บังคับ RefX ขึ้น foreground ซึ่งแย่ง focus จาก dialog ที่ต้อง
พิมพ์ลงไปพอดี · หลักฐานที่มีแทนคือ dialog ทั้งสองเปิดจริง + เส้นทางอ่านไฟล์และ
วางขึ้นจอเป็น **ฟังก์ชันเดียวกัน** กับที่แถบกู้คืนใช้ ซึ่งถ่ายภาพยืนยันแล้ว

### 2.25 ★★★ ประตูที่จำสถานะไว้ แทนที่จะอ่านสถานะจริง (14 ส.ค. 2026 · `c87aa25`)

`Fuzz (nightly)` แดงหลัง P4-1 — **ไม่ใช่เพราะ `fuzz_document` เจอ crash**
(มันยิงจริง 21m43s ผ่าน) แต่เพราะ job `unwired-targets`

สคริปต์เดิมเขียนว่า "สองตัวนี้ยังไม่ต่อ — ล้มถ้า module มีโค้ด" ซึ่งถูกตอนเขียน
แต่พอ `fuzz_document` ถูกต่อครบสามจุดแล้ว **มันยังล้มอยู่ดี เพราะไม่เคยถามว่า
"ต่อหรือยัง" มันจำมาว่า "ยังไม่ต่อ"**

#### ★ ทำไมไม่เห็นตอน P4-1 — และกฎที่ตามมา

ยืนยันประตูด้วยสคริปต์ที่ **เขียนเลียนแบบขึ้นมาเอง** ซึ่งตรวจ `[[bin]]` กับ matrix
ด้วย ส่วนของจริงไม่ได้ตรวจ · สิ่งที่พิสูจน์ได้จึงเป็น **"สคริปต์ที่เพิ่งเขียน
พอใจ" ไม่ใช่ "ประตูพอใจ"** → `docs/08 §3.9` ข้อ 9 ถูกขยายให้ครอบเคสนี้แล้ว
พร้อมส่วนที่ใช้กับเทสต์: **เทสต์ที่เรียกฟังก์ชันจำลองแทนฟังก์ชันที่ production
ใช้จริง พิสูจน์ได้แค่ว่าตัวจำลองทำงาน**

เจอซ้ำทันทีในรอบถัดมา: เทสต์ฆ่าโปรเซสของ P4-2 ใช้ `std::fs::rename` แทน
`rename_durable` → แก้แล้ว (`a629fce`) ด้วย dev-dependency

ประตูรุ่นใหม่ **อ่านสภาพจากไฟล์ทั้งสองฝั่ง** บังคับ
`module มีโค้ด ⟺ target ต่อครบสามจุด` — ไม่มีรายชื่อให้ค้างอีก และจับทิศกลับได้
(ต่อ target ไว้แต่ module ว่าง = fuzz เขียวโดยไม่ยิงอะไร) · negative control
รัน **สคริปต์จริงที่ดึงจาก YAML** แดงครบสามทิศ

### 2.3 กับดัก pointer ของ egui — ถึงเวลาแก้ที่ต้นเหตุแล้ว

`docs/03 §1` บันทึกทางแก้ชั่วคราวที่ใช้อยู่ (`egui_is_using_pointer()` +
`canvas.contains(cursor)`) พร้อมเงื่อนไขว่า **ทางที่ถูกคือทำ canvas เป็น widget จริง
ด้วย `ui.allocate_response(rect, Sense::click_and_drag())` แล้วขับทุกอย่างจาก response นั้น**

P2-4 คือจุดที่มันคุ้มแล้ว เพราะปุ่มซ้ายต้องเปลี่ยนหน้าที่จาก "pan" ไปเป็น
"เลือก/ลากกรอบ" ซึ่งแปลว่าต้องรื้อการตัดสินใจเรื่อง pointer อยู่ดี
และ `docs/03 §1` เตือนไว้ว่า **ถ้าเอา widget ของ egui ไปวางในช่อง canvas
ก่อนจะแก้ตรงนี้ เราจะแย่ง event นั้นไป**

ปุ่มกลาง = pan (กล้องไม่ใช่สถานะของ board จึงไม่ผ่าน `Command`)

> ⚠️ **ข้อความเดิมตรงนี้พูดถึง `layout.rs` ที่ยังเป็นไฟล์ TODO — ล้าสมัยแล้ว**
> (P3-2 ต่อ `fuzz_layout` ครบสามขั้นไปแล้ว) · ที่ยังเหลือคือ `refx-io::dto`
> และ `refx-io::journal`: job `unwired-targets` ใน `fuzz.yml` จะ **แดงทันที**
> ที่ไฟล์พวกนั้นมีโค้ดเกิน 2 บรรทัดโดยที่ fuzz target ยังไม่ถูกต่อ

---

## 3. ตัวเลขจริงที่วัดได้ (ใช้เป็นเส้นฐานเทียบ)

### เวลาโหลดภาพ — dataset 4000×3000, JPEG 70% / PNG 30%

| | cache เย็น | cache อุ่น |
|---|---|---|
| 1 ไฟล์ | **148.8 ms** | **4.5 ms** |
| 100 ไฟล์ (616 MB) | **8.09 s** (~81 ms/ไฟล์) | **24.6 ms** |
| คาด 1000 ไฟล์ | ~80 s | ~250 ms |

สร้าง dataset ด้วย `cargo xtask gen-testdata`

### Render — 10,000 quad

| N | p50 | ต้นทุน/quad |
|---|---|---|
| 0 (เส้นฐาน) | 0.297 ms | — |
| 1,000 | 0.303 ms | 6.3 ns |
| 10,000 | 0.360 ms | 6.3 ns |
| 100,000 | 1.088 ms | 8.1 ns |

ที่ 1,000 ภาพ ต้นทุน **98% คือ egui shell + present** ไม่ใช่ quad pipeline

#### ★ A/B ตอนลง P2-5 handle (4 ส.ค. 2026) — วัดสองรุ่นในเซสชันเดียวกัน

ตัวเลขข้างบนถ่ายไว้คนละวันคนละสภาพเครื่อง เทียบข้ามเซสชันแล้ว **ไม่มีความหมาย**
(docs/08 §3.9 ข้อ 6) จึง build รุ่นก่อนหน้า (`03d703e`) ใน worktree แล้ววัดสลับกัน
รุ่นละ 2 รอบ · p50 (ms) · `--bench-seconds=6` · `PresentMode::Immediate`

| N | ก่อน (`03d703e`) | หลัง | ต่าง |
|---|---|---|---|
| 0 | 0.334 · 0.325 | 0.298 · 0.300 | **−9%** (เร็วขึ้น) |
| 1,000 | 0.333 · 0.336 | 0.325 · 0.323 | **−3%** |
| 10,000 | 0.378 · 0.387 | 0.391 · 0.385 | **+1.4%** |

ความต่างระหว่างสองรอบของ**รุ่นเดียวกัน**อยู่ที่ ~3% อยู่แล้ว ตัวเลขทั้งหมดจึงอยู่ในนั้น
· **ไม่มีตัวไหนแย่ลงเกิน 5%**

> เส้นทางนี้แทบไม่ควรขยับเลย: `--demo-quads` สร้าง `QuadInstance` ตรง ๆ
> **ไม่ผ่าน `quad_for` และไม่ผ่าน `Board`** ส่วน `rebuild_quads` เรียกตอน board
> เปลี่ยนเท่านั้น ไม่ใช่ทุกเฟรม · handle เป็นรูปทรงของ egui 8 ชิ้นในเมชที่มีอยู่แล้ว
> ไม่ได้เพิ่ม draw call หรือ resource บน GPU (status bar ยัง `4 draws` เท่าเดิม)

> ⚠️ วัด frame time ต้องใช้ `--bench-seconds` ซึ่งบังคับ `PresentMode::Immediate`
> ถ้าวัดใต้ `AutoVsync` จะได้ตัวเลขของ **รอบ vsync จอ** ไม่ใช่ต้นทุนการวาด (เคยพลาดมาแล้ว)

#### ★ A/B ตอนลง P2-9 (5 ส.ค. 2026) — 1000 ภาพ · **กดค้างลากทั้งช่วงวัด**

ตารางเต็มพร้อมเหตุผลอยู่ใน §2.8 · สรุปตัวเลขที่ควรจำ:

| | ก่อน (`cadf3c4`) | หลัง | ต่าง |
|---|---|---|---|
| idle p50 | 0.364 ms | 0.388 ms | +6.6% (**คือปุ่ม 8 ตัวบน toolbar** พิสูจน์แล้ว) |
| ลากค้าง p50 | 0.465 ms | 0.4945 ms | +6.3% |
| **ต้นทุนที่การลากเพิ่มเข้ามา** | 0.101 ms | 0.107 ms | **+6 µs — ใต้ noise** |

> ★ อย่าอ่านแค่คอลัมน์ "ต่าง" ของสองแถวบน — มันรวมต้นทุนคงที่ที่ไม่เกี่ยวกับ
> การลากไว้ด้วย · ตัวที่ตอบว่า "ลากแล้วเฟรมตกไหม" คือ **แถวล่างสุด** เท่านั้น

### อื่น ๆ

- atlas เติมกลับหลังกู้ device: **8/8 ภาพใน 0.23 ms** (วัดใหม่ 29 ก.ค. 2026)
  > ⚠️ ตัวเลขเดิม "100/100 ภาพใน 0.99 ms" จริงตอนที่วัด แต่หลังจากนั้น atlas
  > เปลี่ยนไปจอง layer แบบ lazy แล้ว **การเติมกลับพังเงียบ ๆ เหลือ 0/8**
  > จนกระทั่งงานเทสต์ device lost (29 ก.ค.) ไปรันของจริงถึงเจอ — ตัวเลขที่วัดครั้งเดียวแล้วไม่มีเทสต์คุม
  > จะกลายเป็นตัวเลขที่ *เคย* จริง โดยไม่มีใครรู้ว่ามันเลิกจริงตั้งแต่เมื่อไหร่
- device lost recovery: หลับ 2.5 s → ยิง → กู้ใน **126 ms** → เติม atlas ครบ → กลับไป idle
  (ยืนยันด้วยมือ 29 ก.ค. 2026: กู้ 1 ครั้งพอดี · validation error 0 · panic 0 · ปิดโปรแกรมแล้วโปรเซสตายสะอาด)
- **binary release: 15.35 MB** (เพดาน 25 MB → headroom เหลือ **9.65 MB**)
  > ตัวเลข "12 MB" ที่เคยเขียนไว้ **ไม่เคยเป็นความจริง** — build commit `b100661` ใหม่
  > ด้วย profile และ toolchain เดียวกันได้ 15.26 MB
  > ตั้งแต่ P1-6 ถึงตอนนี้ binary โตแค่ **92 KB** (33 KB โค้ด + 59 KB ฟอนต์ไทย)

---

## 4. ★ การตัดสินใจที่ผูกมัดแล้ว (ห้ามย้อน ห้ามเดาใหม่)

| # | กฎ | เหตุผล | อยู่ที่ |
|---|---|---|---|
| 1 | **ห้าม memmap ไฟล์ผู้ใช้** ใช้ `metadata()` → `File::take()` | ไฟล์ถูกตัดระหว่าง map = **SIGBUS** ซึ่งเป็น signal ไม่ใช่ panic → `catch_unwind` จับไม่ได้ โปรเซสตายทันที เกิดจริงกับ Dropbox/OneDrive sync | `docs/06 §3` |
| 2 | **ไม่ใช้ BC7** ใช้ `Rgba8UnormSrgb` อย่างเดียว | encode ช้าระดับวินาที ชนกับ "ภาพต้องขึ้นทันที" · ประหยัดแค่ 48 MB จากงบ 384 MB ไม่คุ้ม · encoder ที่มีเป็น C wrapper | `docs/04 §4` |
| 3 | **`max_pixels` ผูกกับ RAM เครื่อง** = `min(total_ram/8/8, 16384²)` | ภาพ 16384² ขอ 2 GiB บนเครื่อง 8 GB ที่เปิด Photoshop = OOM = งานหาย | `docs/05 §2` |
| 4 | **cache key = `(hash, mtime, size)`** ไม่ใช่ hash เดี่ยว | fast hash ของไฟล์ >64 MB อ่านแค่หัว/ท้าย → PSD/TIFF ที่แก้เลเยอร์กลางไฟล์ได้ hash ชนกัน → เห็น thumbnail เวอร์ชันเก่า **แบบเงียบ ๆ** | `docs/05 §3` |
| 5 | **atlas ต้องเติมกลับหลังกู้ device** | ไม่งั้น board ว่างเปล่าหลัง driver อัปเดต ผู้ใช้แยกไม่ออกจาก "งานหาย" ทำให้ P0-5 เสียเปล่า | `docs/04 §4` |
| 6 | **หน้าต่างมี surface ได้ทีละอันเดียว** ต้อง drop ชุดเก่าก่อนสร้างใหม่ | Vulkan ตอบ `Native window is in use` แล้ว panic → `stack` เป็น `Option<_>` + `is_usable()` | `docs/04 §7` |
| 7 | **`DeviceLostReason::Destroyed` ห้ามนับเป็นอุบัติเหตุ** | ยิงตอนเรา drop device เอง ถ้านับจะกู้วนไม่รู้จบ | `docs/04 §7` |
| 8 | **ต้องสร้าง `egui::Context` ใหม่ทั้งก้อนตอนกู้** | font atlas ผูกกับ `Renderer` เดิม ถ้าเก็บ Context ไว้ UI หายหมด | `docs/04 §7` |
| 9 | **panic hook ต้องเงียบตอน decode** (thread-local flag) | ไฟล์เสีย 500 ไฟล์ = backtrace 500 ชุด → log หมุนทะลุ 5 MB → ทับ crash log จริง | `docs/06 §3` |
| 10 | **cache eviction ห้ามแตะโฟลเดอร์ `logs/`** | crash log คือสิ่งเดียวที่ผู้ใช้มีให้ส่งเวลารายงานปัญหา | `docs/08 §5` |
| 11 | **flag จำลอง device lost ต้องมีสองแบบ** นับเฟรม + **นับเวลา (`-ms`)** | ตัวนับเฟรมใช้ไม่ได้ถ้า I-1 ถูกต้อง (แอปวาด ~6 เฟรมแล้วหลับ) · ตัว `-ms` ทดสอบเคสจริงคือ device ตายตอนแอปหลับ | `ROADMAP P0-5` |
| 13 | **เรียก `fast_image_resize` ได้จาก `refx-asset::resize` ที่เดียว** (`#[inline(never)]`) | เรียกจากสองจุดทำให้ binary โต **2.6 MB** จาก monomorphization · ต้นทุนอยู่ที่*จำนวนจุดที่เรียก* ไม่ใช่ตัวเลือกที่ส่งเข้าไป · เจอตอน P1-7 เพราะเพดานใน CI จับได้ | `docs/08 §6` |
| 12 | **เพดาน RAM ต้องคุมรวมทุก worker** ไม่ใช่ต่อ job | 6 worker × 1 GiB = 6 GB แย่ง RAM กับ Photoshop · ปัจจุบัน 256 MB รวม + `Condvar` รอ + ใบจองเป็น RAII | `docs/05 §2` |
| 14 | **ภาพจาก clipboard ไม่เข้า cache.sqlite** (ตัดสิน P1-8) | ข้อ 4 บังคับคีย์ `(hash, mtime, size)` แต่ clipboard ไม่มี mtime · ใส่ค่าปลอมแทน = จุดบอดของ fast hash กลับมาทันที ซึ่ง mtime มีไว้ปิดพอดี · ภาพที่วางมาใช้ครั้งเดียวเป็นปกติ เก็บไว้มีแต่จะไล่ thumbnail ของไฟล์จริงออกจาก LRU | `pool.rs` `JobSource::Clipboard` |
| 15 | **ลำดับฟิลด์ของ `Assets` คือลำดับ drop — ห้ามสลับ** (`pool` → `io_tx` → `_io`) | `IoThread::drop` join เธรด IO ซึ่งจบก็ต่อเมื่อ sender หมดทุกใบ ถ้า `_io` ถูก drop ก่อน `io_tx` = **ปิดหน้าต่างแล้ว RefX.exe ไม่ตาย** ล็อก single-instance ค้าง เปิดใหม่ไม่ได้อีกเลย (เกิดจริง ยืนยันด้วยมือ 29 ก.ค. 2026) | `refx-ui/src/app.rs` + เทสต์ `dropping_assets_finishes_instead_of_hanging_forever` |
| 16 | **resource ที่ผูกกับ device ต้องสร้างผ่าน `DeviceBound::build` จุดเดียว** และรับด้วยการ destructure | เปิดโปรแกรมกับกู้ device เคยเป็นโค้ดคนละชุด แล้ว **drift**: P1-7 เพิ่ม `WorkingCache` แต่ `recover_device()` ไม่ได้สร้างใหม่ → หลังกู้ยังถือ texture/bind group ของ device ที่ตายแล้ว · destructure ทำให้เพิ่ม resource ใหม่แล้ว **คอมไพล์ไม่ผ่านทั้งสองที่** จนกว่าจะจัดการครบ (หลักการเดียวกับ `GpuStack`) | `refx-ui/src/app.rs` |
| 17 | **เติม atlas กลับ ต้อง `resize(layers_needed(n))` ก่อนเริ่มเติมเสมอ** | atlas ที่เพิ่งสร้างมี **0 layer** (จอง lazy ตั้งแต่ 28 ก.ค.) ถ้าเติมเลยจะได้ `NeedsResize` ตั้งแต่ภาพแรก แล้ว**ทุกภาพกลายเป็น placeholder** = board ว่างเปล่าหลัง driver อัปเดต (เกิดจริง: `restored=0 total=8`) · ขยายกลางคันไม่ได้เพราะ `resize()` ล้างตัวจัดสรรทั้งชุด | `refx-render::atlas::layers_needed` + เทสต์ `refilling_a_fresh_atlas_needs_a_resize_first` |
| 18 | **`refx-asset` ห้าม depend `refx-platform`** (ตัดสิน 2 ส.ค. 2026) | ชั้น asset ที่รู้จัก windowing/dialog/clipboard ลาก `rfd`→`ashpd`→`zbus` เข้า fuzz จนคอมไพล์ nightly ไม่ผ่าน · ของที่ต้องถาม OS ให้ **ผู้เรียกส่งเข้ามา** หรือกลับทิศด้วย trait ใน `refx-core` | `refx-asset/Cargo.toml` (มีคอมเมนต์ห้ามไว้) + `refx-core::clipboard` |
| 19 | **เขียน `Arena` เอง ห้ามกลับไปใช้ `slotmap`** | `slotmap` ไม่มี API ใส่ของกลับที่คีย์เดิม (คีย์ที่ลบแล้วตายถาวร) → undo ของ "ลบภาพ" จะคืน `ItemId` **ใหม่** แล้ว `z_order`/`Selection`/`ItemMeta::group` ที่ถือคีย์เก่าจะห้อยหมด = ภาพกลับมาแต่ลำดับและการเลือกหาย · ทำ I-3 ไม่ได้ตั้งแต่ต้น | `refx-core::arena::Arena::insert_at` |
| 20 | **`Arena`/`Selection` `PartialEq` เทียบสิ่งที่ผู้ใช้สัมผัสได้ ไม่ใช่โครงข้างใน** | `Arena` เทียบ **คีย์+ค่าของสิ่งที่มีชีวิต** ไม่ใช่ `slots` ดิบ — นับช่องว่างท้าย vec ด้วยจะทำให้ undo ของ "เพิ่มภาพ" ไม่มีวันคืนสภาพได้ ทั้งที่นั่นคือการวัด*ตัวจัดสรร* · ส่วน `Selection` เทียบ **ตามลำดับ** ไม่ใช่แบบเซต เพราะ anchor ของ align ขึ้นกับลำดับคลิก | `arena.rs` / `selection.rs` + เทสต์คู่ที่อธิบายทั้งสองทิศ |
| 22 | **ไฟล์ `.refx` ที่มี blob ฝังอยู่ = v2 · ไม่มี = v1** (ไม่ใช่ "ผู้ใช้เลือก packed = v2") | รุ่นเก่าเปิดไฟล์ที่มี blob ได้โดยบังเอิญ (`decode` ตัดที่ `doc_len`) แล้วเห็นเป็น `Missing` · **save ทับเมื่อไหร่ ภาพที่ฝังไว้หายถาวร** (`docs/07 §2` · I-3) → v2 ให้รุ่นเก่าปฏิเสธทั้งเปิดและเขียนทับ · ไฟล์ที่ไม่มีของให้เสียยังเป็น v1 รุ่นเก่าจึงเปิดงานประจำวันได้ | `refx-io::packed` §2.27 |
| 23 | **Linked ก็ฝัง — ใบที่ไม่มีไฟล์ต้นทางถูกฝังเสมอ** และ **ห้ามถามผู้ใช้ว่าจะฝังไหม** | ภาพจาก clipboard ไม่มีไฟล์ต้นทาง บันทึก linked ตรง ๆ = `AssetRef` ชี้ที่ว่าง = ภาพหายถาวร (I-3 เงียบที่สุด) · คนที่วางภาพจากเบราว์เซอร์ไม่รู้ว่า linked/packed ต่างกันยังไง การถามตอนกำลังจะบันทึกคือโยนการตัดสินใจไปให้คนที่มีข้อมูลน้อยกว่า | `packed::plan_embeds` |
| 24 | **spool ของภาพที่วาง: เพดานเป็น *ไบต์* 512 MB · ห้าม JPEG** | PNG ของภาพ 6000×4000 = **78 MB** (วัดแล้ว) เพดานแบบนับไฟล์ 64 ใบจึงกลายเป็น ~5 GB โดยไม่มีใครตั้งใจ · JPEG เล็กกว่ามากแต่ **spool คือต้นฉบับเดียวที่เหลือของภาพที่วาง** การบีบแบบสูญเสีย = ทำต้นฉบับของผู้ใช้แย่ลงถาวรโดยเขาไม่ได้สั่ง แล้วมันจะถูกฝังลง `.refx` ในสภาพนั้นต่อไป · ✅ ทำแล้ว: `spool::MAX_BYTES` + `Swept::protected_over_cap` (§2.28) | `refx-io::spool` §2.27 |
| 25 | **`AssetRef::hash` ของภาพที่วาง = hash ของ *พิกเซล* (hash ทั้งก้อน ห้าม fast path) ไม่ใช่คีย์ของงาน** | ชื่อไฟล์ใน spool คือ hash · `sweep` แปลงชื่อกลับเป็น hash แล้วถาม board ว่าใครอ้างถึง คีย์ไม่ตรงเมื่อไหร่ = **ลบภาพของผู้ใช้ทิ้งเงียบ ๆ** (I-3) · และคีย์ของงาน (`clipboard:N`) ต่างกันทุกครั้งที่วาง ภาพเดิมจึงจะได้ไฟล์ละใบ · fast path ของ `hash_file` ปลอดภัยได้เพราะ cache key มี `mtime` คร่อมจุดบอด (ข้อ 4) แต่ภาพที่วาง**ไม่มี mtime** — คีย์ชนกัน = ภาพใบที่สองหาย · ราคาที่วัดได้ +6 ms ที่ 6000×4000 | `refx_asset::hash::hash_pasted` §2.28 |
| 28 | **`Board` เป็นคนบอกว่า item เป็นภาพหรือไม่ — `render_state` ไม่ใช่** | `render_state` เป็น cache ที่อยู่ยาวกว่าสถานะของ item **โดยตั้งใจ** (ช่อง atlas ต้องรอด undo/redo ของ "ลบภาพ" — §2.5) · `rebuild_quads`/`plan_working_textures` ที่ถามแค่ *"มี `render_state` ไหม"* จะวาดภาพเก่าของใบที่กลายเป็น `Missing` ไปแล้ว = **จอไม่ตรงกับเอกสาร** และผู้ใช้เชื่อว่าไฟล์ยังอยู่ · เจอตอนกด `Ctrl+Z` บนแอปจริง ไม่มีเทสต์ไหนจับได้ก่อน | `refx-ui::app::rebuild_quads` §2.30 |
| 27 | **`AssetRef::hash` = คีย์ของ *เนื้อ* เสมอ ทั้งไฟล์และภาพที่วาง — ห้ามกลับไปใช้คีย์ของงาน** | `docs/02 §2.3` ผูกสามสัญญาไว้กับข้อนี้: ย้ายไฟล์แล้ว thumbnail ไม่หาย · **relink ค้นด้วย hash (P4-6 ขั้น 3/5)** · ไฟล์ซ้ำถูกยุบ · คีย์ของงานถูกตั้งบน UI thread ก่อนใครจะแตะไฟล์ จึงเป็นคีย์ของ *ที่อยู่* โดยธรรมชาติ → worker ส่งคีย์จริงกลับมาทาง `ContentOrigin` · ★ กิ่งที่ลืมง่ายที่สุดคือ **cache hit** ซึ่งเป็นกิ่งที่ผู้ใช้เดินทุกวัน — ลืมแล้วสัญญาจะจริงเฉพาะตอน cache เย็น · ★★ **item ที่มาจากไฟล์ห้ามถูกตั้งชื่อใหม่** เอกสารเป็นเจ้าของคีย์ของตัวเอง (ไฟล์ PNG ใน spool hash แล้วได้คนละค่ากับชื่อของมันเอง) | `refx-asset::pool::ContentOrigin` §2.29 |
| 26 | **`Done` ของภาพที่วางต้องออกไปก่อนไฟล์ลงดิสก์ — บังคับด้วยชนิดข้อมูล** | encode PNG ของ 6000×4000 = **~320 ms (release)** ซึ่ง `docs/07 §2` ห้ามไม่ให้มาขวางภาพขึ้นจอ · ร่างเดิมใน §2.27 (`Done { spooled: Option<PathBuf> }`) ทำตามตัวอักษรไม่ได้เพราะ path มีค่าก็ต่อเมื่อเขียนเสร็จ → ใช้ **`Option<ContentHash>`** แทน (path หาได้จาก hash) แล้วให้ `run_job` คืนงานที่เลื่อนออกไปเป็น**ค่าคืน** จึงเขียนผิดลำดับไม่ได้เลย | `refx-asset::pool` §2.28 |
| 21 | **ค่าคงที่เป็น "สัญญา" ต้องเป็น `pub` และเทสต์/fuzz ต้อง assert ด้วยตัวมัน ห้าม hard-code ซ้ำ** | `fuzz_layout` เขียน `size > 0.0` แทนสัญญาจริง `size >= MIN_SIDE` → **157,000 รอบไม่จับการถอดด่านสุดท้ายออก** เพราะขนาด `0.525` ก็ยัง "มากกว่าศูนย์" · **assertion ที่อ่อนกว่าสัญญา = target ที่เขียวโดยไม่ได้ตรวจอะไร** ชนิดเดียวกับ `fuzz_decode` · **ใช้กับ target อื่นทุกตัว**: เจอที่ไหน assert ด้วยเลขที่คัดมาเอง ให้เปลี่ยนไปอ้างค่าคงของสัญญาแทน | `layout::MIN_SIDE`/`MAX_SIDE` + `fuzz_layout` |

---

## 5. เรื่องที่ spec เคยผิดแล้วแก้แล้ว (อย่าเชื่อเอกสารเก่าที่จำมา)

- `docs/05 §6` เคยเขียนว่า 1000 ภาพ cache เย็น ≈ **4 วินาที** → ของจริง **~80 วินาที** (ผิด 20 เท่า เพราะเดาจากภาพเล็ก)
- `docs/03 §1` เคยใช้ `SidePanel` / `TopBottomPanel` / `CentralPanel::show` ซึ่ง egui 0.34 **deprecate หมดแล้ว**
  ของจริง: `egui::Panel::top/left/right/bottom` + `.show_inside(ui)` + `default_size`
  **`egui::Panel::center` ไม่มีจริง** ใช้ `CentralPanel::default().show_inside(ui, …)`
- `docs/09` มีตาราง wgpu 29 API delta: `push_constant_ranges`→`immediate_size`,
  `bind_group_layouts` รับ `&[Option<&_>]`, `multiview`→`multiview_mask`
- `tracing-appender` 0.2 **หมุนไฟล์ตามขนาดไม่ได้** (มีแค่ตามเวลา) ต้องเขียน writer เอง
- `docs/02 §2` เคยให้ `refx-core` ถือ `ImageFormat` กับ `LoadError` ของ crate `image`
  ซึ่ง **ทำไม่ได้และไม่เคยทำได้** (`refx-core` depend `image` ไม่ได้ · `image::ImageError`
  serialize ไม่ได้ แต่ §7 บังคับให้ทุกอย่างใน `Board` ลง DTO ได้) → แก้แล้วที่ **§2.2.5**:
  core นิยาม enum เอง ส่วน `impl From<&LoadError> for MissingReason` อยู่ `refx-asset` จุดเดียว
  **`MissingReason::Unknown` ต้องมีเสมอ** — ไฟล์จากรุ่นใหม่กว่าอาจมีเหตุผลที่รุ่นนี้ไม่รู้จัก
  อ่านเจอต้องตกมาที่นั่น ห้าม error ทิ้งทั้งไฟล์ (I-3) · **กฎนี้ใช้กับทุก enum ที่ลงไฟล์**
- `docs/02 §5` เคยเขียน zoom `0.02..=32.0` ทั้งที่โค้ดใช้ `0.01..=64.0` มาตั้งแต่ P0-7
  → แก้เอกสารให้ตรงโค้ด (เอกสารเป็นฝ่ายผิด)
- `HANDOFF §2.0` เดิมเขียนว่าย้าย `panic_guard` อย่างเดียวแล้ว dependency หายทั้งเส้น
  → ของจริงมีสามอย่าง (ดู §2.0)

### ★ false green ตัวที่สี่: `Fuzz (nightly)` ไม่เคยรันบน nightly เลย (2 ส.ค. 2026)

หลังแยกชั้น `refx-asset` แล้ว `fuzz-lint` เขียว แต่ job `fuzz` ยังแดง — **คนละสาเหตุกับ `zbus`**
log บอกตรง ๆ ว่า rustc ที่ถูกเรียกคือ `1.92-x86_64-unknown-linux-gnu`

`rust-toolchain.toml` ที่รากตรึง `channel = "1.92"` ไว้ และ **ไฟล์นั้นชนะ**
`rustup default nightly` ที่ `dtolnay/rust-toolchain` ตั้งให้ ผลคือ:

- job `fuzz` ล้มเสียงดัง เพราะ `cargo fuzz` ต้องใช้ `-Zsanitizer` (ดีแล้ว)
- job `fuzz-lint` **ผ่านแบบเงียบ ๆ บน stable** ทั้งที่หน้าที่เดียวของมันคือตรวจว่า
  โค้ดคอมไพล์บน nightly ได้ → **ชื่อของ job บอกตรงข้ามกับสิ่งที่มันทำ**

แก้: `RUSTUP_TOOLCHAIN: nightly` ระดับ workflow (ชนะ toolchain file ตามลำดับของ rustup)
**ไม่ใช่การปักหมุดวันที่** — ยังเป็น nightly ล่าสุดเสมอ

> ★ **นี่คือครั้งที่สี่ของรูปแบบเดิม** (canvas ว่าง · `fuzz_decode` stub · เพดาน binary
> ที่ไม่มีใครตรวจ · toolchain ที่ไม่ตรงชื่อ) และ **ทั้งสี่ครั้งเจอเพราะมีอย่างอื่นแตก
> แล้วไปตามต่อ ไม่ใช่เพราะมีใครไปตรวจว่าตัวตรวจทำงานจริงไหม**
>
> ทางแก้ที่ใช้ได้คือทำให้สภาพผิดเป็น **ความล้มเหลวที่ส่งเสียง** ไม่ใช่ความเงียบ —
> ทั้งสอง job จึงมีขั้น `rustc --version | grep nightly` ที่แดงถ้า toolchain หลุด
> **เพิ่ม job/harness ใหม่เมื่อไหร่ ให้ถามก่อนว่า "ถ้ามันไม่ได้ทำงาน จะรู้ได้ยังไง"**

---

## 6. หนี้ที่ค้างอยู่

| เรื่อง | หมายเหตุ |
|---|---|
| **★★★ `Board::view` ไม่เคยถูกซิงค์เลย — P4-1 ต้อง*แก้* ไม่ใช่แค่ serialize** | `ViewState { canvas, arrange, mode }` อยู่ใน `Board` และตั้งใจให้ persist ลง `.refx` (เปิดไฟล์มาแล้วกลับมุมมองเดิม — docs/02 §5) แต่ **ไม่มีโค้ดไหนเขียนมันเลยสักบรรทัด**: กล้องจริงคือ `Gfx::camera` · การเลื่อนของ Arrange อยู่ใน `ArrangeView::scroll` · โหมดอยู่ใน `ShellState::mode` — ทั้งสามตัวแยกกันอยู่คนละที่และไม่มีใครสะท้อนกลับเข้า `Board` · ★ **กับดักของ P4-1: การเขียน DTO ให้ `ViewState` แล้วเทสต์ round-trip ผ่านหมด จะดู "เสร็จ" ทั้งที่สิ่งที่ถูกบันทึกคือกล้องค่าปริยายเสมอ** ผู้ใช้เปิดไฟล์แล้วมุมมองไม่กลับมา โดยไม่มี error ที่ไหน · งานจริงมีสองครึ่ง: (ก) ตัดสินว่าซิงค์ตอนไหน (ทุกเฟรม / ตอน save / ตอนกล้องหยุดนิ่ง) แล้วเพิ่ม setter ใน `Board` ที่ **ไม่ผ่าน `Command` และไม่ทำให้ `dirty`** ตามข้อยกเว้นที่ docs/08 §4 อนุญาตไว้แล้ว (ข) ค่อยเขียน DTO · **ทำครึ่ง (ข) อย่างเดียว = false green** |
| ~~**★★ `cargo deny` แดง: `RUSTSEC-2026-0257`**~~ | ✅ **ปิดแล้ว 13 ส.ค. 2026** — ทำครบทั้งสองทาง แยกสอง commit (อัปเป็น 1.2.4 แล้วถอด feature `links` ทิ้ง) ดู §1 · ไม่มีข้อยกเว้นถูกเพิ่มใน `deny.toml` · ★ **ที่ยังยืนยันไม่ได้: ไม่มีเทสต์ไหนพิสูจน์ว่า clipboard ทำงานจริงบน Linux** เทสต์ที่มีรับ `Err` ว่าผ่าน |
| ~~**★★★ งานที่ยังไม่เคยบันทึกไม่มี autosave**~~ | ✅ **ปิดแล้ว 18 ส.ค. 2026** (`c0a87b9` · `f53dfa7`) — `<data_dir>/recovery/<session-id>.refx` · นโยบายตัวเดียวกับ P4-3 เป๊ะ (คนที่ยังไม่เคยบันทึกไม่ได้รุ่นด้อยกว่า) · ★ **ที่เหลือ: ผู้ใช้ยังเอามันกลับมาบนจอไม่ได้** จนกว่าจะมี dialog |
| **★★ `directories::data_dir()` ชี้ Roaming บน Windows** | ไม่ใช่หนี้แล้ว (ใช้ `data_local_dir()` แล้ว) แต่ **บันทึกไว้เป็นกับดัก**: ชื่อ API อ่านแล้วดูถูกทุกประการ ผลคือ snapshot ของงานที่ยังไม่เคยบันทึกถูกอัปขึ้นเครือข่ายองค์กรทุกครั้งที่ผู้ใช้ล็อกอิน (และช้าด้วย เพราะเขียนบ่อย) · ★ **ไม่มีเทสต์ไหนถามได้ว่า "โฟลเดอร์นี้ถูกซิงค์ขึ้นเน็ตไหม"** — เจอเพราะรันของจริงแล้วเปิดโฟลเดอร์ดู · ตอนนี้กันด้วยเทสต์ `on_windows_data_lives_under_local_appdata` + ชื่อ API ที่ถูกถูกเขียนลง `docs/07 §4` แล้ว |
| ~~**★★ ยังไม่มี dialog กู้คืน 3 ตัวเลือก**~~ | ✅ **ปิดแล้ว 18 ส.ค. 2026** (`6f79c76`) — และมันปลดล็อกอีกสองอย่างพร้อมกันตามที่คาดไว้: เกณฑ์ "เปิดใหม่ได้งานคืน" ยืนยันบนของจริงได้แล้ว และเพดาน 10/30 วันถูกต่อเข้าตอนผู้ใช้ตอบ |
| ~~**★★ ยังไม่มี "เปิดไฟล์" (`Ctrl+O`)**~~ | ✅ **ปิดแล้ว 18 ส.ค. 2026** (`6f79c76`) — `Ctrl+O` → `read_document` → `adopt_board` · round-trip ทุกฟิลด์ (pos/size/rotation/crop/filter/tag/rating/group/note) มีเทสต์ที่ยิง `read_document` **ตัวที่ปุ่มใช้จริง** · ★ **ที่ยังยืนยันไม่ได้: วงจร Ctrl+S → ปิด → Ctrl+O ผ่าน native dialog แบบอัตโนมัติ** — `ui-drive.ps1` บังคับ RefX ขึ้น foreground ซึ่งแย่ง focus จาก dialog ที่ต้องพิมพ์ลงไปพอดี · หลักฐานที่มีแทน: ทั้งสอง dialog เปิดจริง (ข้อความบน status bar) + เส้นทางอ่าน/วางบนจอเป็นตัวเดียวกับที่แถบกู้คืนใช้ ซึ่งถ่ายภาพยืนยันแล้ว |
| **undo history ข้าม session ไม่ได้** | ยอมเสียตาม `docs/07 §4` — หลังกู้คืนแล้ว `Ctrl+Z` ย้อนไปก่อน crash ไม่ได้ · เป็นราคาของการเลือก snapshot แทน command journal |
| **`fsync` ไม่มีเทสต์คุม** | เทสต์ฆ่าโปรเซสพิสูจน์ **atomicity ของ rename** ไม่ใช่ `fsync` · การฆ่าโปรเซสไม่ทำให้ข้อมูลที่ OS รับไปแล้วหาย → ยืนยันแล้วด้วย negative control: ถอด `sync_all()` แล้วเทสต์ยัง**เขียว** · จะทดสอบจริงต้องตัดไฟจริงหรือ `dm-flakey` |
| **`MOVEFILE_WRITE_THROUGH` ไม่ใช่หลักประกันเต็ม** | MSDN ผูก guarantee ไว้กับ *copy and delete* (ย้ายข้ามไดรฟ์) ส่วนกรณีเรา (โฟลเดอร์เดียวกัน) **ไม่ได้ระบุตรง ๆ ว่าครอบ** → "ดีที่สุดเท่าที่ API เปิดให้" ไม่ใช่ปิดสนิท |
| **ฟิลด์ที่ยังไม่มีใครเขียนค่าจริง 7 ตัว** (เดิม 10 — P3-7 ปลดล็อกไป 3) | ตารางเต็มใน §2.19 · มีประตู `no_field_stays_unwritten_without_us_knowing` คุมไม่ให้เพิ่มตัวใหม่โดยไม่มีใครรู้ · ที่เหลือ: `ItemCanvas::locked`/`visible` (รอ UI) · `AssetRef::format` (§6) · ~~`AssetRef::embedded`~~ (P4-5 ตัดสินแล้วว่า **ไม่เก็บลงไฟล์** — อนุมานจาก asset table ดู §2.27) · `BoardSettings::background` (อ่านแล้ว รอ UI) · `ArrangeState::sort`/`descending` (รอ P4-1 ตัดสินว่าต้อง persist ไหม) |
| ~~**★ P2-9 ยังไม่ยืนยันบนของจริง**~~ | ✅ **ปิดแล้ว 5 ส.ค. 2026** — ครบทั้งสามข้อ ดู §2.8 · อาการ "ลากแล้วภาพไม่ขยับ" เป็นของสคริปต์ ไม่ใช่ของโค้ด |
| **สคริปต์ขับเมาส์: กฎสองข้อ** | อยู่ใน `scripts/ui-drive.ps1` แล้วทั้งคู่ — (ก) **ต้องยืนยัน foreground** ก่อนทุกขั้นที่คลิก/ถ่ายภาพ ไม่งั้นได้หลักฐานปลอม (§2.8) · (ข) `move` แล้วต้องรอก่อน `down` ไม่งั้น egui รวม event เข้าเฟรมเดียวแล้ว `Press` ไปตกที่ตำแหน่งหลังขยับ (เจอครั้งแรกตอน P2-5) |
| **board รับได้แค่ 3,072 ภาพ (= ขนาด atlas)** | ✅ **ยอมรับเพดานนี้แล้ว** (ROADMAP P3-3) และ**ไม่เงียบแล้ว** (§2.15) · ยกเพดานเมื่อไหร่ต้องแก้สองชั้นพร้อมกัน (atlas เป็น LRU ตามสิ่งที่อยู่ในจอ + thumbnail อ่านกลับจาก `cache.sqlite` ได้) ซึ่งแตะทั้ง I-6 และ threading → **P5 พร้อมตัวเลขจากผู้ใช้จริง** |
| **ปุ่มบน toolbar มีราคาต่อเฟรมที่วัดได้** | ~1.8 µs ต่อปุ่มต่อเฟรมที่ 1000 ภาพ (§2.8) — ยังห่างเพดานมาก แต่ **ควรวัดซ้ำก่อนเติมปุ่มชุดใหญ่** เพราะ §3 บอกว่า 98% ของต้นทุนที่ 1000 ภาพคือ egui shell อยู่แล้ว |
| ~~**★ ยังไม่รู้ว่าการสลับ keyboard layout ทำให้แอปค้างไหม**~~ | ✅ **ปิดแล้ว 6 ส.ค. 2026 — ไม่ค้าง** ดู §2.12 · มี `scripts/ui-layout-switch.ps1` ให้รันซ้ำได้ |
| **หัวข้อ `X / Y / W / H` ใน Inspector ยังไม่มีตัวเลข** | เห็นตอนถ่ายภาพหลักฐาน P2-9: หัวข้อสัญญาว่าจะบอกพิกัด/ขนาด แต่ข้างล่างมีแต่ opacity/filter · ทำให้ต้องวัดตำแหน่งจากพิกเซลบนภาพหน้าจอแทนที่จะอ่านจากจอตรง ๆ · **ยังไม่ตัดสินว่าจะเติมช่องหรือแก้หัวข้อ** — ยังไม่ได้ทำใน P2-10/P2-11 (ทั้งคู่เติมช่องอื่นในแผงนี้แต่ไม่ได้แตะหัวข้อ) → **เลื่อนไป P3-1** ที่ทำ inspector เต็ม |
| **★ เทสต์ล้มหนึ่งครั้งโดยไม่รู้ว่าตัวไหน (4 ส.ค. 2026)** | ระหว่างทำ audit ฟิลด์→shader `nextest --workspace` ล้มหนึ่งครั้ง (`299/418 tests were not run due to test failure`) ในรอบที่ต่อท้าย `cargo fmt` + `clippy` ในคำสั่งเดียวกัน · **ไม่ได้ชื่อเทสต์** เพราะ grep ตัดทิ้ง แล้ว nextest ไม่ได้เก็บผลไว้ · หลังจากนั้นรันซ้ำ **16 รอบไม่ล้มอีกเลย** (รวม `--test-threads=16` 3 รอบ และเฉพาะ `refx-render` ซึ่งแตะ GPU อีก 5 รอบ) · สมมติฐานที่ยังไม่ได้พิสูจน์: สร้าง wgpu device ตอนเครื่องกำลัง build อยู่ · **ถ้าเจออีกให้จดชื่อเทสต์ทันที** อย่าปล่อยผ่าน — flake ที่ไม่มีใครตามคือจุดเริ่มของการเลิกเชื่อสีแดง (`docs/08 §3.9` ข้อ 5b) |
| **`arboard` จอง RAM ของ pixel ก่อนเพดานของเราได้ตรวจ** | API ของมันไม่เปิดให้อ่าน byte ดิบของ clipboard มาเข้า `decode_guarded` — มันอ่าน `CF_DIBV5`/PNG แล้วถอดรหัสเองจนเสร็จก่อนคืนค่า เพดาน `max_pixels` ของเราจึงเป็นด่าน **หลัง** การจองก้อนนั้นหนึ่งครั้ง ตอนนี้กันด้วยการจำกัดให้วางได้ทีละครั้ง · จะปิดสนิทต้องอ่าน clipboard เองใน `refx-platform` (ต้องเพิ่ม dep `clipboard-win` — **ต้องถามก่อน**) |
| ~~**ภาพที่วางจาก clipboard คมได้แค่ระดับ thumbnail**~~ | ✅ **ปิดแล้ว 20 ส.ค. 2026** (§2.28) — spool `<data_local_dir>/pasted/<hash>.png` เขียนตั้งแต่ตอน paste · **ยืนยันบนแอปจริงด้วยตัวเลข**: neighbour-contrast ที่ซูมสุด **0.01 → 5.09** (ภาพ checker 1 px ที่ thumbnail จะเฉลี่ยเป็นเทาเรียบ) และ status bar ขึ้น `Sharp 28.3 MB` · เทสต์ที่รักษามันไว้: `the_image_reaches_the_screen_before_the_png_reaches_the_disk` (ลำดับ) · `a_pasted_image_the_board_points_at_is_never_swept` + คู่ negative control (คีย์) · ★ ที่เทสต์คุมไม่ได้คือ "ตาเห็นคม" เอง — **ยืนยันด้วยมือ 20 ส.ค. 2026** พร้อมภาพหน้าจอ |
| `--open-dir` วาง quad เป็นตาราง 16 คอลัมน์ตายตัว | ยังไม่ใช่ layout จริง รอ P2/P3 |
| `xtask gen-testdata` | ทำแล้วบางส่วน · ยังไม่มี `bench` / `package` (P5-1) |
| egui memory ตอนกู้ device | ตอนนี้ scroll position / panel ที่เปิดค้างรีเซ็ต · P5 ให้ clone `ctx.memory()` ก่อนกู้แล้วคืน |
| จุดบอด fast hash ตรงกลางไฟล์ | ปิดด้วย mtime แล้ว แต่จุดบอดตัว hash เองยังอยู่ (มีเทสต์บันทึกไว้ว่าตั้งใจ) |
| ~~เทสต์ GPU ถูกข้ามบน CI~~ | ✅ **ปิดแล้ว 1 ส.ค. 2026** — ทั้งสอง runner รันจริง (WARP / lavapipe) และ `REFX_REQUIRE_GPU=1` ทำให้ "ไม่มี adapter" = แดง ดู §1 |
| ชั้น UI ของการกู้ device ยังไม่มี unit test | `recover_device()` ต้องมีหน้าต่างจริงจึงเรียกในเทสต์ไม่ได้ · ตอนนี้คุมด้วย (ก) `DeviceBound` ที่บังคับตอนคอมไพล์ (ข) `debug_assert` เทียบ generation ของ `WorkingCache` ทุกเฟรม (ค) รันจริงด้วย `--force-device-lost-after-ms` |
| **ผู้ถือลิขสิทธิ์ใน LICENSE เป็นชื่อผลิตภัณฑ์ ไม่ใช่บุคคล/นิติบุคคล** | `Copyright (c) 2026 RefX` · ตามกฎหมายผู้ถือลิขสิทธิ์ควรเป็นบุคคลหรือนิติบุคคล · ตอนนี้ repo เป็น private และไม่มี contributor คนอื่น จึงยังไม่มีผล → **ทบทวนก่อนเปิด public หรือก่อนรับ contributor คนแรก** |
| ~~`refx-asset` / `refx-io` / `refx-ui` / `refx-app` ยังไม่เคยคอมไพล์บน Linux~~ | ✅ **ปิดแล้ว 1 ส.ค. 2026** — job `check (ubuntu-latest)` build ครบทุก crate + รันเทสต์ + วัดขนาด binary (17.01 MB) |
| **★ enum ที่ลงไฟล์ต้อง round-trip ได้ ไม่ใช่แค่ทนได้** (ยกระดับกฎ 3 ส.ค. 2026) | `ColorLabel` เป็นตัวอย่างที่ชัดที่สุด: ผู้ใช้ติดป้ายสีด้วยรุ่นใหม่ → เปิดด้วยรุ่นเก่า → อ่านเป็น `None` → ขยับภาพใบเดียวแล้วบันทึก → **ป้ายสีหายถาวรโดยไม่มีอะไรเตือน** กลไกที่สร้างมากัน I-3 กลายเป็นตัวทำให้ข้อมูลหายเสียเอง · ทางแก้: ค่าเดี่ยว ๆ (`ColorLabel`/`Flip`/`ImageFormat`) เก็บ**ค่าดิบ**ใน DTO แล้วเขียนกลับตามเดิม UI แสดงว่า "ไม่รู้จัก" ได้แต่ห้ามแปลงค่าทิ้ง · variant ใหม่ของ `ItemKind` round-trip ไม่ได้จริง ต้องกันที่ระดับไฟล์: **major version สูงกว่าที่เข้าใจ → เปิดอ่านอย่างเดียว** แล้วบอกให้ผู้ใช้อัปเดตก่อน (ยอมให้แก้ไม่ได้ชั่วคราว ดีกว่าปล่อยให้บันทึกทับแล้วงานหาย) · **งานจริงตอน P4-1 (dto)** |
| **คอลัมน์ `format` ใน `cache.sqlite` ไม่เคยมีความหมาย** | `pool.rs` เขียน `0` ตายตัวมาตลอด และ `AssetRef.format` ตอนนี้เป็น `ImageFormat::Unknown` เพราะ cache hit ไม่ได้แตะไบต์ของไฟล์ · งานที่ต้องทำ: ร้อย format จริงจาก `image::guess_format` ผ่าน `decode` → `Thumbnail` → `ThumbEntry` **แล้วตัดสินพร้อมกันว่าจะให้ความหมายคอลัมน์นี้หรือลบทิ้ง** (แตะ schema ของ cache = ต้องถามก่อนตาม CLAUDE.md) |
| ~~**`render_state` ไม่เคยถูกล้าง**~~ | ✅ **ปิดแล้ว 4 ส.ค. 2026 (P2-6)** — `History` เป็นเจ้าของอายุของ thumbnail ดู §2.5 |
| **ชื่อขั้น undo ยังไม่ถูกแปล** | `Command::label()` คืน `&'static str` อังกฤษ (`"Add items"` ฯลฯ) แต่ `refx-ui::text` ยังไม่มีตารางแปลให้ · ต้องทำตอนที่ UI มีเมนู/ปุ่ม undo จริง (P2-4 ขึ้นไป) — อย่าเอา `label()` ไปแสดงตรง ๆ กับผู้ใช้ |
| **การเลือกเข้า undo stack — ต้องรีวิว UX** | `SelectItems` เป็น `Command` เพราะ `selection` อยู่ใน `Board` และ docs/08 §4 ข้อ 10 บังคับ · ยุบเป็นขั้นเดียวด้วย `merge` แล้ว แต่ผลคือ **กด Ctrl+Z แล้วการเลือกกลับมาด้วย** ซึ่งโปรแกรมส่วนใหญ่ไม่ทำ · ทางเลือกอื่นคือเจาะรูให้แก้ `Board` ได้โดยไม่ผ่าน `Command` ซึ่งทำให้กฎที่คอมไพเลอร์บังคับอยู่กลายเป็นกฎที่ต้องจำเอง — **ยังไม่ตัดสิน รอเห็นของจริงบนจอก่อน** |
| ข้อความ **CLI** กับ **crash dialog** ยังเป็นภาษาไทย | `tracing::` กวาดเป็นอังกฤษครบแล้ว (84 จุด) แต่ `--help` / error ของ CLI ใน `main.rs` และ `dialog::show_crash_dialog` ยังเป็นไทยล้วน · เป็นผิวที่ผู้ใช้ต่างชาติเจอได้เหมือนกัน แต่เป็นคนละระบบกับ log จึงแยกทำทีหลัง (crash dialog ควรไปอยู่ใต้ `refx-ui::text` ตอนที่ P5-3 ทำระบบภาษาเต็มรูปแบบ) |

---

## 7. วิธีทำงานที่คาดหวัง

- ทำทีละ task ให้จบและเทสต์ผ่านก่อนขึ้นตัวถัดไป **ห้ามเขียนหลายโมดูลค้างพร้อมกัน**
- เขียนเทสต์ไปพร้อมโค้ด โดยเฉพาะ `refx-core` ที่ต้องเทสต์ได้โดยไม่มี GPU
- **เทสต์ต้องพิสูจน์ว่ากลไกทำงานจริงตอนนั้น ไม่ใช่แค่ว่ามีโค้ดอยู่**
  (ตัวอย่างที่ดีในโปรเจกต์: `panic_hook_sees_label_while_unwinding`,
  `bulk_cancellation_actually_skips_work`, bomb test ที่บังคับว่าต้องได้ `ImageTooLarge` พร้อม w/h เป๊ะ)
- **เทสต์ล้มเพราะสมมติฐานผิด → แก้สมมติฐาน ไม่ใช่แก้ตัวเลขให้ผ่าน**
- เจอสิ่งที่ spec ไม่ระบุ → เลือกทางที่ปลอดภัยกว่า แล้วบันทึกว่าตัดสินใจอะไรและทำไม
- **เจอ spec ที่ขัดกันเองหรือผิด → หยุด ถาม ห้ามแก้ไฟล์ใน `docs/` เอง** (รายงานให้ผู้ใช้แก้)
- ห้ามเพิ่ม dependency ที่ไม่มีใน `docs/09` · ห้าม refactor นอกขอบเขต task

### ★ commit ทุกครั้งที่จบ task — ไม่ใช่รอจบ session

**จบ task ไหนแล้วเทสต์ผ่าน ให้ commit ทันที** อย่าสะสมงานหลาย task ไว้ commit ทีเดียวตอนจบ

เหตุผล:

- commit ก้อนใหญ่ที่มีหลาย task ปนกัน **รีวิวไม่ได้จริง** — แยกไม่ออกว่าการเปลี่ยนแปลงไหน
  เป็นของงานไหน และถ้าต้องย้อนก็ย้อนเฉพาะส่วนที่ผิดไม่ได้
- session อาจจบกลางคัน (context เต็ม, เครื่องดับ) งานที่ยังไม่ commit **หายทั้งหมด**
  ซึ่งเป็นความเสี่ยงแบบเดียวกับที่ I-3 ห้ามไว้กับงานของผู้ใช้
- ตัวเลข benchmark ที่วัดได้จะผูกกับ commit ที่เจาะจงได้ ทำให้ย้อนหาสาเหตุ regression ได้

หนึ่ง commit = หนึ่ง task ที่จบและเทสต์ผ่านแล้ว
ข้อความ commit บอก **"ทำไม"** ไม่ใช่แค่ "ทำอะไร" (diff บอกอยู่แล้วว่าทำอะไร)

**ก่อน commit ทุกครั้ง**

```bash
cargo fmt
cargo clippy --all-targets --all-features -- -D warnings
cargo nextest run --all-features
cargo deny check
cargo tree -d | grep '^wgpu'     # ต้องว่าง
cargo tree | grep -iE 'reqwest|hyper|tokio|curl|ureq|openssl'   # ต้องว่าง (I-8)
git ls-files --eol | grep -c 'w/crlf'                          # ต้องเป็น 0
```

**จบทุก session ด้วยการรายงาน:** อะไรเสร็จ/ไม่เสร็จ · ผล 4 คำสั่ง · ตัวเลขที่วัดได้จริง ·
สิ่งที่ spec ไม่ระบุแล้วตัดสินใจเอง · **อะไรที่ spec เขียนไว้แล้วทำจริงไม่ได้** ← ข้อนี้มีค่าที่สุด
