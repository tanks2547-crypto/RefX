# RefX 1.0 — รุ่นทดลองก่อนปล่อยจริง (rc.1)

โปรแกรมจัดการภาพ reference สำหรับนักวาด · เปิดค้างไว้ทั้งวันข้าง ๆ Photoshop
หรือ Clip Studio ได้โดยไม่แย่งเครื่อง

**ไม่ต่อเน็ตเลย** — ไม่มี auto-update ไม่มี telemetry ไม่มีบัญชีให้สมัคร
ภาพของคุณไม่ออกจากเครื่อง

---

## ทำอะไรได้

* **ลากภาพจาก Explorer เข้ามาได้เลย** — ขึ้นทันที ไม่ต้องรอ (วัดได้ 6 ms ต่อ 3 ใบ)
* **สองโหมด** — *Canvas* วางอิสระเหมือนกระดานจริง · *Arrange* จัดเป็นตาราง
* **เครื่องมือที่ใช้ทุกวัน** — grayscale · กลับด้าน · ปรับความทึบ · ความสว่าง/คอนทราสต์ · หมุน
* **บันทึกเป็นไฟล์ `.refx`** สองแบบ
  * *ลิงก์ไปที่ไฟล์ภาพ* — ไฟล์เล็กมาก แต่ถ้าย้ายภาพต้นฉบับต้องชี้ที่ใหม่
  * *เก็บภาพไว้ข้างใน* — ไฟล์เดียวจบ พกไปเครื่องอื่นได้
* **ส่งออกเป็นภาพเดียว** (PNG หรือ JPEG) เอาไปแปะในไฟล์งานได้
* **undo/redo ทุกการกระทำ**
* **งานที่ยังไม่ได้เซฟไม่หาย** — ถ้าโปรแกรมดับไป เปิดใหม่แล้วมันจะถามว่าจะเอางานกลับมาไหม

---

## ติดตั้ง

| คุณใช้ | โหลดตัวไหน | ทำยังไง |
|---|---|---|
| Windows แบบไม่อยากติดตั้ง | `refx-1.0.0-windows-x86_64.zip` | แตกไฟล์ที่ไหนก็ได้ แล้วดับเบิลคลิก `refx.exe` |
| Windows แบบติดตั้งปกติ | `refx-1.0.0-windows-x86_64.msi` | ดับเบิลคลิก แล้วกดถัดไป |
| Linux (Ubuntu/Debian) | `refx_1.0.0-1_amd64.deb` | `sudo apt install ./refx_1.0.0-1_amd64.deb` |

> ### ⚠️ Windows จะเตือนตอนเปิดครั้งแรก — **เป็นเรื่องปกติ ไม่ใช่ไวรัส**
>
> จะขึ้นหน้าจอน้ำเงิน *"Windows protected your PC"* เพราะโปรแกรมนี้
> **ยังไม่ได้ซื้อใบรับรองสำหรับเซ็นชื่อ** (ปีละหลักหมื่น) ไม่ใช่เพราะมีอะไรผิดปกติ
>
> กด **More info** → **Run anyway**
>
> ถ้าไม่สบายใจ ให้เอาไฟล์ไปสแกนที่ virustotal.com ก่อนได้

★ **ไม่ต้องลงอะไรเพิ่มทั้งสิ้น** — ไม่ต้องมี Visual C++ Runtime, ไม่ต้องมี .NET

---

## ข้อจำกัดที่รู้อยู่ (รุ่นนี้)

* **ตัวเลขใน Inspector ยังแก้ไม่ได้** — ดูได้อย่างเดียว ต้องลากด้วยเมาส์
* **ภาพที่ส่งออกคมเท่าที่เห็นบนจอ** ไม่ใช่ความละเอียดเต็มของไฟล์ต้นฉบับ
  (แถบส่งออกบอกเพดานให้เห็นทุกครั้ง)
* **ยังไม่รองรับภาษาจีน/ญี่ปุ่น/เกาหลี** ในชื่อไฟล์บนหน้าจอ — ไทยกับอังกฤษใช้ได้
* **แผง Library ยังว่าง** ยังไม่ขึ้นชื่อโฟลเดอร์
* **ยังไม่มีตัวติดตั้งสำหรับ macOS**

รายละเอียดครบอยู่ใน `KNOWN-LIMITATIONS.md` ที่มากับโปรแกรม

---

## rc แปลว่าอะไร

**rc = release candidate** — ตัวที่เราคิดว่าพร้อมปล่อยจริงแล้ว แต่อยากให้คนจริง
ลองก่อนสักรอบ · ถ้าไม่มีอะไรโผล่มา ตัวนี้จะกลายเป็น 1.0 โดยไม่แก้อะไรเลย

**เจออะไรแปลก ๆ ช่วยส่งไฟล์นี้มาด้วย:**

```
%LOCALAPPDATA%\RefX\cache\logs\refx.log
```

(Linux: `~/.local/share/RefX/cache/logs/refx.log`)

ในนั้นไม่มีอะไรที่เป็นความลับของคุณ — มีแต่ชื่อไฟล์ ขนาดภาพ และเวลาที่ใช้
