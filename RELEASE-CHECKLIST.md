# รายการตรวจก่อนปล่อยรุ่น / Release checklist

> ★★★ **กฎของไฟล์นี้: ทุกแถวต้องบอกว่า *ใครตรวจ* ไม่ใช่แค่ *ตรวจอะไร***
>
> แถวที่เขียนว่า "ตรวจแล้ว" โดยไม่บอกว่าเครื่องมือไหนตัดสิน คือแถวที่จะถูกติ๊ก
> ด้วยความทรงจำในรอบถัดไป · แถวที่เป็นของมนุษย์ต้องน้อยที่สุดเท่าที่จะทำได้
> เพราะรายการที่ยาวเกินคือรายการที่ไม่มีใครทำจนจบ และข้อที่ถูกข้ามคือข้อที่น่าเบื่อ
> ซึ่งเป็นที่ที่บั๊กเงียบอาศัยอยู่

---

## 1. สิ่งที่เครื่องตัดสิน — ห้ามติ๊กด้วยมือ

| ตรวจอะไร | ใครตัดสิน | แดงเมื่อ |
|---|---|---|
| เทสต์ทั้งชุด (ทุก feature) | `cargo nextest run --workspace --all-features` ใน `ci.yml` | เทสต์ตก · เทสต์ค้างเกิน 4 นาที |
| clippy ไม่มี warning | `cargo clippy --all-targets --all-features -- -D warnings` | มี warning ใด ๆ |
| ฟอร์แมต | `cargo fmt --all --check` | ไฟล์ไหนยังไม่ฟอร์แมต |
| ใบอนุญาต dependency | `cargo deny check` | ใบอนุญาตที่ไม่อนุญาต · advisory |
| `THIRD-PARTY-LICENSES.md` ตรงกับ `Cargo.lock` | `cargo xtask licenses --check` | dependency ขยับแล้วไฟล์ไม่ขยับ |
| ไอคอนทุกรูปแบบตรงกับ `.svg` | `cargo xtask icon --check` | ใครแก้ `.svg` แล้วลืมสร้างใหม่ |
| ไม่มี network dependency (I-8) | job `no-network` | มี `reqwest`/`hyper`/`tokio`/… เข้าทรี |
| wgpu / png เวอร์ชันเดียว | job `single-wgpu` | มีสองเวอร์ชันในทรี |
| ขนาด binary ไม่เกิน 25 MB | `scripts/check-binary-size.sh` + NC | เกินเพดาน `docs/08 §6` |
| benchmark ไม่เกินเพดาน | `scripts/check-bench.sh` + NC | ช้าลงระดับหายนะ |
| GPU ถูกตรวจจริง ไม่ได้ข้าม | `REFX_REQUIRE_GPU=1` | ไม่มี adapter → แดง ไม่ใช่ข้ามเงียบ |
| **แพ็กเกจมีไฟล์ใบอนุญาตครบ** | `xtask package` · `verify-deb` · `verify-msi` + NC | ไฟล์หายจาก **ตัวแพ็กเกจ** |
| **ไบนารีของ Windows พึ่งตัวเองได้** | `xtask package` ประตู 3 | ไบนารี import `VCRUNTIME140.dll` = เครื่องที่เพิ่งลง Windows ใหม่จะ **ติดตั้งสำเร็จแล้วเปิดไม่ขึ้น** |
| **build จากศูนย์ผ่าน ทั้งสอง OS** | `cold-build.yml` (รายสัปดาห์) + `cold-build-status` บนทุก push | cache ปิดบังเส้นทางที่ไม่เคยถูกเดินใหม่ · หรือรอบรายสัปดาห์หายไปเกิน 10 วัน |
| **เวอร์ชันตรงกันสามที่** | ประตูเดียวกัน | `Cargo.toml` ≠ `--version` ≠ ชื่อแพ็กเกจ |
| **ถอน MSI แล้วงานผู้ใช้ต้องอยู่** (I-3) | `xtask verify-msi` + NC | ตัวถอนลบไฟล์ใน `%APPDATA%`/`%LOCALAPPDATA%` |
| **binary ที่แจกเป็น PIE + BIND_NOW** | `xtask verify-deb` (`readelf`) | ธง hardening หายไปจากของจริง |

★ ทุกแถวที่มี "NC" มี negative control ที่พิสูจน์ว่าประตู **แดงเป็น** ไม่ใช่แค่เขียว

---

## 2. สิ่งที่มนุษย์ต้องทำ — หนึ่งรอบเต็มบนของที่จะแจกจริง

> ★★★ **ต้องรันจาก artifact ที่ CI สร้าง ไม่ใช่ `target/release` ของเครื่องตัวเอง**
> เทสต์ 1,078 ตัวไม่มีตัวไหนเปิดหน้าต่างแล้วใช้งานติดกันทั้งรอบเลยสักตัว —
> คลาสบั๊ก "ทุกชิ้นถูก แต่ประกอบแล้วผิด" มีแต่การรันจริงที่เห็น (`docs/08 §3.9` ข้อ 5)

1. โหลด artifact จากรอบ CI ที่จะปล่อย แล้ว**แตก/ติดตั้งจากตัวนั้น**
2. เปิดโฟลเดอร์ภาพจริง → ภาพต้องขึ้นครบและเร็ว
3. เลือกภาพ → Inspector ต้องมีค่าให้ปรับ
4. ลากย้ายภาพ → ตำแหน่งต้องเปลี่ยนตามมือ
5. `Ctrl+S` → เลือกโหมดเก็บภาพ → ตั้งชื่อไฟล์ → ต้องได้ไฟล์จริง
6. **ปิดโปรแกรม แล้วเปิดไฟล์นั้นใหม่** → ทุกตำแหน่งต้องกลับมาตรงเป๊ะ (I-3)
7. `Ctrl+E` → export → ต้องได้ PNG ที่มีภาพครบทุกใบ
8. ปล่อยไว้เฉย ๆ อย่างน้อย **120 วินาที โดยไม่แตะหน้าต่างเลย** → ต้องผ่าน
   **ทั้งสี่ข้อของ I-1** (`ARCHITECTURE §1`, ถ้อยคำใหม่ 22 ก.ย. 2026)

   | # | เกณฑ์ | วัดยังไง |
   |---|---|---|
   | 1 | `ControlFlow::Wait` เท่านั้น | คอมไพเลอร์ + review (ไม่ใช่ขั้นตอนของรอบนี้) |
   | 2 | **ตัวนับเฟรมไม่ขยับ** ขณะไม่มี input | อ่าน `Frames` สองครั้งห่างกัน ≥ 120 วิ |
   | 3 | **`worst_quiet` < 32** | ปิดโปรแกรมแล้วอ่านบรรทัด `shutting down` |
   | 4 | CPU ตอนนิ่ง **≤ 1.0 %** ของหนึ่งคอร์ | `TotalProcessorTime` สองครั้งห่างกัน ≥ 120 วิ |

   > ★★★ **เกณฑ์เดิมเขียนว่า "CPU ต้องเป็น 0%" ซึ่งไม่มีใครผ่านได้เลยทั้งโปรเจกต์**
   > — ค่าจริงคือ 0.11–0.28 % มาตลอด และเกณฑ์ที่ไม่มีใครผ่านได้คือเกณฑ์ที่ทุกคน
   > เรียนรู้ที่จะเพิกเฉย (`docs/08 §3.9` ข้อ 11) · ข้อ 4 ตั้งจากค่าที่วัดได้บวก
   > เผื่อสามเท่า
   >
   > ★★ **ข้อ 2 อ่านจากภาพหน้าจอได้ แต่การถ่ายภาพเองรบกวนการวัด** — `attach`
   > ของ `ui-drive.ps1` เรียก `MoveWindow`/`ShowWindow` ซึ่งสร้างเฟรมเอง ส่วน
   > `shot` เปล่า ๆ จะถูกปฏิเสธถ้าโฟกัสหลุด · **ข้อ 3 คือข้อที่ตอบได้โดยไม่ต้อง
   > แตะอะไรเลย** และเป็นข้อที่ควรเชื่อเมื่อสองข้อไม่ตรงกัน

### ผลรอบล่าสุด — 18 ก.ย. 2026 · **artifact ของ `391058c`** (แท็ก `v1.0.0-rc.1`)

รันจากซิปที่ CI สร้าง (`refx-1.0.0-windows-x86_64.zip` · แตกแล้วรันจากตัวนั้น)
ไม่ใช่ `target/release` ของเครื่อง

| ขั้น | ผล |
|---|---|
| 1 แตกจาก artifact จริง | ✅ ซิปอันเดียว 7,755,620 ไบต์ · เวอร์ชันสามที่ตรงกันที่ **1.0.0** (`Cargo.toml` · ชื่อไฟล์ · `refx --version` = `refx 1.0.0`) · ไม่ import `VCRUNTIME140`/`MSVCP140`/`VCRUNTIME140_1` เลย |
| 2 เปิดโฟลเดอร์ภาพจริง | ✅ **`Opened 20 files in 32 ms`** · `20 items` |
| 3 เลือก → Inspector มีค่า | ✅ **X 2064.0 · Y 2048.0 · W 128.0 · H 96.0 · Rotation 0.0° · img0000.jpg · 2400 × 1800 px · 1.5 MB** + path เต็ม + Opacity/Grayscale/Invert/Brightness/Contrast/Flip |
| 4 ลากย้าย | ✅ **X 2064.0 → 2320.0 · Y 2048.0 → 2552.0** — Δ +256/+504 = เมาส์ +64/+126 px ที่ zoom 25% คูณสี่พอดี |
| 5 `Ctrl+S` → Linked → ตั้งชื่อ | ✅ `round.refx` **1,122 ไบต์** · ★ **ไม่มี `.refx.tmp` ค้าง** (จุดที่ `save.rs` เปลี่ยน 137 บรรทัด) |
| 6 ปิด → เปิดใหม่ | ✅ **X 2320.0 · Y 2552.0 ตรงเป๊ะ** · `Opened 20 files in 8 ms` · แท็บ `round.refx` ไม่มีดอกจัน |
| 7 `Ctrl+E` export | ✅ **2048 × 486 px · 298,021 ไบต์** ตรงกับที่แถบประเมินไว้ · นับในภาพได้ **20 ใบครบ** (15 + 4 + ใบที่ย้าย 1) |
| 8 ปล่อยนิ่ง | ✅ ดูกล่องข้างล่าง |

**เพิ่มจากรอบนี้ — P4-4 ทำงานบนไบนารีที่จะแจกจริง:** เปิดมาแล้วขึ้นแถบ
*"Unsaved work from last time · 33 items from 4 h ago · [Bring it back] [Keep it,
decide later] [Throw it away]"* โดยไม่ต้องสั่งอะไร — งานที่ไม่เคยเซฟจากโปรเซส
ที่ถูกฆ่าไม่หายไปไหน

> ### ★★ ขั้น 8 — ตัวเลขจริง และถ้อยคำของเกณฑ์ที่เข้มกว่าที่โปรเจกต์ยอมรับเอง
>
> | วัด | ผล |
> |---|---|
> | CPU ปล่อยนิ่ง 330 วิ | **406 ms = 0.123 %** ของหนึ่งคอร์ |
> | CPU ปล่อยนิ่ง 180 วิ (รอบยืนยัน) | **297 ms = 0.165 %** |
> | **`worst_quiet` ตลอดอายุโปรเซส** | **6** จากเพดาน **32** |
> | `redraws` ตอนปิด | `UserInput=674 · TextureReady=28 · EguiRepaint=681` |
> | I-1 warning ระหว่างรอบนี้ | **ไม่มีเลย** |
>
> `Frames` ขยับ **485 → 592** ใน 330 วินาที (≈ 1 เฟรมทุก 3 วินาที) ซึ่งตาม
> ตัวอักษรคือ "ไม่หยุดนิ่ง" · แต่ `worst_quiet=6` พิสูจน์ว่า**ไม่ใช่การวาดวน** —
> ถ้าวาดติดกันโดยไม่มี input ตัวจับต้องร้องที่ 32 และมันไม่เคยร้อง
> → เฟรมพวกนั้นถูกคั่นด้วย event จริงตลอด
>
> ★ สมมติฐาน (**ยังไม่ได้พิสูจน์**): รอบก่อน (`b4c35a5`, 120 วิ) เฟรมไม่ขยับเลย
> ต่างกันตรงรอบนี้มี **แถบกู้งานค้างอยู่บนจอตลอดเวลา** ซึ่งรอบก่อนไม่มี
>
> ★★ **ถ้อยคำของเกณฑ์ ("CPU ต้องเป็น 0%") เข้มกว่าที่โปรเจกต์ยอมรับเองมาตลอด** —
> รอบ `b4c35a5` บันทึก 0.11–0.28 % ว่าผ่าน · ตัวเลขรอบนี้อยู่ในช่วงเดียวกัน
> → เป็นเรื่องของถ้อยคำ ไม่ใช่การถอยหลังของโปรแกรม · **เจ้าของโปรเจกต์เป็นคน
> ตัดสินถ้อยคำ I-1 เอง ไม่ใช่ผมไล่บีบให้เป็นศูนย์**

### ผลรอบก่อน — 17 ก.ย. 2026 · build ของ `b4c35a5`

| ขั้น | ผล |
|---|---|
| 1–2 เปิดโฟลเดอร์ภาพจริง | ✅ 20 ภาพ |
| 3 เลือก → Inspector มีค่า | ✅ **X 2704.0 · Y 2872.0 · W 128.0 · H 96.0 · Rotation 0.0° · img0003.jpg · 2400 × 1800 px · 1.6 MB · path เต็ม** |
| 4 ลากย้าย | ✅ ตำแหน่งเปลี่ยนตามมือ |
| 5 `Ctrl+S` → เลือกโหมดเก็บภาพ → ตั้งชื่อ | ✅ 1,119 ไบต์ |
| 6 ปิด → เปิดใหม่ | ✅ ตำแหน่งตรงเป๊ะ · `Opened 20 files in 8 ms` |
| 7 `Ctrl+E` export | ✅ PNG 2048×745 · 301 KB |
| 8 ปล่อยนิ่ง | ✅ เฟรมไม่ขยับใน 120 วิ · CPU 0.11–0.28 % ของหนึ่งคอร์ |

**เพิ่มจากรอบนี้ — GPU หายกลางทาง (ตัวแทนของ sleep/resume):**

```
device-lost simulation armed for the idle case ms=6000
graphics ready ... generation=0
simulating device lost on the timer (app was idle)   ← หายจริง
graphics ready ... generation=1                      ← กลับมาใน 118 ms
```

ภาพครบ 20 ใบ · VRAM 16 MB เท่าเดิม · โปรแกรมไม่ปิดตัวเอง

> ★ รอบแรกที่รันข้อนี้ **ไม่ได้พิสูจน์อะไรเลย**: `ui-drive.ps1` รับอาร์กิวเมนต์
> แค่ฟิลด์เดียว (`$parts[2]`) และ **ทิ้งฟิลด์ที่เกินมาเงียบ ๆ** → แอปรันโดยไม่มี
> ธงจำลอง แล้วผ่านเพราะไม่มีอะไรเกิดขึ้น · จับได้เพราะไปดู log แล้วเห็น
> `generation=0` ครั้งเดียว — **ถ้าดูแค่ภาพหน้าจอกับ "แอปยังเปิดอยู่" จะรายงานผ่าน**

**บั๊กที่รอบก่อนจับได้:** artifact ที่ CI อัปโหลดมีซิป **สองอัน** — ของจริงกับของที่
negative control สร้าง · แก้แล้วโดยให้ NC เขียนลงคนละโฟลเดอร์

---

## 3. ★★ สองข้อที่ต้องให้ **คน** ทำ — ขั้นตอนอยู่ตรงนี้ ทำตามได้เลย

> ใช้เวลารวมไม่เกินสิบนาที · ไม่ต้องรู้เรื่องโค้ดเลย
> **เขียนผลลงตารางท้ายหัวข้อนี้ทุกครั้งที่ทำ** แม้จะผ่านหมด

### 3.1 ลากรูปจาก File Explorer เข้าโปรแกรม

1. เปิด RefX ไว้ ให้เห็นพื้นที่ว่างสีเทาตรงกลาง
2. เปิด File Explorer หาโฟลเดอร์ที่มีรูปสัก 3 ใบ วางหน้าต่างให้เห็นทั้งสองอัน
3. **เลือกรูปทั้ง 3 ใบ** แล้วลากมาปล่อยกลางพื้นที่สีเทา

**ผ่าน = ทั้งสามข้อนี้จริง**

- รูปขึ้นครบทั้ง 3 ใบ ภายในไม่กี่วินาที
- ตัวเลข `items` ที่แถบล่างเพิ่มขึ้น 3
- ลากซ้ำอีกรอบด้วยรูปเดิม แล้ว**ไม่มีอะไรพัง** (ได้รูปซ้ำถือว่าปกติ)

**ไม่ผ่าน** = ไม่มีอะไรเกิดขึ้นเลย · ขึ้นแค่บางใบ · หรือโปรแกรมค้าง/ปิดตัวเอง

> ★ ทำไมต้องเป็นคน: การลากปล่อยของ Windows ต้องมีเมาส์จริง สคริปต์สั่งไม่ได้
> · เคยลองแล้วได้ภาพหน้าจอของหน้าต่างอื่นมาเป็นหลักฐาน ซึ่งแย่กว่าไม่ทดสอบเลย

### 3.2 ปิดจอ/พักเครื่อง แล้วกลับมา

1. เปิด RefX พร้อมรูปสัก 10 ใบ **แล้วปล่อยทิ้งไว้ อย่าเซฟ**
2. สั่งเครื่อง **Sleep** (เมนู Start → ปุ่ม Power → Sleep) รอให้จอดับจริง
3. รอสัก 1 นาที แล้วปลุกเครื่อง เข้าระบบตามปกติ
4. กลับมาดูหน้าต่าง RefX

**ผ่าน = ทั้งสามข้อนี้จริง**

- ภาพยังอยู่ครบ **ไม่กลายเป็นจอดำหรือช่องว่าง**
- ลากภาพไปมาได้ทันที ไม่ต้องคลิกปลุกอะไรก่อน
- แถบล่าง `Frames` ยังเดินเมื่อขยับภาพ (ไม่ค้างอยู่ที่เลขเดิม)

**ไม่ผ่าน** = ภาพหาย · จอดำ · โปรแกรมไม่ตอบสนอง · หรือปิดตัวเอง

> ★ ถ้าเครื่องมีทั้งการ์ดจอในตัวและการ์ดแยก ให้ลองซ้ำอีกรอบตอนเสียบ/ถอดสายไฟ
> ด้วย — บางเครื่องสลับการ์ดจอตอนนั้น ซึ่งเป็นเหตุการณ์เดียวกันกับที่ข้อนี้จับ

> **ถ้าไม่ผ่าน** ให้แนบไฟล์ log มาด้วย: `%LOCALAPPDATA%\RefX\cache\logs\refx.log`

### ผลที่บันทึกไว้

| วันที่ | 3.1 ลากจาก Explorer | 3.2 sleep/resume | โดยใคร |
|---|---|---|---|
| 18 ก.ย. 2026 | ✅ **ผ่านครบสามข้อ** | ✅ **ผ่านครบสามข้อ** (มีข้อแม้ — อ่านข้างล่าง) | เจ้าของโปรเจกต์ |

#### 3.1 — สิ่งที่ยืนยันได้จาก log จริง (`refx.log`, shipping build `74fc382`)

| เงื่อนไขผ่าน | ผล | หลักฐาน |
|---|---|---|
| รูปขึ้นครบ 3 ใบ ในไม่กี่วินาที | ✅ | `drag & drop → every image on screen files=3 ms=6.458` — **6.5 ms** ไม่ใช่ "ไม่กี่วินาที" |
| `items` เพิ่มขึ้น 3 | ✅ | `undo_depth=3` (สาม command) · เจ้าของยืนยันตัวเลขบนแถบล่างด้วยตา |
| ลากซ้ำด้วยรูปเดิม แล้วไม่มีอะไรพัง | ✅ | **สามครั้งติดโดยไม่ลบอะไรเลย** `05:54:17 depth=14` → `05:54:18 depth=17` → `05:54:20 depth=20` · กระดาน 3 → 6 → 9 ใบ · ไม่มี error ไม่มีการปิดตัว |

★ ครั้งแรกที่รายงานว่า "ลากซ้ำแล้ว" ยัง**ไม่นับ** เพราะ log แสดงว่าลบสามใบแรก
ทิ้งก่อน (`undo_depth` 3→9 = เพิ่ม·ลบ·เพิ่ม) → กระดานว่าง เส้นทาง "ของซ้ำ"
ไม่เคยถูกเดินผ่าน · **รอบนี้ทั้งรอบเป็นเรื่องของการไม่ติ๊กว่าตรวจแล้วในสิ่งที่ยังไม่ได้ตรวจ**

> ★★ เวลา decode ครั้งแรกของชุด (11.6 ms) สูงกว่าครั้งถัดไป (5.7 · 5.6 ms) ราวเท่าตัว
> — ตรงกับที่ควรเป็นเมื่อ cache ของภาพเดิมร้อนแล้ว

#### 3.2 — sleep/resume ของจริง (เครื่องเจ้าของ · RTX 4060 · 9 ภาพ ยังไม่เซฟ)

| เงื่อนไขผ่าน | ผล | หลักฐาน |
|---|---|---|
| ภาพยังอยู่ครบ ไม่เป็นจอดำ | ✅ | เจ้าของยืนยันด้วยตา · 9 ใบครบ |
| ลากได้ทันที ไม่ต้องปลุกก่อน | ✅ | เจ้าของยืนยันด้วยตา |
| `Frames` เดินเมื่อขยับ ไม่ค้าง | ✅ | วัดยืนยันต่อ: idle 10 วิ = **15.6 ms CPU (0.156 % ของหนึ่งคอร์)** · `Responding=True` · ตัวจับ I-1 **ไม่เคยแตะเพดาน 32** → ไม่ใช่การวาดต่อเนื่องตอน idle |

★ ข้อสามรายงานมาว่า "frame เพิ่มขึ้นเรื่อย ๆ" ซึ่ง**กำกวม**: ถ้าเดินตอนไม่แตะอะไร
นั่นคือ I-1 พัง · จึงวัด CPU ต่อแทนที่จะตีความเอง — 0.156 % คือค่าพื้นเดิม ไม่ใช่การถอยหลัง

> ### ⚠️ ข้อแม้ที่ต้องเขียนไว้ — **การหลับรอบนี้ไม่ได้ทดสอบเส้นทางกู้ device**
>
> | ตรวจ | ก่อน sleep | หลังตื่น |
> |---|---|---|
> | pid | 13648 | **13648** — โปรเซสเดิม |
> | `generation` | 0 | **0** — device ไม่เคยหาย |
> | บรรทัด log | 14,734 | **14,734** — เงียบสนิทตลอดการหลับ |
>
> ไดรเวอร์ NVIDIA บนเครื่องนี้พา device รอดข้ามการหลับมาได้ · **สิ่งที่ข้อนี้พิสูจน์
> คือ "โปรแกรมรอดการหลับ" ไม่ใช่ "กู้ device ได้"** · เส้นทางกู้ถูกตรวจแยกด้วย
> `--force-device-lost-after-ms` (generation 0→1 · 118 ms · ภาพครบ) ซึ่งเป็นคนละข้อกัน
> → เครื่องที่ไดรเวอร์ทำ device หายตอนหลับจริง (iGPU บางรุ่น · หลัง driver update)
> **ยังไม่เคยมีใครลอง** — อยู่ใน `KNOWN-LIMITATIONS`

---

## 3ก. สิ่งที่เครื่องนี้ทำแทนไม่ได้ — เขียนไว้ให้เห็น ไม่ใช่ซ่อน

| เรื่อง | ทำไมยังตรวจไม่ได้ | ใกล้เคียงที่สุดที่ทำแล้ว |
|---|---|---|
| ติดตั้ง MSI **บนเครื่องที่ไม่ใช่ของนักพัฒนา** | runner ของ CI สะอาดก็จริง แต่ไม่ใช่เครื่องผู้ใช้ | `verify-msi` ติดตั้ง→ถอน→ยืนยันงานผู้ใช้รอด · และ **ประตูใหม่: ไบนารีต้องไม่พึ่ง VC++ redistributable** ซึ่งเป็นสาเหตุอันดับหนึ่งของ "ติดตั้งแล้วเปิดไม่ขึ้น" |
| **ดิสก์เต็มจริง** | สร้าง/ผูก VHDX ต้องใช้สิทธิ์ผู้ดูแลระบบ ซึ่ง agent ไม่มี | ยิง `ErrorKind::StorageFull` เข้าเส้นทางเซฟจริงทั้งสามเส้น — **เจอบั๊กจริงจากข้อนี้** (ไฟล์ `.tmp` ค้าง) แก้แล้ว |
| ไดรเวอร์จอเปลี่ยน · สลับ iGPU↔dGPU | ต้องมีเหตุการณ์ของ OS จริง | `--force-device-lost-after-ms` จำลอง "device ตายตอนแอปหลับ" |
| `.deb` บนเดสก์ท็อป Linux จริง | ไม่มีเครื่อง Linux | ประตูสามบานที่แตกจากตัว `.deb` (ใบอนุญาต · เวอร์ชัน · PIE+BIND_NOW) |

> ### ★ ถ้าจะทำ "ดิสก์เต็มจริง" ให้ครบ — คำสั่งอยู่ตรงนี้ (ต้องเปิด PowerShell แบบ Run as administrator)
>
> ```powershell
> $vhd = "$env:TEMP\refx-full.vhdx"
> New-VHD -Path $vhd -SizeBytes 16MB -Fixed | Mount-VHD -Passthru |
>   Initialize-Disk -PassThru | New-Partition -AssignDriveLetter -UseMaximumSize |
>   Format-Volume -FileSystem NTFS -Confirm:$false
> ```
>
> แล้วสั่ง RefX เซฟลงไดรฟ์ใหม่นั้น · **ต้องได้**: ไฟล์เดิมไม่เสีย ·
> มีข้อความบอกว่าเซฟไม่สำเร็จเพราะอะไร · **ไม่มีไฟล์ `.refx.tmp` ค้าง**
> · ถอนด้วย `Dismount-VHD -Path $vhd`

---

## 4. ลำดับการปล่อยของ — **แท็กก่อน แล้วค่อยเดิน §2**

> ### ★★★ ทำไมลำดับนี้ (เปลี่ยนหลัง rc.1 · 24 ก.ย. 2026)
>
> rc.1 เดิน §2 **ก่อน** แท็ก → แท็กไปอยู่คอมมิตถัดมา → ต้องมาพิสูจน์ย้อนหลังว่า
> ไบนารีเท่ากัน · แล้ว hash ของ zip **ก็ไม่ตรงจริง ๆ** (26 ไบต์: PE `TimeDateStamp`
> · debug directory 3 รายการ · PDB GUID) ทั้งที่ซอร์สเหมือนกันทุกไบต์
>
> ลำดับใหม่ทำให้ **hash ใช้ได้ตรง ๆ** ไม่ต้องพิสูจน์อะไรย้อนหลังอีก
> · `docs/08 §3.9` ข้อ 9 มีทางสำรอง (ข.) สำหรับกรณีที่เลี่ยงไม่ได้

### ขั้นที่ 1 — ก่อนกดแท็ก

- [ ] `git status` สะอาด · อยู่บน `main`
- [ ] CI เขียวครบบนคอมมิตที่จะแท็ก (**ไม่ใช่แค่ในเครื่อง**)
- [ ] `ROADMAP.md` · `HANDOFF.md` · `KNOWN-LIMITATIONS.md` ตรงกับของจริง
- [ ] จำนวนเทสต์ในรายงาน **ผูกกับคอมมิต**
- [ ] §1 ข้างบน (สิ่งที่เครื่องตัดสินได้) เขียวหมด

### ขั้นที่ 2 — แท็ก แล้ว build จากแท็ก

- [ ] `git tag -a vX.Y.Z-rc.N` แล้ว `git push origin vX.Y.Z-rc.N`
- [ ] `package.yml` ที่แท็กจุดให้รันเอง **เขียวทั้งสาม job**
- [ ] โหลด artifact **จาก run ของแท็กนั้น** แล้วจด SHA256 ไว้

### ขั้นที่ 3 — เดิน §2 บน artifact ของแท็ก

- [ ] แปดขั้นใน §2 ครบ **บนไฟล์ที่โหลดมาจาก run ของแท็ก**
- [ ] §3 (สองข้อที่คนต้องทำ) ครบ
- [ ] ★ **hash ของไฟล์ที่ส่งมอบ = hash ที่จดไว้ในขั้นที่ 2 ตรง ๆ**
      ไม่ตรง = หยุด · ไล่ให้รู้ว่าต่างตรงไหนก่อน อย่าส่ง

> ★★ **ถ้าเดิน §2 แล้วเจอบั๊กที่ต้องแก้** → แก้ · แล้ว **แท็กใหม่เป็น rc.N+1**
> ไม่ใช่ย้ายแท็กเดิม · แท็กที่ถูกย้ายคือแท็กที่ไม่มีใครอ้างอิงได้อีกเลย

### ✅ `v1.0.0-rc.1` — แท็กเมื่อ 18 ก.ย. 2026 (**ด้วยลำดับเก่า**)

★ artifact ที่เดิน §2 สร้างจาก **`391058c`** ส่วนแท็กอยู่บนคอมมิตถัดมาที่เพิ่ม
**เฉพาะเอกสาร** — พิสูจน์ได้ ไม่ใช่คำรับรอง:

```bash
git diff --stat 391058c..v1.0.0-rc.1 -- crates/ .cargo/ Cargo.toml Cargo.lock xtask/
# ต้องว่าง = input ของ build เหมือนกัน (ทางสำรอง ข. ของ docs/08 §3.9 ข้อ 9)
```

★★ **ไบต์ไม่เท่ากันจริง และรู้แล้วว่าทำไม** — zip ของแท็กกับ zip ที่เดิน §2
ต่างกัน **26 ไบต์จาก 18,601,984** ทั้งหมดคือเวลา build (PE `TimeDateStamp`
· debug directory 3 รายการ · PDB GUID · ห่างกัน 3.19 ชม. ตรงกับสอง run)
· **ไฟล์ที่ส่งมอบคือตัวที่เดิน §2** (`1b34380e…c4d1`) ไม่ใช่ตัวของแท็ก
· ลำดับใหม่ข้างบนมีไว้เพื่อไม่ให้ต้องอธิบายย่อหน้านี้อีก

★ ทำไมเลขใน `Cargo.toml` เป็น `1.0.0` ไม่ใช่ `1.0.0-rc.1`: **MSI เขียน
pre-release ไม่ได้** — Windows Installer บังคับว่า `ProductVersion` ต้องเป็น
`a.b.c` ตัวเลขล้วน · ความเป็น rc จึงอยู่ที่แท็กกับ GitHub pre-release
ส่วนเลขสามที่ (`Cargo.toml` · `--version` · ชื่อแพ็กเกจ) ตรงกันที่ `1.0.0`
