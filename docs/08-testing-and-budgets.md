# 08 — Testing, Benchmarks & Definition of Done

หลักคิด: ข้อกำหนด "เสถียร + เบา" ที่ไม่มีตัววัดอัตโนมัติ = ความหวัง ไม่ใช่วิศวกรรม
ทุกข้อในเอกสารนี้ต้องเป็นสิ่งที่ CI ทำให้ fail ได้

---

## 1. ชั้นของการทดสอบ

| ชั้น | ที่ไหน | ทดสอบอะไร | ต้องเร็วแค่ไหน |
|---|---|---|---|
| Unit | `refx-core` | layout algorithm, command/undo, arena, geometry | < 2 s ทั้งหมด |
| Property | `refx-core` (`proptest`) | undo(apply(x)) == x, layout ไม่คืน NaN | < 30 s |
| Integration | `refx-io`, `refx-asset` | save/load round-trip, journal recovery, cache hit/miss | < 20 s |
| Fuzz | `fuzz/` | decoder, document parser, journal, layout | nightly |
| Golden image | `refx-render` | render off-screen เทียบภาพอ้างอิง | < 60 s |
| Benchmark | `benches/` | frame time, memory, startup | ทุก PR |
| Manual | checklist | device lost, drag&drop, ปิดเครื่องกลางคัน | ก่อน release |

### Property test ที่ต้องมี (คุ้มที่สุด)

```rust
// 1. undo คืนสภาพเดิมเป๊ะเสมอ — นี่คือหัวใจของ I-3
proptest! { fn undo_restores_exactly(cmds: Vec<AnyCommand>) {
    let before = board.snapshot();
    for c in &cmds { history.apply(c); }
    for _ in &cmds { history.undo(); }
    assert_eq!(before, board.snapshot());
}}

// 2. layout ไม่เคยคืนค่าที่ไม่ finite ไม่ว่า input จะบ้าแค่ไหน
proptest! { fn layout_always_finite(items: Vec<ItemAspect>, p: LayoutParams) {
    for (_, pos, size) in layout(&items, &p) {
        assert!(pos.is_finite() && size.is_finite() && size.x > 0.0);
    }
}}

// 3. save → load ได้ของเดิม
proptest! { fn roundtrip(board: Board) {
    assert_eq!(board, load(&save(&board)?)?);
}}

// 4. layout เป็น deterministic
proptest! { fn layout_deterministic(items: Vec<ItemAspect>, p: LayoutParams) {
    assert_eq!(layout(&items, &p), layout(&items, &p));
}}
```

---

## 2. Benchmark ที่ CI ต้องบังคับ

> # ⚠️ ยังไม่มีอยู่จริง — อ่านก่อน (แก้ 4 ส.ค. 2026)
>
> **โฟลเดอร์ `benches/` ไม่มีอยู่ในโปรเจกต์** และ 9 ใน 10 แถวข้างล่างนี้
> **ไม่เคยถูกวัดหรือบังคับเลยสักครั้ง** · `criterion = "0.5"` ค้างอยู่ใน
> `[workspace.dependencies]` โดยไม่มีใครเรียก (dependency ที่ไม่มีใครใช้ตัวที่สี่)
>
> **สิ่งเดียวที่บังคับจริง: `binary_size`** ผ่าน `scripts/check-binary-size.sh` ใน CI
>
> ตารางนี้คือ **เป้าหมายของ P5-1** ไม่ใช่สถานะปัจจุบัน — เขียนไว้ตอนออกแบบ
> แล้วอ่านเหมือนว่ามีอยู่แล้วมาตลอดหลาย session
> ตรงตาม `§3.9` เป๊ะ: *เครื่องมือตรวจที่ไม่เคยเดินจริง = ไม่มีเครื่องมือ*
> ต่างกันแค่รอบนี้ **ไม่มีแม้แต่เครื่องมือให้เดิน** — มีแต่ตารางที่ทำให้ทุกคนคิดว่ามี
>
> เพดานในตารางยังใช้เป็น *เป้าหมาย* ได้ แต่ห้ามอ้างว่า "ผ่านเกณฑ์" จนกว่าจะมีตัววัดจริง

`benches/` (P5-1) จะใช้ criterion + dataset สังเคราะห์ 1000 ภาพ (สร้างด้วย `xtask gen-testdata`)

| Bench | เพดาน | fail CI ถ้าเกิน |
|---|---|---|
| `frame_pan_1000` | 8.0 ms | ✅ |
| `frame_pan_1000_p99` | 16.0 ms | ✅ |
| `cull_1000` | 200 µs | ✅ |
| `build_instances_1000` | 300 µs | ✅ |
| `layout_justified_1000` | 2 ms | ✅ |
| `open_board_1000_warm_cache` | 1.5 s | ✅ |
| `startup_to_window` | 400 ms | ✅ |
| `rss_idle_1000` | 250 MB | ✅ |
| `vram_idle_1000` | 200 MB | ✅ |
| `binary_size` | 25 MB | ⚠️ warn |

> ★ **`rss_idle_1000` / `vram_idle_1000` ไม่ต้องใช้ dataset 4000×3000** (27 ส.ค. 2026)
> thumbnail เป็น 128 px คงที่เสมอ (64 KB/ใบ) ส่วนบัฟเฟอร์ตอน decode ถูกคืนไปแล้ว
> → **RSS ตอน idle แทบไม่ขึ้นกับขนาดภาพต้นฉบับ** · ภาพ 1000 ใบขนาดสมจริงใดก็ได้
> (วัดจริงที่ 2400×1800: RSS 167 MB · VRAM 64 MB = 4 layer × 16 MB ซึ่งสอดคล้องกัน)
>
> ประหยัดการสร้าง dataset 6 GB ที่ใช้เวลา ~2 ชั่วโมงโดยไม่ได้ข้อมูลใหม่
> **ถ้าวันหนึ่ง RSS ตอน idle เริ่มขึ้นกับขนาดต้นฉบับ นั่นคือ regression ในตัวมันเอง**

> ### ★ `startup_to_window` วัด **warm** — และ cold คือคนละเรื่องที่ต้องรู้ (27 ส.ค. 2026)
>
> วัดจริง: launch **แรกหลัง build** = 485–586 ms (**เกินเพดาน**) · ทุกครั้งถัดมา = 65–74 ms (n=6)
>
> ความต่างคือเวลาที่ OS ต้องอ่าน binary 17 MB เข้า file cache — **ไม่ใช่ flake ที่ควรเฉลี่ยทิ้ง**
> มันคือสิ่งที่ผู้ใช้เจอจริงตอนเปิดโปรแกรมครั้งแรกหลังติดตั้งหรืออัปเดต
>
> | | วัดอะไร | เพดาน |
> |---|---|---|
> | **warm** (ค่าในตาราง) | การเปิดครั้งที่สองเป็นต้นไป = เกือบทุกครั้งที่ผู้ใช้เปิด | **400 ms** |
> | **cold** | ครั้งแรกหลังติดตั้ง/อัปเดต | **ยังไม่ตั้ง** |
>
> **ไม่ตั้งเพดาน cold ตอนนี้** เพราะมีข้อมูลจากเครื่องเดียวและมันขึ้นกับดิสก์ของผู้ใช้มาก
> (SSD vs HDD ต่างกันหลายเท่า) การเดาตัวเลขคือสิ่งที่ทำให้ "1000 ภาพ 4 วินาที" ผิด 20 เท่า
>
> ★ **แต่รู้ตัวแปรที่คุมมันแล้ว: cold start โตตามขนาด binary**
> `binary_size` ที่บังคับใน CI อยู่แล้วจึงเป็นคันโยกของแถวนี้ไปในตัว —
> ถ้าวันหนึ่ง binary โตขึ้นมาก ให้ถือว่า cold start แย่ลงตามโดยไม่ต้องวัด


> **ค่าปัจจุบัน (28 ก.ค. 2026): 15.35 MB → เหลือ 9.65 MB**
> ตัวเลข "12 MB" ในบันทึกเก่าผิด — วัดซ้ำจาก commit เดิมด้วย profile/toolchain เดียวกันได้ 15.26 MB
>
> **ต้องบังคับใน CI ไม่ใช่วัดด้วยมือ** — งบที่ไม่มีใครตรวจอัตโนมัติคืองบที่จะถูกละเมิดเงียบ ๆ
> (บทเรียนเดียวกับ §3.9) ให้ job หลัง `cargo build --release --all-features` วัดขนาดไฟล์
> แล้วล้มถ้าเกินเพดาน พร้อมพิมพ์ค่าปัจจุบันทุกครั้งเพื่อให้เห็นแนวโน้ม
>
> เรื่องนี้สำคัญเพราะ headroom ถูกใช้ไปกับ P2–P5 ที่ยังไม่ได้เขียน (working texture,
> file format, export, packaging) และเราปฏิเสธฟอนต์ CJK ไปแล้วโดยอ้างเพดานนี้

### ★ นโยบายการลดขนาด binary (28 ก.ค. 2026)

**ลำดับความสำคัญใน `CLAUDE.md` ยังบังคับอยู่: เสถียร → ปลอดภัย → เบา → ฟีเจอร์**
ขนาดไฟล์อยู่อันดับ **3** การลดขนาดที่แลกมาด้วยความเสถียรหรือความปลอดภัย = ไม่เอา

#### ห้ามเด็ดขาด

| ห้าม | เหตุผล |
|---|---|
| `panic = "abort"` | ทำให้ `catch_unwind` พัง → ภาพเสีย 1 ไฟล์ล้มทั้งโปรแกรม (**ผิด I-7**) มีคอมเมนต์เตือนไว้ใน `Cargo.toml` แล้ว |
| ตัด backend ของ wgpu จนเหลือตัวเดียว | เครื่องที่ driver มีปัญหาต้องมีทางถอย — Windows เก็บ **DX12 + Vulkan** · Linux เก็บ **Vulkan + GL** |
| ตัด format ภาพที่ผู้ใช้มีจริง | ดู `docs/05 §7` — เป็นการตัดฟีเจอร์ ไม่ใช่การ optimize ต้องถามก่อน |

#### ต้องวัดก่อนแก้เสมอ

`cargo bloat --release --crates` และ `--filter` ต่อ crate **ห้ามเดาว่าอะไรใหญ่**
เราเพิ่งเสียเวลาไปกับตัวเลข "12 MB" ที่ไม่มีใครวัด

#### เป้าที่ตรวจแล้วว่ามีจริง (เรียงตามความเสี่ยงต่ำ→สูง)

1. **dependency ที่ประกาศไว้แต่ไม่มีใครเรียก** — `zune-jpeg` (ยืนยันแล้วว่าไม่มีโค้ดไหนใช้)
   และ `memmap2` (ถอดออกจาก `refx-asset` แล้วตั้งแต่ห้าม mmap แต่ยังค้างใน `[workspace.dependencies]`)
   *ความเสี่ยง: ไม่มี* — ถ้า `image` ดึง `zune-jpeg` เข้ามาเองอยู่แล้ว ขนาดจะไม่ลด แต่ก็ควรลบเพื่อความชัดเจน
2. **backend ของ wgpu ที่ build ไม่ได้ใช้** — เช่น Metal บน Windows/Linux
   ตัดแบบ per-OS ได้โดยไม่เสียทางถอย · น่าจะเป็นก้อนใหญ่ที่สุด
3. **format ภาพที่แทบไม่มีใครใช้** — `tga` (แทบไม่เจอในงานนักวาด) และ `bmp`
   `tiff` **ให้เก็บไว้** (สแกนงานจริงใช้) และมันคือ decoder ที่ควร fuzz ที่สุด
   → ตัด `tga`/`bmp` ต้องแก้ `ALLOWED_FORMATS` + ข้อความ error + `docs/05 §7` พร้อมกัน
4. **`rusqlite` feature `bundled`** = คอมไพล์ SQLite (ภาษา C) เข้ามาทั้งก้อน
   น่าจะเป็นก้อนใหญ่อันดับต้น ๆ **แต่ห้ามเปลี่ยนไปใช้ SQLite ของระบบ** —
   บน Windows ไม่มีให้ใช้แน่นอน และการพึ่ง library ภายนอกทำลายความคาดเดาได้
   → **วัดว่ามันกินเท่าไหร่ แล้วรายงาน ยังไม่ต้องแก้**
5. `opt-level = "s"` หรือ `"z"` — **ทางเลือกสุดท้าย ต้องวัดคู่กับ benchmark เสมอ**
   decode ใช้เวลา ~81 ms/ไฟล์อยู่แล้ว ถ้าช้าลงอีกเพื่อประหยัดไม่กี่ MB ถือว่าขาดทุน

#### เกณฑ์ตัดสิน

รายงานเป็นตาราง **ก่อน/หลัง ทั้งขนาดและ benchmark** ทุกครั้ง
ถ้าเวลา decode หรือ frame time แย่ลงเกิน 5% ให้ถอยการเปลี่ยนนั้นออก ไม่ว่าจะประหยัดได้เท่าไหร่

> ### ★★ ยกเลิกกฎ "ช้าลง > 10% → บล็อก merge" (27 ส.ค. 2026)
>
> วัดจริงแล้ว **noise ของเครื่องเดียวกัน รุ่นเดียวกัน คือ ±8–13%** (6–7 รอบต่อแถว)
> และ runner ของ CI มี 2 core แชร์กับคนอื่น ซึ่งแกว่งกว่านั้นอีกมาก
>
> เกณฑ์ที่แคบกว่า noise = **flake ที่รับประกันว่าจะเกิด** ซึ่ง `§3.9 ข้อ 5b` ห้ามไว้เอง
> และ flake ทำให้คนเริ่มเชื่อสีแดงน้อยลง — อันตรายกว่าไม่มีเกณฑ์
>
> **แทนที่ด้วย:**
> 1. **เพดานสัมบูรณ์เท่านั้น** (ตารางข้างบน) — จับ "ช้าลงจนพัง" ไม่ใช่ "ช้าลงนิดหน่อย"
> 2. **พิมพ์ตัวเลขทุกครั้งที่รัน** ไม่ว่าผ่านหรือไม่ → แนวโน้มเห็นได้ใน log ของ CI
>    รูปแบบเดียวกับ `check-binary-size.sh`
> 3. การเทียบสองรุ่นให้ทำด้วยมือตาม `§3.9 ข้อ 7` (เครื่องเดียวกัน เซสชันเดียว + รายงาน noise)
>    ไม่ใช่ให้ CI ตัดสิน
>
> ### ★ เพดานปัจจุบันหลวมเกินไป — แต่ยังไม่ปรับ
>
> ค่าที่วัดได้ห่างจากเพดาน **150–190 เท่า** (`cull_1000` 1.32 µs vs 200 µs) จึงจับได้
> เฉพาะหายนะ ไม่ใช่ regression จริง
>
> **ห้ามรัดเพดานด้วยตัวเลขจากเครื่องพัฒนา** — runner ของ CI ช้ากว่ามากและเป็นตัวที่บังคับจริง
> รัดจากเลขเครื่องเร็วจะได้ CI ที่แดงตลอดโดยไม่มีอะไรเสีย
>
> → รอให้ bench รันบน CI สัก 5–10 รอบก่อน แล้วตั้งเพดานใหม่ที่ **~5–10 เท่าของ p95 บน CI**
>   เป็นงานของ P5-2 ไม่ใช่ตอนนี้

### ทดสอบ idle CPU = 0% (I-1)

ทดสอบด้วยเครื่องมืออัตโนมัติได้:

```rust
#[test]
fn idle_produces_no_redraw() {
    let mut app = headless_app_with(1000);
    app.pump_events(Duration::from_secs(5));   // ไม่มี input เลย
    assert_eq!(app.redraw_count_since_settled(), 0, "มีบางอย่างขอ redraw ตอน idle");
}
```

เทสต์นี้สำคัญมาก — เป็นตัวเดียวที่จับ "มีใครแอบเรียก `request_repaint()` ทุกเฟรม" ได้ก่อนที่จะสายเกินแก้

---

## 3. Manual test checklist (ก่อนทุก release)

> ★ **ข้อที่เครื่องมือตัดสินได้ถูกย้ายออกไปแล้ว** (P5-2 · 28 ส.ค. 2026)
> เช็คลิสต์ที่ยาวเกินไปคือเช็คลิสต์ที่ไม่มีใครทำครบ และข้อที่ถูกข้ามคือข้อที่น่าเบื่อ
> ซึ่งเป็นที่ที่บั๊กเงียบอยู่
>
> | ครอบด้วยอะไรแล้ว | ข้อที่ย้ายออก |
> |---|---|
> | CI ทุก push | `cargo tree` (network · wgpu ซ้ำ · png ซ้ำ) · `cargo deny` |
> | `scripts/checklist-app.ps1` | ไฟล์เสีย 20 ไฟล์ · ถอดสื่อระหว่างใช้ · `.refx` ไบต์มั่ว |
> | `scripts/measure-app.ps1 -Pan` | 1000 ภาพ pan 3 นาที (RAM ไม่ไต่) |
> | เทสต์ในชุดปกติ | สลับ mode · apply layout + undo · End Task · decompression bomb · path traversal |

**ต้องทำด้วยมือจริง — ไม่มีเครื่องมือไหนแทนได้**

- [ ] อัปเดต GPU driver ระหว่างที่โปรแกรมเปิดอยู่ → กู้คืนได้ ไม่ crash
- [ ] Sleep แล้ว resume → กู้คืนได้
- [ ] สลับ iGPU ↔ dGPU (โน้ตบุ๊ก) → กู้คืนได้
- [ ] ดิสก์เต็มตอน save → error ชัดเจน ไฟล์เดิมไม่เสียหาย
- [ ] ลากภาพจาก Explorer / เบราว์เซอร์เข้าโปรแกรม (OLE drag ของจริง)
- [ ] `Ctrl+V` ภาพจาก clipboard
- [ ] Undo/redo 100 ครั้งรวด → ไม่มี state เพี้ยน

> ★ สามข้อแรกจำลอง code path ได้ด้วย `--force-device-lost-after-ms` แต่ **ไม่ใช่ของจริง**
> — ห้ามติ๊กผ่านด้วยการจำลอง

### ถ้อยคำที่แก้ตามพฤติกรรมจริง (P5-2)

- ~~"ถอด USB → item เป็น Missing ทันที"~~ → **board ต้องไม่ว่างเพราะไดรฟ์สะดุดวินาทีเดียว**
  item คงภาพที่ cache ไว้ต่อ และกลายเป็น `Missing` ตอนเปิดเอกสารใหม่เท่านั้น
  *(พฤติกรรมจริงดีกว่าที่เอกสารเดิมเขียน)*
- ข้อความตอนเปิด `.refx` ไม่ได้ ต้องแยก **"ไฟล์เสียหาย"** ออกจาก **"มาจากรุ่นใหม่กว่า"**
  (`docs/07 §1`) — ตัวอ่านแยกสองกรณีนี้ได้อยู่แล้ว แต่ UI ยังรวมเป็นข้อความเดียว

---

## 3.9 ★ กฎเหล็ก: การตรวจทุกอย่างต้องพิสูจน์ได้ว่า "ล้มเป็น"

โปรเจกต์นี้โดนสัญญาณเขียวปลอมมาแล้ว **สองครั้ง** และทั้งสองครั้งกินเวลาหลาย session:

| เหตุ | อาการ | ทำไมไม่มีใครเห็น |
|---|---|---|
| canvas ว่างเปล่าตั้งแต่ P0-6 | เทสต์ 170 ตัวผ่าน · log บอกวาดสำเร็จ | ไม่มีเทสต์ไหนถามว่า "แล้วตาเห็นอะไร" |
| `fuzz_decode` เป็น stub 5 session | CI รัน cron ทุกคืน ผ่านทุกครั้ง | `fuzz/` อยู่ใน `exclude` ของ workspace → `clippy --all-targets` มองไม่เห็น |

**กฎที่ตามมา — ใช้กับทุก test / CI job / harness ที่เพิ่มใหม่:**

1. **ต้องพิสูจน์ว่ามันล้มเป็น** ก่อนเชื่อว่าที่มันผ่านคือของจริง
   วิธีที่ถูกคือใส่ **negative control** — จงใจทำให้พังชั่วคราว ดูว่ามันจับได้ แล้วถอดออก
   (ตัวอย่างที่ทำถูกในโปรเจกต์: `assert!(data.first() != Some(&0x89))` ใน `fuzz_decode`
   ยิง seed PNG แล้วยืนยันว่า harness ล้มพร้อมชี้บรรทัด)
   > ★ **negative control ที่ไม่แดง มีสองความหมาย — ต้องแยกให้ออก** (4 ส.ค. 2026)
   >
   > | ความหมาย | ต้องทำ |
   > |---|---|
   > | **เทสต์อ่อน** — พังจริงแล้วมันยังไม่รู้ | แก้เทสต์ |
   > | **ดีไซน์ทำให้พังแบบนั้นไม่ได้** — เช่นสองฝั่งเรียกฟังก์ชันเดียวกัน ถอดออกก็พังพร้อมกัน | **ดีกว่ามีเทสต์คอยจับ** แต่ต้อง *แก้คำอธิบายในเทสต์* ให้ตรง ไม่งั้นคนอ่านทีหลังจะเชื่อว่ามีตาข่ายที่ไม่มีอยู่ |
   >
   > เคสจริง (P2-10): เทสต์คุมว่า picker กับ shader อ่าน pixel เดียวกัน ทดสอบด้วยการถอด `flip`
   > → ยังเขียว เพราะทั้งสองฝั่งเรียก `source_span` ตัวเดียวกันไปแล้ว
   > ทางที่ถูกคือหาเคสที่ยัง**เขียนฝั่งเดียวได้** (กลับแกน y ใน `source_uv`) → แดงทันที
   > แล้วเขียนตารางไว้ในเทสต์ว่าอันไหนจับได้ อันไหนจับไม่ได้ และทำไม

1b. **★ เทสต์เกราะความปลอดภัยต้องใช้ input ที่ "ถูกต้องทุกอย่างยกเว้นเจตนา"**
   ไฟล์ที่บิตพลิกเพราะดิสก์เสีย กับไฟล์ที่ถูกดัดแปลงอย่างตั้งใจ **เป็นคนละภัย**
   และด่าน checksum จะจับอันแรกได้ก่อนเสมอ → **บังหน้าด่านที่เรากำลังจะทดสอบ**

   เคสจริง (P4-5): เทสต์ชื่อ `an_entry_pointing_outside_the_file_is_rejected`
   แก้ `len` เป็น `u64::MAX` โดยไม่คำนวณ `table_crc` ใหม่ → ไฟล์ตกที่ checksum
   ก่อนถึงด่านขอบเขตเลย · **ถอดด่านขอบเขตออกแล้วเทสต์ยังเขียว**
   = พิสูจน์คนละเรื่องกับชื่อของมัน

   → input ต้อง **ประกอบ checksum ให้ถูกด้วย** แล้วผิดเฉพาะสิ่งที่กำลังทดสอบ
   → I-4 พูดถึงไฟล์ที่ *ตั้งใจโจมตี* ไม่ใช่แค่ไฟล์ที่ *เสียหาย* — เทสต์ต้องสะท้อนข้อนั้น

   > ### ★★ รูปแบบเดียวกันนอกเรื่องความปลอดภัย: **input ต้องไปถึงกิ่งที่กำลังทดสอบได้จริง**
   >
   > เคสจริง (P5-3b): เทสต์ Dvorak ที่คุม *"logical ต้องชนะ physical"* ยิงด้วย
   > `KeyCode::Period` ซึ่ง **ไม่มีในแผนที่ physical เลย** → ตาข่ายรองคืน `None` ทุกกรณี
   > ผลจึงตกกลับเป็น logical เสมอ **ไม่ว่ากฎจะเป็นแบบไหน**
   >
   > พิสูจน์แล้วว่ามันจับไม่ได้: NC ที่ให้ *physical ชนะเสมอ* → **เทสต์เดิมเขียว** ทั้งที่กฎพังแล้ว
   >
   > → ก่อนเชื่อว่าเทสต์คุมกิ่งไหน **ต้องรู้ว่า input ของมันไปถึงกิ่งนั้นได้** —
   >   คีย์ที่ไม่อยู่ในตาราง · path ที่ตกด่านแรก · ไฟล์ที่ตกที่ checksum
   >   ล้วนทำให้เทสต์ "ผ่านโดยไม่เคยแตะสิ่งที่ชื่อมันอ้าง"
   > → วิธีตรวจที่ถูกคือ **NC ที่ทำให้กิ่งนั้นพัง แล้วดูว่าเทสต์แดงไหม** ไม่ใช่การอ่านโค้ดเทสต์

2. **โครงเปล่าห้ามเงียบ** — target/เทสต์/job ที่ยังไม่ได้ต่อกับโค้ดจริง
   ต้องทำให้ CI **แดง หรือ ข้ามอย่างชัดเจน** ห้ามรันแล้วผ่านแบบว่างเปล่า
   *สัญญาณที่โกหกแย่กว่าไม่มีสัญญาณ* เพราะมันปิดโอกาสที่จะมีใครกลับมาดู
3. **ทุกไดเรกทอรีที่มี `Cargo.toml` ต้องถูก lint** ถึงจะอยู่นอก workspace ก็ตาม
   (`fuzz/` เคยหลุดเพราะ `exclude` — ปิดด้วย job `fuzz-lint` แล้ว)
4. **CI ต้องรันชุดเดียวกับเกณฑ์ในเครื่องเป๊ะ** ถ้า CI อ่อนกว่า คำว่า "CI เขียว"
   จะแปลว่าน้อยกว่าที่ทุกคนเข้าใจ โดยไม่มีใครรู้ตัว
5. **งานที่ผู้ใช้มองเห็น ต้องมีภาพหน้าจอจริงก่อนบอกว่าเสร็จ** (ดู `docs/03 §1`)

   > ★ **ทำไมข้อนี้ถึงคุ้มแม้ต้นทุนต่อ task จะสูง** — เพราะมันเป็นเครื่องมือเดียวที่จับ
   > **"ทุกชิ้นถูก แต่ประกอบผิด"** ได้ ซึ่งเป็นคนละชนิดกับสองข้อล่าง:
   >
   > | ชนิดของบั๊ก | เครื่องมือที่จับได้ |
   > |---|---|
   > | มีที่ว่างรอไว้ ไม่มีใครเติม (`rotation` ไม่ถึง shader) | **audit + ประตูที่ล้มเมื่อมีฟิลด์ใหม่** |
   > | สัญญาถูกเฉพาะเมื่อเรียกถูกจังหวะ (`take_forgotten`) | **ข้อ 8** — เทสต์ที่เรียกแบบเลื่อนออกไป |
   > | ทุกชิ้นถูก ประกอบผิด (`C` ไม่ติด · ดับเบิลคลิกแล้วภาพกระโดด) | **รันของจริงแล้วดู** — ไม่มีอย่างอื่นจับได้ |
   >
   > บั๊กชนิดที่สามผ่าน unit test ได้ทุกตัวโดยนิยาม เพราะแต่ละชิ้น *ถูกจริง ๆ*
   > สิ่งที่ผิดอยู่ระหว่างชิ้น ซึ่งไม่มีเทสต์ตัวไหนเป็นเจ้าของ
   > ในเซสชัน P2-5..P2-7 บั๊ก 3 ใน 5 ตัวถูกจับด้วยวิธีนี้ ไม่ใช่ด้วยเทสต์
   > **ห้ามตัดข้อนี้ทิ้งเพราะมองว่าเป็นพิธีกรรมที่แพง**
5b. **★ ห้าม assert เวลานาฬิกา — assert "งานที่ทำ" แล้ว *พิมพ์* เวลาแทน**
   เกิดมาแล้วสองครั้ง: `lower_priority_value_runs_first` (ล้มบน runner 2 core)
   และ `hashes_100mb_file_quickly` (`elapsed < 200ms` บน runner ที่แชร์กับคนอื่น)
   เทสต์แบบนี้ **รับประกันว่าจะ flake สักวัน** และ flake ทำให้คนเริ่มเชื่อสีแดงน้อยลง
   ซึ่งอันตรายกว่าไม่มีเทสต์
   → หาสิ่งที่เทสต์นั้น *ตั้งใจ* จะปกป้อง แล้ว assert สิ่งนั้นแทน
     "ไฟล์ใหญ่ต้องไม่ถูก hash ทั้งไฟล์" → พลิกไบต์ตรงกลางแล้ว hash ต้องไม่เปลี่ยน
     (คุณสมบัติเชิงอัลกอริทึม เหมือนกันทุกเครื่อง)
     "hit-test ต้องเร็ว" → นับว่าตรวจกี่ item ไม่ใช่จับเวลา
   → เวลายังพิมพ์ไว้ได้เพื่อดูแนวโน้ม แค่อย่าเอาไปตัดสินผ่าน/ไม่ผ่าน
5c. **★ คำกล่าวอ้างว่า "ปิดช่องแล้ว" ต้องชี้ไปที่เทสต์ที่รักษามันไว้**
   ถ้าชี้ไม่ได้ มันคือ *ความหวัง* ไม่ใช่ *การปิดช่อง* และจะกลายเป็นข้อมูลเท็จในเอกสาร

   เคสจริง (P4-3 → P4-4): `HANDOFF §2.24` เขียนว่าปิดช่อง "dirty + idle = ไม่มี snapshot"
   ไปแล้ว · ของจริง `next_deadline()` ถูกต่อเข้า `wake_deadline()` และเธรดตื่นตรงเวลาจริง
   **แต่ `on_wake()` ไม่มีกิ่งไหนรับ** พอคืน `None` ก็หลับยาวต่อ
   → กลไกทั้งชุด **ไม่เคยทำงานเลยสักครั้ง** ขณะที่เอกสารบอกว่าเรียบร้อย

   → ทุกครั้งที่เขียนใน `HANDOFF` ว่าปิดช่องอะไรไปแล้ว **ต้องระบุชื่อเทสต์กำกับ**
   → ถ้าเป็นเรื่องที่เทสต์คุมไม่ได้ (ต้องรันจริง) ให้เขียนว่า *"ยืนยันด้วยมือเมื่อ <วันที่>"*
     ไม่ใช่ *"ปิดแล้ว"* — สองอย่างนี้ให้ความมั่นใจต่างกันมาก
   → เจอข้อความในเอกสารที่รู้แล้วว่าไม่จริง **ให้แก้ทันที** อย่ารอรอบถัดไป

6. **ตัวเลขที่วัดครั้งเดียวแล้วจดไว้ จะเสื่อมสภาพเงียบ ๆ**
   ตัวอย่างจริง: "atlas เติมกลับ **100/100** ใน 0.99 ms" ถูกจดไว้ตอนวัดและเป็นความจริงตอนนั้น
   แต่พอเพิ่ม lazy allocation ทีหลัง atlas ที่เพิ่งสร้างมี 0 layer → ของจริงกลายเป็น **0/8**
   โดยไม่มีอะไรส่งเสียง ตัวเลขในเอกสารยังบอก 100/100 อยู่หลาย session
   → **คุณสมบัติที่สำคัญพอจะจดตัวเลขไว้ ต้องสำคัญพอที่จะมีเทสต์คุม**
   ตัวเลขในเอกสารคือ *ภาพถ่าย ณ เวลานั้น* ไม่ใช่คำรับประกัน
7. **เทียบประสิทธิภาพ ต้องวัดสองฝั่งบนเครื่องเดียวกันในเซสชันเดียวกัน**
   ตัวเลขใน `HANDOFF §3` เป็น *ภาพถ่าย* จากเครื่องและช่วงเวลาหนึ่ง — ใช้เป็นการเช็คคร่าว ๆ ได้
   แต่ **ห้ามใช้ตัดสินว่า "แย่ลงเกิน 5%"** เพราะความต่างอาจมาจาก driver, ความร้อน,
   หรือโปรแกรมอื่นที่เปิดอยู่ ไม่ใช่จากโค้ดที่เพิ่งแก้
   → build โค้ดก่อนแก้ใน `git worktree` แล้ววัดสลับกันกับของใหม่ในรอบเดียว
   → รายงาน noise ของรุ่นเดียวกันด้วย ไม่งั้นไม่มีทางรู้ว่า +1.4% แปลว่าอะไร

   > ### ★ ระวัง: input ชุดเดียวกันไม่ได้แปลว่างานเท่ากัน (19 ส.ค. 2026)
   >
   > egui **รวบ event ล้อต่อเฟรม** → build ที่วาดเฟรมได้มากกว่าจะ **ซูมได้ลึกกว่า**
   > ทั้งที่ส่ง input ชุดเดียวกันเป๊ะ · การเทียบสองรุ่นด้วย "ส่งคีย์/ล้อชุดเดียวกัน"
   > จึงเทียบคนละงานกันโดยไม่รู้ตัว และรุ่นที่เร็วกว่าจะดู *แย่กว่า* เพราะมันทำงานมากกว่า
   >
   > → เทียบที่ **สถานะปลายทาง** (ระดับซูมเท่ากัน · จำนวน item เท่ากัน) ไม่ใช่ที่ input
   > → ถ้าเทียบจาก input ไม่ได้จริง ๆ ให้รายงานว่าสองฝั่งจบที่สถานะต่างกันแค่ไหน
8. **★ สัญญาที่ถูกต้องเฉพาะเมื่อผู้เรียกเรียก "ถูกจังหวะ" คือกับดัก**
   audit จับรูปแบบนี้ไม่ได้ เพราะโค้ดมีครบและทำงานถูกตามตัวอักษรของสัญญา
   สิ่งที่ผิดคือ **เวลา** ที่เรียก

   เคสจริง (P2-6): `take_forgotten()` สัญญาว่า "คืนทุก id แล้วผู้เรียกไปเช็ค `board` เอง"
   — ถูกเฉพาะถ้าเช็คทันที ของจริง drain ทีหลัง ตอนนั้น id ชุดนั้นถูกลบพอดี
   จึงถูกตัดสินว่าตาย → ทิ้ง thumbnail → **undo แล้วภาพกลับเข้า board แต่ไม่ขึ้นจอ**
   เทสต์ชุดแรกผ่านหมด **เพราะทุกตัวเรียกทันที** ซึ่งไม่ใช่สิ่งที่ของจริงทำ

   **สองข้อที่ต้องทำ:**

   1. **ออกแบบ:** ถ้าสัญญาต้องพึ่งจังหวะของผู้เรียก ให้ **ย้ายการตัดสินเข้าไปในฝั่งที่มีข้อมูลครบ**
      แทนที่จะบอกให้ผู้เรียกทำเอง (`History` มี `board` อยู่ในมือตอนนั้นพอดี)
      API ที่ถูกได้ทางเดียวย่อมดีกว่า API ที่ต้องใช้ให้ถูก
   2. **เทสต์:** ถ้าสัญญามีเงื่อนไขเรื่องเวลา **ต้องมีอย่างน้อยหนึ่งเทสต์ที่เรียกแบบ *เลื่อนออกไป*
      ตามที่ของจริงเรียก** ไม่ใช่เรียกทันทีทุกตัว — เทสต์ที่เรียกทันทีอย่างเดียว
      ยืนยันได้แค่ว่า "ถูกในกรณีที่ง่ายที่สุด"

9. **★ เครื่องมือที่ *ผลิตหลักฐาน* ต้องพิสูจน์ตัวเองด้วยว่าไม่ได้โกหก**
   ภาพหน้าจอมีค่าเพราะมันบอกว่า "ของจริงเป็นแบบนี้" — ถ้าตัวถ่ายภาพผิด
   เราจะได้ไฟล์ที่**หน้าตาเหมือนหลักฐานทุกประการ** แต่เป็นของหน้าต่างอื่น

   เคสจริง (P2-9): `SetForegroundWindow` ถูก Windows ปฏิเสธเงียบ ๆ เมื่อโปรเซสที่เรียก
   ไม่ได้อยู่ foreground · สคริปต์ไม่ดูค่าที่คืนมา → คลิกทุกครั้งตกไปที่หน้าต่างที่บังอยู่
   และ `CopyFromScreen` ถ่ายหน้าต่างนั้นออกมา **RefX ไม่เคยเห็นการกดพวกนั้นเลย**
   ทั้ง session ถัดมาเสียไปกับการไล่หาบั๊กในโค้ดแอปที่ไม่เคยมีอยู่

   → สคริปต์ขับ UI ต้อง **ตรวจว่าหน้าต่างเป้าหมายอยู่ foreground จริง** แล้วล้มถ้าไม่ใช่
   → หลักฐานเชิงพิกเซลต้อง **diff เป็นตัวเลข** ภาพหน้าจอเป็นของประกอบ
     (ตาอ่านผิดได้ง่ายกับลายซ้ำ ๆ และกับภาพที่ซูมออกมาก)
   → เครื่องมือพวกนี้ต้องอยู่ใน repo ไม่ใช่ใน session — ไม่งั้นบทเรียนหายไปพร้อมกัน

   > ### ★★ ตรวจ *ก่อน* ไม่พอ — ต้องตรวจ *หลัง* ด้วย (10 ส.ค. 2026)
   >
   > รอบ P2-9 เราปิดรูนี้ด้วย `Assert-Focus` **ก่อน** ถ่ายภาพ แล้วคิดว่าจบ
   > แต่ยังหลุดอีกครั้ง: ภาพถ่ายออกมาเป็นหน้าต่างโปรแกรมอื่นทั้งใบ **ทั้งที่ด่านผ่าน**
   > เพราะระหว่างด่านกับ `CopyFromScreen` มีช่องว่างไม่กี่ร้อย ms ที่หน้าต่างอื่นแทรกได้
   >
   > **การตรวจเงื่อนไขก่อนทำ ไม่ได้รับประกันว่าเงื่อนไขยังจริงตอนทำ**
   > → ต้องตรวจ **หลังได้หลักฐานมาแล้ว** ด้วย และ **ทิ้งหลักฐานนั้นถ้าไม่ผ่าน**
   >   ไม่ใช่บันทึกไว้แล้วค่อยเตือน — ไฟล์ที่บันทึกไปแล้วจะถูกใช้เป็นหลักฐานเสมอ
   >
   > ใช้กับทุกอย่างที่ผลิตหลักฐาน ไม่ใช่แค่ภาพหน้าจอ: log ที่ก๊อปมา · ตัวเลขที่วัดได้ ·
   > ไฟล์ที่ dump ออกมา — ถ้าสภาพแวดล้อมเปลี่ยนได้ระหว่างเก็บ ต้องยืนยันซ้ำตอนจบ

   > ### ★★ ห้ามยืนยันประตูด้วยสิ่งที่เขียนเลียนแบบประตู (11 ส.ค. 2026)
   >
   > ตอน P4-1 ประตู `unwired-targets` ถูก "ยืนยัน" ด้วยสคริปต์ที่เขียนขึ้นใหม่ให้ทำงานคล้ายกัน
   > แทนที่จะรันตัวจริงจาก `ci.yml` — สิ่งที่พิสูจน์ได้จึงเป็น **"สคริปต์ที่เพิ่งเขียนพอใจ"**
   > ไม่ใช่ "ประตูพอใจ" · ของจริงมีรายชื่อ target ฝังไว้แบบตายตัวและไม่เคยอ่านสภาพจริงเลย
   > → CI แดงในรอบถัดมา ทั้งที่รายงานบอกว่าตรวจแล้ว
   >
   > **รันของจริงเสมอ** — ถ้ามันฝังอยู่ใน YAML ให้ดึงออกมารันตรง ๆ
   > สิ่งที่เขียนเลียนแบบจะสะท้อน *ความเข้าใจของคนเขียน* ไม่ใช่ *พฤติกรรมของของจริง*
   > และความต่างระหว่างสองอย่างนั้นคือที่ที่บั๊กอยู่พอดี
   >
   > หลักการเดียวกันใช้กับเทสต์: เทสต์ที่เรียกฟังก์ชันจำลองแทนฟังก์ชันที่ production ใช้จริง
   > พิสูจน์ได้แค่ว่าตัวจำลองทำงาน

   > ### ★★ เครื่องมือตรวจต้อง **รายงานสิ่งที่มันทำจริง** ไม่ใช่แค่สำเร็จเงียบ ๆ
   >
   > `ui-drive.ps1` โกหกมาแล้ว **สามครั้ง คนละแบบทั้งสามครั้ง**:
   >
   > | เมื่อไหร่ | อาการ | ผลลัพธ์ที่ได้ |
   > |---|---|---|
   > | 4 ส.ค. | `SetForegroundWindow` ถูกปฏิเสธเงียบ | ภาพของหน้าต่างอื่นที่ดูเหมือนหลักฐาน |
   > | 10 ส.ค. | focus หลุด **ระหว่าง** `CopyFromScreen` | ภาพผิดทั้งที่ด่านก่อนถ่ายผ่าน |
   > | 12 ส.ค. | ส่ง arg เข้า `powershell -File` แล้ว parser ลูกกิน `\|` | **รัน step แรกอันเดียว ข้ามที่เหลือ ไม่พิมพ์อะไร exit 0** |
   > | 27 ส.ค. | สคริปต์ negative control พิมพ์ภาษาไทยลงคอนโซล **cp1252** | **ตายกลางคัน แต่ `exit 0` เพราะต่อ `\| tail`** — เคสที่ 2–4 ไม่เคยรัน |
   > | 4 ก.ย. | คลิกไม่โดนภาพ → selection ว่าง → `Ctrl+Z` ไปย้อน*การเพิ่มภาพ* | **อ่านได้เหมือน "ปุ่ม `[` ไม่ติด" เป๊ะ** |
   > | 5 ก.ย. | `--mode=arrange` เขียนแค่ `ShellState` ซึ่งถูกทับก่อนเฟรมแรก | **ภาพ Canvas ทุกใบ ตั้งชื่อไฟล์ว่า `arrange`** |
   > | 5 ก.ย. | `ui-layout-switch.ps1` ยิง `WM_NULL` เช็คชีพ**ทันที**หลัง post | **`SURVIVED` ทั้งที่หน้าต่างค้าง** — เร็วกว่าที่อาการจะปรากฏ |
   >
   > ★ ตัวที่สี่มีสองชั้น: **encoding ทำให้ตาย** และ **pipe กลืน exit code**
   > → สคริปต์ที่ผลิตหลักฐานควรพิมพ์ **ASCII เท่านั้น** และ **ห้ามต่อ pipe ตอนตรวจผล**
   > (บน PowerShell/bash ให้ตรวจ `$LASTEXITCODE` / `PIPESTATUS` ถ้าเลี่ยง pipe ไม่ได้)
   >
   > ★★ ตัวที่ห้าไม่ใช่ *สคริปต์โกหก* แต่เป็น **สภาพเริ่มต้นที่ไม่เคยถูกยืนยัน** —
   > การวัดอ่าน "จำนวนใบลดลงหลัง `Ctrl+Z`" เป็นสัญญาณว่าปุ่มสร้างคำสั่งได้
   > โดยไม่เคยตรวจว่า **มีอะไรถูกเลือกอยู่จริงไหม** · เมื่อ selection ว่าง
   > `Ctrl+Z` ไปย้อนสิ่งอื่นแทน แล้วให้ตัวเลขที่หน้าตาเหมือนข้อสรุปตรงข้ามเป๊ะ
   > → **precondition ทุกข้อที่การตีความผลขึ้นอยู่กับมัน ต้องอ่านค่ากลับมาก่อนวัด**
   >   ไม่ใช่แค่ "สั่งไปแล้ว" (ที่นี่: ยืนยัน selection จาก Inspector ก่อนกดปุ่ม)
   >
   > ทุกครั้ง**ให้ผลที่ดูสมเหตุสมผล** และครั้งที่สามพาไปสรุปผิดว่าเป็นบั๊กของแอป
   >
   > → เครื่องมือต้อง **พิมพ์ว่ามันทำอะไรไปกี่ขั้น** ทุกครั้ง (`steps: N`)
   >   ตัวเลขที่ไม่ตรงกับที่สั่งจะเห็นได้ในบรรทัดแรก โดยไม่ต้องรอให้ผลลัพธ์ผิด
   > → "สำเร็จโดยไม่พิมพ์อะไร" กับ "ไม่ได้ทำอะไรเลย" ต้องแยกออกจากกันได้เสมอ
   >
   > ### ★★★ ตัวเลขที่ดู "ครบ" คือคำโกหกที่เนียนที่สุด — **1,035 ไม่ใช่ 1,040** (12 ก.ย. 2026)
   > `cargo test --all` รายงาน **1,035 ผ่านทั้งหมด** ซึ่งอ่านได้ว่าครบถ้วนสมบูรณ์
   > ความจริงคือ `--all` แปลว่า *"ทุก package"* ไม่ใช่ *"ทุก feature"* →
   > เทสต์หลัง feature gate **ถูกข้ามโดยไม่มีบรรทัดไหนบอก**
   > ห้าตัวที่หายคือ `ForcedLoss` — เส้นทางกู้คืน GPU device lost ที่ `docs/04 §7` พึ่งพา
   >
   > **CI รัน nextest มาตลอด** เลขในเครื่องจึงต่ำกว่าที่ CI ตรวจจริง
   > = เครื่อง dev อ่อนกว่าที่คิดมาตลอด **โดยที่หน้าจอบอกว่าเขียวครบ**
   >
   > → ธงที่ทำให้ขอบเขต**แคบลง** ต้องประกาศตัวเลข · "ผ่านหมด" ไม่มีความหมาย
   >   ถ้าไม่รู้ว่า *หมด* คือเท่าไหร่ → **เทียบจำนวนกับรอบก่อนเสมอ ไม่ใช่ดูแค่สี**
   >
   > ### ★★ negative control **ก็เป็นเครื่องมือผลิตหลักฐาน** — โกหกได้เหมือนกัน
   > 5 ก.ย. 2026: NC ของประตู tofu ใส่ `ui.monospace("→")` แทน `ui.label("→")`
   > ซึ่งใช้ฟอนต์คนละชุดตรงจุดที่กำลังทดสอบพอดี → NC เขียว → เกือบสรุปว่าประตูดีแล้ว
   > **รูปเดียวกับ "ตรวจประตูด้วยของที่หน้าตาเหมือน" ที่ข้อนี้ห้ามไว้อยู่แล้ว
   > แต่คราวนี้เกิดกับ NC ไม่ใช่กับประตู**
   > → NC ต้องแตะ **จุดเรียกเดิม** ไม่ใช่จุดที่ทำงานคล้ายกัน · ถ้า NC ไม่แดง
   >   ให้สงสัย NC ก่อนสรุปว่าประตูดี (`§3.9 ข้อ 1` ท้ายข้อ)
   >
   > ### ★★ NC ต้อง**พิสูจน์ว่ามันถูกรัน** ไม่ใช่แค่ถูกใส่เข้าไป
   > 7 ก.ย. 2026: NC ของตัวจับ I-1 ถูกใส่ท้าย `on_wake()` ซึ่งแทบไม่เคยถูกเรียก
   > → **เงียบ ไม่ใช่แดง** และเกือบสรุปว่าตัวจับใช้ได้ · ที่ถูกคือท้าย `redraw()`
   > → NC ที่วางในโค้ดที่ไม่ทำงาน ให้ผลหน้าตาเดียวกับ NC ที่พิสูจน์ว่ากฎแข็งแรง
   >   **แยกสองอย่างนี้ออกจากกันไม่ได้ถ้าไม่บังคับให้ NC พิมพ์ว่ามันวิ่งผ่าน**
   >   — หลักการ `steps: N` ของข้อนี้ ใช้กับ NC ด้วย
   >
   > ### ★★★ สองตัวหลังคือรูปเดียวกัน: **ถามเร็วเกินกว่าที่คำตอบจะจริง**
   > `--mode=arrange` ตั้งค่าแล้ว *ยังไม่ทันถึงเฟรมแรก* ก็ถูกทับ ·
   > `WM_NULL` ถามว่ายังไม่ตายไหม *ก่อนที่การตายจะเกิด*
   > → **สถานะที่เพิ่งสั่ง ยังไม่ใช่สถานะที่เป็นจริง** · ต้องอ่านค่ากลับมา
   >   **จากปลายทางที่ผู้ใช้เห็น** หลังผ่านจุดที่มันอาจถูกเปลี่ยน — ที่นี่คือ
   >   หลังเฟรมแรก / หลังรอเวลาที่อาการจะปรากฏ
   > → เครื่องมือถ่ายภาพหลักฐานต้อง **ยืนยันโหมดจากภาพเอง** ไม่ใช่จากชื่อไฟล์ที่ตัวเองตั้ง
   >
   > ### ✅ รูปที่ **ถูก** ของเครื่องมือผลิตหลักฐาน — `ui-drive.ps1` (บันทึก 6 ก.ย. 2026)
   > มันปฏิเสธการถ่ายภาพแอปที่ค้าง (**exit 3**) แทนที่จะถ่ายมาแล้วปล่อยให้คนตีความ
   > → หลักฐานที่ได้จึงเป็น **"ถูกต้อง" หรือ "ไม่มี" เท่านั้น ไม่มีทางเป็นบวกปลอม**
   >
   > นี่คือคุณสมบัติที่เครื่องมือทุกตัวควรมี: **ล้มเหลวแบบดังเสมอ ไม่เดาแทนคน**
   > ตอนถอนข้อสรุปที่พึ่ง `PostMessage` ทิ้ง คุณสมบัตินี้คือสิ่งเดียวที่ทำให้
   > **ผลการกดคีย์บน layout ไทยทั้งหมดไม่ถูกกระทบ** — ของที่เสียถูกกันออกไปตั้งแต่ต้นทาง

10. **★ ทุกอย่างที่โปรแกรม *ประกาศว่ารองรับ* ต้องถูกทดสอบจริงอย่างน้อยหนึ่งครั้ง**
    เครื่องที่เราพัฒนาอยู่คือ **หนึ่งสภาพแวดล้อม** ไม่ใช่ทั้งหมด
    มิติที่ต่างกันแล้วไม่มีใครสังเกต = โค้ดที่ไม่เคยถูกรันเลยแม้แต่ครั้งเดียว

    เกิดมาแล้วสองครั้ง คนละมิติ:

    | ประกาศว่ารองรับ | ความจริง | เจอตอน |
    |---|---|---|
    | Linux (CI matrix มี ubuntu) | โค้ด `#[cfg(target_os = "linux")]` **ไม่เคยถูกคอมไพล์เลย** | CI รันครั้งแรก (1 ส.ค.) |
    | **ภาษาไทยเป็นภาษาที่สอง** (`docs/03 §0`) | **คีย์ลัดทุกตัวใช้ไม่ได้บน layout ไทย** มาตั้งแต่ P2-4 | 6 ส.ค. |

    ตัวหลังมองไม่เห็นเพราะทุก session อยู่บน layout อังกฤษ — matcher ดูแต่ *logical key*
    ซึ่งบน layout ไทยปุ่ม `C` ส่ง `แ` มา ผู้ใช้ไทยจึงกดคีย์ลัดไม่ได้เลยสักปุ่ม

    → **ทำรายการว่าเราประกาศรองรับอะไรบ้าง** (OS · ภาษา · layout · ชนิด GPU · ขนาดจอ)
      แล้วระบุว่าแต่ละอย่างถูกตรวจจริงด้วยอะไร
    → อันที่ตรวจอัตโนมัติไม่ได้ ต้องมี **ขั้นตอนตรวจด้วยมือที่เขียนไว้** และทำก่อนปล่อย
    → **"เครื่องผมใช้ได้" ไม่ใช่หลักฐาน** สำหรับสิ่งที่ผู้ใช้กลุ่มอื่นเจอคนละแบบ

11. **★ I-1 ต้องตรวจ *ทุกสถานะที่แอปอยู่ได้* ไม่ใช่สถานะเดียว**
    "ปล่อยไว้แล้วเฟรมนิ่ง" ถูกยืนยันมาแล้วหลายสิบครั้งตลอดโปรเจกต์
    แต่**ทุกครั้งวัดในสถานะเดียวกัน**: เปิดโปรแกรม ไม่แตะอะไร แล้วรอ

    ผลคือช่องข้อความที่มี focus **เผา CPU ที่ 13 fps ตลอดเวลา** (เคอร์เซอร์กะพริบ)
    มาตั้งแต่ P2-11 — ผ่านการตรวจ I-1 ทุกครั้งเพราะไม่มีครั้งไหนที่ focus อยู่ในช่องข้อความ
    โปรแกรมที่เปิดค้างทั้งวันข้าง Photoshop โดยเผลอคลิกช่องโน้ตไว้ = กิน CPU ทั้งวัน

    **ต้องวัดอย่างน้อยสถานะเหล่านี้ทุกครั้งที่แตะ UI:**

    | สถานะ | ทำไมต้องแยก |
    |---|---|
    | ว่างเปล่า ไม่มีภาพ | เส้นฐาน |
    | มีภาพเต็ม board | culling / atlas ทำงานอยู่ |
    | **focus อยู่ในช่องข้อความ** | ← ตัวที่หลุดมา 3 phase |
    | เครื่องมือที่ไม่ใช่ Select เปิดอยู่ | overlay อาจขอวาดเอง |
    | มีของถูกเลือกอยู่ | handle / กรอบอาจ animate |
    | หลังข้อความ error หรือ toast | ตัวจับเวลาอาจค้าง |

    หลักการเดียวกับกฎข้อ 10 — สภาพแวดล้อมเดียวไม่ใช่ทั้งหมด รอบนี้คือ *สถานะ* แทน *เครื่อง*

    > ### ★★ I-1 ที่รั่ว **เป็นครั้งคราว** ต้องแก้ด้วยตัวจับถาวร ไม่ใช่การวัดซ้ำ
    > 6 ก.ย. 2026: เจอ ~1.5 Hz สองครั้ง แล้ววัดซ้ำ 13 หน้าต่างไม่เจออีก
    > (ทั้งบิลด์ใหม่และ HEAD · จงใจทำซ้ำตามเงื่อนไขที่เดาไว้ก็ไม่เกิด)
    >
    > **การวัดด้วยมือจับเหตุการณ์ 2-ใน-15 ไม่ได้** — ไม่ว่าจะวัดอีกกี่รอบ
    > "ไม่เจอ" ก็ไม่ได้แปลว่าไม่มี และเวลาที่เสียไปไม่ให้ข้อมูลใหม่
    >
    > → เปลี่ยนเป็น **ตัวนับที่เปิดอยู่ตลอด**: จำนวน redraw ระหว่างที่ไม่มี input
    >   พร้อมสิ่งที่ขอ redraw ครั้งนั้น · ครั้งหน้าที่มันเกิด จะมีร่องรอยให้ตาม
    >   แทนที่จะต้องพยายามทำให้มันเกิดซ้ำ
    >
    > ### ★★★ ตัวจับต้อง **เงียบสนิทตอนทุกอย่างปกติ** (ทำแล้ว 7 ก.ย. 2026)
    > `TextureReady` ต้อง**รีเซ็ต**ช่วงเงียบ ไม่ใช่นับเพิ่ม — ไม่งั้นการเปิด board
    > 3,072 ใบสร้างช่วงยาวเป็นพัน แล้วเสียงเตือนดังทุกครั้งที่เปิดไฟล์ใหญ่
    >
    > **ตัวจับที่ร้องตอนทุกอย่างปกติ คือตัวจับที่ไม่มีใครอ่าน** — และเมื่อไม่มีใครอ่าน
    > มันแย่กว่าไม่มีตัวจับ เพราะทุกคนเชื่อว่ามีอะไรเฝ้าอยู่
    > → เกณฑ์ต้องมาจาก **ค่าที่เป็นไปได้ตอนปกติ** (ที่นี่: animation 200 ms
    >   ที่ 60 fps = 12 เฟรม → เพดาน 32) และการลดเกณฑ์ต่ำกว่านั้น
    >   ควร **build ไม่ผ่าน** ไม่ใช่เทสต์แดงทีหลัง

12. **ถ้าทั้ง matrix ของ CI ข้ามเทสต์กลุ่มหนึ่งพร้อมกัน = ไม่มีใครตรวจกลุ่มนั้นเลย**
   การข้ามพร้อมพิมพ์เหตุผล (ข้อ 2) ถูกต้องในระดับ job เดียว
   แต่ต้องมีตัวยืนยันว่า **อย่างน้อยหนึ่ง job รันจริง** ไม่งั้นได้เขียวโดยไม่มีความครอบคลุม


13. **★★★ เครื่อง dev สร้าง output บางรูปไม่ได้เลย — ต้องเอารูปนั้นมาไว้ในบ้าน**

    5 ก.ย. 2026: CI แดงสามรอบ **ไม่ใช่เพราะ bench ช้าลง แต่เพราะอ่านผลไม่ได้สักแถว**
    runner ที่ checkout ใหม่ไม่มี `target/criterion/` → criterion พ่น
    `Criterion.rs ERROR: …/base/sample.json` ทาง stderr **ผ่าบรรทัด
    `test NAME … bench:` ออกเป็นสองท่อน** → ชื่อกับตัวเลขคนละบรรทัด

    เครื่อง dev **ไม่มีวันเห็นรูปนี้** เพราะมี baseline ค้างอยู่ตลอด
    ไม่ใช่ "ลืมทดสอบ" แต่คือ **ทดสอบไม่ได้ด้วยเครื่องนั้น**

    | ต่างกันตรงไหน | ผลที่ตามมา |
    |---|---|
    | dev มี state สะสม · CI สะอาดทุกครั้ง | เส้นทาง first-run ไม่เคยถูกเดิน |
    | dev เป็น Windows · CI มี Linux ด้วย | Linux ไม่เคยคอมไพล์จนกระทั่ง CI รัน (`ข้อ 10`) |
    | dev มี GPU จริง · CI ใช้ lavapipe/WARP | เส้นทาง fallback ไม่เคยถูกเดิน |

    → **เก็บ output จริงจาก CI ไว้เป็นไฟล์ตัวอย่างใน repo**
      แล้วให้ตัวอ่านผล **ตรวจตัวเองกับไฟล์นั้นก่อนรันทุกครั้ง**
      เครื่อง dev จึงถูกบังคับให้เห็นรูปที่ตัวเองสร้างไม่ได้ — ทุกครั้ง ไม่ใช่ตอนที่นึกได้

    > ★ ข้อนี้กว้างกว่า bench: **ตัวแยกวิเคราะห์ (parser) ทุกตัวคือโค้ดที่ต้องมีเทสต์**
    > ไม่ว่าจะเขียนด้วย shell หรือ Rust · การที่มันอยู่ในสคริปต์ CI ไม่ได้ยกเว้นมัน
    > → และ **ตัวอ่านผลพังเงียบ ๆ ดูเหมือน "ของที่วัดพัง"** ซึ่งพาไปแก้ผิดที่

14. **★★★ ของที่ใช้ทดสอบ "อ่านถูกไหม" ต้องไม่ถูกผลิตด้วย **ตัวเขียนของเราเอง****

    เทสต์ round-trip (เขียนแล้วอ่าน เทียบกัน) **มองไม่เห็นบั๊กที่สมมาตร** —
    ถ้าตัวเขียนกับตัวอ่านผิดในทางเดียวกัน มันก็ยังตรงกันเป๊ะ และเขียวตลอดไป

    รูปที่ถูก (6 ก.ย. 2026 · การคืนกล้องจากไฟล์): สคริปต์ Python คลาย zstd →
    แก้ `f32` ของ zoom ตรง ๆ → บีบใหม่ → **คำนวณ crc ใหม่เอง** → `xtask dump-refx`
    ยืนยันว่าไฟล์ไม่เสีย → เปิดในแอปแล้ว **เห็น 200% จริง**
    → **ตัวเขียนของแอปถูกถอดออกจากลูปหลักฐานทั้งหมด**

    → เมื่อคำถามคือ *"เราอ่านไฟล์ของคนอื่นถูกไหม"* ของทดสอบต้องมาจากข้างนอก
      ไม่ว่าจะเขียนด้วยมือ ด้วยสคริปต์ หรือด้วยเครื่องมืออื่น

    > ### ★★ รูปเดียวกันตอนรันจริง: **ด่านที่เทียบของกับตัวมันเองคือด่านที่ผ่านตลอดกาล**
    > 9 ก.ย. 2026 · การยืนยันขนาดไฟล์หลัง export: ตัวตั้งต้องเป็น
    > **ไบต์ที่ตัวเข้ารหัสส่งให้เรา** ไม่ใช่ขนาดไฟล์ที่เพิ่งเขียน
    > `len(file) == len(file)` เป็นจริงเสมอแม้ไฟล์จะถูกตัดครึ่ง
    > → ทุกด่านต้องมี **ตัวตั้งที่มาจากคนละทาง** กับสิ่งที่กำลังตรวจ

    > ### ★★★ รูปที่สามของกฎเดียวกัน: **เทสต์ที่ยืนยันค่าที่ตัวมันเองเพิ่งตั้ง**
    > 11 ก.ย. 2026 · `answering_once_is_enough_for_the_whole_run` ยืนยันค่าที่ตัวเอง
    > ตั้งไว้ และฟังก์ชันจริงคืนที่ด่านแรกทุกครั้ง → **เขียวตลอดกาลโดยไม่ตรวจอะไรเลย**
    > · `keeps_at_most_three_old_files` ยืนยันแค่ว่า `.4` ไม่มี —
    > `rotate()` ที่ **ไม่ทำอะไรเลย** ก็ผ่าน
    >
    > → assertion ต้องอ้างถึงสิ่งที่ **โค้ดที่กำลังทดสอบเป็นคนผลิต**
    >   ไม่ใช่สิ่งที่ setup ของเทสต์วางไว้
    > → ★ และ **ชื่อเทสต์คือคำสัญญา** · ชื่อที่กว้างกว่าสิ่งที่ assert จริง
    >   ทำให้ทุกคนหลังจากนั้นเชื่อว่ามีคนตรวจแล้ว — **ชื่อที่โกหกแย่กว่าไม่มีเทสต์**

15. **★★ ห้ามแก้ assertion เป็นชุดใหญ่ในคอมมิตเดียวกับ refactor**

    6 ก.ย. 2026 ตอนยุบ `on_input`: จุดเรียก wrapper 94 จุดในเทสต์กำลังจะต้องแก้ทั้งหมด
    → แทนที่จะไล่แก้ **ย้าย wrapper ทั้ง 15 ตัวลง `mod tests` โดยไม่แก้สักไบต์**
    ผลคือ **assertion ไม่ถูกแตะแม้แต่จุดเดียว**

    เหตุผล: **การแก้ assertion เป็นชุดใหญ่พร้อม refactor คือที่ที่การอ่อนลงของเทสต์
    ซ่อนตัวได้ดีที่สุด** — diff ใหญ่จนไม่มีใครอ่านทีละบรรทัด และทุกบรรทัด
    "ก็ควรจะเปลี่ยนอยู่แล้ว" จึงไม่มีอะไรดูผิดปกติ

    → ถ้าเลี่ยงไม่ได้จริง ๆ ให้แยกเป็นสองคอมมิต: **แก้เทสต์ก่อนโดยโค้ดยังเหมือนเดิม**
      (ต้องเขียวทั้งคู่) แล้วค่อย refactor

16. **★★★ เพดานต้องบังคับ ณ จุดที่ทุกชิ้น**มีชีวิตอยู่พร้อมกัน** ไม่ใช่ทีละชิ้น**

    8 ก.ย. 2026 ระหว่างทำ export: `BandRenderer` ถือบัฟเฟอร์แถบของตัวเอง
    แล้วตัวเข้ารหัสถืออีกก้อน = **32 MB สองก้อนพร้อมกัน** → พังที่ความกว้าง 8192
    **โดยที่ตัวเลขของแต่ละฝั่งแยกกันดูยังสวยอยู่ทั้งคู่**

    และก่อนหน้านั้น: tile จตุรัส 4096² = **67 MB ใบเดียวกินทั้งเพดาน 64 MB**
    ทั้งที่ "render ทีละ tile" ฟังดูเหมือนการประหยัดหน่วยความจำ

    | วัดแบบไหน | เห็นอะไร |
    |---|---|
    | ทีละองค์ประกอบ | ทุกตัวผ่าน |
    | **RSS จริงตอนทำงาน (โพลจากเธรดแยก)** | ยอดดอยที่ไม่มีใครตั้งใจ |

    → **ค่าก่อน–หลังจับยอดดอยไม่ได้** ต้องโพลระหว่างทำงาน (ที่นี่ทุก 2 ms)
    → และมาตรวัดต้อง **พิสูจน์ว่ามันขยับจริง** ก่อนถูกใช้เป็นหลักฐาน (`ข้อ 9`)
    → ทางแก้เชิงโครงสร้าง: **ให้ผู้เรียกส่งบัฟเฟอร์มา** แทนที่จะให้แต่ละชั้นจองเอง
      เพราะแบบหลังไม่มีใครเห็นผลรวม

    > ### ★★ มาตรวัดที่วัด **ทั้งโปรเซส** ต้องรันลำพัง
    > 9 ก.ย. 2026: การวัด RSS ของ export ได้ **+16 MB ตอนรันลำพัง**
    > แต่ **+104 MB ตอนรันทั้งชุด** — ตัวการคือ NC ของ tile จตุรัสข้าง ๆ
    > ซึ่งจอง 64 MB อยู่ในโปรเซสเดียวกัน
    > → ย้ายไป **binary เทสต์แยก** และรวมทุกขั้นไว้ใน `#[test]` เดียวเพื่อบังคับลำดับ
    > → ★ เทสต์ที่วัดทรัพยากรระดับโปรเซส **ไม่ใช่ unit test** และวางปนกันไม่ได้

17. **★★ การตรวจต้องไม่พึ่งไฟล์ที่วางไว้ด้วยมือ**

    8 ก.ย. 2026: `damaged-refx` ใน `checklist-app.ps1` แดงเพราะหา `*.refx` ที่ `E:\` ไม่เจอ
    → แก้ชั่วคราวด้วยการวางไฟล์ตัวอย่างไว้ **แต่มันจะแดงอีกบนเครื่องอื่น ทุกเครื่อง**

    รูปเดียวกับ `ข้อ 13`: สภาพของเครื่องหนึ่งกลายเป็นเงื่อนไขที่มองไม่เห็น
    → **การตรวจต้องสร้างของที่มันต้องใช้เอง** ลงโฟลเดอร์ชั่วคราว แล้วเก็บกวาดหลังเสร็จ
    → ★ และ **"ข้ามเพราะหาของไม่เจอ" ต้องไม่ใช่ FAIL แต่ก็ต้องไม่ใช่ PASS** —
      สคริปต์นับ skip เป็น FAIL นั้น**ถูกแล้ว** (`ข้อ 2`) สิ่งที่ผิดคือการที่มันต้องข้าม

18. **★★★ ผลที่มาจากเธรดอื่น ต้องมาพร้อม "คนปลุก" — และเทสต์ธรรมดามองไม่เห็นข้อนี้**

    9 ก.ย. 2026: เลือกไฟล์เสร็จแล้วกล่อง export ยังเขียนว่า *"กำลังรอให้เลือกที่บันทึก"*
    **จนกว่าผู้ใช้จะขยับเมาส์** — แอปหลับด้วย `ControlFlow::Wait` ตาม I-1
    และผลจากเธรด dialog ไม่มีใครปลุกให้มาอ่าน

    **นี่คือครั้งที่สามของบั๊กรูปนี้:**

    | | อาการ |
    |---|---|
    | "Save and close" | บันทึกแล้วแต่ไม่ปิด — ผลมาถึงหลัง `on_close_requested` ตอบไปแล้ว |
    | autosave | เส้นปลุกต่อไว้แล้วแต่ `on_wake()` ไม่มีกิ่งรับ — กลไกที่ HANDOFF อ้างว่าปิดช่องโหว่ **ไม่เคยวิ่งเลยสักครั้ง** |
    | export dialog | ผลมาถึงแต่ไม่มีใครตื่นมาอ่าน |

    > ### ทำไมเทสต์ทั้งชุดเขียวทั้งสามครั้ง
    > **เทสต์ไม่มี event loop** — มันเรียกฟังก์ชันตรง ๆ จึงไม่มีสภาพ "หลับ"
    > ให้ผิดพลาดได้ · ทุกชิ้นถูกหมด ผิดแค่ว่าไม่มีใครมาเรียก (`ข้อ 8`)
    >
    > → บั๊กชนิดนี้ **ต้องใช้เครื่องมือคนละอย่าง** ไม่ใช่เทสต์เพิ่ม:
    >   **ประตูที่ไล่รายการช่องทางผลข้ามเธรดทุกช่อง แล้วยืนยันว่าแต่ละช่องมีเส้นปลุก**
    >   ช่องใหม่ที่ไม่มีเส้นปลุก = build ไม่ผ่าน ไม่ใช่รอให้ผู้ใช้เจอ

    ★ และ **`WaitUntil` ไม่ใช่คำตอบ** — มันทำงานได้เหมือนกัน แต่ต้องมีคนคอยดูแลว่า
    ปิดตอนไม่มีงานจริงไหม ซึ่งเป็นคำถามที่ I-1 แพ้ได้เงียบ ๆ · **ตัวปลุกไม่มีคำถามนั้น**

    > ### ★★★ แก้แล้ว 10 ก.ย. 2026 — **"ปลุก" กับ "วาดใหม่" ไม่ใช่สิ่งเดียวกัน**
    > ทางของ `Waker` จบที่ `request_redraw` เสมอ · แต่ autosave ต้องการให้มี**คนกลับมา
    > เก็บผล** ไม่ได้ต้องการให้วาด → ใส่ `Waker` แล้วได้ `Frames 516 → 517`
    > ทั้งที่การวาดใหม่ไม่ได้ทำให้ snapshot ขึ้นดิสก์เลยสักไบต์
    >
    > **และนั่นทำให้ประตู `ui-idle-diff` แดงให้กับพฤติกรรมที่ถูกต้อง —
    > ซึ่งฝึกให้คนเลิกเชื่อประตู** (คู่แฝดของกฎ "ตัวจับต้องเงียบตอนปกติ" ใน `ข้อ 11`)
    >
    > → ทะเบียนจึงมี **สาม**ชนิด ไม่ใช่สอง: `InThread` · **`OwnTimer`** · `NotNeeded`
    >   งานที่ต้องเกิดโดยไม่วาด ให้ตั้งนาฬิกาไว้ข้างหน้า (250 ms) แล้ว **ตื่นโดยไม่วาด**

    > ### ★★ ทะเบียนต้องนิยามด้วย **คุณสมบัติ** ไม่ใช่ด้วย **กลไก**
    > `copy.rs` ส่งผลกลับทาง `Arc<Mutex<..>>` — ประตูที่ไล่ `mpsc::channel`
    > **จะมองมันไม่เห็นเลย** และเงียบอย่างมั่นใจ
    > → คำถามที่ถูกคือ *"อะไรบ้างที่ผลิตผลนอก UI thread"* ไม่ใช่ *"channel มีกี่ตัว"*
    > → ★ ประตูรุ่นแรกใช้ `contains("wake()")` แล้วนับ `fn claims_to_wake()`
    >   เป็น "ปลุกแล้ว" — **ประตูข้อความจับคำศัพท์ของตัวเองติดมาด้วยเสมอ** (`ข้อ 9`)

    > ### ★★★ ประตูทะเบียนต้อง **ค้นหาสมาชิกเอง** ไม่ใช่ถูกยื่นรายชื่อให้ (11 ก.ย. 2026)
    > `sidecar.rs` เกิดพร้อมช่องข้ามเธรดสองช่อง แล้ว **ประตูเขียวผ่านไปเงียบ ๆ**
    > เพราะ `SCANNED` เป็นรายชื่อหกไฟล์ที่เขียนไว้ตอนสร้างประตู
    >
    > **ประตูที่ป้องกัน "ของใหม่ที่ไม่มีใครขึ้นทะเบียน" พังด้วยโรคเดียวกับที่มันรักษา**
    > → ให้มันเดินหาไฟล์เองทั้งต้นไม้ (ที่นี่ 80 ไฟล์) · รายชื่อคงที่ = ประตูที่หมดอายุ
    >   ตั้งแต่วันที่เขียน และไม่มีอะไรบอกว่ามันหมดอายุแล้ว
    >
    > ★ และคำอ้างในทะเบียนต้องพิสูจน์ได้: `OwnTimer` รุ่นแรกตรวจแค่ว่า
    > *ไฟล์เดียวกัน* มีคำว่า `_deadline(` → ผ่านด้วยนาฬิกาตัวไหนก็ได้
    > **และตกกับนาฬิกาที่อยู่คนละไฟล์ทั้งที่นั่นถูกพอกัน**
    > → ต้องชี้ไปที่ **ตัวฟังก์ชันนาฬิกาจริง** (`clock_in` · `clock_fn` · `proof`)

19. **★★★ ทำให้สภาพที่ห้าม *เขียนไม่ออก* ดีกว่าตรวจว่าไม่มีใครเขียน**

    11 ก.ย. 2026 · `WritePermit` ของ sidecar: สร้างนอกโมดูลไม่ได้ · `Load::HandsOff`
    ไม่มีใบให้ → โค้ดที่พยายามเขียนทับไฟล์ที่พังหรือใหม่กว่า **คอมไพล์ไม่ผ่าน**

    ก่อนหน้านี้รูปเดียวกันเคยใช้กับ `Tab`: สองฝั่งถามคำถามเดียวกันในช่วงที่คำตอบคงที่
    → **เป็นไปไม่ได้ที่ทั้งคู่จะทำงานหรือทั้งคู่จะไม่ทำงาน**

    | วิธีคุม | อยู่ได้นานแค่ไหน |
    |---|---|
    | คอมเมนต์ว่า "ห้าม…" | จนถึงคนถัดไปที่ไม่ได้อ่าน |
    | เทสต์/ประตูที่ตรวจว่าไม่มีใครทำ | จนถึงทางเข้าที่ประตูมองไม่เห็น |
    | **ชนิดข้อมูลที่ทำให้เขียนไม่ออก** | **ตลอดไป — และไม่ต้องมีใครจำ** |

    → เมื่อกฎข้อไหนสำคัญพอจะเขียนลง spec **ให้ถามก่อนเสมอว่ามันกลายเป็น
      ข้อผิดพลาดตอนคอมไพล์ได้ไหม** · ได้เมื่อไหร่ กฎข้อนั้นเลิกต้องมีคนเฝ้า

20. **★★ เครื่องมือวิเคราะห์ห้ามแก้ working tree ของจริง — ไม่ว่ากรณีใด**

    12 ก.ย. 2026 · ตัวรัน mutation แก้ไฟล์ซอร์สจริงแล้วคืนค่าเมื่อจบ
    → **กด Ctrl+C กลางคัน = ไฟล์ของผู้ใช้ค้างอยู่ในสภาพที่ถูกดัดแปลง**
    และรอบนั้นกินเวลาหลายชั่วโมง ซึ่งเป็นช่วงที่คนกดหยุดมากที่สุด

    รูปเดียวกับ I-3 แต่เกิดกับซอร์สแทนที่จะเป็นงานผู้ใช้: **สิ่งที่กู้คืนใน
    `finally` กู้ไม่ได้เมื่อโปรเซสไม่ได้เดินไปถึงบรรทัดนั้น**

    → ทำงานบน **สำเนา** เสมอ (worktree หรือโฟลเดอร์ชั่วคราว)
    → ★ เกณฑ์ทั่วไป: เครื่องมือที่ *ต้องการ* การเก็บกวาดเพื่อความถูกต้อง
      ออกแบบผิดตั้งแต่ต้น · ที่ถูกคือออกแบบให้ **ไม่มีอะไรต้องเก็บกวาด**

    > ### ★★★ ตัวกวาด mutation ต้องรันบน **OS ที่โค้ดนั้นถูกคอมไพล์จริง** (13 ก.ย. 2026)
    > ตัวกวาดหาด่านด้วยการอ่าน **ข้อความในซอร์ส** แล้วแทรกบรรทัดเข้าไป
    > ครึ่งหนึ่งของ `refx-platform` อยู่หลัง `#[cfg(target_os = "windows")]`
    > → บน ubuntu การแทรกลงไปในฟังก์ชันที่ **ไม่ได้ถูกคอมไพล์** ไม่เปลี่ยนอะไรเลย
    > เทสต์เขียว และด่านทุกตัวในนั้น **รายงานว่า "รอด" ด้วยเหตุผลที่ไม่เกี่ยวกับเทสต์**
    >
    > แล้วเราจะอ่านตัวเลขนั้นว่า *"ไม่มีใครเทสต์ส่วนนี้"* ซึ่งผิดคนละเรื่อง —
    > **ประตูโกหกโดยให้ตัวเลขที่ดูน่าเชื่อ** (`ข้อ 9`)
    > → matrix ต้องผูก `os` กับ crate · `refx-platform` = `windows-latest`
    >   คูณสองของ Windows คุ้มเสมอเมื่อทางเลือกคือตัวเลขที่อ่านผิด

    > ### ★★ "ข้าม" ไม่ใช่ "ผ่าน" — บรรทัดสรุปต้องแบกทั้งสามตัวเลข
    > `refx-asset` มี 2 ด่านที่คอมไพล์ไม่ผ่านหลังแทรก (`if let Some(x) = … { return f(x); }`
    > — body ที่แทรกอ้างตัวแปรที่ยังไม่เกิด) · ตัวกวาดพิมพ์ `ข้าม` ระหว่างทางจริง
    > แต่บรรทัดสรุปเขียนแค่ `รอดจากทุกเทสต์: 0` ซึ่ง **อ่านได้ว่าครบ**
    > → สรุปต้องมี **ค้นเจอ / ทดสอบ / ข้าม** เสมอ · รูปเดียวกับ `steps: N`

    > ★★ และเครื่องมือนี้จ่ายค่าตัวเองไปแล้วก่อนเริ่มใช้จริง — เจอ
    > `unreachable statement` ใน `command.rs` ที่ **clippy รันคู่กันมาตลอดแต่ไม่เห็น**
    > · เครื่องมือคนละชนิดเห็นคนละอย่าง ไม่ใช่ของซ้ำซ้อนกัน

---

## 4. Definition of Done (ทุก PR)

PR จะ merge ได้ต่อเมื่อ **ครบทุกข้อ**:

1. `cargo build --release` ผ่านบน Windows + Linux
2. `cargo clippy --all-targets -- -D warnings` ไม่มี warning เลย
3. `cargo fmt --check` ผ่าน
4. **`cargo nextest run --workspace --all-features`** ผ่าน — **ไม่ใช่ `cargo test --all`**
   · `--all` = *ทุก package* ไม่ใช่ *ทุก feature* → ข้ามเทสต์หลัง feature gate เงียบ ๆ
   · `cargo test` ไม่มี timeout → เทสต์ค้างกลายเป็นซอมบี้แทนที่จะแดงพร้อมชื่อ
   · **CI รัน nextest มาตลอด** ข้อนี้เคยสั่งของที่อ่อนกว่าที่ CI ตรวจจริง 10 ตัว
   (แก้ 13 ก.ย. 2026 — CC รายงานว่าสเปกขัดกันเองสองรอบติด · รายละเอียดที่ `§3.9 ข้อ 9`)
   · ★ **เทียบจำนวนเทสต์กับรอบก่อนทุกครั้ง** ส่วนต่างที่อธิบายไม่ได้ = ยังไม่ผ่านข้อนี้
5. `cargo deny check` ผ่าน
6. Benchmark ที่เกี่ยวข้องยังอยู่ใต้เพดานสัมบูรณ์ (กฎ 10% ถูกยกเลิกแล้ว — ดู §2)
7. โค้ดใหม่ไม่มี `unwrap()` / `expect()` / `panic!()` บนเส้นทางที่ข้อมูลมาจากผู้ใช้หรือจากไฟล์
   *(ในเทสต์ใช้ได้ ในโค้ดที่เป็น invariant ภายในใช้ `expect("เหตุผลชัดเจน")` ได้)*
8. ไม่มี `unsafe` นอก `refx-platform`
9. ทุก path ที่แตะดิสก์ผ่าน `validate_path`
10. ทุก mutation ของ `Board` ผ่าน `Command` (ไม่มี `&mut board` หลุดออกไปที่อื่น)
11. ถ้าเพิ่ม image format → ต้องมี fuzz target มาด้วยใน PR เดียวกัน
12. ถ้าแก้ file format → ต้องมี migration + fixture ไฟล์เก่ามาด้วย
13. `cargo tree -d | grep '^png'` **ต้องว่าง** (บังคับใน CI job `single-wgpu` ด้วย)

    > ★★ P5-4 ตรึง `png` ตรง ๆ เพื่อเขียน PNG ทีละแถบ (`image` ไม่มี API สตรีมสำหรับ PNG
    > — ดู `docs/07 §6` และ `docs/09`) · ถ้าเวอร์ชันที่เราตรึง**เลื่อนออกจากที่ `image` ใช้**
    > จะได้ png สองก๊อปปี้ในทรี: binary โตฟรี ๆ และมี **ตัวเข้ารหัสสองตัวที่อาจให้ผล
    > ไม่เหมือนกัน** ระหว่าง export กับ spool · `^` สำคัญ — ไม่งั้นจับ crate ลูกที่ชื่อ
    > ลงท้ายด้วย png ติดมาด้วย · เป็นข้อเดียวกับประตู `^wgpu` ที่มีอยู่แล้ว

---

## 5. Logging

```rust
tracing::info!  // เหตุการณ์ระดับผู้ใช้: เปิด/บันทึกไฟล์, สลับ mode
tracing::warn!  // กู้คืนได้: decode ล้ม, ไฟล์หาย, cache miss ผิดปกติ
tracing::error! // เสียหาย: save ล้ม, device lost, DB เปิดไม่ได้
tracing::debug! // ปิดใน release
```

- Log ไปที่ `<cache_dir>/logs/` หมุนไฟล์ที่ 5 MB เก็บ 3 ไฟล์
  **`tracing-appender` 0.2 หมุนตามขนาดไม่ได้** (รองรับแค่ minutely/hourly/daily)
  ต้องเขียน writer เองแล้วห่อด้วย `non_blocking` — ห้ามใช้ daily แทน เพราะ error ที่วนซ้ำ
  ทำให้ไฟล์เดียวโตระดับ GB ได้ภายในวันเดียว
- **ห้ามเขียน log ลง stderr ใน release** — `windows_subsystem="windows"` ไม่มี console
  และการเขียน stderr เกิดบน UI thread (ผิด I-2)
- **cache eviction ห้ามแตะโฟลเดอร์ `logs/` เด็ดขาด** — crash log คือสิ่งเดียวที่ผู้ใช้มีให้ส่งเวลารายงานปัญหา
  ถ้าโดนล้างพร้อม thumbnail cache เราจะ debug ปัญหาของผู้ใช้ไม่ได้เลย
- **ห้ามมี path เต็มใน log ระดับ info** (มีชื่อผู้ใช้อยู่ในนั้น) ใช้ชื่อไฟล์อย่างเดียว
- ต้องมีเมนู "เปิดโฟลเดอร์ log" ให้ผู้ใช้ส่ง log เองเวลารายงานปัญหา

---

## 6. Profiling

- `puffin` หรือ `tracy` ผ่าน feature flag `profiling` (ปิดใน release)
- ต้อง profile ก่อน optimize เสมอ — **ห้าม optimize ตามความรู้สึก**
- แต่ละ frame แบ่ง scope: `cull`, `build_instances`, `upload`, `egui`, `submit`
- `heaptrack` (Linux) / `dhat` สำหรับหา memory leak — รันเดือนละครั้งเป็นอย่างน้อย
